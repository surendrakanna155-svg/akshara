name: Flutter Patrol RC

# Staged Patrol gates: fast smoke on release branches; full regression on manual
# dispatch AND on a nightly schedule.
#
# QW1 QA-X-035: the full Patrol E2E suite (not just the 4-target smoke on the PR
# gate) runs nightly here so money/RBAC/persistence-chain regressions are caught
# within 24h even when nothing is pushed — a scheduled nightly that blocks release.
on:
  push:
    branches:
      - main
      - 'release/**'
  schedule:
    # 03:00 UTC nightly — full Patrol regression (resolves to FULL mode below).
    - cron: '0 3 * * *'
  workflow_dispatch:
    inputs:
      coverage_mode:
        description: 'Patrol coverage mode'
        required: true
        default: fast
        type: choice
        options:
          - fast
          - full

jobs:
  patrol:
    runs-on: macos-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install Patrol CLI
        run: dart pub global activate patrol_cli

      - name: Install dependencies
        run: flutter pub get

      - name: Run ERP coverage
        env:
          ERP_COVERAGE_MODE: ${{ github.event_name == 'workflow_dispatch' && inputs.coverage_mode || (github.ref == 'refs/heads/main' && 'full' || 'fast') }}
          PATH: ${{ env.PATH }}:${{ env.HOME }}/.pub-cache/bin
        run: bash qa/patrol/run_erp_coverage.sh

      - name: Upload Patrol reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: patrol-reports-${{ github.run_id }}
          path: qa/patrol/reports/erp_coverage/
