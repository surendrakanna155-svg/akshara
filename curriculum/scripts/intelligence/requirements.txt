# Knowledge Intelligence Engine — Python data-lane dependencies
# Runtime: Python 3.14 (Homebrew removed 3.13 mid-project; PyMuPDF ships an abi3 wheel that
#   runs on 3.14, Pillow ships cp314 wheels — all install binary-only, no compiler needed).
#   python3.14 -m venv curriculum/.venv
#   curriculum/.venv/bin/pip install --only-binary=:all: -r curriculum/scripts/intelligence/requirements.txt
#
# NOTE: the venv symlinks to the Homebrew python; a `brew upgrade`/`brew install` that bumps the
# base python can orphan this venv — recreate it with the command above if imports vanish.
#
# OCR also needs the system Tesseract binary:  brew install tesseract
#
# Everything runs locally. No PDF/book is ever sent to an AI model (LLM policy).

# --- Phase 1: Repository Verification -----------------------------------------
pypdf==6.14.2            # pure-python PDF open/page-count/text-sample → integrity + readiness

# --- Phase 2: Local Parser ----------------------------------------------------
pymupdf==1.28.0          # PRIMARY parser: text, coordinates, blocks, images, tables, TOC
pdfplumber==0.11.10      # table fallback only, where PyMuPDF finds no table on a ruled page
pytesseract==0.3.13      # OCR wrapper for scanned PDFs (needs system `tesseract`)
Pillow==12.3.0           # image handling for the OCR pipeline

# --- Phases 5-8: analysis / generation (staged) -------------------------------
sympy==1.14.0            # deterministic re-derivation: the QIE/QPL certification gates (independent_solve,
                         #   dimensional analysis, distractor mis_relation, step replay) all execute via sympy
# numpy>=2                 # vector/statistics for question-intelligence
