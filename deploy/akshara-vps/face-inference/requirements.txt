# Pinned: an inference service whose model output silently shifts under a
# transitive upgrade would invalidate every enrolled reference.
fastapi==0.115.6
uvicorn==0.34.0
onnxruntime==1.20.1
numpy==2.2.1
pillow==11.1.0
pydantic==2.10.4
mediapipe==1.0.0
