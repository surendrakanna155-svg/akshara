# Copy to postgrest.env on the VPS (chmod 600). authenticator pw must match the ALTER ROLE.
PGRST_DB_URI=postgres://authenticator:__AUTH_PW__@akshara-postgres:5432/akshara_db
PGRST_DB_SCHEMAS=public
PGRST_DB_ANON_ROLE=anon
PGRST_JWT_SECRET=__SHARED_MIN32_SECRET__
PGRST_SERVER_PORT=3000
