# Architecture Review — v15.6 Production Validation

**Release:** v15.6 (`v15.6-production-validation`)  
**Date:** June 2026  
**Score:** 88 / 100 (pilot-weighted)

---

## Scope Reviewed

| Milestone | Area | Verdict |
|-----------|------|---------|
| v15.1 | Health routes, deploy scripts | ✅ Code complete; staging blocked |
| v15.2 | Device validation matrix | ✅ Script + existing mobile tests |
| v15.3 | Timetable move, room allocation, editor | ✅ End-to-end mock + contract |
| v15.4 | Lesson topic completion | ✅ Wired to existing API |
| v15.5 | Parent activation dashboard | ✅ Backend + Flutter + RBAC |
| v15.6 | Provider health, delivery webhook | ✅ Routes added; not live on staging |

---

## Strengths

- Consistent repository pattern for new methods (`movePeriod`, `allocateRooms`, `getParentActivationDashboard`)
- Hybrid repositories delegate to API with mock fallback
- Readiness script distinguishes deploy-blocked 404s from auth failures
- RBAC guards added for `parentActivationDashboard` and `roomAllocation`

---

## Gaps

| Gap | Severity | Owner |
|-----|----------|-------|
| Staging Edge not redeployed | High | Ops — run `phase21_deploy_verify.sh` with token |
| v14 intelligence routes 404 | High | Same deploy |
| `onReorder` deprecated in timetable editor | Low | Flutter 3.41+ migration to `onReorderItem` |
| Manual real-device pass not automated | Medium | QA — post-deploy checklist |

---

## Recommendations

1. Add `SUPABASE_ACCESS_TOKEN` to CI for automated deploy + probe gate
2. Extend `device_validation_matrix.py` with parent-activation and timetable-move probes post-deploy
3. Migrate `ReorderableListView.onReorder` → `onReorderItem` when upgrading Flutter SDK baseline

---

## Endpoint Inventory (Phase 21 additions)

| Method | Path | Module |
|--------|------|--------|
| GET | `/health/storage` | Platform |
| GET | `/health/providers` | Platform |
| POST | `/academic/timetables/periods/move` | Timetable |
| GET | `/school/parent-activation/dashboard` | School completion |
| POST | `/communications/delivery/webhook` | Communications |
