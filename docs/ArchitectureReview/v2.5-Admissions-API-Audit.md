# v2.5 Admissions API Audit

## API layer review

### Endpoints

- **Read (v1.9):** 11 GET endpoints — unchanged
- **Write (v2.5):** 19 mutation endpoints — fully scaffolded in `AdmissionsApiPaths` + `AdmissionsRemoteDataSource`

### DTOs

| File | Purpose |
|------|---------|
| `create_lead_request_dto.dart` | POST `/leads` |
| `update_lead_request_dto.dart` | PUT `/leads/{id}` |
| `assign_counselor_request_dto.dart` | PATCH assign |
| `followup_request_dto.dart` | Follow-ups + notes |
| `application_request_dto.dart` | Create/update application |
| `document_verification_request_dto.dart` | Upload + verify |
| `approval_request_dto.dart` | Approve/reject/note |
| `finance_handoff_request_dto.dart` | Send + status patch |
| `enrollment_request_dto.dart` | Submit + generate number |

### Interceptors (unchanged)

- `AuthInterceptor`
- `TenantInterceptor`
- `CorrelationIdInterceptor`
- `ApiErrorInterceptor`

### Contract tests

- DTO serialization parity (snake_case payloads)
- Mock repository write round-trips
- Path mapping verification

### Score: 9.0 / 10

Client API layer is production-ready; backend contract validation pending live server.
