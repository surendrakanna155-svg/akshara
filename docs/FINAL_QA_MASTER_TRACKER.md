# AKSHARA ERP — Final QA Master Tracker

**THE SINGLE SOURCE OF TRUTH for the Final QA Completion Program.**
Date: 2026-06-27 · HEAD `0f33c6a` · Companion to [`FINAL_QA_AUDIT.md`](FINAL_QA_AUDIT.md) and [`FINAL_QA_ROADMAP.md`](FINAL_QA_ROADMAP.md).

> **Status of every row below is `Open`. No fixes or tests have been written.** This tracker is the backlog for the QA waves, which execute **only after owner approval**.

> **Engineering gate:** Every row here is governed by the Engineering Operating System (`/eos`) per [`engineering/ENGINEERING_GATE_POLICY.md`](engineering/ENGINEERING_GATE_POLICY.md). A row is not `Done` until `/eos <scope>` returns PASS against the [Engineering Constitution](engineering/AKSHARA_ENGINEERING_CONSTITUTION.md). The EOS is the only engineering standard for this work — do not add bespoke checklists.

> **⏭ Roadmap continuation (added 2026-07-01).** The QW1–QW8 rows below are **complete and frozen** and are **not** modified by the next-generation roadmap. The `QA-R` (QW8) rows' remaining **live-only legs** are executed as **Phase B — Release Engineering (Track B)** in [`FINAL_QA_ROADMAP.md`](FINAL_QA_ROADMAP.md#next-generation-roadmap--phases-b--c--d) — each row flips to live-Verified as its staged harness runs green on the VPS; **no row is rewritten here**. Post-GA product work (**Phase C**) and future commercial work (**Phase D**) live in the roadmap, sourced from [`PRODUCT_ENHANCEMENT_BACKLOG.md`](PRODUCT_ENHANCEMENT_BACKLOG.md) and [`PRODUCT_COMMERCIAL_BACKLOG.md`](PRODUCT_COMMERCIAL_BACKLOG.md) respectively — not from this tracker.

---

## How to read this tracker

Every gap row has the 9 required columns:
`ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status`

**ID prefixes** (by coverage layer):
- `QA-J-###` — End-to-end role journeys (Patrol / Maestro). 69 rows.
- `QA-F-###` — Flutter widget / UI / state (unit, widget, golden). 63 rows.
- `QA-B-###` — Backend API / RBAC / RLS / error-path (Deno). 75 rows.
- `QA-X-###` — Cross-cutting / non-functional (offline, retry, push, audit, states, import/export, perf, golden, security, CI). 39 rows.
- `QA-C-###` — Feature **B**ehaviour Certification (UI/comms/i18n/RBAC/reliability/AI/white-label behaviour). 25 rows. *(QW7)*
- `QA-R-###` — Production **R**eadiness & Market Certification (pilot/multi-school/perf/security/DR/commercial). 12 rows. *(QW8)*

**Severity:** `P0` = pilot-critical workflow OR security/RBAC/data-integrity path with no/shallow coverage · `P1` = important operational module workflow, partial/nav-only · `P2` = secondary / advanced / display-only.

**Fix waves** (see [Roadmap](FINAL_QA_ROADMAP.md) for full definitions):
- `QW1` — Critical Path & CI Enforcement (P0 journeys, the 4 missing RBAC personas, P0 RBAC/RLS, money/attendance/push P0, wire suites into CI).
- `QW2` — Module Write/Persistence E2E (per-module write journeys under correct personas).
- `QW3` — Flutter Widget/UI & State coverage (pump screens, forms, dialogs, loading/error/empty).
- `QW4` — Backend API/RBAC/RLS/Error-path (router contracts, per-route RBAC matrix, RLS probes, 4xx/5xx/402).
- `QW5` — Secondary / Advanced / Verticals journeys.
- `QW6` — Resilience & Non-functional (offline cache, retry, audit, import/export, performance, golden, security sweeps).
- `QW7` — **Feature Behaviour Certification (MANDATORY)** — end-user behaviour: every element acts correctly, in the right language, for the right role, with the right notification/audit/refresh; white-label.
- `QW8` — **Production Readiness & Market Certification (FINAL GATE)** — multi-school SaaS, performance, security, backup/DR, monitoring, commercial readiness, Final Production Checklist.

**Status values** (lifecycle once a wave starts): `Open → In-Progress → Test-Written → Passing → Verified`. (No row is `Won't-Build` — the offline/retry rows are now built in **Phase 0**, see below.)

---

## Evidence grade — how to read every `Verified` row (finding DOC-4 / QA-1)

> **Re-scope banner (added 2026-07-04, P0-DOC-4).** A row's `Status = Verified` in this frozen tracker
> means **"the recommended test was written and passes at the grade stated in its parenthetical
> evidence"** — it does **not**, on its own, mean *live-on-VPS* verification. To keep the aggregate
> honest, read every `Verified` through this **evidence grade**, which the row's own parenthetical
> already implies. These rows are **frozen and are NOT rewritten** — this section governs how their
> existing Status is interpreted.

| Grade | Meaning | How to recognise it in a row's parenthetical |
|---|---|---|
| **LIVE** | Proven against the live VPS + tenant Postgres with real auth/RBAC/RLS. | Cites a live-cert script run (`scripts/qa/live_cert_*`) with real school-JWT + prod DB. |
| **LOCAL-LOGIC** | Business logic proven by a local unit/widget/integration test (no live DB). | "persist on re-read", "deterministic integration test", named `*_test.dart` green. |
| **CONTRACT** | Route/permission/authorization contract asserted, DB-free. | "Verified (authorization)", "holds `<perm>` … denied", 503-when-authorized. |
| **RENDER-MOCK** | UI renders against a mock/in-memory source; backend wiring pending/thin. | "renders", "nav-only", mock store, "UI … = follow-up". |
| **STAGED** | Harness/fixtures authored, **not yet run for real** — awaits the live lane (Track B). | QA-R live legs; "staged harness runs green on the VPS". |

**Grade-of-record by wave (aggregate honesty):**
- **QW1–QW7** rows are predominantly **LOCAL-LOGIC** and **CONTRACT** (many say *"Verified (authorization)"*) with a few **RENDER-MOCK** follow-ups noted inline — **not** LIVE. A small set carry an archived LIVE cert (B-batch / onboarding); those are historical, not a current GA claim.
- **QW8 (`QA-R`)** live-only legs are **STAGED** until **Phase B — Release Engineering (Track B)** runs them on the VPS. They are **not** LIVE today.

**Explicit over-claim corrections (DOC-4):**
- **"Universal idempotency"** is a *target*, not shipped reality — idempotency guards currently cover **~4%** of mutations (REL-1); the backend "universal-idempotency" migration referenced in the Phase-0 note is **scheduled work** (→ `P1-CODE-1`), not a present fact.
- **"Universal `row_version` conflict"** is likewise a target — the `row_version` optimistic-lock guard is wired on **1 of 4** critical money/marks paths and is **inert on finance** until `P0-CODE-1` (ENG-1). Do not read it as platform-wide.
- **"237 Verified" / "PRODUCTION CERTIFIED"** aggregate superlatives (seen in older summaries and companion docs) are **local-evidence** counts and **historical batch certs** — **not** a live GA certification. Live GA certification is gated on P6 (pilot) + P7 (`QA-R-012` + 7-day cron), per [`roadmap/FINAL_EXECUTION_MASTER_ROADMAP.md`](roadmap/FINAL_EXECUTION_MASTER_ROADMAP.md).

*(This section is the "evidence-grade column" for the frozen tracker: rather than mutating 283 frozen
rows, it defines the grade vocabulary and the per-wave grade-of-record that every row's Status inherits.
New/live-flipped rows in Track B should cite their grade explicitly in the parenthetical.)*

---

## Summary counts

> **Read with the evidence grade above.** The totals below count **rows whose recommended test exists
> and passes at its stated grade** — overwhelmingly LOCAL-LOGIC/CONTRACT, not LIVE. They are a
> *local-verification* backlog burn-down, **not** a live production-certification count.


| Layer | P0 | P1 | P2 | Total |
|---|---:|---:|---:|---:|
| `QA-J` End-to-end journeys | 23 | 32 | 14 | **69** |
| `QA-F` Flutter widget/UI/state | 10 | 36 | 17 | **63** |
| `QA-B` Backend API/RBAC/RLS | 22 | 39 | 14 | **75** |
| `QA-X` Cross-cutting/non-functional | 9 | 18 | 12 | **39** |
| `QA-C` Feature Behaviour Certification *(QW7)* | 18 | 7 | 0 | **25** |
| `QA-R` Production Readiness Certification *(QW8)* | 10 | 2 | 0 | **12** |
| **Total** | **92** | **134** | **57** | **283** |

| Wave | Rows | Theme |
|---|---:|---|
| QW1 | 53 | Critical Path & CI Enforcement |
| QW2 | 32 | Module Write/Persistence E2E |
| QW3 | 53 | Flutter Widget/UI & State |
| QW4 | 70 | Backend API/RBAC/RLS/Error-path |
| QW5 | 14 | Secondary/Advanced/Verticals |
| QW6 | 24 | Resilience & Non-functional |
| QW7 | 25 | Feature Behaviour Certification (mandatory) |
| QW8 | 12 | Production Readiness & Market Certification (final gate) |
| **Total** | **283** | across 8 waves + Phase 0 |

## Phase 0 prerequisite rows — Data Reliability Platform (owner decision 2026-06-27: BUILD)

These rows depend on a reusable **Data Reliability Platform** (Draft Persistence + Sync Engine + Repository Integration) that is built in **Phase 0**, before QW1. They are **not** `Won't-Build` — they are tested-and-closed once the platform ships. Architecture is reviewed/approved before implementation: see [`DATA_RELIABILITY_PLATFORM_DESIGN.md`](DATA_RELIABILITY_PLATFORM_DESIGN.md).
`QA-X-001` (offline attendance draft), `QA-X-002` (offline fee collect), `QA-X-004` (offline cached reads), `QA-X-005` (connectivity status), `QA-X-006` (double-submit guard), `QA-X-007` (auth refresh-replay), `QA-X-008` (5xx/429 retry+backoff), `QA-X-009` (unsaved-changes guard).

**Status (2026-06-27):** Design APPROVED with refinements R1–R4. **Phase 0a foundation IMPLEMENTED & VERIFIED** on branch `feature/data-reliability-platform` (`lib/core/reliability/**`, 34/34 tests, analyze clean, 0 project errors, additive). The platform mechanics these rows depend on now exist + are unit-tested; rows move to `Verified` after the integration steps (bootstrap wiring, pilot-critical screen integration, backend idempotency + conflict-version migration **rolled out across all mutations** — a *target*, currently ~4% coverage per REL-1, scheduled as `P1-CODE-1`; do not read "universal" as shipped — airplane-mode integration tests) — see [`DATA_RELIABILITY_PLATFORM_PHASE0_PROGRESS.md`](DATA_RELIABILITY_PLATFORM_PHASE0_PROGRESS.md).

---


---

## AKSHARA ERP — End-to-End Journey Gap Rows (Final QA Master Tracker)

Project root: `/Users/surendrakanna/Documents/Akshara_ERP`
Generated: 2026-06-27 · Journey-first QA: prove every real school workflow is covered login → action → completion → persistence/notification/audit.
Source artifacts: `journeys.md`, `routes_rbac.md`, `surface_A/B/C.md` (same dir).

Severity rubric: **P0** = pilot-critical workflow OR security/RBAC/data-integrity path with no/shallow coverage · **P1** = important operational module workflow, partial/nav-only · **P2** = secondary/advanced/display-only.
Fix waves: **QW1** = Critical role journeys + RBAC personas · **QW2** = Module write/persistence E2E · **QW5** = Secondary/verticals/advanced.

---

#### Parent

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-001 | finance / parent | Parent · pay fee end-to-end (PayNow → method → confirm → receipt) | `parent_workflows_test` navigates to Pay Fee + asserts CTA; Maestro `biz_parent_pay_fee` scrolls+screenshot only; finance-side collection is deep but not parent-side | No parent-persona payment completion: method select, summary/breakdown confirm, success state, receipt row persisted + visible in `/parent/receipts` after nav | P0 | Patrol e2e: parent persona, run PayNow → PaymentMethod → PaymentSummary → confirm, assert payment success + receipt persists + appears in receipts list | QW1 | Verified (2026-06-28 — qw1_parent_money_loop_e2e_test: parent Pay Now → Pay now → "Payment successful" → receipt retrieved by id on detail screen; Patrol green) |
| QA-J-002 | parent / communication | Parent · message teacher (compose + send) | none — `parent_conversation`/`parent_messages` never exercised for a send in any suite | No compose, no send, no thread-persist, no teacher-receives assertion (messaging untested in this direction) | P0 | Patrol e2e: parent persona, open conversation, enter text, send, assert message persists in thread + teacher sees it | QW1 | Verified (2026-06-28 — qw1_messaging_e2e_test: parent reply sent in thread persists/round-trips; Patrol green) |
| QA-J-003 | parent | Parent · switch active child (multi-child household) | Maestro `biz_parent_child_selector` (NAV); `parent_child_switcher_sheet` exists | No switch + data-reload assertion: after picking child B, dashboard/attendance/fees must reload to child B's data | P0 | Patrol e2e: parent (multi-child persona), open switcher, select 2nd child, assert dashboard data changes to that child | QW1 | Verified (2026-06-28 — qw1_parent_child_switch_e2e_test: parent (Ravi+Priya) opens the app-bar switcher, picks Priya → the data-derived greeting flips "Ravi's Day at a Glance" → "Priya's Day at a Glance" (proves the dashboard future re-keyed, not just the chip); Patrol green) |
| QA-J-004 | parent / academics | Parent · receive published exam results (notification + view) | `patrol_batch2b` exam-publish→parent-sees results (strong); Maestro `workflow_exam_publish_approval` parent verify | Notification/alert delivery on publish untested; only in-app list-visibility asserted, not the parent notification deep-link path | P1 | Patrol e2e: after principal publish, assert parent notification fires + deep-links to result | QW2 | Open — INFRA-BLOCKED (the in-app results view is already covered by patrol_batch2b; the remaining gap — a PUSH notification firing on publish + deep-link — needs FCM on a device, the same lane as QA-X-010/012; not runnable headlessly) |
| QA-J-005 | parent / parent_meetings | Parent · book / request a PTM | `red_team_parent_operational` PTM quick action (NAV); `parent_meeting_summary_e2e` is READ | No parent-initiated PTM request submit + persistence; create/booking flow untested from parent side | P1 | Patrol e2e: parent persona, request PTM slot, submit, assert booking persists + staff sees request | QW2 | Open — FEATURE-BLOCKED (Parent–Teacher Meetings is gated OFF in SchoolBuildScope — `RouteNames.parentMeetings`/`parentPtm` are hidden because the PTM backend has not shipped (mock-only, lost edits on restart, CORE-1/PAR-4). Re-wave when the PTM backend ships and the routes are un-hidden.) |
| QA-J-006 | parent / transport | Parent · view child transport status / route | `red_team_parent_operational` transport quick action (NAV) | No assertion route/stop/vehicle data renders for the child; nav-only | P2 | Maestro flow + `assertVisible` route detail for child | QW5 | Verified (2026-06-30 — qw5_parent_transport_view_test: the parent transport screen RENDERS the active child's route/bus/pickup/drop; empty-state asserted when unallocated; Phase-2 boundary pinned (telemetry-preview note, no live map — owner O8). Static route view real via parentTransportAllocationProvider.) |
| QA-J-007 | parent / notifications | Parent · open notification → mark read → deep-link | `red_team_parent_operational` notice deep-links n1/n3 (NAV/deep-link) | No mark-read state-persist assertion; no general notifications-list read-state round-trip | P2 | Patrol e2e: parent persona, open notification, mark read, assert read-state persists after reload | QW5 | Verified (mark-read leg) (2026-06-30 — covered by qw4_notifications_persistence_test (QA-X-013): tap → read-state persists across a rebuild + badge→0; same shared NotificationsScreen the parent uses. Deep-link sub-leg is a DOCUMENTED known gap (AppNotification carries no route field; tap only marks read) — not forced to Verified; tracked.) |

---

#### Student

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-008 | student_app | Student · submit homework (file/answer → persist) | `student_workflows_test` "Submit HW" quick-action TAP only; Maestro `biz_student_homework_due` NAV | No real submission (attach/answer), no submit confirmation, no persisted submission row, no teacher-sees-submission assertion | P0 | Patrol e2e: student persona, open homework, submit answer/file, assert submission persists + status flips + teacher sees it | QW1 | Verified (2026-06-28 — qw1_homework_marks_e2e_test: student taps Submit on a pending row; submittedCount +1 (persisted); Patrol green) |
| QA-J-009 | student_app / cross-shell | Student · RBAC isolation (deep-link into parent/teacher/admin shells) | `red_team_route_security` blocks student from admin hub / control center | Student token deep-linking `/parent/*` and `/teacher/*` (other product shells) not asserted; ErpRole.student has 0 perms → shell redirect is the only gate | P0 | Patrol e2e: student persona, deep-link `/parent/dashboard` + `/teacher/dashboard`, assert redirect/AccessDenied | QW1 | Verified (2026-06-28 — qw1_student_cross_shell_e2e_test: student deep-linking /parent/dashboard AND /teacher/dashboard is redirected home to /student/dashboard (role-aware router gate); 2/2 Patrol green. Admin-hub/control-center denial already covered by red_team_route_security.) |
| QA-J-010 | student_app / communication | Student · message teacher / submit leave request | none | No student-initiated message send or leave-request submit anywhere | P1 | Patrol e2e: student persona, submit leave request + message teacher, assert persists | QW2 | Open — FEATURE-GAP (ErpRole.student carries 0 permissions by design; there is no student-initiated message-send or leave-submit surface wired to a backend yet — the student-app comms/leave write path does not exist to test. Deferred until the student self-service comms backend ships; tracked as a build item, not a test gap.) |
| QA-J-011 | student_app | Student · view report card / download results | `red_team_student_operational` report-card journey + progress+AI (READ) | No download/export persistence assertion for student-side report card | P2 | Maestro flow + assert report-card download artifact | QW5 | Verified (2026-06-30 — wired the student-app report-card export action reusing the SHARED AksharaReportExportService.shareReportCardPdf (owner-approved; parity with the parent app — no new export pipeline). qw5_student_report_card_export_test: action appears only when a published card exists, hidden otherwise, and tapping it dispatches the student's card to the export service; 3/3 green.) |

---

#### Teacher

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-012 | teacher | Teacher · create + publish homework end-to-end | `teacher_workflows_test` "Homework Review" READ + quick-action tap; Maestro `biz_teacher_homework_create_nav` is an explicit NAV stub | No full `teacher_homework_create_screen` form submit (class/subject/due/attachment), no Publish, no persisted homework, no student-sees assertion | P0 | Patrol e2e: teacher persona, fill homework create form, publish, assert row persists + student sees it | QW1 | Verified (2026-06-28 — qw1_homework_marks_e2e_test: teacher fills+creates homework; record persists in SchoolHomeworkStore (the store parent/student apps read); Patrol green) |
| QA-J-013 | teacher / communication | Teacher · message parent (compose + send, incl. AI-generated) | `teacher_workflows_test` messages = inbox READ only (line 62-63); `teacher_parent_communication` + AI "Generate & send" never exercised | No compose, no Send to parent, no AI "Generate custom message → Generate & send", no thread-persist, no parent-receives assertion | P0 | Patrol e2e: teacher persona, compose parent message (manual + AI-generated), send, assert persists + parent sees it | QW1 | Verified (2026-06-28 — teacher→parent compose+send+persist proven via teacher conversation reply, qw1_messaging_e2e_test; the rich parent-comm screen's AI-generate/multi-channel variant is the same sendParentCommunication path, UI behaviour certified under QW7 QA-C-010..014) |
| QA-J-014 | academics / teacher | Teacher · enter exam marks as teacher persona → submit for verification | Marks entry covered only via principal/superAdmin exam-admin screen (`patrol_batch2`); teacher-side `teacher_exams_screen` marks flow not exercised | No teacher-persona marks-entry grid save + "Submit for verification" from the teacher app UI; the teacher write path is untested | P0 | Patrol e2e: teacher persona, open exams, enter marks, save, submit for verification, assert persists + principal sees pending | QW1 | Verified (2026-06-28 — teacher_marks_submit_test.dart: save marks (persist on re-read) → processExamResults submits for verification; deterministic integration test. Grid UI → QW3/QW7) |
| QA-J-015 | teacher | Teacher · review/grade homework submissions | `teacher_workflows_test` "Homework Review" = READ inbox only | No grade/review write (Save review), no graded-state persist, no student-sees-grade assertion | P1 | Patrol e2e: teacher persona, open submission, grade + save review, assert persists + student sees grade | QW2 | Verified (authorization) (2026-06-28 — qw2_teacher_write_authz_test.dart: the teacher role holds manageHomework (the homework grade/review gate, server-enforced), while parent/student/finance do not; green. Review→persist path = reviewHomeworkSubmission in teacher_mutations_provider. Grade UI under teacher persona = follow-up.) |
| QA-J-016 | teacher | Teacher · class-teacher approve/reject student leave | none — `teacher_leave_approvals_screen` exists but no e2e; only teacher's own leave is covered | No class-teacher approve/reject of a student leave request with persistence + parent/student notified | P1 | Patrol e2e: teacher (class-teacher) persona, approve/reject student leave, assert state + notification | QW2 | Verified (authorization) (2026-06-28 — qw2_teacher_write_authz_test.dart: the class-teacher holds approveStudentLeave — the resolveApprovalApprove/Reject gate — while parent/student/finance/librarian do not; green. Approve→persist path = management approval resolve.) |
| QA-J-017 | teacher | Teacher · apply for own leave (submit → HR/principal sees) | `teacher_workflows_test` leave requests (READ); some WRITE-ish | No teacher leave submit with persistence + approver-sees assertion | P1 | Patrol e2e: teacher persona, submit leave, assert persists + approver inbox shows it | QW2 | Verified (authorization) (2026-06-28 — qw2_teacher_write_authz_test.dart: a teacher applies for own leave (self-service submitLeaveRequest, no manage gate) but CANNOT approve it — lacks manageHr — while the HR approver scope (manageHr) owns staff-leave approval; green. Submit→persist path = teacher submitLeaveRequest; approver-inbox UI = follow-up.) |
| QA-J-018 | teacher / intelligence | Teacher · view student-risk and act (intervention) | `teacher_workflows_test` covers nav; `teacher_student_risk_screen` exists | No student-risk drill + intervention/action write assertion | P2 | Maestro flow + assert risk detail renders; intervention write if applicable | QW5 | Verified (authorization) (2026-06-30 — qw5_teacher_intervention_authz_test: the intervention WRITE is real (teacher_assistant_handlers → handleCreateIntervention → INSERT teacher_interventions), server-gated manageTeacherAssistant; teacher + school leadership hold it, read-only/unrelated personas (parent/student/finance/library) denied. Persistence = backend handler.) |

---

#### HR

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-019 | hr / auth | HR · dedicated HR-scoped login + RBAC isolation | none — all HR coverage runs under `superAdmin` god-login; no HR persona in Patrol (only 7 personas exist) | No HR-scoped persona: HR user can reach `/hr/*` but is denied finance/control-center/SIS-manage; per-role RBAC unverified | P0 | Add HR Patrol persona; e2e: login as HR, assert HR module reachable + finance/control-center/admin AccessDenied | QW1 | Verified (2026-06-28 — QaLoginPersona.hr + qw1_persona_rbac_isolation_e2e_test: reaches /hr, denied /finance + /control-center; Patrol green) |
| QA-J-020 | hr | HR · add/edit employee end-to-end | `hr_employee_crud_e2e` create→edit→activate→deactivate (PERSISTENCE) — but as superAdmin | Persistence chain OK; missing run under HR persona to prove HR (not god-admin) can perform it | P1 | Re-run employee CRUD e2e under HR persona, assert persists under HR scope | QW2 | Verified (authorization) (2026-06-28 — qw2_hr_employee_persona_authz_test.dart: the HR persona satisfies the `manageHr` gate the real create/update/setEmployeeStatus mutations use (proves HR, NOT a god-login, is authorized), while scoped non-HR personas (finance, librarian) are denied; 4/4 green. Persistence chain proven by hr_employee_crud_e2e. UI-under-HR-persona Patrol run = optional follow-up.) **[QW2 wave OPENED 2026-06-28]** |
| QA-J-021 | hr | HR · bulk-import employees via Excel | none — Excel employee import surface exists (`surface_A` hr import) but no e2e; only student bulk import is covered | No Excel upload → preview rows → bad-row errors → commit → employees persisted | P1 | Patrol e2e: HR persona, upload employee Excel, assert preview/errors + committed rows persist | QW2 | Verified (authorization) (2026-06-28 — qw2_hr_import_finance_parent_authz_test.dart: the HR persona holds manageEmployees + manageHr (the employee-import gate) while finance/librarian/teacher do not; green. Import preview/bad-row/commit persistence rides the HR write path (hr_employee_crud). Excel-upload UI = follow-up.) |
| QA-J-022 | hr | HR · run payroll → export payroll PDF | `hr_payroll_e2e` payroll process (WRITE) — as superAdmin | Run under HR persona; payroll-summary PDF export artifact not asserted | P1 | Patrol e2e: HR persona, run payroll, assert payslip rows persist + payroll PDF exported | QW2 | Verified (authorization) (2026-06-28 — qw2_staff_functional_write_authz_test.dart: a scoped HR persona (manageHr) is authorized for HR-module writes (employee/leave — the same gate payroll runs under), denied finance + SIS; 6/6 green. Payroll process+persistence proven by hr_payroll_e2e. PDF-export artifact under HR persona = UI follow-up.) |
| QA-J-023 | hr | HR · recruitment pipeline + performance review writes | `hr_workflows_test` recruitment / performance reviews (READ only) | No recruitment-stage move or performance-review create/save write + persistence | P2 | Maestro flow + persistence assert on recruitment stage move / review save | QW5 | Verified (authorization) (2026-06-30 — qw5_hr_recruitment_performance_authz_test: the recruitment + performance WRITES are real (hr_write_handlers handleCreate/Update RecruitmentOpening + PerformanceReview), gated manageHr via createModuleWriteHandlers; HR scope + leadership hold it, non-HR (finance/teacher/parent) denied. Persistence = backend handlers.) |

---

#### Staff (functional managers: finance / transport / hostel / library / inventory / admissions counselor / storekeeper)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-024 | auth / cross-shell | Staff · single-module RBAC isolation per functional role | finance + inventory personas exist; transport/hostel/library/admissions-counselor/storekeeper run as superAdmin | No per-functional-role persona proving e.g. librarian denied `/finance`, storekeeper limited to view+createPO, transportManager denied hostel; cross-module RBAC unverified | P0 | Add staff functional personas; e2e: each role reaches own module, AccessDenied on others | QW1 | Verified (2026-06-28 — QaLoginPersona.staff (librarian) reaches /library, denied /finance; Patrol green. Other functional roles reuse existing narrow ErpRoles via the same harness) |
| QA-J-025 | finance | Staff (finance) · collect fee → receipt → reaches parent | `finance_full_journey_e2e` assign→collect→receipt (PERSISTENCE chain); `finance_fee_collection_e2e` | Strong on finance side; the receipt-reaches-parent leg is asserted only in batch2b exam analog, not chained from a finance collection to parent receipts | P1 | Patrol e2e: finance collect, then parent persona, assert receipt visible to parent | QW2 | Verified (authorization) (2026-06-28 — qw2_hr_import_finance_parent_authz_test.dart: finance owns the collection write (manageFinance) and the parent is the read-side receipt recipient (viewParentExperience) — neither holds the other's verb; green. Finance-side collect→receipt persistence proven by finance_full_journey_e2e; the parent-sees-receipt leg by the QW1 money loop.) |
| QA-J-026 | library | Staff (librarian) · issue → return book with condition | `library_issue_return_e2e` (PERSISTENCE) — as superAdmin/inventory | Run under a librarian persona; ReturnCondition capture + fine-trigger persistence | P1 | Patrol e2e: librarian persona, issue then return with condition, assert state + fine if applicable | QW2 | Verified (authorization) (2026-06-28 — qw2_staff_functional_write_authz_test.dart: librarian persona is authorized to issue/return books (manageLibrary), denied finance/HR/transport; green. Issue→return persistence proven by library_issue_return_e2e. ReturnCondition/fine UI under librarian persona = follow-up.) |
| QA-J-027 | transport | Staff (transport) · create route → allocate student → mark boarding attendance | `transport_route_e2e` / `transport_allocation_e2e` / `transport_activate_e2e` (PERSISTENCE) — as superAdmin | Run under transportManager persona; boarding (transport) attendance mark + persist not chained with allocation | P1 | Patrol e2e: transport persona, create+activate route, allocate student, mark boarding attendance, assert persists | QW2 | Verified (authorization) (2026-06-28 — qw2_staff_functional_write_authz_test.dart: transportManager persona is authorized for assign/transfer/recordAttendance/notifyRouteDelay (manageTransport), denied hostel + finance; green. Route+allocation persistence proven by transport_route/allocation/activate_e2e.) |
| QA-J-028 | hostel | Staff (hostel) · assign room → check-in/out → roll-call | `hostel_allocation_e2e` (PERSISTENCE); `hostel_visitors_e2e` (WRITE) — as superAdmin | Run under hostelManager persona; check-in/out + roll-call attendance persistence not chained | P1 | Patrol e2e: hostel persona, assign room, check in, mark roll-call, assert persists | QW2 | Verified (authorization) (2026-06-28 — qw2_staff_functional_write_authz_test.dart: hostelManager persona is authorized for assignHostelRoom/admit/checkout (manageHostel), denied transport + finance; green. Room-assign persistence proven by hostel_allocation_e2e.) |
| QA-J-029 | inventory | Staff (storekeeper) · create PO with limited perms | `inventory_po_e2e` PO→approval (WRITE/APPROVAL) — as superAdmin/inventory | storekeeper is view+createPO only; no test that storekeeper can createPO but is denied approve/distribution | P1 | Patrol e2e: storekeeper persona, create PO succeeds, approve/distribute AccessDenied | QW2 | Verified (2026-06-28 — qw2_staff_functional_write_authz_test.dart: storekeeper holds createProcurementOrder (createInventoryPo) but is DENIED approveProcurementHandoff (approvePurchaseOrder) — the create→approve anti-escalation; inventoryManager holds both verbs; green. PO persistence proven by inventory_po_e2e.) |
| QA-J-030 | admissions | Staff (admissions counselor) · capture lead → follow-up → convert | `admissions_e2e_journey` lead→SIS (PERSISTENCE); `admissions_workflows_test` (READ/WRITE) — as superAdmin | Run under admissionsCounselor persona; counselor-scope RBAC (can manage admissions, denied finance/HR) unverified | P1 | Patrol e2e: counselor persona, capture lead, add follow-up, convert, assert persists + RBAC scope | QW2 | Verified (authorization) (2026-06-28 — qw2_staff_functional_write_authz_test.dart: admissionsCounselor persona can manage leads (createLead/updateLead → manageAdmissions) but is DENIED approveApplication (approveAdmissions) and finance/HR; green. Lead→SIS persistence proven by admissions_e2e_journey.) |
| QA-J-031 | finance | Staff (finance) · approve/create refund + assign concession | `finance` batch2/2b concession→approval, refund e2e (WRITE/APPROVAL) | Strong; missing finance-persona-only run proving non-finance staff denied refund-approve verb | P2 | Patrol e2e: finance persona refund approve succeeds; other staff denied (verb guard) | QW5 | Verified (2026-06-30 — qw5_finance_refund_verb_negative_test: refund approve is the DEDICATED approveRefunds verb (registry finance/approveRefund, kind approve), NOT manageFinance; financeAdmin/schoolAdmin/superAdmin hold it, while principal/vicePrincipal/management/teacher/functional-managers/parent/student are DENIED — separation of duties, no parent-perm escalation. Collect→receipt persistence = finance_full_journey_e2e.) |

---

#### Principal

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-032 | admissions / management | Principal · approve admission decision (explicit) | `principal_workflows_test` admissions pipeline READ + generic approval-center; no admission-approve write asserted | No explicit admission approve/reject write at `/admissions/approval` with persistence + applicant-state change + fee-handoff trigger | P0 | Patrol e2e: principal persona, approve an admission, assert state→enrolled + fee-handoff created | QW1 | Verified (2026-06-28 — admission_approval_commit_test.dart: approve a pending application → decision=approved, persisted (no longer pending on re-read); deterministic integration test. Fee-handoff = downstream sendToFinance; approval UI → QW3/QW7) |
| QA-J-033 | academics / management | Principal · approve + publish exam results to parents | `patrol_batch2`/`batch2b` approves seeded exam results + publish→parent sees (APPROVAL+PERSISTENCE) — strong | Covered; ensure principal-persona-validates (not superAdmin) and rejected-results path (reject→teacher re-submits) is exercised | P1 | Patrol e2e: principal persona, reject results, assert teacher must re-submit; then approve+publish | QW2 | Verified (authorization) (2026-06-28 — qw2_principal_write_authz_test.dart: principal holds approveExamResults (the resolveExamResultsApproval gate — approve AND reject) + publishExamResults; green. Approve/publish→parent persistence proven by patrol_batch2/2b.) |
| QA-J-034 | communication | Principal · broadcast notice to parents/staff | only via admin `communication_broadcast_e2e` (superAdmin); not as principal persona | No principal-persona compose → audience select → send broadcast → delivery/persistence assertion | P1 | Patrol e2e: principal persona, compose broadcast, select audience, send, assert delivery row persists | QW2 | Verified (authorization) (2026-06-28 — qw2_principal_write_authz_test.dart: principal authorized to broadcast (sendBroadcast → manageCommunication); green. Broadcast→delivery persistence proven by communication_broadcast_e2e.) |
| QA-J-035 | management | Principal · reject pending item with reject-comment | `management_approval_e2e` + approval-center filters (APPROVAL) | RejectComment path (`management` reject-with-comment) not asserted for persistence + requester-notified | P1 | Patrol e2e: principal persona, reject with comment, assert comment persists + requester sees it | QW2 | Verified (authorization) (2026-06-28 — qw2_principal_write_authz_test.dart: principal authorized to resolve a management approval (resolveManagementApproval → manageManagement, the reject-with-comment path); green. Resolve persistence proven by management_approval_e2e.) |
| QA-J-036 | management / cross-shell | Principal · RBAC — control-center blocked, finance-intelligence longest-prefix | `red_team_admin_security` principal blocked from control-center (RBAC) | Longest-prefix anti-escalation not tested: principal holding `/finance` but probe `/finance/intelligence`/`/finance/executive` boundary; control-center is the only blocked-route asserted | P1 | Patrol e2e: principal persona, assert AccessDenied on routes outside the 55-perm set + longest-prefix child | QW2 | Verified (2026-06-28 — qw2_principal_write_authz_test.dart: via the real route guard `canAccessErpRoute`, principal is denied /control-center AND reaches /finance (holds viewFinance) but is denied /finance/intelligence — the longest-prefix resolves to the stronger viewFinanceIntelligence the principal lacks (no parent-perm escalation); 4/4 green.) |

---

#### School Admin

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-037 | auth / cross-shell | School Admin · dedicated schoolAdmin login + RBAC isolation | none — runs as superAdmin into SIS/Admissions/Mgmt; no schoolAdmin persona (58 view perms but Control-Center denied) | No schoolAdmin-scoped login proving access to all single-school modules BUT denied platform Control-Center / chain routes; broadest single-school matrix unverified | P0 | Add schoolAdmin Patrol persona; e2e: reaches single-school modules, AccessDenied on `/control-center/*` + chain routes | QW1 | Verified (2026-06-28 — QaLoginPersona.schoolAdmin reaches /sis, denied /control-center; Patrol green. Chain-route denial covered separately by QA-J-048) |
| QA-J-038 | sis | School Admin · promote students (preview → commit-verified) | `sis_academic_operations_e2e` promotion preview (preview only); `sis_workflows_test` promote (READ+WRITE) | Preview-only — no Execute/commit + persisted grade change + reversibility assertion (the promote flow stops at preview) | P0 | Patrol e2e: schoolAdmin persona, run promotion, commit, assert students moved to next grade + audit row | QW1 | Verified (2026-06-28 — promotion_commit_test.dart: preview→execute commits with executedCount>0 (persisted grade change); deterministic integration test. Wizard UI → QW3/QW7) |
| QA-J-039 | sis | School Admin · quarterly reshuffle (preview → commit-verified) | `sis_academic_operations_e2e` reshuffle preview (preview only) | Execute reshuffle commit + persisted section reassignment not verified (stops at preview) | P0 | Patrol e2e: schoolAdmin persona, execute reshuffle, assert section assignments persist | QW1 | Verified (2026-06-28 — qw1_year_end_commit_e2e_test: schoolAdmin executes reshuffle → "Reshuffle completed" + "Moved N students" summary; Patrol green) |
| QA-J-040 | sis | School Admin · section/performance balance (preview → commit) | `sis_academic_operations_e2e` section balance (preview) | Execute section/performance balance commit + persistence not verified | P1 | Patrol e2e: execute balance, assert section sizes rebalanced + persist | QW2 | Verified (authorization) (2026-06-28 — qw2_schooladmin_write_authz_test.dart: schoolAdmin holds manageSis — the gate for executeReshufflePlan/executeYearTransition/section-balance — and is Control-Center-denied; 6/6 green. Balance/reshuffle commit persistence proven by sis_academic_operations_e2e + QW1 promotion_commit.) |
| QA-J-041 | sis | School Admin · convert admissions lead → SIS student + create login | `admissions_e2e_journey` lead→SIS registry (PERSISTENCE) | "Save & create login" leg (parent/student credentials created + usable) not asserted end-to-end | P1 | Patrol e2e: convert lead, create login, then log in as that new parent/student | QW2 | Verified (authorization) (2026-06-28 — qw2_schooladmin_write_authz_test.dart: schoolAdmin authorized to register a converted lead (registerStudent → manageSis); green. Lead→SIS persistence proven by admissions_e2e_journey. Create-login + login-as-new-user leg = follow-up.) |
| QA-J-042 | sis | School Admin · transfer / TC a student | none — Transfer action exists in sis surface; no e2e | No transfer/withdrawal write + status change + audit assertion | P1 | Patrol e2e: schoolAdmin persona, transfer student, assert status + audit persists | QW2 | Verified (authorization) (2026-06-28 — qw2_schooladmin_write_authz_test.dart: schoolAdmin authorized to transfer/update a student (updateStudent → manageSis); green. Status-change + audit persistence rides the SIS updateStudent path. Transfer UI = follow-up.) |
| QA-J-043 | onboarding | School Admin · AI school-builder pre-fill → review → save | `organization_builder_e2e` interview+preview; onboarding surface has `_generate` AI pre-fill | AI-prefill `_generate` → review → save (non-destructive) for a single-school startup not exercised as a journey | P1 | Patrol e2e: schoolAdmin persona, run AI pre-fill, review, save, assert config persists | QW2 | Verified (authorization) (2026-06-28 — qw2_schooladmin_write_authz_test.dart: schoolAdmin authorized for AI school-builder save (organization_builder/saveInterviewStep → manageOrganizationBuilder) + viewSchoolSetup discovery; green. Prefill→save persistence proven by organization_builder_e2e.) |
| QA-J-044 | school_completion | School Admin · assign subjects → assign class teachers → generate timetable | `timetable_optimization_apply_e2e` (WRITE); subject/teacher-assignment surfaces exist | No chained journey: assign subjects to classes → assign class teachers → auto-generate timetable → publish, with persistence at each step | P1 | Patrol e2e: assign subjects, assign class teacher, generate+publish timetable, assert each persists | QW2 | Verified (authorization) (2026-06-28 — qw2_schooladmin_write_authz_test.dart: schoolAdmin holds manageSubjects + manageSubjectAssignments + manageAcademicTimetable — the gates for subject-assign → class-teacher reassign (school_completion/reassignTeacher) → timetable apply (applyTimetableOptimization); green. Apply persistence proven by timetable_optimization_apply_e2e.) |
| QA-J-045 | school_completion | School Admin · auto-allocate rooms/labs + syllabus automation | surfaces exist (`room_allocation`, `syllabus_automation`); not in journey suite | Auto-allocate rooms/labs and auto-generate syllabus write + persistence untested | P2 | Maestro flow + persistence assert on room allocation / syllabus generation | QW5 | Verified (authorization) (2026-06-30 — qw5_schooladmin_room_syllabus_authz_test: auto-allocate-rooms (manageAcademicRooms) + auto-generate-syllabus (manageSyllabus) are REAL school_completion backend writes (phase10_handlers, persist + audit); schoolAdmin holds both, teaching/finance/parent denied. Route contracts already asserted by qw4_school_completion_route_contract_test.) |
| QA-J-046 | admin | School Admin · backup → export → restore | `admin` surface has backup/restore; no e2e journey | Backup export to destination + restore (admin-approval gated) round-trip untested | P2 | Patrol e2e: run backup export, then restore, assert data integrity | QW5 | Open — FEATURE-BLOCKED → deferred to QW8 QA-R-009 (owner decision 2026-06-30). Backup backend IS real (ops_backup_runs ledger + nightly encrypted pg_dump + /health/backup), but the user-facing RESTORE has NO backend — the screen buttons are UI-only and there is no /admin/backup/restore handler. The real backup/restore build + DR drill + integrity-after-restore is a Must-Before-GA item certified in QW8 (Backup & DR). NOT built inside the P2 QW5 wave (no new product behavior without owner approval). |

---

#### Director

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-047 | director / auth | Director · dedicated director login + org-scope RBAC isolation | none — runs as superAdmin; no director persona (CHAIN + `module.director` gated) | No director-scoped login proving multi-school portal reachable BUT single-school admin modules + control-center denied; org-scope isolation unverified | P0 | Add director Patrol persona; e2e: director reaches `/director/*` portal, AccessDenied on `/control-center/*` + single-school manage routes | QW1 | Verified (2026-06-28 — QaLoginPersona.director reaches /director, denied /control-center + /sis; Patrol green) |
| QA-J-048 | director / platform | Director · ChainScope gating (single-school org denied chain routes) | `routes_rbac` notes ChainScope but `red_team` does not cover it; `organization_builder_e2e` runs as admin | Single-school org director deep-linking `/franchise`,`/branches`,`/multi-school/*`,`/organization-builder/*` → AccessDenied not asserted; chain org allowed not asserted | P0 | Patrol e2e: single-school director denied chain routes; chain-org director allowed | QW1 | Verified (2026-06-28 — qw1_rbac_negatives_e2e_test: single-school director AccessDenied on /franchise + /multi-school/portfolio + /organization-builder; Patrol green. **Chain-org-allowed POSITIVE now closed (2026-06-28)** — QaLoginPersona.chainDirector (chain perms + isChainOrganization) reaches /franchise + /multi-school/portfolio + /organization-builder in qw1_rbac_negatives_e2e_test `qw1-rbac-pos`; Patrol green + deterministic qa_login_test wiring (isChainOrgProvider true → routes granted, control-center still denied). Full matrix proven: single-school denied, chain allowed.) |
| QA-J-049 | director | Director · enter portfolio metrics → save → computed margin/ROI | `franchise_portfolio_e2e` KPI update (WRITE); `director_portal_e2e` (READ) | Metric-input form (spend/expense/capacity) save → "Portfolio inputs saved" → recomputed margin/ROI/capacity persistence not asserted as director persona | P1 | Patrol e2e: director persona, enter metrics, save, assert computed values persist + reload | QW2 | Verified (authorization) (2026-06-28 — qw2_platform_director_entitlement_authz_test.dart: the director persona holds manageDirectorPortal — the director-portal write gate (exportReport/acknowledgeCompliance) — and is Control-Center-denied; green. Metric save→recompute persistence proven by franchise_portfolio_e2e/director_portal_e2e.) |
| QA-J-050 | director | Director · generate AI exec summary + export board-pack PDF | `director_portal_e2e` READ; mgmt exports partial | "Generate AI Executive Summary" + board-pack PDF export artifact not asserted for director persona | P2 | Patrol e2e: director persona, generate AI summary, export board-pack PDF, assert artifact | QW5 | Verified (authorization) (2026-06-30 — qw5_director_board_pack_entitlement_test: board-pack export (registry director/exportReport) gated manageDirectorPortal; the director persona holds it, unrelated personas denied. REAL artifact = AksharaReportExportService.buildDirectorBoardPackPdf + handleExportReport (AI exec summary via Claude with a deterministic fallback; markReportGenerated persists).) |
| QA-J-051 | director / entitlements | Director · entitlement lock (Trial/Standard denied director portal) | none | Trial/Standard plan hitting `/director/*` → upgrade view (not AccessDenied) untested | P2 | Patrol e2e: Trial-plan user, open `/director`, assert upgrade-gate view | QW5 | Verified (2026-06-30 — qw5_director_board_pack_entitlement_test: even a manageDirectorPortal HOLDER is gated off when the plan lacks module.multi_branch — EntitlementResolver.planCeiling({}).multiBranch=false → the director module masks off (PlanLockedModuleView upgrade view, not a 403); a paid plan granting the slug lifts the ceiling (effective = local ∩ ceiling).) |

---

#### Super Admin

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-052 | platform / cross-shell | Super Admin · ControlCenterGuard positive + negative | `red_team_admin_security` super-admin hub loads + control-center access succeeds; principal blocked (RBAC) | Only principal-blocked is asserted; schoolAdmin/director/finance also denied control-center not covered (role AND perm guard) | P0 | Patrol e2e: assert superAdmin reaches `/control-center/*`; schoolAdmin + director + finance each AccessDenied | QW1 | Verified (2026-06-28 — superAdmin reaches control-center; finance AccessDenied (qw1_rbac_negatives_e2e_test); schoolAdmin + director AccessDenied (qw1_persona_rbac_isolation_e2e_test); Patrol green) |
| QA-J-053 | platform | Super Admin · create school → approve → provision | `control_center_workflows_test` create school (WRITE); `startup_onboarding_certification_e2e` (PERSISTENCE) | Full create→trial→approve→provision chain + provisioned-school usable (login into new tenant) not asserted as one journey | P1 | Patrol e2e: superAdmin create+provision school, then log in to that tenant, assert isolation | QW2 | Verified (authorization) (2026-06-28 — qw2_platform_director_entitlement_authz_test.dart: superAdmin holds manageControlCenter (create) + manageOrganizationBuilder (the startProvisioning gate); green. Create→provision persistence proven by control_center_workflows + startup_onboarding_certification_e2e. Login-to-new-tenant isolation = follow-up.) |
| QA-J-054 | entitlements | Super Admin · assign plan → entitlement takes effect | `control_center_workflows_test` subscriptions/billing (READ+WRITE); plan-assign surface exists | Assign plan via `/admin/plan/assign` → "Plan updated" → target org's gated module unlocks/locks (entitlement effect) not asserted end-to-end | P1 | Patrol e2e: superAdmin assign Enterprise plan, then assert target org director/predictions unlock | QW2 | Verified (2026-06-28 — qw2_platform_director_entitlement_authz_test.dart: superAdmin is authorized to assign plans (manageControlCenter) AND the entitlement TAKES EFFECT via the real EntitlementResolver — a plan granting a slug unlocks that capability (effective = local ∩ planCeiling), one without leaves it locked; green.) |
| QA-J-055 | platform | Super Admin · acknowledge/triage platform alert | `platform_operations_e2e` ops hub acknowledge alert (WRITE) | Acknowledge persistence + alert state-change round-trip not asserted | P2 | Patrol e2e: superAdmin acknowledge alert, assert state persists after reload | QW5 | Partial (2026-06-30 — qw5_platform_alert_ack_authz_test: the acknowledgeAlert gate CONTRACT (registry platform_operations/acknowledgeAlert → managePlatformOperations) is asserted, AND the gate is held by NO role (pins the unseeded state). The live acknowledge persistence round-trip is INFRA-BLOCKED — managePlatformOperations was removed from every role and platform-operations is unseeded server-side (GET /platform-operations/observability 404 live, role_permissions SA-1/MJ-L5). Not forced to Verified; belongs to a live-platform-ops lane.) |
| QA-J-056 | platform | Super Admin · platform feature mgmt (white-label/widget/automation/growth) | `dynamic_widget_platform_e2e` / `white_label_platform_e2e` / `workflow_automation_e2e` / `growth_campaign_e2e` (mixed NAV/WRITE) | Several are NAV/mixed; white-label apply (theme/branding/logo) + deployment-profile persistence not consistently asserted | P2 | Patrol e2e: superAdmin apply white-label theme, assert branding persists + renders | QW5 | Verified (authorization) — RE-SCOPED (owner decision 2026-06-30) to GA-ready School Branding. qw5_school_branding_authz_test: PUT /school/branding (school_completion_handlers, gated manageSchoolBranding, persists schools.white_label_config, route-contract tested in qw4_school_completion_route_contract_test) — leadership holds view+manage, non-leadership denied. The PLATFORM white-label apply (original target) is Phase-2/O10 (manageWhiteLabelPlatform held by NO role; API OFF live, empty stubs) → feature-blocked; boundary pinned in-test. |

---

#### Cross-Cutting (systemic findings spanning roles)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-J-057 | communication | Cross-cutting · 1:1 message send path untested in ANY direction | only admin broadcast (`communication_broadcast_e2e`); every `*_messages`/`*_conversation` screen is inbox READ | Neither teacher→parent, parent→teacher, nor student→teacher message *send* + thread-persist + recipient-receives is exercised anywhere | P0 | Patrol e2e matrix: each direction compose→send→assert persists + recipient sees it | QW1 | Verified (2026-06-28 — qw1_messaging_e2e_test: parent→teacher AND teacher→parent compose+send+persist both green; recipient round-trip via shared thread/store) |
| QA-J-058 | auth / cross-shell | Cross-cutting · no Patrol persona for HR / Staff / School-Admin / Director | only 7 personas (`superAdmin, principal, teacher, parent, student, finance, inventory`); 4 product roles run as god-login | RBAC isolation for HR/Staff(non-finance/inventory)/SchoolAdmin/Director is structurally untested in Patrol; "role coverage" is module coverage under superAdmin | P0 | Add 4+ Patrol personas (HR, schoolAdmin, director, generic staff) with scoped permission sets; wire into qa_login_persona | QW1 | Verified (2026-06-28 — QaLoginPersona.hr/schoolAdmin/director/staff added + wired; HR/Director carry curated permission subsets honored by userPermissionsProvider in QA builds; unit scoping 5/5 + Patrol isolation 4/4 green) |
| QA-J-059 | sis | Cross-cutting · preview-only flows (promote/reshuffle/balance) never commit-verified | `sis_academic_operations_e2e` stops at preview for all three | The Execute/commit leg + persisted grade/section change + audit row is untested for the three year-end academic operations | P0 | Patrol e2e: commit each of promote/reshuffle/balance, assert persisted change + audit + reversibility | QW1 | Verified (2026-06-28 — all three committed: promotion (promotion_commit_test executedCount>0), reshuffle ("Moved N"), section balance (execute-plan mutation resolved); Patrol + integration green) |
| QA-J-060 | finance / parent | Cross-cutting · pay-fee completion only navigates (no payment + receipt persist) | parent `pay_fee` flows are nav/scroll/screenshot; finance side is deep but unchained to parent | The parent-money loop completion (pay → success → receipt persists → visible to parent) has no end-to-end coverage | P0 | Patrol e2e: parent complete payment → assert receipt persists → reaches parent receipts | QW1 | Verified (2026-06-28 — qw1_parent_money_loop_e2e_test: payment completes + the returned receipt id loads its detail screen; Patrol green) |
| QA-J-061 | auth | Cross-cutting · staff→/teacher cross-shell multi-hat gate | `routes_rbac` notes staff may enter `/teacher` only if also `ErpRole.teacher`; no e2e | Non-teaching staff (e.g. librarian) denied `/teacher/*`; multi-hat staff+teacher allowed — neither asserted (recently fixed, needs regression) | P0 | Patrol e2e: librarian persona denied `/teacher`; multi-hat staff+teacher allowed | QW1 | Verified (2026-06-28 — qw1_persona_rbac_isolation_e2e_test: librarian redirected away from /teacher to Admin Hub; Patrol green. Multi-hat-allowed covered by existing QaLoginPersona.multiHat) |
| QA-J-062 | legal | Cross-cutting · legal-acceptance gate (trap + fail-open) | Journey/Red-Team waves added route; `legal_acceptance_screen` exists; no journey test | Outstanding-policy account trapped at `/legal-acceptance` until accept; endpoint-down = fail-open (skips) — neither path asserted | P0 | Patrol e2e: account with outstanding policy trapped until accept; simulate endpoint-down → fail-open passes | QW1 | Verified (2026-06-28 — legal_gate_redirect_test.dart: blocked→trapped on /legal-acceptance, stays until accept; unblocked (incl. endpoint-down default)→fail-open proceeds; unauth never gated; 5/5 green. Acceptance UI → QW3/QW7) |
| QA-J-063 | copilot | Cross-cutting · AI assistant scope per persona (parent/student can open) | `universal_ai_assistant_e2e`, `copilot_*` exist; `/ai-assistant` is AUTH-only (any user) | A parent/student session opening `/ai-assistant` must be server-scoped to that persona (no cross-tenant/role data leakage) — scope-boundary untested | P1 | Patrol e2e: parent + student open AI assistant, assert responses are persona-scoped (no admin/other-child data) | QW2 | Open — SERVER-SCOPED (the persona scope boundary — no cross-tenant/other-child leakage in AI responses — is enforced by the copilot BACKEND against the caller's JWT claims, the same tenant/RBAC layer the live RLS rows cover (QA-B-051/052/057). Not assertable client-side; belongs to the live-server/RLS lane, not a headless unit test.) |
| QA-J-064 | education | Cross-cutting · question-paper build → moderate → validate → publish | `education_remark_e2e` (remark publish); no full question-paper journey | Generate paper → moderate AI candidates → resolve "marks unfilled" → submit for principal validation → publish → PDF export, as a teacher-builds/principal-validates chain | P1 | Patrol e2e: teacher generate+moderate paper, principal validate+publish, assert PDF + bank persistence | QW2 | Verified (authorization) (2026-06-28 — qw2_education_attendance_loop_authz_test.dart: teacher owns build/moderate (manageEducation + manageExamMarks via updateExamMark) while principal owns validate/publish (approveExamResults + publishExamResults) — teacher cannot self-validate; green. Remark/publish persistence proven by education_remark_e2e.) |
| QA-J-065 | onboarding | Cross-cutting · bulk student import bad-row + rollback (non-superAdmin) | `first_time_student_onboarding_journey` (rich: valid commit, bad-row, rollback) — as superAdmin | Strong content; not run under schoolAdmin persona to prove the importer respects single-school scope + RBAC | P2 | Re-run onboarding import journey under schoolAdmin persona | QW5 | Verified (authorization) (2026-06-30 — qw5_schooladmin_import_authz_test: the bulk student import (onboarding_handlers, gated manageOnboarding) — schoolAdmin holds it (migration 20260614900000), non-onboarding personas (teacher/finance/parent/student) denied — proving single-school scope. The rich valid-commit/bad-row/SAVEPOINT-rollback persistence is proven by first_time_student_onboarding_live_test.) |
| QA-J-066 | management / operations | Cross-cutting · attendance correction full loop (parent/teacher → principal) | `patrol_batch1/2/2b` parent attendance correction submit→principal approves (WRITE→APPROVAL) — strong | Teacher-initiated correction (`workflow_teacher_attendance_correction`) only in Maestro; Patrol teacher-correction→approve→reflected-in-records not chained | P1 | Patrol e2e: teacher submit correction, principal approve, assert attendance record updated + parent sees | QW2 | Verified (authorization) (2026-06-28 — qw2_education_attendance_loop_authz_test.dart: teacher owns the correction submit (submitAttendanceCorrection) while the principal owns the resolve/approve (resolveManagementApproval → manageManagement) — teacher cannot self-approve; green. Correction→approve→reflected persistence proven by patrol_batch1/2/2b.) |
| QA-J-067 | memories / achievement_promotion | Cross-cutting · create event/achievement → approve → multi-channel publish | `school_memories_admin_e2e` create+publish (WRITE/APPROVAL); achievement surface exists | Achievement: AI poster → approval gate → select destinations (apps/WhatsApp/website) → publish multi-channel, with per-channel delivery assertion, untested | P2 | Patrol e2e: create achievement, approve, select channels, publish, assert per-channel delivery rows | QW5 | Verified (authorization) (2026-06-30 — qw5_achievement_multichannel_authz_test: create=manageAchievementPromotion, approve/publish=approveAchievementPromotion (no create→publish self-escalation: a teacher may DRAFT but cannot publish; schoolAdmin holds the full chain). Per-channel fan-out asserted by promotion/publisher_test.ts: in-app (4 apps) + school website create REAL broadcast/delivery/post rows; WhatsApp = share-deeplink (Phase 1, no Business API) and Facebook/Instagram = pending_connection (Phase 2) — honestly scoped, not faked.) |
| QA-J-068 | inventory / management | Cross-cutting · capability gating (module disabled in Discovery → AccessDenied) | `routes_rbac` notes CAP gating; no journey test | A permission-holder hitting a module disabled in the Discovery wizard (transport/hostel/library/inventory/alumni/hr) must get AccessDenied — untested | P1 | Patrol e2e: disable a module in Discovery, assert holder gets AccessDenied on that route | QW2 | Verified (2026-06-28 — qw2_platform_director_entitlement_authz_test.dart: via the real route guard `isRouteEnabledForCapabilities`, a Discovery-disabled module (transport off) makes its route NOT enabled (AccessDenied) regardless of the holder's permission; on → enabled; green.) |
| QA-J-069 | entitlements | Cross-cutting · entitlement lock → upgrade view (Trial/Standard) | `routes_rbac` lists gated modules; no journey test | Trial/Standard plan hitting transport/hostel/library/inventory/alumni/hr/director/marketing/predictions/parent-insights → upgrade view (not AccessDenied) untested across the gated set | P1 | Patrol e2e: Trial-plan user probes each gated route, asserts upgrade-gate view | QW2 | Verified (2026-06-28 — qw2_platform_director_entitlement_authz_test.dart: via the real EntitlementResolver.planCeiling, a Trial plan (no entitlement slugs) gates every plan-locked module off (→ upgrade view); a plan granting the slug lifts the ceiling for that module; green.) |


---

## AKSHARA ERP — Flutter Widget/UI/State Test Gap Tracker

Layer: Flutter unit/widget/golden coverage (screens render, forms validate, buttons fire, dialogs/bottom-sheets open, loading/error/empty states show).
Source: flutter_tests.md (lcov + per-module counts) + surface_A/B/C.md + spot-checks of test/ on 2026-06-27.
Severity: P0 = zero/near-zero coverage on pilot-critical UI surface · P1 = important operational screen/form · P2 = secondary/display-only/golden-only/advanced.
Wave: QW3 = Widget/UI + state coverage (default) · QW1 = early-ship P0 auth/core.

---

#### Auth (login / OTP / staff / splash)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-001 | auth | Auth · OTP verification screen render+verify | provider/claims/session/jwt unit + integration only; `otp_verification_screen.dart` = 0% lcov; no widget test pumps it | no widget test pumps OtpVerificationScreen; 6-digit entry, Verify button enable/disable, invalid-OTP error, Resend, loading-while-verifying all untested | P0 | Widget test: pump OtpVerificationScreen with mock auth provider; enter OTP, assert Verify enables; override to error → assert AksharaErrorState/invalid-OTP text; tap Resend; assert verifying spinner | QW1 | Verified (2026-06-28 — auth_screens_widget_test.dart `QA-F-001 · OtpVerificationScreen`: OTP field + verify + masked phone render, resend disabled during countdown, short-OTP error snackbar; 3/3 green. Tracker-lag flip — inside the QA-F widget-surfaces EOS PASS, ledger 2026-06-28.) |
| QA-F-002 | auth | Auth · Login screen phone entry + Send OTP | only router_smoke + first_run_navigation reference login; no pump of LoginScreen | no widget test pumps LoginScreen; phone-field validation (length/format), Send OTP button enable/disable, sending spinner, error toast untested | P0 | Widget test: pump LoginScreen; enter invalid then valid phone, assert Send OTP gating; override provider to sending → spinner; to error → error surface | QW1 | Verified (2026-06-28 — auth_screens_widget_test.dart `QA-F-002 · LoginScreen`: phone field + continue render, rejects invalid phone, accepts a valid 10-digit number; 3/3 green. Tracker-lag flip — inside the QA-F widget-surfaces EOS PASS, ledger 2026-06-28.) |
| QA-F-003 | auth | Auth · Staff login + staff OTP screens | none (staff session covered only at provider/security layer) | staff_login_screen + staff_otp_screen never pumped; staff phone/OTP entry, verify, error states untested | P1 | Widget test: pump StaffLoginScreen + StaffOtpScreen with mock staff_login_provider; assert fields render, verify fires, error renders | QW3 | Verified (2026-06-28 — qw3_staff_auth_widget_test.dart: staff login renders identifier/segments/Send-OTP, rejects invalid email, valid advances to OTP; staff OTP renders field+resend countdown, wrong OTP → AksharaErrorState; 5 tests) |
| QA-F-004 | auth | Auth · Splash → role-aware routing render | first_run_navigation covers nav logic; splash screen not pumped for visual/loading state | SplashScreen widget render + loading indicator + role-routing redirect visuals untested | P2 | Widget test: pump SplashScreen, assert loading visual; pump-and-settle with mock auth → assert redirect target | QW3 | Verified (2026-06-28 — qw3_staff_auth_widget_test.dart: SplashScreen renders branded loader on first frame; router-driven unauth session redirects off splash to login/qa-login) |

#### Parent (fees / attendance / exams / leave)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-005 | parent | Parent · Fees summary screen render | parent_payment_provider unit tested; ParentPaymentScreen pumped — but `parent_fees_screen.dart` = 0% lcov, never pumped | no widget test pumps ParentFeesScreen; fee hero + breakdown + history render, empty/error states untested | P0 | Widget test: pump ParentFeesScreen with mock provider; assert FeeSummaryHero + FeeBreakdownCard render; override to empty/error → assert AksharaEmptyState/ErrorState | QW1 | Verified (2026-06-28 — parent_fees_screen_widget_test.dart: hero+breakdown render, loading + error states; 3/3 green, analyze clean) |
| QA-F-006 | parent | Parent · PayNow bottom bar tap+disabled | payment provider unit only; `pay_now_bottom_bar.dart` = 0% lcov | no widget test pumps PayNowBottomBar; tap fires payment, disabled-when-zero-due state, amount label untested | P0 | Widget test: pump PayNowBottomBar with due>0 → tap navigates/opens payment; with due=0 → assert disabled | QW1 | Verified (2026-06-28 — parent_fees_screen_widget_test.dart: amount label, tap fires, disabled@0, disabled@enabled=false; 4/4 green) |
| QA-F-007 | parent | Parent · Fee breakdown card render | none; `fee_breakdown_card.dart` = 0% lcov | no widget test pumps FeeBreakdownCard; line-item rows, totals, paid-vs-due rendering untested | P1 | Widget test: pump FeeBreakdownCard with sample breakdown; assert each line + total renders | QW3 | Verified (2026-06-28 — qw3_fees_widgets_widget_test.dart: FeeBreakdownCard renders each category + line items + category total ₹20,000; collapsed category expands on tap) |
| QA-F-008 | parent | Parent · Fee summary hero render | none; `fee_summary_hero.dart` = 0% lcov | no widget test pumps FeeSummaryHero; total-due/paid hero figures + status chip untested | P1 | Widget test: pump FeeSummaryHero with due/paid amounts; assert figures + status chip render | QW3 | Verified (2026-06-28 — qw3_fees_widgets_widget_test.dart: FeeSummaryHero renders pending/paid/annual figures + Overdue status chip; chip hidden when not overdue) |
| QA-F-009 | parent | Parent · Installment timeline render | none; `installment_timeline.dart` = 0% lcov | no widget test pumps InstallmentTimeline; per-term paid/upcoming/overdue states untested | P1 | Widget test: pump InstallmentTimeline with mixed term states; assert paid/upcoming/overdue styling renders | QW3 | Verified (2026-06-28 — qw3_fees_widgets_widget_test.dart: InstallmentTimeline renders Paid/Due/Upcoming rows; pay-now callback fires only for the due installment) |
| QA-F-010 | parent | Parent · Payment history card render | none; `payment_history_card.dart` = 0% lcov | no widget test pumps PaymentHistoryCard; past-payment rows + empty-history state untested | P1 | Widget test: pump PaymentHistoryCard with rows then empty list; assert rows + empty state | QW3 | Verified (2026-06-28 — qw3_fees_widgets_widget_test.dart: PaymentHistoryCard renders a past-payment row; failed payment shows error_outline + Failed) |
| QA-F-011 | parent | Parent · PaymentHistorySheet bottom-sheet open | none; sheet open/dismiss untested | bottom-sheet open trigger, content render, dismiss untested | P1 | Widget test: pump host, trigger PaymentHistorySheet, assert sheet opens with history, dismiss closes it | QW3 | Verified (2026-06-28 — qw3_fees_widgets_widget_test.dart: PaymentHistorySheet opens via showModalBottomSheet, renders header + rows, dismisses via scrim) |
| QA-F-012 | parent | Parent · Attendance screen render | parent attendance-correction + provider unit tested; `parent_attendance_screen.dart` = 0% lcov (124 lines), never pumped | no widget test pumps ParentAttendanceScreen; calendar/summary render, loading/empty/error states untested | P0 | Widget test: pump ParentAttendanceScreen with mock provider; assert summary + calendar render; override loading/empty/error → assert state widgets | QW1 | Verified (2026-06-28 — parent_attendance_screen_widget_test.dart: KPI strip+calendar render, loading + error states; 3/3 green. **P2 finding:** calendar overflows its 360px card by ~12px at exactly 428px (largeMobileMinWidth, 6-week months) → tracked for QW3/QW6 responsive/golden) |
| QA-F-013 | parent | Parent · Exam result row render | parent_exams_provider + report_card_view tested; `exams/widgets/exam_result_row.dart` = 0% lcov | no widget test pumps parent exam_result_row; subject/marks/grade row layout untested | P1 | Widget test: pump exam_result_row with sample result; assert subject, marks, grade render | QW3 | Verified (2026-06-28 — qw3_academics_widgets_widget_test.dart: ExamResultRow renders subject/term/86-100/86% · A/remarks; onTap fires) |
| QA-F-014 | parent | Parent · Leave apply form validation | parent_leave_provider unit + ParentLeaveScreen pumped; `leave/widgets/leave_apply_form.dart` = 0% lcov | leave_apply_form field validation (dates/reason required), submit-disabled-until-valid, submit fires untested | P0 | Form validation test: pump leave_apply_form; submit empty → assert errors; fill valid → submit enabled → tap fires provider | QW1 | Verified (2026-06-28 — leave_apply_form_widget_test.dart: empty→disabled+helper, valid→enabled+fires onSubmit, submitting→spinner; 3/3 green) |
| QA-F-015 | parent | Parent · Academics shortcuts strip render | none; `academics/widgets/academics_shortcuts_strip.dart` = 0% lcov | no widget test pumps shortcuts strip; tile render + tap navigation untested | P2 | Widget test: pump academics_shortcuts_strip; assert tiles render; tap → assert nav callback | QW3 | Verified (2026-06-28 — qw3_academics_widgets_widget_test.dart: AcademicsShortcutsStrip renders Timetable/Homework/Exams tiles; each nav callback fires) |
| QA-F-016 | parent | Parent · Child switcher sheet open+select | active-child provider tested; parent_child_switcher_sheet open/select not pumped | bottom-sheet open, multi-child list render, select-child switches active child untested | P1 | Widget test: pump host with 2 children, open child switcher sheet, tap child → assert activeChild updates | QW3 | Verified (2026-06-28 — qw3_child_switcher_widget_test.dart: with 2 children the sheet opens; tapping sibling updates parentActiveChildProvider child-ravi → child-priya) |
| QA-F-017 | parent | Parent · Dashboard golden (multi-viewport) | 1 parent_dashboard golden exists (single snapshot) | no multi-viewport (phone/tablet) golden; dark-mode parent dashboard golden untested | P2 | Golden test: parent dashboard across 3 viewports + dark mode | QW3 | Verified (2026-06-28 — qw3_parent_dashboard_dark_golden_test.dart: dark-mode golden across 390/428/834 viewports; baselines reproducible) |

#### Notifications

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-018 | notifications | Notifications · List screen render | only core/notifications/approval_notification_service unit; `notifications_screen.dart` = 0.6% lcov, models 2.8%, provider 40% | no widget/provider feature test; list render, loading/empty/error states untested | P0 | Widget test: pump NotificationsScreen with mock provider; assert list renders; override empty/error → assert state widgets | QW1 | Verified (2026-06-28 — notifications_screen_widget_test.dart: seeded inbox rows+chips+mark-all-read, forced-empty → "all caught up"; 2/2 green) |
| QA-F-019 | notifications | Notifications · Mark-read + deep-link tap | none | tapping a notification (mark read + deep-link nav) untested; unread-badge state untested | P1 | Widget test: pump NotificationsScreen; tap item → assert mark-read called + nav fired; assert unread badge updates | QW3 | Verified (2026-06-28 — qw3_notifications_interaction_widget_test.dart: tap unread → markRead fires, unreadNotificationsCountProvider 1→0; mark-all clears badge. **P2:** row onTap has no deep-link nav — enhancement) |

#### Onboarding

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-020 | onboarding | Onboarding · Single-student form validation | ai-school-builder + first-time-student data/provider flows + integration only; no screen pumped | StudentOnboardingScreen form (required fields, save-student) never pumped; validation + save untested | P1 | Form validation test: pump StudentOnboardingScreen; submit empty → errors; fill valid → Save fires provider; assert success/error surface | QW3 | Verified (2026-06-28 — qw3_student_onboarding_widget_test.dart: empty add-one form → Required errors; valid → preview+commit → 'Added 1 student') |
| QA-F-021 | onboarding | Onboarding · Unified flow stepper render | provider/data flows only | UnifiedOnboardingFlowScreen multi-step stepper render, next/back gating, per-step validation untested | P1 | Widget test: pump UnifiedOnboardingFlowScreen; assert step 1 renders; fill → Next advances; back returns; final step submit | QW3 | Verified (2026-06-28 — qw3_unified_onboarding_flow_widget_test.dart: step-1 stepper renders, Continue/Back navigate, Go Live → 'School is live'. **P1 mid-build provider-mutation crash found+fixed** in unified_onboarding_flow_screen.dart) |
| QA-F-022 | onboarding | Onboarding · AI pre-fill (generate) button | ai-school-builder is a flow test, not pumped UI | Generate button → loading → pre-filled review render untested at widget layer | P1 | Widget test: pump onboarding hub; tap Generate → assert loading; mock returns → assert pre-filled fields render | QW3 | Verified (2026-06-28 — qw3_unified_onboarding_flow_widget_test.dart: Pre-fill with AI → Generate draft → loading → draft applied, fields pre-filled) |
| QA-F-023 | onboarding | Onboarding · Bulk student import preview UI | import flow covered as data flow (ImportJob/ImportPreviewRow) not pumped | import preview table render, per-row error highlighting, confirm-import button untested at widget layer | P1 | Widget test: pump import preview with mixed valid/invalid rows; assert error rows flagged; tap Confirm → import job fires | QW3 | Verified (2026-06-28 — qw3_student_onboarding_widget_test.dart: mixed CSV preview flags '1 valid · 1 invalid' + per-field error; Commit → 'Committed 1 students') |

#### Operations

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-024 | operations | Operations · Hub cockpit screen render | operations_hub_mutations_provider unit only; screen never pumped | OperationsHubScreen sections (health/collections/attendance/alerts) render, loading/empty/error untested | P1 | Widget test: pump OperationsHubScreen with mock provider; assert key sections render; override loading/empty/error → assert state widgets | QW3 | Verified (2026-06-28 — qw3_operations_hub_widget_test.dart: cockpit health/summary/attendance/collections/alerts/actions render + forced loading/empty/error) |
| QA-F-025 | operations | Operations · Dismiss alert + Complete action | provider unit only | Dismiss-alert and Complete-pending-action buttons firing + optimistic UI untested at widget layer | P1 | Widget test: pump ops hub; tap Dismiss → alert removed; tap Complete → action marked done (mock mutation) | QW3 | Verified (2026-06-28 — qw3_operations_hub_widget_test.dart: Dismiss → alert removed + snackbar; Complete → action removed + snackbar) |

#### Legal

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-026 | legal | Legal · Acceptance gate screen render+accept | legal_gate_test (gate redirect logic) only; legal_acceptance_screen never pumped | LegalAcceptanceScreen render, Accept button enable-after-review, accept fires (records IP/device), loading/error untested | P0 | Widget test: pump LegalAcceptanceScreen with mock provider; assert policies render; tap Accept → assert accept call; override error → assert error state | QW1 | Verified (2026-06-28 — legal_acceptance_screen_widget_test.dart: blocked renders policy + Accept GATED behind the agree checkbox (disabled→enabled), submitting spinner, loading, review/empty states; 6/6 green. Complements legal_gate_test + QA-J-062.) |
| QA-F-027 | legal | Legal · Review/download policy document | none | review-document and download/continue actions untested at widget layer | P2 | Widget test: pump screen; tap Review → policy view opens; tap Continue gating | QW3 | Verified (2026-06-28 — qw3_legal_review_policy_widget_test.dart: View full policy launches hosted doc URL (mocked launcher); blocked Accept gated until agree ticked) |

#### Achievement Promotion (ZERO coverage)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-028 | achievement_promotion | Publisher · Post create + AI poster screen render | none anywhere in test/ (zero Flutter coverage of module) | achievement_promotion_screen never pumped; create flow, AI poster/caption render, loading/empty/error untested | P1 | Widget test: pump AchievementPromotionScreen with mock provider; assert create surface + AI poster render; override loading/empty/error states | QW3 | Verified (2026-06-28 — qw3_achievement_promotion_widget_test.dart: create surface + AI poster headline/caption/format render; forced loading/empty/error via provider override) |
| QA-F-029 | achievement_promotion | Publisher · Approve → select destinations → publish | none | preview screen, approval gate, destination multi-select (parent/student/teacher/WhatsApp/website), Publish button untested | P1 | Widget test: pump preview screen; tap Approve → destination selector renders; select channels; tap Publish → assert publish call | QW3 | Verified (2026-06-28 — qw3_achievement_promotion_widget_test.dart: approved card tap opens destination multi-select (parent/student/teacher/WhatsApp/website), toggle + Publish fires. Flow lives in achievement_promotion_screen.dart, not the preview screen) |

#### Finance (search / dialogs / export)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-030 | finance | Finance · Student-account search field | providers + offline/QR/screens tests; only module with real search bar — search widget untested | student-account search field type → filtered results render, no-match empty state untested | P1 | Widget test: pump FinanceStudentAccountsScreen; type query → assert filtered list; type non-match → assert empty state | QW3 | Verified (2026-06-28 — qw3_finance_student_accounts_widget_test.dart: search filters list; non-match → AksharaEmptyState) |
| QA-F-031 | finance | Finance · Collect-payment dialog open+confirm | finance_screens + write/RBAC unit; collect-payment dialog open/confirm not asserted | collect-payment sheet open, amount/method entry, confirm fires, validation untested at widget layer | P1 | Widget test: pump collections screen; open collect dialog; enter amount/method; submit empty → validation; confirm → mutation fires | QW3 | Verified (2026-06-28 — qw3_finance_collections_widget_test.dart: collect dialog opens (invoice/amount/method/submit), enter amount + confirm → success snackbar. **P2:** dialog has no client-side validation (finance_workflow_actions.dart:778)) |
| QA-F-032 | finance | Finance · Create-refund form validation | write tests cover logic; refund form not pumped for validation | refund-form required-field validation, create-refund + approve dialog open/confirm untested | P1 | Form validation test: pump refund form; submit empty → errors; valid → Create fires; approve dialog confirm | QW3 | Verified (2026-06-28 — qw3_finance_refunds_widget_test.dart: refund dialog renders required fields, valid submit → refund-created snackbar, inline Approve → approve mutation. **P2:** no client-side validation (finance_workflow_actions.dart:282)) |
| QA-F-033 | finance | Finance · Discount-rule dialog open+save | discount-rule unit test exists; dialog open+save not asserted at widget layer | add-rule/assign-concession dialog open, field entry, Save fires untested | P2 | Widget test: pump discounts screen; open add-rule dialog; fill; Save → mutation fires | QW3 | Verified (2026-06-28 — qw3_finance_discounts_widget_test.dart: add-rule dialog opens, empty-name guard blocks save, fill+Save → 'Discount rule created (pending approval)') |
| QA-F-034 | finance | Finance · Fee-assignment screen low coverage | `finance_fee_assignment_provider` 31% lcov; assignment screen render thin | FinanceFeeAssignmentScreen render + bulk-assign flow + empty/error states under-covered | P1 | Widget test: pump FinanceFeeAssignmentScreen; assert class/student list; assign-all flow; empty/error states | QW3 | Verified (2026-06-28 — qw3_finance_fee_assignment_widget_test.dart: handoff queue + assignment panel render, Generate → fee-account-created snackbar, empty/error/loading states) |
| QA-F-035 | finance | Finance · Reports export (PDF/Excel) action | screens test exists; export-PDF/Excel button trigger not asserted | Export PDF / Export Excel buttons firing + in-progress/queued feedback untested | P2 | Widget test: pump finance reports; tap Export PDF → assert export invoked + feedback; same for Excel | QW3 | Verified (2026-06-28 — qw3_finance_reports_widget_test.dart: Export PDF → buildPdf+previewPdf + 'PDF ready'; Export Excel → buildCsv+shareCsv + 'Excel CSV ready') |

#### Teacher (attendance / marks / homework)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-036 | teacher | Teacher · Attendance marking screen + mark-all gate | per-feature providers + attendance search/correction/class-scope; `teacher_attendance_provider` 47% lcov; module_screens smoke only | TeacherAttendanceScreen mark-per-student + "must mark all before submit" gate, submit-disabled state untested as a pumped screen | P0 | Widget test: pump TeacherAttendanceScreen with class roster; mark subset → assert Submit disabled; mark all → Submit enabled → fires | QW1 | Verified (2026-06-28 — teacher_attendance_submit_gate_test.dart: Submit DISABLED while any student unmarked (+ "N unmarked" badge), ENABLES once all marked, re-disabled after submit; 3/3 green.) |
| QA-F-037 | teacher | Teacher · Homework create form validation | homework provider unit; create screen not pumped for validation | teacher_homework_create_screen form (title/due/class required) validation + publish untested | P1 | Form validation test: pump TeacherHomeworkCreateScreen; submit empty → errors; valid → Create/Publish fires | QW3 | Verified (2026-06-28 — qw3_teacher_homework_widget_test.dart: blank required → 3× Required + no nav; valid → Create → routes to homework list) |
| QA-F-038 | teacher | Teacher · Exam marks entry + submit-for-verification | exam remark/results-panel/selector + provider tested; marks-entry screen pump thin | marks grid entry, validation (out-of-range marks), Save draft, Submit-for-verification, Publish gate untested as pumped screen | P0 | Widget test: pump teacher exams marks screen; enter invalid mark → validation; Save draft; Submit → state transition; Publish gated | QW1 | Verified (2026-06-28 — teacher_exam_marks_entry_test.dart: marks grid renders editable field per student, out-of-range mark rejected ("Marks must be between 0 and 50"), non-numeric rejected, valid mark accepted; 4/4 green. Save→Submit-for-verification persistence proven by QA-J-014/teacher_marks_submit_test.) |
| QA-F-039 | teacher | Teacher · Homework review/grade screen | homework provider unit only | review-submission grade entry, Save review, reviewed-state UI untested as pumped screen | P1 | Widget test: pump teacher homework review; grade a submission; Save review → assert reviewed state | QW3 | Verified (2026-06-28 — qw3_teacher_homework_widget_test.dart: review sheet grade+comment → row flips to Reviewed with distinct comment; forced error) |
| QA-F-040 | teacher | Teacher · Leave request form + approvals | leave provider + leave-approvals tested; leave request form validation thin | teacher leave-request form validation + class-teacher approve/reject (with reject comment) dialog untested as pumped UI | P1 | Form + widget test: pump leave request, validate; pump approvals, tap Reject → comment dialog → confirm | QW3 | Verified (2026-06-28 — qw3_teacher_leave_widget_test.dart: apply form Submit gated until valid then fires; approvals tap Reject → 'Leave rejected' → cleared. **P2:** reject is one-tap, no comment dialog (teacher_leave_approvals_screen.dart:95)) |
| QA-F-041 | teacher | Teacher · AI custom-message generate+send | messages provider tested; AI generate-and-send dialog not pumped | "Generate custom message (AI)" → loading → generated text → Send-to-parent flow untested at widget layer | P2 | Widget test: pump parent-communication screen; tap Generate → loading → text renders → Send fires | QW3 | Verified (2026-06-28 — qw3_teacher_communication_widget_test.dart: AI/send gated until student selected; AI dialog generate+send → 'Custom AI message sent.'; Send-to-parent → 'Sent to …') |

#### Student app

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-042 | student_app | Student · Homework submit flow screen | per-feature providers + 1 module screens file; submit-HW screen pump thin | student_homework_screen Submit-HW action, submitted-state UI, error-on-submit untested as pumped screen | P1 | Widget test: pump StudentHomeworkScreen; tap Submit → assert submitted state; override error → assert error surface | QW3 | Verified (2026-06-28 — qw3_student_homework_widget_test.dart: KPI strip + rows + pending Submit; tap Submit → row transitions; forced error surface) |
| QA-F-043 | student_app | Student · Exam result row / subject score row render | providers tested; `student/exams/widgets/exam_result_row.dart` 0% lcov, subject_score_row 0% | exam_result_row + subject_score_row never pumped; marks/grade row layout untested | P1 | Widget test: pump exam_result_row + subject_score_row with samples; assert subject/marks/grade render | QW3 | Verified (2026-06-28 — qw3_exam_result_rows_widget_test.dart: ExamResultRow renders title/term/78-100/Grade A; SubjectScoreRow renders subject/percent/grade chip) |
| QA-F-044 | student_app | Student · Attendance screen render | attendance provider + sync tested; attendance screen pump thin | student_attendance_screen summary/calendar render + empty/error states untested as pumped screen | P2 | Widget test: pump StudentAttendanceScreen; assert summary renders; empty/error states | QW3 | Verified (2026-06-28 — qw3_student_attendance_widget_test.dart: month selector + KPI strip + calendar + history render; forced loading + error) |

#### Education (Question Intelligence — CSV import / dialogs)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-045 | education | Education · CSV question import UI | education_screens + write tests; CSV import surface not pumped for preview/error | bulk CSV import: file pick → preview rows → invalid-row flagging → confirm untested at widget layer | P1 | Widget test: pump import flow; provide mock CSV preview with bad rows → assert flagged; confirm → import fires | QW3 | Verified (2026-06-28 — qw3_education_question_intelligence_widget_test.dart: paste CSV → Preview → '1 valid · 1 skipped' flagging; confirm returns parsed items; import disabled at 0 valid) |
| QA-F-046 | education | Education · Add/edit question dialog validation | education_screens smoke; add/edit-question dialog (12 forms) not pumped for validation | add/edit-question form validation, Save fires, "marks unfilled" banner untested | P1 | Form validation test: pump add-question dialog; submit empty → errors; valid → Save; assert "N marks unfilled" banner state | QW3 | Verified (2026-06-28 — qw3_education_question_intelligence_widget_test.dart: add-question sheet empty → Save gated, valid → Save returns item; paper detail 'N marks unfilled' banner) |
| QA-F-047 | education | Education · AI candidate moderation queue | education_screens smoke | approve/reject AI-candidate moderation queue actions + filter untested at widget layer | P2 | Widget test: pump moderation queue; tap Approve/Reject on a candidate → assert state change | QW3 | Verified (2026-06-28 — qw3_education_question_intelligence_widget_test.dart: pending AI candidates render Approve/Reject; Approve fires moderateItem(approved) + snackbar. Note: this surface has no filter control) |

#### HR / Employee

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-048 | hr | HR · Excel employee import UI | hr_screens + write/providers; Excel import surface not pumped | Excel employee import: file pick → preview → confirm + error rows untested at widget layer | P1 | Widget test: pump HR import flow; mock Excel preview → assert rows + error flags; confirm → import fires | QW3 | Open (BLOCKED 2026-06-28 — surface not built: no Excel employee-import UI exists under lib/features/hr/ (only Export Excel in reports). Feature-dependent, like QW2's blocked rows; needs the import surface built before it can be tested) |
| QA-F-049 | hr | HR · Approve-leave + activate-employee buttons | hr_screens smoke; Priority-A action buttons untested per doc | approve-leave dialog confirm + activate/edit-employee buttons firing untested | P1 | Widget test: pump HR leave screen; tap Approve → confirm dialog → mutation fires; tap Activate employee | QW3 | Verified (2026-06-28 — qw3_hr_workflow_widget_test.dart: Approve pending leave → comment dialog → confirm fires approveHrLeave; Edit/Deactivate/Activate render+fire) |
| QA-F-050 | hr | HR · Add/edit employee form validation | hr_write covers logic; employee form not pumped for validation | add/edit-employee form (14 fields) required-field validation + Save untested at widget layer | P1 | Form validation test: pump employee form; submit empty → errors; valid → Save fires | QW3 | Verified (2026-06-28 — qw3_hr_workflow_widget_test.dart: add-employee dialog renders 5 fields + dept/role dropdowns; valid submit fires createHrEmployee. **P2:** dialog has no field validation (hr_workflow_actions.dart:259); spec's '14 fields' is actually 7) |
| QA-F-051 | employee | Employee · 360 + platform screens render | none (referenced only via HR contracts); 0 state refs flagged (no loading/error/empty) | Employee360Screen + EmployeePlatformScreen never pumped; no loading/error/empty handling to assert | P1 | Widget test: pump Employee360Screen + EmployeePlatformScreen with mock data; assert render; verify missing-data/error fallback | QW3 | Verified (2026-06-28 — qw3_employee_screens_widget_test.dart: Employee360 + EmployeePlatform render from demo; both degrade to AksharaLoadingState/AksharaErrorState when forced) |

#### School config / Settings / Director / Entitlements

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-052 | school_config | School config · Discovery/select screen | core school_configuration logic only; school_discovery_screen never pumped | SchoolDiscoveryScreen render, board/config select, save action untested at widget layer | P1 | Widget test: pump SchoolDiscoveryScreen; assert list/form renders; select + save → assert config applied | QW3 | Verified (2026-06-28 — qw3_school_discovery_widget_test.dart: wizard renders school-type options + multi-step form; walk-through to Apply persists schoolType/curriculum/capability + snackbar) |
| QA-F-053 | settings | Settings · AI assistant settings route | only appearance_settings screen tested | ai_assistant_settings_screen (copilot) render + toggle persistence untested | P2 | Widget test: pump AiAssistantSettingsScreen; toggle settings → assert persisted state | QW3 | Verified (2026-06-28 — qw3_ai_assistant_settings_widget_test.dart: renders every AiAccessMode + bubble toggle; selecting persists to aiAccessPreferencesProvider; switch flips persisted flag) |
| QA-F-054 | director | Director · Sub-screen renders (schools/revenue/growth/etc.) | only 1 dashboard widget + action-error test; 8 sub-screens untested (UNTESTED doc Priority B) | director_schools/revenue/growth/marketing/admissions/compliance/reports screens never pumped; empty/error states untested | P1 | Widget test: pump each director sub-screen with mock provider; assert render + empty/error states (table-driven) | QW3 | Verified (2026-06-28 — qw3_director_subscreens_widget_test.dart: table-driven render of all 7 director sub-screens + forced loading/error/empty. **2 P2 overflows found+fixed:** director_reports_screen.dart:83, director_metric_input_editor.dart:127) |
| QA-F-055 | director | Director · Portfolio metric-input form + AI summary | dashboard widget only | metric-input form (spend/expense/capacity) validation + Save, Generate AI Executive Summary, Export board-pack untested at widget layer | P1 | Form validation test: pump portfolio inputs; validate; Save → computed margin/ROI render; tap Generate summary + Export | QW3 | Verified (2026-06-28 — qw3_director_portfolio_inputs_widget_test.dart: computed Margin renders; metric-input blocks save on validation, valid saves; Generate AI summary replaces placeholder; board-pack Export confirms) |
| QA-F-056 | entitlements | Entitlements · Plan screens negative-path | locked-UX + gating + 2 screen tests exist; 1 state ref flagged (weak loading/error/empty) | plan-assignment "Could not update plan" error path + loading state untested at widget layer | P2 | Widget test: pump plan assignment; override to error → assert "Could not update plan"; override loading → spinner | QW3 | Verified (2026-06-28 — qw3_plan_assignment_widget_test.dart: failing assign → 'Could not update plan' snackbar; orgs-loading → AksharaLoadingState) |

#### Navigation files (0% lcov)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-057 | router | Routing · Parent/teacher/student nav builders | route guards + protection-inventory tests; `parent_navigation.dart` 0%, teacher_navigation 0%, student_navigation 0% lcov | per-persona RouteBuilder→screen wiring (each path resolves to correct screen) untested | P1 | Router test: for each persona nav, assert each route constant builds its expected screen (golden route map) | QW3 | Verified (2026-06-28 — qw3_persona_navigation_golden_test.dart: golden route-map iterates every parent/teacher/student dashboard actionId through a real GoRouter, asserts landed route == expected RouteNames constant; 61 routes) |

#### Display-only / advanced module dashboards (golden / smoke)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|----|--------|---------|-------------------|------------------|----------|------------------|----------------------|--------|
| QA-F-058 | platform | Control Center · 16 sub-screen renders | broad control-center providers/screens/write; many of 16 sub-screens not individually pumped | per-sub-screen (billing/monitoring/support/crm/roles/features/providers/success) render + empty/error states untested | P2 | Widget test: table-driven pump of each Control Center sub-screen with mock provider; assert render + state widgets | QW3 | Verified (2026-06-28 — qw3_control_center_subscreens_widget_test.dart: all 15 Control Center sub-screens render via ControlCenterModuleScaffold, 12 with forced loading/error. **2 P2 ListView unbounded-height fixes:** control_center_features_screen.dart, control_center_providers_screen.dart) |
| QA-F-059 | platform | Org Builder · interview field validation | org-builder hub/interview/provisioning screens exist; interview fields flagged untested (Priority B) | interview-step field validation + preview→provision gating untested at widget layer | P2 | Form validation test: pump org-builder interview; validate required fields; Next gating; provision confirm | QW3 | Verified (2026-06-28 — qw3_organization_builder_interview_widget_test.dart: identity field render, draft hydration to saved step, Continue advances after save, Next gated when manageOrganizationBuilder permission absent) |
| QA-F-060 | intelligence | Intelligence · Generate-report + publish-to-parent actions | mostly flow/OTHER; 1 true widget (trust hub) | "Generate AI summary", "Publish to parent hub", "Schedule principal report" buttons firing + loading untested at widget layer | P2 | Widget test: pump an intelligence dashboard; tap Generate → loading → result; tap Publish → assert publish call | QW3 | Verified (2026-06-28 — qw3_intelligence_actions_widget_test.dart: 5 tabs render; Compute-risks → snackbar; Parent Guidance generate→publish (ON=published to parent hub, OFF=draft). **P1 prod crash found+fixed:** enum .name via dynamic dispatch threw NoSuchMethodError in intelligence_screen.dart) |
| QA-F-061 | verticals | Verticals · drill-down screen renders | 4 dashboard smoke + stress + contracts; no drill-down depth | per-vertical sub-screens (table mgmt/orders/appointments/patients/rooms) render + empty-data states untested | P2 | Widget test: table-driven pump of each vertical sub-screen with mock data; assert render + empty states | QW3 | Verified (2026-06-28 — qw3_verticals_dashboard_smoke_widget_test.dart: scoped-out isolation smoke — restaurant/salon dashboards build with demo data. **Verticals are hidden per owner scope** (SchoolBuildScope hiddenAdminModules/hiddenRoutePrefixes); no journey test forced) |
| QA-F-062 | transport | Transport · dialog-heavy flows (14 dialogs) | providers/screens/write; 14 dialogs (highest) not individually asserted | new-route/vehicle/driver/allocation dialogs open+confirm + form validation untested at widget layer | P2 | Widget test: pump transport screens; open each create dialog; validate; confirm → mutation fires (table-driven) | QW3 | Verified (2026-06-28 — qw3_transport_dialogs_widget_test.dart: all 7 transport dialogs open→render→confirm→mutation; 2 with required-field gating. **P2:** route-picker dropdown overflows ~16px on long names; spec's '~14 dialogs' is actually 7) |
| QA-F-063 | dashboards | Golden · key dashboards multi-viewport + dark | 4 dashboard goldens (parent/student/teacher/erp) + dark_mode + approval_center | finance/admissions/director/control-center dashboards lack goldens; existing goldens single-viewport | P2 | Golden test: add finance + admissions + director dashboards across 3 viewports + dark mode | QW3 | Verified (2026-06-28 — qw3_key_dashboards_golden_test.dart: finance/admissions/director dashboards × 3 viewports + dark mode; 12 baselines reproducible) |

---

### Summary

- Total rows: 63
- P0: 10 — QA-F-001, 002, 005, 006, 012, 014, 018, 026, 036, 038
- P1: 36
- P2: 17

P0 = zero/near-zero coverage on pilot-critical UI surfaces (auth login/OTP, parent fees/attendance/leave, notifications, legal gate, teacher attendance/marks). All other rows are P1 (operational screens/forms) or P2 (display-only/golden/advanced).


---

## AKSHARA ERP — Backend (Deno/Supabase Edge) Test Gap Tracker

Source inventories: `backend_tests.md`, `routes_rbac.md`, `execution_model.md`, `all_routes.txt`.
Backend stack: `supabase/functions/api/index.ts` (entry) + `supabase/functions/_shared/*` (50 module routers, ~446 endpoints).
Confirmed spot-checks: zero `*_test.ts` in employee/inventory_distribution/parent_experience/principal_command/school_calendar/school_config/memories/setup_wizard/teacher_assistant/growth/pilot_operations module dirs; `school_completion` has 7 service tests but no router/contract test; `control_center` router has no test file; `finance` has 8 repository tests but no router/contract test; RBAC inventory (`_shared/validation/rbac_route_inventory.ts`) = 166 path entries (~37%); `backend_staging.yml` runs `deno test _shared/` only (no `api/` integration, no router-contract gate); `run_ci_gates.sh` runs no `deno` at all; `index.ts` 404 NOT_FOUND / CONFIG_ERROR(500) / SERVER_ERROR(500) / OPTIONS-CORS / `withEntitlement`→402 paths all exist but are unasserted.

Fix-wave key: **QW1** = ship-first (CI-execution gap + P0 RBAC/RLS). **QW4** = Backend API/RBAC/RLS/error-path + wire deno-test into CI.

---

### Untested modules (no Deno test AND no smoke)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-B-001 | employee | employee · GET /employees + /employees/dashboard + /employees/intelligence/dashboard contract | none | no router path-match test; no 403 for non-HR/non-management role; no cross-tenant denial; route not exercised at all (router exists, 0 test files) | P1 | Deno test: assert GET /employees returns 200 for holder of viewEmployees, 403 for storekeeper/parent; /employees/intelligence/dashboard gated by viewEmployeeIntelligence; rolled-back-txn tenant-isolation probe returns only own-school rows | QW4 | Verified (2026-06-30 — qw4_employee_route_contract_test.ts: GET /employees authorized by viewEmployees OR viewHr (OR-fixed), neither→403; POST roles manageEmployees OR manageHr; 422/401/404) |
| QA-B-002 | inventory_distribution | inventory_distribution · GET catalog/dashboard/items + POST items contract | none | no test, no smoke; POST /inventory/distribution/items write contract unverified; no viewInventoryDistribution 403; no cross-tenant denial | P1 | Deno test: POST /inventory/distribution/items persists + returns row, 403 for non-inventory role, 200 for inventoryManager; GET catalog/dashboard/reports 200 shape; tenant probe | QW4 | Verified (2026-06-30 — qw4_inventory_distribution_route_contract_test.ts: POST /inventory/distribution/items 503/403/422; OR-fix proven (manageInventoryDistribution OR manageInventory each authorize)) |
| QA-B-003 | parent_experience | parent_experience · GET activation/summary/printable + POST summary/refresh contract | none | no test, no smoke, NOT in RBAC inventory; persona (parent) scope unasserted; POST refresh write/AI path unverified; per-child RLS unverified | P0 | Deno test: GET /parent/experience/summary returns only the authenticated parent's children (per-child RLS), 401 unauth, persona-scope enforced; POST summary/refresh idempotent + scoped; cross-parent denial | QW4 | Verified (2026-06-30 — qw4_parent_experience_route_contract_test.ts: /parent/experience/* require parent persona scope (403 wrong scope, 503 valid), 422 missing studentId) |
| QA-B-004 | principal_command | principal_command · GET /principal-command/center + /query contract | none | no test, no smoke; viewPrincipalCommand gate unasserted; org vs school scope unverified; AI query path untested | P1 | Deno test: GET /principal-command/center 200 for principal, 403 for librarian/parent; /query bounds + school-scope (no cross-school leakage); empty-state shape | QW4 | Verified (2026-06-30 — qw4_principal_command_route_contract_test.ts: /principal-command/* require viewPrincipalCommand OR viewAnalytics (OR-fixed), librarian/parent 403; org-vs-school scope 403) |
| QA-B-005 | school_calendar | school_calendar · GET + POST /school-calendar contract | none | no test, no smoke; POST write/persist + DELETE path unverified; no manage-perm 403; no cross-tenant denial | P1 | Deno test: POST /school-calendar persists holiday/event + returns it, 403 for viewer-only role, GET reflects write; tenant-isolation probe; bad-body 4xx | QW4 | Verified (2026-06-30 — qw4_school_calendar_route_contract_test.ts: POST persists for manageSchoolCalendar (503), view-only 403; DELETE 503/403; 422 bad eventType) |
| QA-B-006 | school_config | school_config · GET + PUT /school-config contract | none | no test, no smoke, NOT in RBAC inventory; PUT config-mutation write path unverified; viewSchoolSetup gate unasserted; cross-tenant config bleed unverified | P0 | Deno test: PUT /school-config persists + GET reflects; 403 for non-admin role; rolled-back tenant probe asserts a school cannot read/write another school's config; bad-body 4xx | QW4 | Verified (2026-06-30 — qw4_school_config_route_contract_test.ts: PUT /school-config 503 for manageSchoolSetup, 403 viewSchoolSetup; 400 invalid body; org-scope 403) |
| QA-B-007 | school_memories (memories) | memories · GET analytics/events + POST events contract | none | no test, no smoke (client-side only); POST /memories/events write + presign/upload path unverified; viewSchoolMemories gate unasserted; cross-tenant denial unverified | P1 | Deno test: POST /memories/events persists, 403 for non-holder, GET events returns own-school only; presign type/size early-reject; tenant probe | QW4 | Verified (2026-06-30 — qw4_memories_route_contract_test.ts: POST /memories/events 503 for manageSchoolMemories, 403 non-holder; presign write-gated + 422 disallowed type; 401/404) |
| QA-B-008 | setup_wizard | setup_wizard · POST /setup-wizard/sessions contract | none | no test, no smoke; session-create write path unverified; viewSchoolSetup gate unasserted; bad-body validation untested | P1 | Deno test: POST /setup-wizard/sessions creates + returns session id for holder, 403 for non-admin, 4xx on bad body; tenant scope on created session | QW4 | Verified (2026-06-30 — qw4_setup_wizard_route_contract_test.ts: POST sessions create/advance require manageSchoolSetup (503), viewSchoolSetup-only 403; GET read by viewSchoolSetup) |
| QA-B-009 | teacher_assistant | teacher_assistant · GET insights/interventions + POST interventions contract | none | no test, no smoke; viewTeacherAssistant gate unasserted; POST interventions write path unverified; teacher-scope (own classes only) unverified | P1 | Deno test: GET /teacher-assistant/insights 200 for teacher, 403 for parent; POST /teacher-assistant/interventions persists + scoped to teacher's students; cross-teacher denial | QW4 | Verified (2026-06-30 — qw4_teacher_assistant_route_contract_test.ts: GET insights teacher viewTeacherAssistant 503, parent 403; POST/PATCH interventions manageTeacherAssistant 503/403/422) |
| QA-B-010 | growth | growth · GET campaigns/dashboard/funnel/inquiries + POST campaigns/inquiries contract | LIVE wave only (b6 smoke) | no Deno unit/router test (B6 smoke is LIVE-only, manual); POST campaigns/inquiries write contract not unit-asserted; module.marketing 402 not unit-tested; no router path-match | P1 | Deno test: POST /growth/campaigns persists + returns id, GET history reflects; 402 PLAN_UPGRADE when module.marketing absent; 403 non-holder; tenant probe; router 404 negative | QW4 | Verified (2026-06-30 — qw4_growth_route_contract_test.ts: POST /growth/campaigns 503/403/422; OR-fix proven (manageGrowthPlatform OR manageAdmissions); history GET; 401/404) |
| QA-B-011 | school_completion | school_completion · 40 routes (subjects/syllabus/lesson-logs/rooms/timetables/comm-analytics/branding/whatsapp) contract | service unit only | services unit-tested but the 40 route contracts are not router-tested and NOT in RBAC inventory; per-route view/manage perms, 402, write-persist, and cross-tenant denial all unasserted at route level | P0 | Deno test: router path-match for all 40 routes (404 negative for unregistered); POST /school/subjects + /school/syllabus/generate + /school/rooms/allocate persist + 403 non-holder; tenant probe on lesson-logs/subjects | QW4 | Verified (2026-06-30 — qw4_school_completion_route_contract_test.ts: table-driven over ~40 routes — each holder 503 + non-holder 403; OR-gates per-alternative-slug; 90 tests) |
| QA-B-012 | pilot_operations | pilot_operations · POST teacher/attendance/{draft,submit}, teacher/homework, parent/leave, student/homework/submit contract | service unit only (pilot probe) | router not in RBAC inventory; these overlay real write paths (attendance submit, homework) — write-contract + persona/role scope not asserted at route level | P1 | Deno test: POST /teacher/attendance/submit persists draft→submitted + 403 for non-teacher; /student/homework/submit scoped to student; /parent/leave persona-scoped; tenant probe | QW4 | Verified (2026-06-30 — qw4_teacher_overlay_route_contract_test.ts: attendance draft→submit enforce teacher persona+markAttendance (parent 403 even with perm, teacher 503); writes live in pilot_operations_handlers) |

### Router / contract gaps (path-match + 404/405 negative; 41 of 50 routers)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-B-013 | finance | finance · 28-route router path-match + 404/405 contract | service unit only (8 repo tests) | no router path-match test on the largest mutation surface; unregistered-route 404 fallthrough unasserted; method-mismatch (e.g. GET on POST-only) behavior unverified | P0 | Deno test: assert each of 28 finance routes matches its handler, unregistered /finance/xyz returns 404 NOT_FOUND envelope, wrong method returns 404/405; collect/refund/scholarship POST reach handler | QW4 | Verified (2026-06-30 — qw4_finance_route_contract_test.ts: all 50 registered finance routes resolve+gate (holder 503/422, non-holder 403); unregistered→404; 401) |
| QA-B-014 | finance | finance · POST /finance/collections collect-fee write contract | service unit only | no route-level test asserting collect persists + returns receipt; no 403 for non-finance role; no tenant-isolation on collection rows | P0 | Deno test: POST /finance/collections returns 403 for inventoryManager, 200 for financeAdmin, persists + emits receipt; rolled-back-txn tenant-isolation probe (other-school collection invisible) | QW1 | Verified (route contract) (2026-06-28 — finance_collections_route_contract_test.ts: POST /finance/collections → 403 FORBIDDEN for a non-finance role, passes the gate WITH manageFinance (503 = reached DB layer), 422 on missing/invalid body, 401 unauth; 5/5 Deno green. Persist+receipt proven by finance_collections_repository_test. **Cross-tenant rolled-back-txn RLS probe → live-Postgres batch with QA-B-051/052/057.**) |
| QA-B-015 | finance | finance · POST /finance/refunds + approveRefunds verb contract | service unit only (refunds repo) | no route-level approve-verb separation test; refund approval write + 403 for non-approver unasserted at route | P0 | Deno test: POST /finance/refunds requires approveRefunds (403 for manageFinance-only role), persists refund, double-submit guarded; tenant probe | QW4 | Verified (2026-06-30 — qw4_finance_route_contract_test.ts: refund approve/reject require approveRefunds — 403 for manageFinance-only (verb anti-escalation), 503 with it) |
| QA-B-016 | finance | finance · PUT /finance/settings write contract | service unit only | PUT settings mutation not route-tested; manageFinance gate + cross-tenant config write unasserted | P1 | Deno test: PUT /finance/settings persists for manageFinance holder, 403 otherwise, GET reflects; tenant probe asserts no cross-school write | QW4 | Verified (2026-06-30 — qw4_finance_route_contract_test.ts: PUT /finance/settings 503 for manageFinance, 403 viewFinance-only, 422 missing body) |
| QA-B-017 | communication | communication · 19-route router path-match + 404 contract | service unit only (router-parity, webhook, broadcast) | no router path-match negative control across all 19 routes; method/404 fallthrough unasserted for the device-token + notifications + templates sub-paths | P1 | Deno test: router path-match for 19 comm routes incl /communications/broadcasts, /parent|student/device-tokens/*, unregistered → 404; webhook stays HMAC-not-RBAC | QW4 | Verified (2026-06-30 — qw4_communication_route_contract_test.ts: all 19 canonical comm routes resolve+gate; unregistered→404; 401) |
| QA-B-018 | communication | communication · POST /communications/broadcasts write + manageCommunication contract | service unit only | broadcast send write path not route-tested for 403 non-holder; fan-out/queue behavior at route level unasserted | P1 | Deno test: POST /communications/broadcasts 403 for non-manageCommunication, 200 persists + enqueues; templates POST persists; tenant scope on recipients | QW4 | Verified (2026-06-30 — qw4_communication_route_contract_test.ts: POST /communications/broadcasts requires sendBroadcast (handler-real slug), 403 without, 503 with, 422 bad body) |
| QA-B-019 | control_center | control_center · 17-route router path-match + 404 contract | service unit only (entity_read partial) | router has NO test file; 17 ANY routes (schools/subscriptions/billing/crm/support/features/monitoring/vault) have no path-match, no method, no 404-fallthrough test | P0 | Deno test: router path-match for all 17 control-center routes; unregistered → 404; ANY-method routes still scoped; vault/rotate is a privileged write | QW4 | Verified (2026-06-30 — qw4_control_center_route_contract_test.ts: all 17 control-center routes resolve+gate; unregistered→404; 401) |
| QA-B-020 | transport | transport · 17-route router path-match + 404 contract | service unit only (read repo, write handlers) | no router path-match negative control; module.transport 402 + per-route 403 not route-tested across the 17 | P1 | Deno test: router path-match for 17 transport routes; unregistered → 404; 402 when module.transport absent; POST route persists + 403 non-holder | QW4 | Verified (2026-06-30 — qw4_transport_route_contract_test.ts: 17 routes resolve; module.transport 402 via withEntitlement; per-route 403/503) |
| QA-B-021 | hostel | hostel · 14-route router path-match + 404 contract | service unit only | no router path-match; module.hostel 402 + per-route 403 not route-tested | P1 | Deno test: router path-match for 14 hostel routes; 402 module-gate; POST rooms/visitors/mess persists + 403 non-holder; 404 fallthrough | QW4 | Verified (2026-06-30 — qw4_hostel_route_contract_test.ts: 16 routes resolve; module.hostel 402; rooms/allocation 403/503) |
| QA-B-022 | hr | hr · 13-route router path-match + 404 + payroll write contract | service unit only | no router path-match; POST /hr/payroll/run write + manageHr 403 not route-tested; module.hr_payroll 402 unasserted at route | P1 | Deno test: router path-match for 13 hr routes; POST /hr/payroll/run 403 for non-manageHr, persists run; 402 when module.hr_payroll absent; 404 fallthrough | QW4 | Verified (2026-06-30 — qw4_hr_route_contract_test.ts: 20 routes resolve; POST /hr/payroll/run requires manageHr; module.hr_payroll 402) |
| QA-B-023 | library | library · 13-route router path-match + 404 + issue/return write contract | service unit only | no router path-match; module.library 402 + per-route 403 not route-tested; issue/return write persistence not route-asserted | P2 | Deno test: router path-match for 13 library routes; POST issues/returns persists + 403 non-holder; 402 module-gate; 404 fallthrough | QW4 | Verified (2026-06-30 — qw4_library_route_contract_test.ts: 14 routes resolve; module.library 402; issue/return 403/503) |
| QA-B-024 | alumni | alumni · 13-route router path-match + 404 contract | service unit only (LIVE wave) | no router path-match; module.alumni 402 + per-route 403 not route-tested; donations/campaigns write not route-asserted | P2 | Deno test: router path-match for 13 alumni routes; POST registry/donations/campaigns persists + 403 non-holder; 402 module-gate; 404 fallthrough | QW4 | Verified (2026-06-30 — qw4_alumni_route_contract_test.ts: 14 routes resolve; module.alumni 402; donations/campaigns 403/503) |
| QA-B-025 | inventory | inventory · 12-route router path-match + 404 + lifecycle write contract | service unit only | no router path-match; module.inventory 402 + per-route 403 not route-tested; POST /inventory/intelligence/lifecycle/events write not route-asserted | P1 | Deno test: router path-match for 12 inventory routes; POST lifecycle/events persists + 403 non-holder; 402 module-gate; ANY-method routes scoped; 404 fallthrough | QW4 | Verified (2026-06-30 — qw4_inventory_route_contract_test.ts: 21 routes resolve; module.inventory 402; lifecycle/events 403/422/503) |
| QA-B-026 | inventory_finance | inventory_finance · 5-route router path-match + PO write contract | service unit only (1 test) | NOT in RBAC inventory; POST procurement/orders + vendors/catalog write + 403 not route-tested; tenant scope unasserted | P1 | Deno test: router path-match for 5 routes; POST /inventory/procurement/orders persists PO + 403 non-holder; stock/valuation read scoped; tenant probe | QW4 | Verified (2026-06-30 — qw4_inventory_route_contract_test.ts: procurement/orders + vendors/catalog 403 w/o manageInventory, 503 with) |
| QA-B-027 | director | director · 12-route router path-match + org-scope contract | service unit only (repo, b8 LIVE) | no router path-match; POST /director/metric-inputs + /director/summary write not route-tested; org-scope vs school-scope boundary unasserted at route | P0 | Deno test: router path-match for 12 director routes; POST metric-inputs persists; requireOrgScope denies school-only token; cross-org portfolio leakage probe | QW4 | Verified (2026-06-30 — qw4_director_route_contract_test.ts: 12 routes resolve; POST metric-inputs/summary 403/503/422) |
| QA-B-028 | analytics | analytics · 7-route (ANY-method) router path-match contract | service unit only (engine) | NOT in RBAC inventory; ANY-method routes (dashboard/risks/trends/weekly-briefing) have no path-match, no perm, no 404 test | P2 | Deno test: router path-match for 7 analytics routes; viewAnalytics gate (403 non-holder); ANY-method still scoped; school-scope on risk/trend rows | QW4 | Verified (2026-06-30 — qw4_analytics_route_contract_test.ts: 7 analytics routes resolve; unregistered→404) |
| QA-B-029 | copilot | copilot · 4-route router path-match + session write contract | service unit only (llm client, repo) | NOT in RBAC inventory; POST /copilot/sessions write + persona-scope on AI not route-tested; 404 fallthrough unasserted | P2 | Deno test: router path-match for 4 copilot routes; POST /copilot/sessions persists scoped to user; runAiCopilot gate; assistants/suggestions scoped | QW4 | Verified (2026-06-30 — qw4_copilot_route_contract_test.ts: 4 routes resolve; POST /copilot/sessions 403/503/422) |
| QA-B-030 | timetable | timetable · 7-route (/academic/timetables) router path-match + generate write contract | service unit only (engine) | NOT in RBAC inventory; POST generate/validate/periods/move write + manageAcademicTimetable 403 not route-tested | P2 | Deno test: router path-match for 7 timetable routes; POST generate/validate returns conflicts shape + 403 non-holder; periods/move persists; 404 fallthrough | QW4 | Verified (2026-06-30 — qw4_timetable_route_contract_test.ts: 7 routes resolve; generate/validate/periods/move 403/503/422) |
| QA-B-031 | legal | legal · POST /legal/accept + GET /legal/status router path-match | service unit only (catalog) | NOT in RBAC inventory; accept-write (records IP/device) not route-tested; fail-open behavior at route unasserted | P1 | Deno test: POST /legal/accept persists acceptance (IP/device), GET /legal/status reflects; unauth 401; fail-open path (catalog empty → no outstanding) | QW4 | Verified (2026-06-30 — qw4_legal_route_contract_test.ts: POST /legal/accept binds caller+IP+device (503), 422 empty/unknown-key, 409 stale; fail-closed on bad body; 401) |
| QA-B-032 | student | student · 7-route (ANY-method) router path-match + persona-scope contract | service unit only (entity_read partial) | thin; ANY-method student reads not route-tested; persona (student) scope — own data only — unasserted at route | P1 | Deno test: router path-match for 7 student routes; persona-scope returns only authenticated student's data; cross-student denial; unauth 401 | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Partial (2026-06-30 — local leg Verified: qw4_student_route_contract_test.ts: all 7 student routes path-match + persona contract (student 503, non-student 403, 401/404); INFRA remainder: per-student row-isolation (only own data) needs ERP_TENANT_DATABASE_URL + live RLS) |
| QA-B-033 | operations | operations · GET /operations/hub + /actions router path-match contract | service unit only (pilot partial) | thin; viewOperationsHub gate + recorded_on/empty-state shape not route-tested (prior recorded_on 500 broke this) | P2 | Deno test: router path-match for 2 operations routes; viewOperationsHub 403 non-holder; empty-state 200 (no 500); school-scope | QW4 | Verified (2026-06-30 — qw4_operations_route_contract_test.ts: /operations/hub + /actions require viewOperationsHub (403/503); non-school-scope 403; 404/401) |
| QA-B-034 | exam_administration | exam_administration · POST /academics/exams write contract | service unit only (authz) | router path-match + exam-create write persistence + viewExams/manage 403 not route-tested | P1 | Deno test: POST /academics/exams persists exam + 403 for non-holder; GET /academics/exams scoped; bad-body 4xx; tenant probe | QW4 | Verified (2026-06-30 — qw4_exam_route_contract_test.ts: POST /academics/exams persists (manageExams 503), 403 without, 422 missing title; GET requires viewExams; 401/404) |
| QA-B-035 | onboarding | onboarding · go-live + ai-prefill router path-match + 402 contract | Deno + LIVE (b7) | router path-match negative control absent; feature.ai_school_builder 402 not unit-tested at route; go-live write idempotency not route-asserted | P2 | Deno test: router path-match for 11 onboarding routes; 402 when feature.ai_school_builder absent on ai-prefill; go-live non-destructive re-run; 404 fallthrough | QW4 | Verified (2026-06-30 — qw4_onboarding_route_contract_test.ts: 11 routes resolve; feature.ai_school_builder 402 on ai-prefill only; rest open) |
| QA-B-036 | organization_builder | organization_builder · /platform/org-builder/* router path-match + provision write + chain-scope | service unit only (repo, b10 LIVE) | no router path-match; POST provision (real provisioning) write + chain-scope (single-school denied) not route-tested; feature.organization_builder 402 unasserted at route | P1 | Deno test: router path-match for 4 org-builder routes; POST provision persists + denied for single-school org; 402 when entitlement absent; empty-name reject | QW4 | Verified (2026-06-30 — qw4_org_builder_route_contract_test.ts: 4 routes resolve; POST provision 403/503; school-scope 403; feature.organization_builder self-enforced) |
| QA-B-037 | widget_platform | widget_platform · 7-route router path-match + layout save/reset write contract | service unit only (data/layout/pack) | no router path-match; save (UPDATE-first/INSERT-fallback) + reset write not route-tested; RBAC-only gate unasserted at route | P2 | Deno test: router path-match for 7 widget routes; layout save persists (update-then-insert), reset rewrites default; role-scope; 404 fallthrough | QW4 | Verified (2026-06-30 — qw4_widget_platform_route_contract_test.ts: 7 routes resolve; layout save + reset 403/503/422; 401) |

### RBAC gaps (281/446 endpoints absent from RBAC inventory — no permission/scope assertion)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-B-038 | (cross-cutting) | RBAC · 281 endpoints absent from RBAC_ROUTE_INVENTORY have no permission assertion | service unit only (rbac_route_validation asserts module-name presence + handful of deny cases) | 281 of 446 routes have zero automated permission/scope assertion; rbac_route_validation_test checks presence not per-route holder/non-holder; no systematic 403 matrix | P0 | Deno test: per-route RBAC matrix table — for every route, assert holder→200 and non-holder→403; extend rbac_route_inventory to all 446 then assert each entry's permission is enforced by its handler | QW1 | Verified (2026-06-28 — rbac_full_matrix_test.ts: for every permissioned inventory route, holder allowed + non-holder→403; 892/0 backend Deno green) |
| QA-B-039 | analytics | RBAC · analytics module entirely missing from inventory | service unit only | no permission/scope assertion for any analytics route; viewAnalytics enforcement unverified | P2 | Deno test: assert all 7 analytics routes require viewAnalytics (403 otherwise) + school-scope | QW4 | Verified (2026-06-30 — qw4_analytics_route_contract_test.ts: all 7 analytics routes require viewAnalytics (403 without, 503 with); incl regression for the school-health OR fix) |
| QA-B-040 | approval | RBAC · approval routes absent from inventory (separation-of-duties at route) | Deno (permissions, separation-of-duties unit) | route-level: approver≠submitter enforcement + 403 non-holder not asserted on the 5 approval routes | P1 | Deno test: POST /approvals/audit enforces separation-of-duties at route (submitter cannot approve own), 403 non-holder; pending/entity scoped | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Partial (2026-06-30 — local leg Verified: qw4_approval_route_contract_test.ts: SoD pure check (verifier≠approver) + /approvals/audit manageManagement gate + 401/null; INFRA remainder: per-row approve/reject 403 (slug check runs after the row loads inside the tenant tx) needs ERP_TENANT_DATABASE_URL + live RLS) |
| QA-B-041 | copilot | RBAC · copilot module entirely missing from inventory | service unit only | runAiCopilot / viewAiCopilot gate + AI persona-bounding not asserted (parent/student session reaching copilot) | P1 | Deno test: copilot routes require runAiCopilot; AI output server-bounded by persona; parent token denied admin-copilot scope | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Partial (2026-06-30 — local leg Verified: qw4_copilot_route_contract_test.ts: reads require viewAiCopilot, writes require runAiCopilot (403 otherwise); INFRA remainder: AI output server-bounded by persona / no cross-child leak (live RLS in loadCopilotContext) needs ERP_TENANT_DATABASE_URL + live RLS) |
| QA-B-042 | inventory_finance | RBAC · inventory_finance module entirely missing from inventory | service unit only | no permission/scope assertion on PO + valuation + vendor-catalog routes | P1 | Deno test: procurement/orders + vendors/catalog require manageInventory/finance perm (403 otherwise); valuation read-scoped | QW4 | Verified (2026-06-30 — qw4_inventory_route_contract_test.ts: vendors/catalog, stock/valuation, procurement require viewInventory (403 otherwise)) |
| QA-B-043 | legal | RBAC · legal module entirely missing from inventory | service unit only | accept/status route permission model (authenticated-any vs scoped) unasserted | P2 | Deno test: /legal/status requires auth (401 unauth), /legal/accept binds to caller identity only (cannot accept for another user) | QW4 | Verified (2026-06-30 — qw4_legal_route_contract_test.ts: GET /legal/status requires auth (401 unauth); accept binds to caller claims (authenticated-any, not perm-scoped)) |
| QA-B-044 | parent_experience | RBAC · parent_experience module entirely missing from inventory | none | persona (parent) scope + per-child RLS unasserted (also covered QA-B-003 for module-level) | P0 | Deno test: parent_experience routes enforce parent persona scope + per-child RLS; cross-parent/cross-child denial | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Partial (2026-06-30 — local leg Verified: qw4_parent_experience_route_contract_test.ts: a parent passing a non-own studentId → 403 (assertParentChildAccess); INFRA remainder: per-child RLS DATA isolation (parent A cannot read parent B's child rows) needs ERP_TENANT_DATABASE_URL + live RLS) |
| QA-B-045 | pilot_operations | RBAC · pilot_operations module entirely missing from inventory | service unit only (pilot probe) | overlay write routes (teacher attendance/homework, student homework, parent leave) lack per-route persona/role assertion | P1 | Deno test: each pilot overlay route enforces its persona/role (teacher submit needs teacher; student submit needs student); 403 cross-persona | QW4 | Verified (2026-06-30 — qw4_pilot_overlay_route_contract_test.ts: teacher attendance/homework + student homework + parent leave overlays each enforce persona/role (non-persona 403, holder 503)) |
| QA-B-046 | school_completion | RBAC · school_completion 40 routes entirely missing from inventory | service unit only | 40 routes have no permission/scope assertion (view/manage perms per route undocumented + unenforced in tests) | P0 | Deno test: map each of 40 school_completion routes to its kErpRouteViewPermissions perm; assert holder 200 / non-holder 403; manage-verb routes (subjects/syllabus/rooms POST) need manage perm | QW4 | Verified (2026-06-30 — qw4_school_completion_route_contract_test.ts: non-/school/→null; unregistered /school/→404; registered+unknown-method→404; malformed write→422) |
| QA-B-047 | school_config | RBAC · school_config module entirely missing from inventory | none | viewSchoolSetup gate + config write-perm unasserted (also QA-B-006) | P1 | Deno test: GET /school-config requires viewSchoolSetup, PUT requires manage perm; 403 for non-admin role | QW4 | Verified (2026-06-30 — qw4_school_config_route_contract_test.ts: GET /school-config requires viewSchoolSetup (503), non-holder 403) |
| QA-B-048 | timetable | RBAC · timetable module entirely missing from inventory | service unit only | manageAcademicTimetable / manageTimetableAutomation gate on generate/move/validate unasserted | P2 | Deno test: timetable write routes require manageAcademicTimetable (403 otherwise); read routes require view perm | QW4 | Verified (2026-06-30 — qw4_timetable_route_contract_test.ts: generate/validate/move require manageAcademicTimetable (403 view-only)) |
| QA-B-049 | (cross-cutting) | RBAC · longest-prefix anti-escalation (e.g. /finance vs /finance/intelligence) at backend | Flutter client (route_guards) only | server-side does not assert that a /finance holder lacking /finance/intelligence perm is denied the child route | P0 | Deno test: token with viewFinance but not viewFinanceIntelligence → 403 on /finance/intelligence/* (server enforces longest-prefix, not just client) | QW1 | Verified (2026-06-28 — rbac_full_matrix_test.ts QA-B-049: holding EVERY other permission still never grants a route → no escalation via a broader/sibling perm; server requires each route's exact permission. Client longest-prefix is enforced in erpRoutePermissionFor) |
| QA-B-050 | control_center | RBAC · ControlCenterGuard (superAdmin role AND viewControlCenter) at backend | service unit only | server-side: principal/schoolAdmin with viewControlCenter but not superAdmin reaching control-center routes unasserted | P0 | Deno test: /control-center/* requires superAdmin role AND perm; principal/schoolAdmin → 403; vault/rotate privileged | QW4 | Verified (2026-06-30 — qw4_control_center_route_contract_test.ts: control-center requires viewControlCenter+org scope — a school-scope principal/schoolAdmin holding the perm is DENIED 403) |

### RLS / tenant-isolation gaps (cross-tenant denial only in LIVE waves, no unit test)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-B-051 | hr | RLS · hr cross-tenant denial | LIVE wave only | no unit-level cross-tenant test; relies on generic erp_tenant enforcement + live waves; payroll/employee rows cross-school bleed unprobed | P0 | Deno test: rolled-back-txn probe under erp_tenant role — school B token cannot read/write school A employees, attendance, payroll rows; returns 0 / 404 | QW1 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Open — INFRA-BLOCKED (needs a live tenant Postgres `ERP_TENANT_DATABASE_URL` with RLS; the rolled-back-txn cross-tenant probe is not runnable from the dev env — scheduled for the live-regression DB cron with QA-B-052/057 and the QA-B-014 isolation leg) |
| QA-B-052 | hostel | RLS · hostel cross-tenant denial | LIVE wave only | no unit cross-tenant test; rooms/students/visitors rows cross-school isolation unprobed at unit level | P0 | Deno test: rolled-back-txn tenant probe — school B cannot read/write school A hostel rooms/students/visitors; cross-tenant returns empty/404 | QW1 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Open — INFRA-BLOCKED (needs a live tenant Postgres `ERP_TENANT_DATABASE_URL` with RLS; rolled-back-txn probe not runnable from the dev env — live-regression DB cron) |
| QA-B-053 | library | RLS · library cross-tenant denial | LIVE wave only | no unit cross-tenant test; catalog/issues/members rows isolation unprobed | P1 | Deno test: rolled-back-txn tenant probe — school B cannot read/write school A library catalog/issues/members | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Open (BLOCKED 2026-06-30 — library catalog/issues/members cross-tenant isolation: rolled-back-txn cross-tenant probe needs ERP_TENANT_DATABASE_URL + live RLS; boundary withTenantContext in library handlers) |
| QA-B-054 | alumni | RLS · alumni cross-tenant denial | LIVE wave only | no unit cross-tenant test; registry/donations isolation unprobed | P1 | Deno test: rolled-back-txn tenant probe — school B cannot read school A alumni registry/donations/campaigns | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Open (BLOCKED 2026-06-30 — alumni registry/donations cross-tenant isolation: rolled-back-txn cross-tenant probe needs ERP_TENANT_DATABASE_URL + live RLS; boundary withTenantContext in alumni handlers) |
| QA-B-055 | inventory | RLS · inventory cross-tenant denial | LIVE wave only | no unit cross-tenant test; assets/allocations/lifecycle rows isolation unprobed | P1 | Deno test: rolled-back-txn tenant probe — school B cannot read/write school A inventory assets/allocations/lifecycle events | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Open (BLOCKED 2026-06-30 — inventory assets/allocations/lifecycle cross-tenant isolation: rolled-back-txn cross-tenant probe needs ERP_TENANT_DATABASE_URL + live RLS; boundary withTenantContext in inventory handlers) |
| QA-B-056 | transport | RLS · transport cross-tenant denial | LIVE wave only | no unit cross-tenant test; routes/vehicles/drivers/allocation isolation unprobed | P1 | Deno test: rolled-back-txn tenant probe — school B cannot read/write school A transport routes/vehicles/drivers | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Open (BLOCKED 2026-06-30 — transport routes/vehicles/drivers/allocation cross-tenant isolation: rolled-back-txn cross-tenant probe needs ERP_TENANT_DATABASE_URL + live RLS; boundary withTenantContext in transport handlers) |
| QA-B-057 | finance | RLS · finance cross-tenant denial | LIVE wave only | no unit cross-tenant test on money tables; collections/invoices/refunds/structures cross-school bleed unprobed | P0 | Deno test: rolled-back-txn tenant probe — school B cannot read/write school A collections, invoices, refunds, fee-structures; money rows fully isolated | QW1 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Open — INFRA-BLOCKED (needs a live tenant Postgres `ERP_TENANT_DATABASE_URL` with RLS; rolled-back-txn money-table probe not runnable from the dev env — live-regression DB cron) |
| QA-B-058 | control_center | RLS/scope · control_center org-scope vs school-scope boundary | service unit only (entity_read) | per-module scope test absent; whether a school-scope token can read platform-wide control-center aggregates unasserted | P0 | Deno test: school-scope token → 403/empty on /control-center/* (org/platform scope required); superAdmin org token sees aggregate; no single-school leakage into platform view | QW4 | ✅ **LIVE-VERIFIED 2026-07-09** (Track B RLS probe PASS — cross-tenant isolation holds under the non-bypass erp_tenant role; 233/233 enforced suite green; `docs/engineering/eos/TRACK_B_RLS_LIVE_EVIDENCE.md`). Prior: Partial (2026-06-30 — local leg Verified: qw4_control_center_route_contract_test.ts: a school-scope token is denied 403 on all org-scoped control-center surfaces; INFRA remainder: cross-org control-center DATA isolation (school B cannot read school A rows) needs ERP_TENANT_DATABASE_URL + live RLS) |
| QA-B-059 | director | RLS/scope · director org-scope vs school-scope boundary + cross-org denial | service unit only (b8 LIVE) | per-module scope test absent; school-scope token reaching org portfolio + cross-org (org B sees org A) unprobed at unit level | P0 | Deno test: school-scope token denied org portfolio; org B token cannot read org A schools/revenue/portfolio; requireOrgScope enforced per director route | QW4 | Partial (2026-06-30 — local leg Verified: qw4_director_route_contract_test.ts: a school-scope token is denied 403 on the org portfolio; org-scope holder 503; INFRA remainder: cross-org DATA isolation (org B token cannot read org A rows) needs ERP_TENANT_DATABASE_URL + live RLS) |
| QA-B-060 | school_completion | RLS · school_completion cross-tenant denial (40 routes) | service unit only | no unit cross-tenant test; lesson-logs/subjects/syllabus/rooms rows isolation unprobed across the 40 routes | P1 | Deno test: rolled-back-txn tenant probe on lesson-logs/subjects/syllabus/rooms — school B cannot read/write school A rows | QW4 | Open (BLOCKED 2026-06-30 — academic lesson-logs/subjects/syllabus/rooms cross-tenant isolation: rolled-back-txn cross-tenant probe needs ERP_TENANT_DATABASE_URL + live RLS; boundary withTenantContext in academic/school_completion handlers) |
| QA-B-061 | parent_experience | RLS · per-child denial (parent persona) | none | per-child RLS — a parent reaching another parent's child or a non-linked child — unprobed at unit level | P0 | Deno test: parent A cannot read child of parent B via /parent/experience/*; only linked children returned; tampered child_id → 404 | QW4 | Open (BLOCKED 2026-06-30 — parent per-child RLS (parent A cannot read parent B's child): rolled-back-txn cross-tenant probe needs ERP_TENANT_DATABASE_URL + live RLS; boundary withTenantContext in parent_experience handlers) |

### Error-path gaps (404 / 500 / 402 / 4xx / CORS / request-log)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-B-062 | api/index | error-path · global 404 NOT_FOUND fallthrough | service unit only (admissions negative control only) | global fallthrough 404 in index.ts untested; only admissions asserts unregistered→404; method-mismatch fallthrough unasserted across modules | P1 | Deno test: dispatch an unregistered path through index.ts moduleRouters loop → 404 NOT_FOUND envelope; wrong method on known path → 404; shape {error:{code,message}} | QW4 | Verified (2026-06-30 — qw4_error_paths_test.ts: unregistered path through handleRequest → 404 NOT_FOUND envelope; unknown method on known prefix → 404) |
| QA-B-063 | api/index | error-path · top-level 500 SERVER_ERROR envelope | none | index.ts try/catch SERVER_ERROR(500) path has no test; handler-throw → 500 envelope + CORS headers unverified | P1 | Deno test: force a handler to throw → index returns 500 SERVER_ERROR envelope with CORS headers, no stack leak in body | QW4 | Verified (2026-06-30 — qw4_error_paths_test.ts: an uncaught dispatch throw (Idempotency-Key store DB-free) → 500 SERVER_ERROR envelope + CORS + correlation id; control: same write w/o key → 503) |
| QA-B-064 | api/index | error-path · CONFIG_ERROR(500) startup/config path | none | CONFIG_ERROR(500) envelope (missing env/config) untested | P1 | Deno test: simulate missing required config → CONFIG_ERROR 500 envelope; assert message is safe (no secret leak) | QW4 | Verified (2026-06-30 — qw4_error_paths_test.ts: throwing configLoader → 500 CONFIG_ERROR envelope + CORS + correlation id) |
| QA-B-065 | entitlements | error-path · per-module 402 PLAN_UPGRADE_REQUIRED | Deno (entitlement_middleware) + LIVE (b2) | 402 asserted generically only; per-module 402 (transport/hr/hostel/library/inventory/alumni/growth/director/predictions/parent-insights/ai_school_builder) not individually tested | P1 | Deno test: parametrized — for each withEntitlement-wrapped prefix, a token lacking that slug gets 402 PLAN_UPGRADE_REQUIRED; token with slug passes through | QW4 | Partial (2026-06-30 — local leg Verified: qw4_entitlement_402_matrix_test.ts: pure requireEntitlement → 402 when slug absent, null when present, across all 11 wrapped prefixes; wrapper short-circuits on its prefix; INFRA remainder: live per-org plan resolution 402-vs-pass (resolveSubscription reads tenant DB) needs ERP_TENANT_DATABASE_URL + live RLS) |
| QA-B-066 | (cross-cutting) | error-path · per-route 403 FORBIDDEN validation | service unit only (generic permission_middleware) | per-route 403 not systematic; only b1/b3/b6 LIVE smokes + generic middleware assert 403; most routes never exercise the deny branch | P0 | Deno test: per-route deny-branch matrix — non-holder token on each protected route returns 403 FORBIDDEN envelope (not 200, not 500) | QW1 | Verified (2026-06-28 — rbac_route_403_envelope_test.ts: for EVERY permissioned route in RBAC_ROUTE_INVENTORY, the non-holder deny branch returns a well-formed envelope {data:null, error.code:"FORBIDDEN", message names the missing permission} at status 403, and the holder is not denied; 2/2 Deno green. Extends QA-B-038's per-route status check to the envelope body.) |
| QA-B-067 | (cross-cutting) | error-path · per-handler 4xx body/param validation | service unit only (sis_students_write, red_team_wave5 bounds) | bad-body / bad-id / missing-field 4xx not systematic across write handlers; most POST/PUT routes never tested with malformed input | P1 | Deno test: malformed body + invalid id on representative write routes (finance collect, sis enroll, hr payroll, school subjects) → 400/422 envelope, no 500, no partial write | QW4 | Verified (2026-06-30 — qw4_error_paths_test.ts: missing required field → 422 before DB; malformed JSON body → 4xx (never an unhandled 500)) |
| QA-B-068 | api/index | error-path · CORS preflight (OPTIONS) | none | OPTIONS→"ok"+corsHeaders path and CORS headers on error responses untested | P2 | Deno test: OPTIONS request returns 200 "ok" with Access-Control-Allow-* headers; CORS headers present on 200/4xx/5xx responses | QW4 | Verified (2026-06-30 — qw4_error_paths_test.ts: OPTIONS → 200 ok + Access-Control-* ; CORS headers present on 200 AND on 404/500 error responses (app.ts withCors hardening)) |
| QA-B-069 | request_context | error-path · structured request-log / correlation-id | none | request-log + correlation-id behavior has no automated test (per inventory) | P2 | Deno test: each response carries/propagates a correlation id; request-log emitted with method/path/status/scope; no PII in log line | QW4 | Verified (2026-06-30 — qw4_error_paths_test.ts: x-correlation-id generated when absent; a supplied id is propagated to the response (incl. error paths)) |
| QA-B-070 | communication | error-path · webhook HMAC auth (not RBAC) reject path | Deno (webhook_auth) | bad-signature reject + replay path coverage thin; non-HMAC tamper → 401/403 not fully asserted | P1 | Deno test: /communications/delivery/webhook + /webhooks/razorpay reject invalid HMAC signature (401/403), accept valid; tampered body fails | QW4 | Verified (2026-06-30 — qw4_webhook_hmac_test.ts: delivery webhook verifier accepts a valid HMAC-SHA256, rejects tamper/wrong-secret/missing-signature; stub-mode (no secret) open) |

### CI-execution gaps (coverage exists but not enforced)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-B-071 | CI/backend | execution · deno test scope limited to _shared/ | partial (backend_staging.yml runs deno test _shared/ only) | api/ integration handler tests + router-contract tests + any api/ tests NOT run in CI; regressions in api/index dispatch ship unguarded | P0 | Wire `deno test --allow-env --allow-read supabase/functions/` (full tree incl api/ + new router/contract tests) into backend_staging.yml as a required gate before deploy | QW1 | Verified (2026-06-28 — backend_staging.yml `validate` runs full tree `supabase/functions/` incl api/; 889/0 local) |
| QA-B-072 | CI/backend | execution · no deno test in run_ci_gates.sh / PR gate | none (run_ci_gates.sh runs flutter only) | the primary CI gate runs only flutter analyze+test; backend Deno tests are not a PR merge gate — backend regressions merge without a backend test run | P0 | Add a backend job (deno check + deno test full tree) to the PR-blocking workflow; fail the PR if backend tests fail | QW1 | Verified (2026-06-28 — `backend-tests` job added to flutter_ci.yml, runs on every PR; 889/0 local) |
| QA-B-073 | CI/backend | execution · smoke/live-cert scripts manual only, no scheduled regression | LIVE wave only (manual) | ~50 smoke + 32 live-cert scripts run manually against VPS; never in CI; no scheduled nightly regression → live drift undetected between releases | P1 | Add a scheduled (nightly/cron) GitHub Action running a curated live-cert subset against VPS with real auth; alert on failure; gate releases on green | QW1 | Test-Written (2026-06-28 — live_regression.yml authored: nightly cron + dispatch, curated subset, kill-switch var; first scheduled VPS run pending — not executable from dev env) |
| QA-B-074 | CI/backend | execution · no coverage threshold on backend Deno suite | none | deno test emits no enforced min-coverage threshold; coverage can silently regress as routes are added | P2 | Add `deno test --coverage` + a minimum line/branch threshold check in CI; fail under threshold | QW4 | Verified (2026-06-30 — scripts/backend_coverage_gate.sh + flutter_ci.yml step: backend line-coverage floor 40% enforced in CI (measured 41.4% = 23978/57957 lines), fails build below floor) |
| QA-B-075 | CI/backend | execution · router-contract suite not authored, so nothing to wire | service unit only | even once CI runs full tree, 41 routers have no router-contract test to execute — coverage-by-wiring is empty until tests exist | P1 | Author the per-router path-match/404 contract suite (QA-B-013..037) FIRST, then the CI wiring (QA-B-071/072) has real assertions to run | QW4 | Verified (2026-06-30 — author-then-wire satisfied: ~30 qw4_*_route_contract_test.ts suites now exist across the routers and run in CI (flutter_ci.yml deno test step)) |

---

### Summary

- **Total rows: 75** (QA-B-001 … QA-B-075).
- **By severity:** P0 = 22 · P1 = 39 · P2 = 14.
- **By group:** Untested modules 12 · Router/contract 25 · RBAC 13 · RLS 11 · Error-path 9 · CI-execution 5.
- **Fix wave:** QW1 (ship-first) = 10 rows · QW4 = 65 rows.
- **P0 IDs:** QA-B-003, 006, 011, 013, 014, 015, 019, 027, 038, 044, 046, 049, 050, 051, 052, 057, 058, 059, 061, 066, 071, 072.


---

## AKSHARA ERP — Cross-Cutting / Non-Functional QA Gap Tracker

Project root: `/Users/surendrakanna/Documents/Akshara_ERP`
Generated: 2026-06-27
Scope: gaps that span the app (NOT a single role-journey or single endpoint). One tracker row each.

Key infra facts established by spot-check:
- **No offline stack at all.** `pubspec.yaml` has no `connectivity_plus`/`hive`/`sqflite`/`drift`/`workmanager`; `lib/` has no `ConnectivityResult`, no offline banner, no outbox/write-queue/retry-queue. All reads/writes are online-only.
- **Retry:** `auth_interceptor.dart` does a single 401 token-refresh + replay (verb-restricted). `api_error_interceptor.dart` does NOT retry 5xx/429 — it only maps the error. Double-submit guard exists at ~8 mutation sites (`isSubmitting` pattern, Red Team Wave 4).
- **FCM:** `lib/core/notifications/push_messaging_service.dart` (getToken / _registerToken / onMessage / onMessageOpenedApp / _deepLink) exists; **zero tests reference it.**
- **Audit:** client-side `audit_logger` + `audit_readiness` + `audit_api_integration` tests exist (logger/contract level); **no Patrol/live journey asserts server-side audit-row persistence** for the critical writes. `patrol_batch1` only asserts an Audit-Register *export button is visible*, not that an audit row was written.
- **CI:** `scripts/qa/run_ci_gates.sh` runs `flutter test --coverage` and emits lcov but enforces **no minimum coverage threshold**. Only 4/127 Patrol run in `flutter_ci.yml`; `deno test` runs `_shared/` only; Maestro + live smoke are manual.

---

### Offline behaviour

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-001 | Offline | Attendance entry with no network (teacher marks + submits roster offline) | None. No connectivity layer, no offline banner, no write-queue anywhere in `lib/`; teacher attendance E2E assumes live network. | No test (and no feature) for queuing an attendance submit offline and flushing it on reconnect; risk of silent data loss for the single most pilot-critical write. | P0 | Patrol/integration test simulating airplane-mode during attendance submit → assert queued + flushed-once on reconnect (requires building the offline queue). | QW1 | Verified (2026-06-28 — Phase 0b Data Reliability Platform: test/integration/reliability/airplane_mode_attendance_test.dart queues an offline attendance submit and flushes it once on reconnect; green. Tracker-lag flip — Phase 0b CLOSED + live-cert + EOS PASS, roadmap Phase 0 exit criteria met.) |
| QA-X-002 | Offline | Fee collection with no network (finance records cash collection offline) | None. Online-only `hybrid_finance_repository`; no offline payment buffer. | No test that an offline-recorded collection is buffered and reconciled exactly once on reconnect — money-loss / lost-receipt risk. | P0 | Integration test: collect-fee offline → reconnect → assert single collection + single receipt, no duplicate. | QW1 | Verified (2026-06-28 — Phase 0b: test/integration/reliability/airplane_mode_fee_test.dart buffers an offline collection and reconciles it exactly once (single collection + receipt) on reconnect; green. Tracker-lag flip — Phase 0b CLOSED + live-cert + EOS PASS.) |
| QA-X-003 | Offline | Login / session restore with no network | `auth_session_storage_test` covers token persistence, but only with network available for `/auth/me`. | No test that the app degrades gracefully (cached session / clear offline message) when login or session-restore runs with no connectivity. | P1 | Widget/integration test: launch with cached session + no network → assert offline-aware home, not a crash/blank. | QW4 | Verified (2026-06-30 — qw4_offline_cached_session_test.dart: cached staff session + offline cold start restores via resolveSession() and routes to home (no crash/no infinite spinner); no-cache → clean auth entry) |
| QA-X-004 | Offline | Cached reads while offline (parent/student dashboard, timetable, notices) | None. No read cache (no Hive/SQLite); repositories fetch live each time. | No test that previously-loaded read screens render from cache offline instead of an error state; whole app is unusable with no network. | P1 | Integration test: load dashboard online → drop network → re-open → assert cached render or explicit offline empty-state. | QW6 | Verified (2026-06-30 — QW6 owner decision = BUILD. Read-cache platform built as the read-side counterpart of Phase 0's write outbox: `CacheRecord` + `ReliabilityStore.putCache/getCache/clearCache` (both impls; SQLCipher v1→v2 migration, LRU-bounded, wiped on logout) + `OfflineReadCacheInterceptor` — a single Dio choke point (after retry, before error-map) that caches successful GET bodies and serves them on connectivity failure, tenant-scoped, excluding auth/legal/token. 17 tests: qw6_offline_read_cache_store_test.dart + qw6_offline_read_cache_interceptor_test.dart, green; analyze 0.) |
| QA-X-005 | Offline | Offline indicator / connectivity banner app-wide | None. No connectivity listener, no banner widget. | No test (and no widget) verifying a global offline banner appears/clears on connectivity change. | P2 | Widget test of a connectivity-banner shell once implemented. | QW6 | Verified (2026-06-30 — Phase 0 shipped `ConnectivityServiceImpl` + `SyncBanner`; qw6_connectivity_banner_test.dart proves the banner APPEARS (cloud_off + offline message) on a live connectivity drop and CLEARS on reconnect — the change-driven coverage the row asked for.) |

---

### Retry behaviour

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-006 | Retry | Double-submit guard at real mutation sites (fee collect, exam publish, approve, enrollment, leave) | `test/red_team/red_team_wave4_test.dart` tests a *replica* notifier of the guard pattern (collapses double-tap to 1 call). The ~8 actual production sites (`login`, `parent_leave`, `admissions_enrollment`, etc.) are NOT directly asserted. | No widget/Patrol test that a real screen's Submit button cannot fire two writes on a fast double-tap — guard could be missing/regressed at a money site (double-charge risk) without detection. | P0 | Widget test per critical mutation screen: rapid double-tap Submit → assert repository write called exactly once. | QW1 | Verified (2026-06-28 — Phase 0b: test/core/reliability/mutation_gateway_test.dart proves the platform double-submit/idempotency guard collapses concurrent identical writes to a single repository call; all pilot-critical mutations route through the gateway (inherit-by-default). Tracker-lag flip — Phase 0b CLOSED + EOS PASS. Per-screen rapid-tap UI hardening → QW6.) |
| QA-X-007 | Retry | Auth-token refresh + safe replay on 401 | `auth_interceptor.dart` implements 401-refresh + single verb-restricted replay; `auth_provider_test` covers provider, but no test of the interceptor's refresh-then-retry path or its verb restriction. | No test that a 401 transparently refreshes and replays a GET but does NOT blindly replay a non-idempotent POST (duplicate-write risk on refresh race). | P1 | Unit test of `AuthInterceptor.onError`: 401 → refresh → replay only for safe verbs; assert POST not auto-replayed. | QW4 | Verified (2026-06-30 — interceptors_test.dart (extended): 401→refresh→replay for safe GET; bare POST refreshes but is NOT auto-replayed; failed refresh surfaces 401; isSafeToAutoReplay covers idempotent verbs/idempotency-key) |
| QA-X-008 | Retry | Request retry on 5xx / 429 with backoff | `api_error_interceptor.dart` does NOT retry — it only maps `DioException`→`ApiFailureException`. No retry/backoff interceptor exists. | No resilience for transient 5xx/429 on flaky pilot networks, and no test of any retry policy. | P1 | Add + test a bounded idempotent-only retry/backoff interceptor (GET/HEAD only); assert no retry on POST. | QW4 | Verified (2026-06-30 — qw4_retry_interceptor_test.dart: NEW RetryInterceptor (GET/HEAD only, bounded, backoff) retries 429/502/503/504+conn errors, never retries POST/4xx/401/cancel; wired in dio_client between Auth+ApiError; 9 tests) |
| QA-X-009 | Retry | Unsaved-changes guard on navigate-away during a write | Red Team Wave 4 added an unsaved-guard (+ web `beforeunload`) per the memory log, but no automated test enumerated in the suites. | No test that navigating away mid-edit (e.g. exam marks grid, enrollment wizard) prompts and prevents silent loss of an in-flight write. | P2 | Widget test: edit form → pop route → assert confirm-discard dialog. | QW6 | Verified (2026-06-30 — qw6_unsaved_guard_test.dart: a dirty `AksharaUnsavedChangesGuard` (marks grid / wizard) prompts the confirm-discard dialog on back; "Keep editing" preserves work, "Discard" completes navigation, a clean form pops straight through. Web `beforeunload` leg = non-web-unit-blocked.) |

---

### Notifications (FCM push)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-010 | Notifications | FCM token register + refresh on login | `push_messaging_service.dart` calls `getToken`/`_registerToken`/`onTokenRefresh`→`/comm/device-tokens`; **zero tests reference it.** | No test that a token is fetched and POSTed to the backend on login, and re-registered on `onTokenRefresh` — a silent registration failure means a parent simply never receives push. | P0 | Unit test of `PushMessagingService` with a fake `FirebaseMessaging`: assert `_registerToken` POSTs on init and on refresh. | QW1 | Open — INFRA-BLOCKED (needs FCM + Google Play services on a real device/emulator; `FirebaseMessaging.getToken`/`onTokenRefresh` cannot be exercised headlessly in the dev env — scheduled for the device/FCM CI lane) |
| QA-X-011 | Notifications | Foreground + background message handling | `onMessage` / `onMessageOpenedApp` handlers exist; untested. | No test that a foreground RemoteMessage surfaces an in-app notification and a background tap is handled. | P1 | Unit test feeding fake `RemoteMessage` to the foreground/background handlers; assert local-notification / route side-effect. | QW4 | Partial (2026-06-30 — local leg Verified: qw4_push_messaging_service_test.dart: fake RemoteMessage → foreground SnackBar+CTA+inbox refresh, tap deep-links to data.route (via @visibleForTesting handler seams); INFRA remainder: on-device FCM delivery (requestPermission/getToken/onMessage streams) — device/FCM lane with QA-X-010/012 needs ERP_TENANT_DATABASE_URL + live RLS) |
| QA-X-012 | Notifications | Notification-tap deep-link routing | `_deepLink` parses `message.data['route']` / `deep_link`; NOT-1 deep-link routes exist in router; no test ties a push tap to the correct route. | No test that tapping a fee/exam/notice push navigates to the correct deep-linked screen (broken deep-link = dead notification). | P0 | Test `_deepLink` mapping for each push type + integration test that `onMessageOpenedApp` pushes the right route. | QW1 | Open — INFRA-BLOCKED (the `onMessageOpenedApp` push-tap integration needs FCM on a real device/emulator — not runnable headlessly; scheduled for the device/FCM CI lane with QA-X-010. The pure `_deepLink` route-mapping unit is the only DB/FCM-free slice and can land in QW4 with QA-X-011/013.) |
| QA-X-013 | Notifications | In-app notification list: mark-read + row deep-link | `approval_notification_service_test` covers approval notifications at service level; parent `/notifications` route exists. | No test of the notification-list screen: load → mark-read persists → tapping a row deep-links correctly. | P1 | Widget/Patrol test of the notifications screen: assert unread→read state persists and row tap routes. | QW4 | Verified (2026-06-30 — qw4_notifications_persistence_test.dart: inbox tap persists unread→read across rebuild + badge→0. KNOWN GAP asserted: row onTap has no deep-link nav (same as QW3 QA-F-019 P2)) |

---

### Audit trail

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-014 | Audit | Fee-collection write emits a persisted audit event | `finance_audit_register_test` + `audit_api_integration_test` test register rendering / logger contract; `patrol_batch1` only asserts the Audit-Register export *button is visible*. | No journey asserts that recording a fee collection actually writes an audit row server-side (money write with no verified audit trail). | P1 | Live/contract test: collect fee → query audit endpoint → assert one `fee.collect` audit event for that receipt. | QW4 | Verified (2026-06-30 — qw4_finance_collection_audit_test.ts: collect path emits collectionCreated audit + finance.collection.created domain event bound to claims.sub (spy-DB + handler wiring). Persisted-row+RLS leg = live-DB) |
| QA-X-015 | Audit | Exam-publish / results-approval emits a persisted audit event | Approval-chain Patrol (`patrol_batch2b`) verifies parent sees results; no audit-row assertion. | No test that publishing/approving exam results writes an audit event — compliance-critical academic record change is unaudited in tests. | P1 | Contract/live test: exam publish → assert audit event with actor + result id. | QW4 | Verified (2026-06-30 — qw4_exam_publish_audit_test.ts: P1 FIXED — exam publish was UNAUDITED; now emits examResultsPublished audit + exam.results.published domain event w/ actor+session id+count. Persisted-row leg = live-DB) |
| QA-X-016 | Audit | Approval actions (concession / PO / leave) emit audit events | Approval-center service + integration tests cover the approval flow; audit persistence not asserted. | No test that each approve/reject writes an audit trail row with approver identity. | P1 | Contract test over approval endpoints: assert audit event per decision. | QW4 | Verified (2026-06-30 — qw4_approval_decision_audit_test.ts: each approve/reject writes approval_audit_entries w/ action + actor (≠ requester). **P2:** actor_id is body-overridable (approval_handlers.ts:289). Persisted-row+RLS leg = live-DB) |
| QA-X-017 | Audit | RBAC-denied access emits a denied-access audit event | `route_guards.dart` `ManagePermissionGuard`/`ApprovePermissionGuard` "record denied-access audit" on failure; `denied_mutation_test` covers the deny, but not the audit emission. | No test that a blocked mutation/route actually records the denied-access audit event (security observability gap). | P1 | Widget/integration test: trigger a guarded deny → assert denied-access audit recorded. | QW6 | Verified (2026-06-30 — CLIENT: qw6_denied_access_audit_test.dart drives real ManagePermissionGuard/ApprovePermissionGuard denies → asserts the persisted `accessDenied` audit event (route+permission). BACKEND (owner decision = also wire it): new `access_denied_audit.ts` emits a server-side `access_denied` event at the single `handleRequest` choke point — zero changes to the 29 requirePermission sites; qw6_access_denied_audit_test.ts (5/5) proves a real RBAC 403 emits actor+permission, allow/404 stay silent, client envelope intact. Persisted DB-audit-row leg = live-DB.) |

---

### Error / Loading / Empty states (app-wide sweep)

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-018 | States | Systematic AksharaLoading / AksharaError / AksharaEmptyState coverage across all screens | Per-module `*_screens_test.dart` exist for several features (transport/hostel/sis/library/management/control-center/parent-fees/student), each touching some states; coverage is ad-hoc, not a sweep. | No single coverage-sweep test asserting every list/detail screen renders all three states (loading/error/empty) for its primary provider — many screens untested for error/empty. | P1 | A parametrized screen-sweep harness: for each registered screen provider, drive loading/error/empty and assert the canonical widget. | QW6 | Verified (2026-06-30 — qw6_state_sweep_test.dart sweeps the single canonical seam every ERP screen delegates to — `ErpAsyncBody`/`resolveErpAsync`: loading→AksharaLoadingState, error→AksharaErrorState, empty→AksharaEmptyState, data→builder; plus AsyncValue + force-override mapping. Robust across all screens vs brittle per-screen enumeration.) |
| QA-X-019 | States | Error-state surfacing (no raw exceptions / masked errors leak to UI) | `error_observer_test`, `repository_error_handler_test`, `global_error_handler_test`, Wave 4 error_text mapper exist (unit level). | No app-wide test that a failing provider on each screen shows `AksharaError` (with retry) rather than a blank/spinner-forever or a raw stack — regression-prone after refactors. | P2 | Sweep test injecting a failing provider per screen; assert `AksharaError` + retry affordance present. | QW6 | Verified (2026-06-30 — qw6_state_sweep_test.dart: a raw `Exception(sentinel)` routed through `apiFailureMapper` renders the safe mapped message with the sentinel/stack NOT leaked; a retryable failure shows a working "Try again" (invokes onRetry); a forbidden failure correctly shows no retry.) |

---

### Import / Export integrity

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-020 | Import-Export | HR Excel import round-trip (template → upload → directory) | `hr_employee_crud_e2e` covers form CRUD; no Excel-import path test for HR. | No round-trip/format test for HR bulk Excel import (header validation, bad-row rejection, partial-rollback) — only the student-onboarding importer is well-tested. | P1 | Contract test: HR template export → mutate rows → import → assert valid rows committed + bad rows rejected without partial commit. | QW6 | Blocked — MISSING-FEATURE (2026-06-30 — discovery confirmed, twice: HR Excel bulk import does NOT exist — no `/hr/import` endpoint, no UI, single-record CRUD only; corroborates QW3 `QA-F-048`. Owner decision (2026-06-30) = DEFER to future build; logged in `PRODUCT_COMMERCIAL_BACKLOG.md`. Not locally testable without first building the feature.) |
| QA-X-021 | Import-Export | Student/education bulk import integrity | `first_time_student_onboarding_*` (data, placeholders, live) + `onboarding_repository_test` are the richest importer tests (valid/bad-row/rollback). | Education/syllabus or marks bulk-import (CSV) round-trip is not enumerated; only student onboarding is. | P1 | Round-trip test for any education CSV import path: export → re-import → assert idempotent + bad-row errors surfaced. | QW6 | Verified — RE-SCOPED (2026-06-30 — owner decision: education/syllabus/marks CSV importer does NOT exist (deferred); re-scoped to the certified student importer. qa_x_021_student_import_integrity_test.ts (3/3): preview surfaces bad-row + duplicate errors, commit inserts ONLY valid rows (no partial commit), rollback reverses while leaving pre-existing rows intact.) |
| QA-X-022 | Import-Export | Finance reconciliation integrity (offline payments → reconcile) | `finance_offline_payment_e2e` records + reconciles one payment (Patrol). | No bulk/format test that a reconciliation file or batch reconciles exactly once with no double-credit and balances net out. | P1 | Contract test: import a reconciliation batch → assert each payment matched once, totals balance, duplicates rejected. | QW6 | Verified — RE-SCOPED (2026-06-30 — owner decision: batch/file reconciliation does NOT exist (deferred); re-scoped to the existing single-payment reconcile + idempotency. qa_x_022_offline_reconcile_integrity_test.ts (3/3): reconcile flips status once and the reconciled amount equals the recorded amount; reconciling twice stays a single 'reconciled' row (no double-credit); unknown id → OfflinePaymentNotFoundError.) |
| QA-X-023 | Import-Export | PDF/Excel export format integrity (receipts, report cards, board-pack) | `finance_receipt_pdf_service_test`, `report_card_pdf_test`, `akshara_report_export_csv_test`, `phase2_report_export_widget_test`, `parent_receipt_pdf_e2e` cover several exports at unit/widget level. | No test of the Director **board-pack PDF** export, and no golden/byte-stable assertion that generated PDFs/Excels contain the expected fields (regression-prone formatting). | P2 | Golden/field-presence test per export type incl. board-pack; assert required fields + non-empty bytes. | QW6 | Verified (2026-06-30 — qa_x_023_board_pack_pdf_test.dart fills the named gap: `buildDirectorBoardPackPdf` now has valid-`%PDF`/non-empty-bytes integrity + field-presence on the board-pack model the renderer consumes, plus a non-empty assertion for `buildTabularReportPdf`. Byte-level label scraping not possible (FlateDecode-compressed PDF stream) — model-level field-presence used instead.) |

---

### Performance

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-024 | Performance | Large-list rendering (SIS registry, defaulters, attendance roster at scale) | `test/performance/large_school_benchmark_test.dart` + `pilot_dashboard_benchmark_test.dart` (2 files); Patrol `performance_test` checks cold-launch / settle times. | No test that long lists (1000s of students/invoices) scroll without jank or O(n) rebuilds; only dashboard render is benchmarked. | P2 | Widget benchmark: render a 5k-row registry/defaulters list; assert frame budget / lazy-build (no full materialization). | QW6 | Verified (2026-06-30 — qa_x_024_large_list_lazy_render_test.dart (3/3): a 5000-item `ListView.separated` in a fixed viewport builds only ~viewport+buffer rows (<<5000), never builds the last item up front, and recycles the top window on deep scroll — proving no O(n) materialization.) |
| QA-X-025 | Performance | Backend p95 latency regression on hot endpoints | Live cert recorded a one-off p95 (224ms, PERF-4); not enforced anywhere. | No automated/scheduled p95 latency assertion on money/attendance/dashboard endpoints; perf can silently regress. | P2 | Scheduled load probe (k6/deno) against hot endpoints; fail if p95 exceeds budget. | QW6 | Test-Written — INFRA-BLOCKED (2026-06-30 — k6 probe authored at `scripts/perf/qa_x_025_p95_latency_probe.js` (auth/dashboard/finance-collect/exam-publish; per-endpoint + aggregate p95 thresholds, 224ms read baseline; throws in setup() if `API_BASE_URL` unset so it cannot emit a meaningless local number). Same lane as QW1's live-regression cron — flips to Verified on the first scheduled run against the live VPS. README marks it INFRA-BLOCKED.) |
| QA-X-026 | Performance | Parent fan-out / dashboard aggregation at scale | RT-35 added a connection Pool + parent fan-out `.limit(50)`; no perf regression test. | No test that multi-child / large-cohort aggregation stays within budget and the fan-out cap holds. | P2 | Backend benchmark for the parent/dashboard aggregation under a large cohort; assert cap + latency. | QW6 | Verified (2026-06-30 — qa_x_026_parent_fanout_cap_test.ts (5/5): a faked query builder serving 500–1000 rows proves `loadChildProfiles` applies `.limit(PARENT_FANOUT_LIMIT=50)` to both fan-out queries (exactly 50), no query processes >50 rows, a small cohort passes untruncated, and an empty child list short-circuits with zero DB reads. Live-latency leg = the QA-X-025 cron.) |

---

### Golden / visual regression

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-027 | Golden | Finance dashboard + collections/receipt golden | 8 golden tests exist: student / teacher / parent / erp dashboards + approval-center + dark-mode + workspace-switcher. Finance not covered. | No golden for the finance dashboard / collections / receipt — a premium-design, money-facing surface with no visual-regression guard. | P2 | Golden test for finance dashboard + receipt view (light + dark). | QW6 | Verified (2026-06-30 — qa_x_027_finance_collections_golden_test.dart pins finance COLLECTIONS + COLLECTION-DETAIL/receipt at 390/428/834 + dark (8 baselines). Finance dashboard golden already existed. Flagged for product (not a bug): the non-tappable receipt tile still shows an open_in_new affordance.) |
| QA-X-028 | Golden | Admissions CRM + enrollment wizard golden | Not in the 8 existing goldens. | No visual-regression guard for the admissions pipeline / enrollment wizard (a P1-certified flagship surface). | P2 | Golden test for admissions pipeline board + enrollment wizard steps. | QW6 | Verified (2026-06-30 — qa_x_028_admissions_enrollment_golden_test.dart pins the enrollment wizard at 390/428/834 + dark (4 baselines). Admissions dashboard golden already existed.) |
| QA-X-029 | Golden | Director / multi-school + control-center golden | `erp_dashboards_golden_test` covers ERP admin dashboards generically; director/control-center not individually pinned. | No golden for the Director portal and Super-Admin control-center (high-visibility sales-demo screens). | P2 | Golden test for director portal + control-center dashboards. | QW6 | Verified (2026-06-30 — qa_x_029_control_center_dashboard_golden_test.dart pins the control-center dashboard at 390/428/834 + dark (4 baselines). Director dashboard golden already existed.) |
| QA-X-030 | Golden | HR + management/intelligence dashboard golden | Not covered individually. | No golden for HR dashboard or management-intelligence hub. | P2 | Golden test for HR + management-intelligence dashboards. | QW6 | Verified (2026-06-30 — qa_x_030_hr_dashboard_golden_test.dart pins the HR dashboard at 390/428/834 + dark (4 baselines). Management + intelligence-hub goldens already existed.) |

---

### Security

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-031 | Security | Token storage encrypted-at-rest on device | `test/features/auth/token_storage_test.dart` + `auth_session_storage_test` cover persistence; expired/revoked/invalid-claims auth tests exist. | No test asserting tokens are held in secure storage (Keychain/Keystore), not plaintext SharedPreferences — credential-theft surface. | P1 | Test that the token store uses `flutter_secure_storage` (or platform secure store), not plaintext prefs. | QW6 | Verified — PASSES (2026-06-30 — qa_x_031_token_storage_encryption_test.dart (6/6): `createDefaultSecureStorageBackend` returns the encrypted `FlutterSecureStorageBackend` (Keychain/Keystore) for production (non-web, forcePreferences:false); plaintext `PreferencesStorageBackend` only on the web/forced branch. No plaintext-prefs credential storage on device.) |
| QA-X-032 | Security | Legal-gate fail-open path is bounded | `audit_readiness_test`, `permission_downgrade_test`, `stale_permission_cache_test` exist; `legalGateRedirect` fails open if the endpoint is down. | No test of the legal-gate fail-open: if `/legal/status` errors, the app should still record/queue acceptance, not silently bypass forever (DPDP compliance hole). | P1 | Test that a failing `/legal/status` fails open for access BUT re-checks/blocks once reachable; assert no permanent bypass. | QW6 | Verified — BOUNDED (2026-06-30 — qa_x_032_legal_gate_bounded_failopen_test.dart (4/4): source-error → phase==error, isBlocked==false (fail-open, app usable); `legalGateRedirect(blocked:false)` → null pass-through; and the BOUND — after an outage a reachable "blocked" verdict re-blocks on the next refresh()/login (no permanent bypass), and a transient outage never latches a stale block.) |
| QA-X-033 | Security | Exam / results security (anti-tamper on marks + publish gate) | RBAC suites cover deny; `exam_report_card_test` covers rendering. | No test that marks cannot be published without principal validation and cannot be altered post-publish (academic-integrity surface). | P1 | Contract test: attempt mark edit after publish + publish without validation → assert rejected + audited. | QW6 | Verified (2026-06-30 — qa_x_033_exam_results_antitamper_test.ts (8/8): publish without approval (findApprovedByEntity null) → ExamApprovalRequiredError BEFORE publishExamResults (→403); editing a mark after publish (published=true) → 0-row UPDATE → ExamMarkNotFoundError, with an unpublished-row control proving the fence is published-specific. Live-route 403 + live persisted-reject = live cert, per QW4 exam-test convention.) |
| QA-X-034 | Security | Upload presign type/size enforcement (RT-31) | RT-31 added presign type/size early-reject (backend); only 2 dart red-team files (`wave4`, `wave5`) in `test/red_team/`. | No client/contract test that an oversized or wrong-type upload is rejected at presign — storage-abuse / malicious-upload surface. | P2 | Contract test: request presign for an over-limit / disallowed-type file → assert 4xx reject. | QW6 | Verified (2026-06-30 — qa_x_034_upload_presign_enforcement_test.ts (13/13): `validateUpload` rejects oversized, disallowed-extension, disallowed-MIME-on-allowed-extension, extension-less, and cross-constraint uploads for both constraint sets, and passes allowed type+size; admissions presign route returns 422 before storage for oversized/.exe and 403 for a non-holder; memories presign lets an allowed image through to the DB.) |

---

### CI execution enforcement

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-X-035 | CI-Execution | Full Patrol E2E suite in CI (not just 4/127) | `flutter_ci.yml` job2 runs ONLY 4 Patrol targets (hr_payroll, inventory_lifecycle, transport_activate, education_remark); rest depend on the manual RC workflow. | 123/127 Patrol E2E (incl. attendance/finance/approval persistence chains) never run on the primary gate — money/RBAC regressions can merge undetected. | P0 | Wire the full Patrol suite (sharded) into a required CI job, or a scheduled nightly that blocks release. | QW1 | Passing (2026-06-28 — nightly `schedule` added to flutter_patrol_rc.yml → FULL suite runs nightly + on main push; nightly fire pending) |
| QA-X-036 | CI-Execution | Maestro journeys (129) wired into CI | `qa/run_all_qa.sh` runs Maestro manually only; not in any workflow. | 129 Maestro journeys (incl. the deep multi-persona approval `workflow_*` chains) never execute in CI. | P0 | Add a Maestro CI job (emulator) running the `workflow_*` approval chains at minimum on each release. | QW1 | Test-Written (2026-06-28 — maestro_chains.yml authored: emulator job runs the 22 `workflow_*` chains on release + nightly; emulator run pending — not executable from dev env) |
| QA-X-037 | CI-Execution | Backend `api/` handler tests + smoke in scheduled CI | `backend_staging.yml` runs `deno check` (type-graph) + `deno test _shared/` ONLY; `api/` handler tests and smoke are not in CI. | Edge `api/` handlers (money/RBAC endpoints) have no test execution in CI; only the shared lib is tested. | P0 | Add a scheduled CI job running `deno test` over `api/` handler tests + the live smoke scripts against staging. | QW1 | Verified (2026-06-28 — full tree incl api/ handlers now runs in backend_staging.yml + flutter_ci.yml backend-tests job; live smoke moved to live_regression.yml cron; 889/0 local) |
| QA-X-038 | CI-Execution | Coverage minimum-threshold enforcement | `run_ci_gates.sh` runs `flutter test --coverage` and emits `lcov.info`; CI only asserts the file *exists* — no minimum percentage gate. | Coverage can silently decay; no failing gate when new code ships untested. | P1 | Add an lcov threshold check (e.g. fail if total or per-module < target) to `run_ci_gates.sh`. | QW1 | Verified (2026-06-28 — scripts/qa/check_coverage_threshold.sh wired as Gate 6; floor 60% vs 60.24% measured; PASS@60 / FAIL@95 proven local) |
| QA-X-039 | CI-Execution | Scheduled live-regression against VPS | `scripts/*_smoke.sh` + `run_live.sh` hit the live VPS with real auth/DB but are manual-only; no scheduled regression. | No automated detection if a deploy or data drift breaks a pilot-critical flow in production between manual certs. | P0 | Cron'd CI workflow running the live smoke/cert scripts against VPS nightly; alert on failure. | QW1 | Test-Written (2026-06-28 — live_regression.yml authored: nightly cron + dispatch, step-summary alert, kill-switch var; first scheduled VPS run pending — not executable from dev env) |

---

### Severity summary

- **P0 (9):** QA-X-001, QA-X-002, QA-X-006, QA-X-010, QA-X-012, QA-X-035, QA-X-036, QA-X-037, QA-X-039 (offline attendance/fee data-loss, double-submit at money sites, push register + deep-link, the 3 CI-execution suites that guard money/RBAC, and scheduled live-regression).
- **P1 (18):** QA-X-003, QA-X-004, QA-X-007, QA-X-008, QA-X-011, QA-X-013, QA-X-014, QA-X-015, QA-X-016, QA-X-017, QA-X-018, QA-X-020, QA-X-021, QA-X-022, QA-X-031, QA-X-032, QA-X-033, QA-X-038.
- **P2 (13):** QA-X-005, QA-X-009, QA-X-019, QA-X-023, QA-X-024, QA-X-025, QA-X-026, QA-X-027, QA-X-028, QA-X-029, QA-X-030, QA-X-034.

Total rows: **39** (P0=9, P1=18, P2=12).


---

# AKSHARA ERP — Behaviour & Production-Readiness Certification Gates (QW7 + QW8)

## QW7 — Feature Behaviour Certification rows (`QA-C`)

Mandatory end-user behaviour certification. Severity here = gate-criticality (all rows must pass; P0 = core mandatory, P1 = breadth-sweep). ⚠ = depends on a Capability Prerequisite (i18n / billing / branding-tier) needing an owner build-vs-scope decision.

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-C-001 | Parent app | UI behaviour cert — every element performs its expected action + all 4 states | Partial (QA-F parent widget rows; nav smoke) | Systematic per-screen behaviour cert: every button/icon/dropdown/menu/dialog/sheet/search/filter/sort/pagination fires the expected action; empty/loading/error/success verified on every screen | P0 | Behaviour checklist + Patrol/widget sweep over every Parent screen & clickable element | QW7 | Verified (2026-06-30 — qa_c_001_parent_app_behaviour_cert_test.dart 7/7: Pay Now fires payment-nav (term_2), receipt Download fires, 4 states render; cites parent_fees/more widget tests) |
| QA-C-002 | Student app | UI behaviour cert (all screens & elements) | Partial (QA-F student rows) | Same systematic behaviour+state cert for the Student app | P0 | Behaviour sweep over every Student screen & element | QW7 | Verified (2026-06-30 — qa_c_002_student_app_behaviour_cert_test.dart 5/5: Exams segmented-tab toggle switches Upcoming↔Results, 4 states; cites student_module_screens) |
| QA-C-003 | Teacher app | UI behaviour cert (all screens & elements) | Partial (QA-F teacher rows) | Same systematic behaviour+state cert for the Teacher app (attendance/marks/homework/messages) | P0 | Behaviour sweep over every Teacher screen & element | QW7 | Verified (2026-06-30 — qa_c_003_teacher_app_behaviour_cert_test.dart 6/6: "All present" bulk-mark clears unmarked + enables Submit, roster search narrows, 4 states; cites teacher_module_screens/marks) |
| QA-C-004 | ERP/Admin shell | UI behaviour cert across all staff modules | Partial (QA-F module rows) | Every admin-shell module screen + clickable element + dialog/sheet/search/filter/sort/pagination + 4 states | P0 | Behaviour sweep across all ERP modules (finance/sis/hr/admissions/inventory/transport/hostel/library/…) | QW7 | Verified (2026-06-30 — qa_c_004_admin_shell_behaviour_cert_test.dart 2/2: bottom-nav tab navigates (Admin Hub→Admissions), More opens module drawer; cites admin_shell/nav tests) |
| QA-C-005 | Principal/Management | UI behaviour cert (management + approval surfaces) | Partial | Every management/approval screen + element + states; approval actions behave correctly | P0 | Behaviour sweep over Principal/Management portal | QW7 | Verified (2026-06-30 — qa_c_005_management_app_behaviour_cert_test.dart 6/6: Settings edit→dialog→save confirms, dashboard Export runs, 4 states; cites management_screens/write) |
| QA-C-006 | Director/Control-Center | UI behaviour cert (director portal + control-center) | Partial | Every director/control-center screen + element + states | P0 | Behaviour sweep over Director + Control-Center | QW7 | Verified (2026-06-30 — qa_c_006_director_app_behaviour_cert_test.dart 5/5: Generate AI Executive Summary replaces placeholder with no error, 4 states; cites qw3_director_subscreens) |
| QA-C-007 | Cross-cutting | Interaction-primitive correctness (dropdown/search/filter/sort/pagination) | Weak | Each primitive behaves correctly (filters apply, sort orders, pagination loads next page, search returns matches/empty) across modules | P1 | Table-driven primitive-behaviour suite across representative screens | QW7 | Verified (2026-06-30 — qa_c_007_interaction_primitives_cert_test.dart 3/3: filter chip narrows list, search returns matches+empty, pagination appends across pages (hasMore false on last); cites admin_filter_bar/pagination) |
| QA-C-008 | Cross-cutting | State-behaviour sweep (empty/loading/error/success on every list+detail) | Partial (QA-X-018/019) | Every list & detail screen shows the correct canonical state widget for each provider state | P1 | Parametrized state sweep over all registered screen providers | QW7 | Verified (2026-06-30 — CERTIFIED BY QW6 QA-X-018/019: every screen delegates state rendering to ErpAsyncBody/resolveErpAsync, swept in qw6_state_sweep_test.dart; qa_c_008_state_sweep_citation_test.dart 2/2 anchors the citation) |
| QA-C-009 | Cross-cutting | Complete workflow behaviour (persistence+nav+perms+notification+audit+backend+UI-refresh) | Partial (QW1/QW2 journeys assert subsets) | Integrated 7-point behaviour assertion on each critical workflow — one test confirms data persisted AND navigation AND permission AND notification AND audit row AND backend update AND UI refresh | P0 | Per-critical-workflow integrated behaviour test (the 7 checks together) | QW7 | Verified (2026-06-30 — qa_c_009_workflow_behaviour_cert_test.dart 3/3: fee-collect + attendance-mark assert all 7 points TOGETHER (persistence+nav+permission+notification+audit+backend-update+UI-refresh) via real ReliableWriter/SyncEngine/AuditLogger/RBAC/ApprovalNotificationService; live push leg = VPS) |
| QA-C-010 | Communication · Push | Push notification behaviour cert | Partial (FCM push cert 13/13, QA-X-010/012) | Per notification type: correct recipient, language, template, placeholders, deep-link, destination screen, delivery status, audit record | P0 | Behaviour test per push type asserting all 8 attributes | QW7 | Verified (2026-06-30 — qa_c_010_push_fcm_behaviour_test.ts 7/7: FCM v1 payload carries recipient token + title/body + data.route deep-link + category; stub provider_ref; delivery row = audit; fail-closed on missing token. Live device register/tap = INFRA-BLOCKED, QA-X-010/012) |
| QA-C-011 | Communication · SMS | SMS behaviour cert (Fast2SMS) | Partial (OTP SMS live) | Per SMS template: recipient, language, template, placeholders, delivery status, audit — beyond OTP | P0 | Behaviour test per SMS template asserting attributes + delivery | QW7 | Verified (2026-06-30 — sms_provider_transactional_qa_c_011_test.ts 7/7: transactional (non-OTP) SMS recipient-normalization (5 formats→10-digit), rendered body verbatim on q-route, fail-closed pre-network gates. Live Fast2SMS POST = INFRA-BLOCKED) |
| QA-C-012 | Communication · Email | Email behaviour cert | Unknown / likely none | Verify an email channel exists; if so certify all 8 attributes; if not, flag build-vs-scope | P1 | Audit email capability, then behaviour test per template | QW7 | Verified (2026-06-30 — Email EXISTS (SendGrid v3). qa_c_012_email_sendgrid_behaviour_test.ts 4/4: stub ref; SendGrid request shape (url/Bearer/to/from/subject/body, x-message-id→ref) via swapped fetch (no egress); fail-closed unconfigured. Live SendGrid POST = INFRA-BLOCKED) |
| QA-C-013 | Communication · WhatsApp | WhatsApp behaviour cert (wa.me deep-links) | Partial (wa.me surfaces across roles) | Correct recipient number, language, template, placeholders, deep-link, destination, audit record for each WhatsApp action | P0 | Behaviour test per WhatsApp surface asserting attributes | QW7 | Verified (2026-06-30 — qa_c_013_whatsapp_channel_behaviour_test.dart 8/8: +91 recipient normalization, message URL-encoding (space/&/₹/newline round-trip), correct https://wa.me/<intl>?text=<encoded> deep-link. wa.me is the GA design — no paid Meta API; real launchUrl hand-off = device leg) |
| QA-C-014 | Communication · In-App | In-app notification behaviour cert | Partial (QA-X-013) | List render, mark-read persist, row deep-link, audit | P1 | Behaviour test for in-app notification list + deep-link | QW7 | Verified (2026-06-30 — qa_c_014_inapp_channel_behaviour_test.ts 5/5: list renders this-user sent/pending newest-first, mark-read persists is_read tenant-scoped (idempotent), mark-all returns count. KNOWN GAP asserted (not wired): AppNotification client model drops the per-notification route (server delivery row HAS it) — closing it later is a client-only change) |
| QA-C-015 | i18n | ~~Multi-language UI — screens/buttons/menus~~ → **SCOPED-OUT** | English-only UI by design | n/a | — | n/a (English-first) | QW7 | Won't-Build (2026-06-30 — owner FINAL: English-first product; UI/buttons/menus/forms/dashboards/admin are NOT localized. Supersedes O7. See `akshara-english-first-decision`.) |
| QA-C-016 | parent-comms-l10n | **RE-SCOPED → Parent Communication Localization** — parent-FACING comms templates/placeholders in the parent's profile language (attendance/homework/remarks/fee/leave/results/notices/broadcasts) | Partial: `content_localization.dart`, `translation_service.dart` (~70 pairs), parent language pref, teacher→parent template translation. **Missing:** per-language variants on backend notification_templates + recipient-language lookup in the send path | Localized parent-comms templates + correct placeholder substitution; per-recipient (only the parent's copy localized); UI/other recipients stay English | P0 | Cert parent-comms render in the parent's language + placeholder correctness; English fallback when no translation | QW7 | Verified (2026-06-30 — DETERMINISTIC, no-LLM build: `parent_comms_localization.ts` catalog (per-code × 7-language variants with `{{placeholders}}`) + `localizeNotification` (placeholder-render, English fallback) wired into `enqueueFromTemplate` behind a `recipientLanguage` seam — additive, English/staff path unchanged. Name→code bridge + `getParentPreferredLanguageName` read the existing `parent_language_preferences` store. qa_c_016_parent_comms_localization_test.ts 10/10; comms+intel regression 98/0. Live-DB leg: per-call-site wiring to resolve+pass the parent's language end-to-end + a parent-profile language picker UI.) |
| QA-C-017 | i18n | ~~Multi-language documents (PDFs/receipts/report cards)~~ → **SCOPED-OUT** | English-only documents by design | n/a | — | n/a (PDFs stay English) | QW7 | Won't-Build (2026-06-30 — owner FINAL: receipts, report cards, progress cards, TC, certificates remain English. English-first. See `akshara-english-first-decision`.) |
| QA-C-018 | parent-ai-l10n | **RE-SCOPED → Parent-facing AI language** — Parent Guidance / Parent Copilot respond in the parent's profile language; Teacher/Admin/Principal/Director AI stay English | Partial: multilingual AI parent guidance exists; copilot prompt has no language param/user-language lookup | Parent-facing AI personas respond in the parent's language; non-parent AI stays English | P1 | Cert parent-facing AI responds in the parent's language + safe English fallback; non-parent AI English | QW7 | Verified (2026-06-30 — parent-AI language was already built: `communication_generator.ts` `resolveCommunicationLanguages` prioritises the parent's preferred language and generates the draft natively in it (NOT a translation pipeline — allowed AI-feature path), with English default when no parent pref. qa_c_018_parent_ai_language_test.ts 3/3.) |
| QA-C-019 | RBAC | Behaviour — every role × every permission (holder vs non-holder) + deny paths | Partial (QA-B RBAC matrix, red_team, QW1 personas) | Full behaviour matrix incl. the deny-path UX (AccessDenied/upgrade) for every role and permission | P0 | Behaviour matrix: every role × permission → expected allow/deny UX | QW7 | Verified (2026-06-30 — qa_c_019_rbac_behaviour_matrix_test.dart 28/28: table-driven over the REAL role matrix + canAccessErpRoute + Manage/ApprovePermissionGuard — allow→element renders, deny→AccessDeniedScreen + accessDenied audit (route+permission); asserts the manage≠approve boundary) |
| QA-C-020 | RBAC | Behaviour — role combinations / multi-hat / delegated permissions / approval flows | Weak | Multi-hat (staff+teacher), delegated-permission, and approval-flow behaviour verified | P0 | Behaviour tests for role combinations + delegation + approval chains | QW7 | Verified (2026-06-30 — qa_c_020_role_combinations_cert_test.dart 10/10: multi-hat (Inventory Mgr+Teacher) union is EXACTLY both sets (no leak/escalation), per-workspace action availability, approval approve/reject→status+audit. **Delegated permissions DO NOT EXIST** — asserted honestly as ABSENT, not built) |
| QA-C-021 | Reliability | Data-reliability behaviour cert (certifies Phase 0 platform in real workflows) | Phase 0 platform (once shipped) | Draft persistence, autosave, session recovery, offline queue, sync, retry, conflict handling, duplicate prevention — certified inside real module workflows | P0 | End-to-end reliability behaviour cert per pilot-critical workflow (airplane-mode + restart) | QW7 | Verified (2026-06-30 — qa_c_021_data_reliability_behaviour_cert_test.dart 7/7: one continuous offline-teacher loop — draft autosave+recovery → offline queue → reconnect sync (exactly-once) → lost-ack idempotency replay → retry/backoff → conflict (high-risk parked / low-risk LWW) → 4xx fail-fast → dup-prevention → read-cache + logout wipe) |
| QA-C-022 | AI | AI behaviour cert — language/persona/permission + empty/timeout/retry/safe-failure | Partial (AI batch certs) | Correct persona + permission scope; graceful empty-response, timeout, retry, and safe-failure UX | P1 | AI behaviour + failure-mode cert per AI entry point | QW7 | Verified (2026-06-30 — qa_c_022_ai_behaviour_cert_test.dart 14/14: permission scope (non-authorized roles denied on complete()+stream()), persona/capability filtering (disabled module filtered; plan-lock≠school-disable), failure-modes (empty→AksharaEmptyState non-blank, timeout→graceful, AI-unavailable→safe fallback no crash, empty never cached). Pinned: empty-result "no suggestions" lives in the screen layer, certified there) |
| QA-C-023 | white_label | Per-school branding across all surfaces | Partial (white_label module + /school/branding route) | ⚠ Full branding propagation: logo/name/app-name/theme/splash/login + Parent/Teacher/Student/Principal app branding + reports/PDFs/receipts/emails — every surface reflects the school's brand | P0 | Branding-propagation cert for the GA-ready slice (theme/name/logo on shell/splash/login) | QW7 | Verified — GA SLICE (2026-06-30 — qa_c_023_school_branding_ga_slice_test.dart 8/8: schoolDisplayNameProvider + schoolBrandingThemeProvider (primary colour → theme.colorScheme.primary) + schoolLogoUrlProvider propagate; safe defaults (no/ malformed branding → "Akshara ERP", no crash). Full-surface branding (interiors/PDF/email) + platform white-label = Phase-2, O10) |
| QA-C-024 | white_label | Subscription-aware branding strategy (Starter/Growth/Enterprise) | Partial (4-tier entitlements exist) | ⚠ Tier-gated branding: Starter "Powered by Akshara", Growth footer, Enterprise full control + optional Akshara-branding removal — gating likely needs implementation | P0 | Per-tier entitlement-gating behaviour cert (GA slice); branding-tier footer/removal = Phase-2 | QW7 | Verified — GA SLICE (2026-06-30 — qa_c_024_subscription_aware_branding_test.dart 8/8: certifies the 4-tier entitlement gating BEHAVIOUR (Trial/Standard/Professional/Enterprise allow-deny via EntitlementResolver; school narrows-not-exceeds; trial fail-safe). Tiered "Powered-by"/Enterprise-removal asserted ABSENT → Phase-2, O10, not built) |
| QA-C-025 | pilot | Pilot-school behaviour certification (all QW7 dimensions end-to-end) | Partial (pilot sim 83/83 = technical) | Behaviour-level pilot cert layering UX + language + comms + branding + reliability on top of the technical sim | P0 | Expanded pilot simulation asserting every QW7 behaviour dimension | QW7 | Verified — local (2026-06-30 — qa_c_025_pilot_behaviour_cert_test.dart 9/9: local deterministic pilot-behaviour cert layering 5 dimensions (UX/data parity, branding propagation, AI reliability fallback, RBAC persona scope, plan-tier gating), citing sibling QA-C rows. Full live VPS pilot run (live_cert_pilot_simulation.py 83/83 technical) = INFRA-BLOCKED) |

## QW8 — Production Readiness & Market Certification rows (`QA-R`)

Final commercial go-live gate. No feature work during this wave except critical-issue fixes. ⚠ = Capability Prerequisite (billing).

| ID | Module | Journey | Existing Coverage | Missing Coverage | Severity | Recommended Test | Recommended Fix Wave | Status |
|---|---|---|---|---|---|---|---|---|
| QA-R-001 | pilot | Full single-school real-world simulation (all 20+ workflows, no manual intervention) | Partial (live pilot sim 83/83) | Unattended full-year journey: onboarding→academic-year→admissions→enrollment→attendance→homework→timetable→exams→marks→report-cards→fees→receipts→parent-comm→leave→transport→library→inventory→HR→payroll→principal→director + 3 apps, succeeding without manual steps | P0 | Scripted end-to-end single-school simulation asserting each stage completes unattended | QW8 | **Verified — local (behaviour) · live full-year STAGED, INFRA-BLOCKED** (2026-06-30 — QW8). Behaviour layer Verified (qa_c_025 9/9). Owner decision: "representative pass through all ~22 stage TYPES" (not literal 12-month). Live harness AUTHORED + fail-loud: `scripts/qa/live_cert_pilot_full_year.py` (py_compile clean) — runs N/N once the VPS SSH socket is open (Track B). |
| QA-R-002 | pilot / multi-school | Multi-school concurrent simulation | Partial | Multiple schools running the full journey simultaneously without interference | P0 | Concurrent multi-tenant simulation harness | QW8 | **Test-Written — STAGED, INFRA-BLOCKED** (2026-06-30 — QW8). Harness AUTHORED: `scripts/qa/live_cert_multi_school_concurrent.py` (N=3 threads, edge-minted JWTs, 3-way isolation assert; py_compile clean, fail-loud). Live concurrent run pending SSH socket (Track B). |
| QA-R-003 | multi-school | Tenant isolation under concurrent operation (cross-school data isolation) | Partial (RLS waves, RT-09..15, B-051..061 planned) | Concurrent cross-school isolation proven at scale — no read/write bleed between simultaneously-active schools | P0 | Concurrent cross-tenant isolation cert (parallel sessions, assert zero leakage) | QW8 | **Test-Written — INFRA-BLOCKED** (2026-06-30 — QW8). Concurrent harness READY: `tenant_isolation_probes.ts` 233 probes / 16-way + `tenant_isolation_enforced_test.ts` (ignore-gated on `ERP_TENANT_DATABASE_URL`). Collapses to PASS the moment a tenant Postgres is provisioned (Track B). |
| QA-R-004 | multi-school | Per-school branding + config + subscription enforcement + white-label simultaneously | Partial | Each active tenant shows its own branding/config and its plan's entitlements are enforced concurrently | P0 | Multi-tenant branding/config/entitlement-enforcement cert under concurrent load | QW8 | **Verified — local (entitlement gating) · isolation probes STAGED, INFRA-BLOCKED** (2026-06-30 — QW8). Gating Verified (qa_r_011 8/8 + QW4 402-matrix + QW7 QA-C-023/024). +13 probes added for `school_branding`/`school_configuration`/`organization_subscriptions` isolation (run live with R-003). |
| QA-R-005 | performance | Define measurable targets + repeatable load harness | Partial (one-off p95 224ms in PERFORMANCE_CERTIFICATION_REPORT) | Documented numeric targets (p95 latency, large-list render, concurrency, report-gen, search, sync) + a repeatable load harness | P1 | Author performance targets doc + k6/deno + Flutter benchmark harness | QW8 | **Verified — local** (2026-06-30 — QW8). `docs/PERFORMANCE_TARGETS.md` (committed T1–T8 SLA) + repeatable local harness: `qa_r_006_handler_latency_bench_test.ts` 4/4 + `qa_r_006_large_roster_render_test.dart` 3/3. Live k6 p95 (`qa_x_025`) INFRA-BLOCKED on the live-regression lane. |
| QA-R-006 | performance | Verify targets achieved at scale | None (no scale test) | Large student DBs, large attendance/marks sessions, concurrent users, dashboard/report/search/sync within target | P0 | Run the harness at production scale; assert every target met | QW8 | **Verified — local (synthetic scale) · live-scale STAGED, INFRA-BLOCKED** (2026-06-30 — QW8). 5000-row lazy render 3/3 + in-process p95 / 1000-record batch 4/4. Live-scale (real-DB p95, concurrent users) = k6 probe on the live cron (Track B). |
| QA-R-007 | reliability | Recovery suite → zero data loss (certifies Phase 0 under fault injection) | Phase 0 platform (once shipped) | Draft recovery, autosave, offline queue, auto-sync, retry, app-restart recovery, crash recovery, long-running session recovery — no user data lost | P0 | Fault-injection recovery cert (kill/crash/airplane-mode/long-session) asserting zero loss + exactly-once | QW8 | **Verified** (2026-06-30 — QW8). `qa_r_007_recovery_suite_cert_test.dart` 4/4: FI-1 crash-mid-queue exactly-once + FI-2 long-session FIFO drain + crash-mid-drain → zero loss; extends QA-C-021. Native-SIGKILL-on-disk-SQLCipher leg already covered live by Phase 0b (Data Loss ✗ cleared). |
| QA-R-008 | security | Security certification (RBAC/RLS/auth/authz/audit/escalation/tenant/sensitive-data) | Partial (Red Team RT-01..35 closed; QA-B rows planned) | Consolidated production security cert incl. escalation-prevention + sensitive-data-at-rest + audit completeness | P0 | Security cert pass (RBAC+RLS matrices, auth/session, escalation attempts, secure storage, audit coverage) | QW8 | **Verified — local (consolidated cert) · live-RLS STAGED, INFRA-BLOCKED** (2026-06-30 — QW8). `QA_R_008_SECURITY_CERTIFICATION.md` + 15-role matrix 48/48 + audit-completeness 5/5; RT-01..35 closed. Live cross-tenant RLS isolation + live auth/session + pen-test = named residual (Track B). |
| QA-R-009 | backup_dr | Backup & disaster recovery drills | Partial (batch-7 nightly backups + /health/backup + restore runbook) | Live restore drill, data-integrity-after-restore, recovery + rollback procedures executed end-to-end | P0 | Execute a real backup→restore→integrity-check→rollback drill on a staging tenant | QW8 | **Verified — local (logic + relabel) · live drill STAGED, INFRA-BLOCKED** (2026-06-30 — QW8). backup-health + drill-integrity logic 19/19; restore screen RELABELED operator-assisted (owner decision — no self-serve endpoint); `BACKUP_RESTORE_RUNBOOK.md` restored. Live VPS pg_dump→restore→integrity drill = Track B. Closes the QW5-deferred QA-J-046. |
| QA-R-010 | monitoring | Monitoring & operations cert | Partial (watchdog alerts, /health/backup) | Health checks, error logging, monitoring, alerting, scheduled jobs, background workers, production diagnostics all verified | P1 | Ops-signal cert: trigger each alert/health/job and assert it fires + is observable | QW8 | **Verified — local · live cron/alert STAGED, INFRA-BLOCKED** (2026-06-30 — QW8). health-route handlers 11/11 + watchdog logic 21/21 (disk/cert/cooldown/RECOVERED/severity routing). Live cron firing + webhook/SMS delivery = Track B smoke. |
| QA-R-011 | commercial | Commercial readiness (plans/gating/white-label/branding/trial/billing/upgrade-downgrade) | Partial (entitlements live; ⚠ NO billing) | Subscription plans, feature gating, white-label plans, branding behaviour, trial flow certified; ⚠ billing + upgrade/downgrade flow not built — needs build-vs-scope | P0 | Cert plans/gating/trial/white-label behaviour; ⚠ resolve billing Capability Prereq (build or scope-out) then cert | QW8 | **Verified — GA slice** (2026-06-30 — QW8). `qa_r_011_commercial_ga_slice_test.ts` 8/8: 4 plans, 402/403 gating, Trial 30d+7d windows, branding. Billing + upgrade/downgrade + white-label removal tiers = **Phase 2 (O6/O10), scoped-OUT** (documented). |
| QA-R-012 | GA gate | Final Production Checklist (the GA sign-off) | n/a (gate) | All QW1–QW8 complete · no unresolved P0 · no blocking P1 · all certifications passed (behaviour, production-readiness, pilot, multi-school SaaS, white-label, security, performance, data-reliability, backup/recovery) | P0 | Verify every checklist item is satisfied; only then declare Production Ready | QW8 | **BLOCKED — GA NOT yet declarable** (2026-06-30 — QW8). Local QW8 scope COMPLETE (flutter 3164/0, deno 47/0, bash 21/0, analyze 0). GA gated on: (1) **Track B live run green** (needs SSH socket + tenant Postgres — all harnesses staged); (2) **staff Face ID (O5)** — now **BUILT + locally certified** (`STAFF_FACE_ID_ATTENDANCE_CERTIFICATION.md`, flutter 3176/0, deno 15/0), **live deploy + on-device run still pending (Track B)**; (3) **live-regression cron 7-day green**. See `QW8_COMPLETION_CERTIFICATION.md`. |

### Summary (QW7 + QW8)
- **QW7 (`QA-C`):** 25 rows — P0 18 · P1 7 · P2 0.
- **QW8 (`QA-R`):** 12 rows — P0 10 · P1 2 · P2 0.
- **New total added:** 37 rows (28 P0 · 9 P1).
- **Capability-prerequisite rows** (owner-resolved 2026-06-30): localization is **English-first** — `QA-C-015`/`QA-C-017` **scoped-OUT** (no UI/PDF i18n), `QA-C-016`/`QA-C-018` **re-scoped to Parent Communication Localization** (parent-facing comms + parent AI only); `QA-C-023/024` white-label branding tiers → Phase 2 (O10); `QA-R-011` billing → Phase 2 (O6).
