# Release v15.8 — Pilot Validation & Polish

**Tag:** `v15.8-pilot-polish`  
**Date:** June 2026  
**Scope:** Real-world pilot validation program — RBAC recovery, parent UX guardrails, validation harness

---

## Summary

Phase 21 staging recovery exposed a second blocker: **phase 8–16 `role_permissions` never applied** on staging (permission migration chain failure). v15.8 re-grants pilot RBAC, routes parent “AI” actions to structured experience hub (no open copilot), and adds the **Pilot Validation Program** orchestrator.

---

## Root causes fixed

| Issue | Fix |
|-------|-----|
| Staging `db push` failed on catalog recovery | `viewParentExperience` used invalid scope `parent` (CHECK allows `self` only for parent-facing perms) |
| School admin 403 on subjects/intelligence | `20260627105000` catalog + `20260627110000` role_permissions recovery |
| Parent 403 on academic summary | Parent role grants: `viewParentExperience`, `viewParentAcademicSummary` |
| Teacher attendance 404 in validation | `/teacher/attendance/classes` path in `demo_school_validate.py` |
| Parent `ai_copilot` dead-end | Routes to `parentExperienceHubPath` |

---

## Validation harness

| Script | Purpose |
|--------|---------|
| `scripts/pilot_validation_program.py` | Phases 1–8 API journeys (7 roles) |
| `scripts/pilot_readiness_verify.sh` | Staging probes (22 checks) |
| `scripts/device_validation_matrix.py` | Mobile role matrix (18 checks) |
| `scripts/demo_school_validate.py` | Demo school E2E |

---

## Tests added

- `pilot_rbac_recovery_test.ts`
- `parent_navigation_pilot_test.dart`
- `large_school_benchmark_test.dart` — scaled page budgets

---

## Pilot readiness

Run after deploy:

```bash
./scripts/pilot_validation_program.py
```

**Target:** 0 failures across program + readiness + device matrix.
