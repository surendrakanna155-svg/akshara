# v15.8.1 — CI Migration Hotfix (Phase 4 Probe Seed)

**Tag:** `v15.8.1-migration-hotfix`  
**Date:** June 2026

---

## Root cause

`supabase db push` failed at **`20260622400001_phase4_probe_seed.sql`** with a foreign key violation.

| Field | Wrong value (probe row id) | Correct value (tenant FK) |
|-------|---------------------------|---------------------------|
| `organization_id` | `e0500000-…` (education probe entity id) | `a1000000-…` (Akshara Staging Organization) |
| `school_id` | `f0500000-…` (intelligence probe entity id) | `a2000000-…` (School A / School B) |

**SQL error (expected on staging):**

```
ERROR: insert or update on table "inv_catalog_items" violates foreign key constraint
DETAIL: Key (organization_id)=(e0500000-0000-4000-8000-000000000001) is not present in table "organizations".
```

## Failure class

Foreign key mismatch — not duplicate object, not migration checksum.

## Fix

Corrected org/school UUIDs in `20260622400001_phase4_probe_seed.sql` to match `20260607100001_staging_seed.sql` and other probe fixtures.

## Deploy

Push to `main` → Backend Staging CI → `supabase db push` → Edge `api` deploy.

```bash
supabase link --project-ref oeicxjpewrumkfgyqnnj
supabase db push
```

Re-login probe users after `permissions_version` bump from downstream `20260627110000` recovery.
