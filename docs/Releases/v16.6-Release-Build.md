# Release v16.6 — Release Build Program

**Tag:** `v16.6-release-build`  
**Date:** June 2026  
**Scope:** Android APK/AAB + iOS IPA pipeline, release configuration, device testing guide — **no new features or business logic**

**Version:** 16.6.0 (build 166)  
**Package:** `com.akshara.erp` (Android) · `com.akshara.erp.aksharaErp` (iOS)

---

## Phase 1 — Release configuration audit

| Item | Status |
|------|--------|
| Flutter release mode | ✅ `--release` with staging dart-defines |
| App versioning | ✅ `16.6.0+166` in `pubspec.yaml` |
| Android applicationId | ✅ `com.akshara.erp` |
| iOS bundle identifier | ✅ `com.akshara.erp.aksharaErp` |
| App display name | ✅ "Akshara ERP" |
| Splash / launch | ✅ Default Flutter launch screens (Android + iOS) |
| App icons | ✅ Default Flutter launcher icons (replace before store submission) |
| Environment | ✅ `APP_ENV=staging` + staging Supabase API URL |
| Debug banner | ✅ Disabled in `AksharaApp` |
| INTERNET permission | ✅ Added to AndroidManifest |

---

## Phase 2 — Android release build

| Setting | Value |
|---------|-------|
| minSdk | Flutter default (21+) |
| targetSdk | Flutter default (latest) |
| Signing | Debug keystore (pilot sideload); replace for Play Store |
| Network | HTTPS staging API only |

**Commands:**

```bash
./scripts/build_release.sh apk
./scripts/build_release.sh aab
```

**Outputs:**

- `build/app/outputs/flutter-apk/app-release.apk`
- `build/app/outputs/bundle/release/app-release.aab`

---

## Phase 3 — iOS release build

| Setting | Value |
|---------|-------|
| Deployment target | iOS 13.0 |
| Bundle ID | `com.akshara.erp.aksharaErp` |
| Signing | Requires Xcode + Apple Developer profile |

```bash
./scripts/build_release.sh ipa
```

See `docs/Testing/Device-Testing-Guide.md` § iOS signing when Xcode is unavailable on the build machine.

---

## Phase 4 — Testing guide

**Path:** `docs/Testing/Device-Testing-Guide.md`

Covers principal, teacher, parent, and student personas with staging phones, mock OTP fallback, and key screen workflows.

---

## Phase 5 — Validation gates

| Gate | Result |
|------|--------|
| `flutter analyze` | 0 issues |
| `flutter test` | All passing |
| Release APK build | ✅ 73.3 MB |
| Release AAB build | ✅ 67.8 MB |
| IPA build | ⚠️ Requires Xcode + CocoaPods on signed Mac |

---

## Build script

`scripts/build_release.sh` — staging dart-defines for all enabled API modules; supports `apk`, `aab`, `ipa`, or `all`.

---

## Pre-store TODO (not blocking pilot)

- Replace default launcher icons with Akshara brand assets
- Configure release signing keystore (Android) + distribution certificate (iOS)
- Register bundle IDs in Play Console / App Store Connect
- Switch to `APP_ENV=production` with `disableDemoAuth` for public launch
