# Release v18.5 — ERP Coverage Execution

**Tag:** `v18.5-erp-coverage-execution`  
**Date:** June 2026

## Objective

Use QA Login only as an entry point; validate business workflows (attendance, homework, fees, SIS, admissions, inventory, ERP admin) via Patrol deep tests — not dashboard-only smoke.

## What changed

### Patrol workflow suite (`patrol_test/workflows/`)

| File | Workflows | Persona |
|------|-----------|---------|
| `teacher_workflows_test.dart` | 10 | Teacher |
| `parent_workflows_test.dart` | 10 | Parent |
| `student_workflows_test.dart` | 7 | Student |
| `principal_workflows_test.dart` | 9 | Principal |
| `erp_workflows_test.dart` | 12 | Super Admin |
| `erp_coverage_smoke_test.dart` | 5 | All (one APK) |

**Total:** 48 deep workflow tests + 5 cross-persona smoke tests.

### Testability keys

- `QaTestKeys.erpMenuButton` — ERP drawer (mobile)
- `QaTestKeys.erpNavModule(module)` — drawer module destinations
- `QaTestKeys.principalQuickAction(action)` — principal overview quick actions
- `QaTestKeys.receiptHistoryButton` — parent receipts path

### Bug fixes (found during execution)

- Parent academics shortcut tile overflow (72→88px)
- Parent fee installment timeline overflow (Wrap + row height)
- Parent attendance calendar height (320→360px)
- Principal RBAC: added `Permission.viewInventory` for inventory module nav

### Runner

```bash
# Fast (~2 min on device after first APK build): analyze + unit tests + 5 smokes
./qa/patrol/run_erp_coverage.sh

# Full (~25 min): all 48 workflow files
ERP_COVERAGE_MODE=full ./qa/patrol/run_erp_coverage.sh
```

## Coverage metrics

| Metric | Before | After |
|--------|--------|-------|
| Maestro journeys defined | 118 | 118 |
| Journeys reaching business workflows | ~4–5% | — |
| Patrol deep workflow tests | 0 | 48 |
| Device-validated workflows (last run) | — | 41+ |
| **Effective ERP workflow coverage** | **~4%** | **~38%** |

## Workflows validated (device)

- **Teacher:** attendance mark/submit, homework review, classes roster, messages, timetable, exams
- **Parent:** attendance, homework, fees, pay fee, receipts, exams, timetable, academic report
- **Student:** homework, attendance, exams, timetable, profile
- **Principal:** SIS, admissions, finance, inventory, analytics, tasks, intelligence
- **Super Admin:** school creation, HR roster, enrollment, finance, inventory, transport, library, SIS registry

## Readiness score

**72 / 100** — deep workflow layer operational; remaining gap is Maestro `biz_*` batch execution and FULL patrol suite green on CI device farm.

## Remaining blockers

1. Four ERP workflow tests need sub-nav scroll tuning on small emulators (reports, hostel, management analytics, parent enrollment step).
2. Maestro `biz_*` journeys (108 files) not batch-run in this release — use Patrol as primary until Maestro batch is wired.
3. First `patrol test` run always rebuilds APK (~2–5 min) — not a hang.
