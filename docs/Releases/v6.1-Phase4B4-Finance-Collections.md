# v6.1 Phase 4B4 — Finance Collections

**Status:** Complete  
**Depends on:** Phase 4B3 Finance Invoices  
**Deployment:** Migration `20260612500000_finance_slice4_collections.sql` + Edge Functions redeploy

---

## Summary

Payment collections record fee receipts against invoices, sync `outstanding_amount` / invoice status (`issued` → `partially_paid` → `paid`), and update student account balances. Client repository extended with create/cancel collection writes.

## Deliverables

| Area | Change |
|------|--------|
| Schema | `finance_collections`, `finance_receipts` tables + RLS |
| Write API | `POST /finance/collections`, `POST /finance/collections/:id/cancel` |
| Read API | `GET /finance/collections`, `GET /finance/collections/:id`, `GET /finance/receipts/:id`, daily summary |
| Sync | Invoice outstanding + status; student account `amount_paid` / `outstanding_amount` |
| Probes | Collection tenant isolation probes |
| Client | `FinanceCollectionResult`, `createCollection`, `cancelCollection` |

## Gate

- `flutter analyze` = 0 issues
- `flutter test` = all passing
- Deno: `finance_collections_repository_test.ts` pass

## Deferred

- Payment recording UI dialog (FN-05 write flow)
- Parent mobile collection read (4B4d)
