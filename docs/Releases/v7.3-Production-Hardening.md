# v7.3 — Production Hardening

**Status:** Complete

## Delivered

| Item | Implementation |
|------|----------------|
| Payment webhook tenant resolution | Resolve org/school from `payment_intents.gateway_order_id` |
| Domain event worker retries | `attempt_count`, `next_retry_at`, exponential backoff (max 5) |
| Notification delivery monitoring | `GET /communications/delivery/metrics` |
| Operational dashboard | `GET /health/operations` (pending events, deliveries, AP, imports) |
| Pilot onboarding runbook | `docs/Operations/Pilot-Onboarding-Runbook.md` |

## Migration

`20260614930000_production_hardening.sql`

## Staging

- Migration applied to `akshara-staging`
- `GET /health/operations` live
- Tenant isolation probes run concurrently (16 workers) to stay within edge timeout
