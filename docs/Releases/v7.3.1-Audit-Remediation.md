# v7.3.1 — Audit Remediation

**Status:** Complete (code); staging deploy pending

## Fixed

- Onboarding view-only RBAC (`requireOnboardingView` OR-permission logic)
- `payment_webhook_events` tenant-scoped RLS policy
- Communication + payment permission seeds (`viewCommunications`, `manageCommunications`, `sendBroadcast`, `viewPayments`)
- `superAdmin` onboarding permissions (SQL + Dart)
- `HybridFinanceRepository` / `HybridSisRepository` mock fallbacks removed for live API methods
- `ADMISSIONS_API_ENABLED` dart-define wiring
- Client audit enum: onboarding + payment events
- Tenant probes: +2 payment webhook probes (**207 total**)

## Migration

`20260614940000_audit_remediation.sql`

## Tests

- `onboarding_handlers_test.ts` — view permission regression
- Probe count tests updated to 207
