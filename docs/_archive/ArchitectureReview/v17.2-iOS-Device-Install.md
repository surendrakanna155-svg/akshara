# Architecture Review — v17.2 iOS Device Install

**Release:** v17.2 (`v17.2-ios-device-install`)  
**Score:** **92 / 100** (local device install)

---

## Summary

Xcode build database / DerivedData issues resolved via scripted recovery. Release build succeeds cleanly. App installed on Kanna's iPhone via `devicectl`. Launch requires one-time developer trust on device — standard iOS security, not a code defect.

---

## Build pipeline

```
ios_build_recovery.sh → flutter build ios --release → devicectl install app
```

Provisioning profile includes device UDID `00008120-001131801A60201E`. Automatic signing valid for 7 days (Xcode-managed).

---

## Verdict

Local iPhone testing unblocked after owner trusts developer certificate on device.
