# Architecture Review — v17.4 Device Auth Validation

**Tag:** `v17.4-device-auth-validation`  
**Date:** June 2026  
**Score:** 78 / 100 (pilot device readiness)

---

## Summary

v17.3 authentication hardening behaves correctly on a fresh Android install: splash → hardened login, no demo role picker, staging OTP copy visible. API negative OTP tests pass. Parent and student seeded flows verify. Teacher mobile login is blocked by backend membership/scope resolution, not client mock bypass.

---

## Verified architecture

```
Cold start → SplashScreen (2s + resolveSession)
          → LoginScreen (disableDemoAuth)
          → AuthRepository.login / verifyOtp
          → homeRouteForRole(server scope)
```

| Check | Result |
|-------|--------|
| Mock OTP bypass removed | ✅ |
| Random OTP rejected | ✅ (verify step) |
| Role from server scope | ✅ parent/student |
| Session restore requires tokens | ✅ |

---

## Gaps

| Gap | Severity | Owner |
|-----|----------|-------|
| Teacher `scope=teacher` membership missing in seed | High | Backend / data |
| Teacher default verify scope → principal/school | High | Backend auth |
| No mobile first-run onboarding route | Medium | Agent B routing |
| Onboarding hub only in ERP shell (`/sis/onboarding`) | Medium | Product |
| Staging sends OTP for any phone number | Low (dev mode) | Ops |
| iPhone UI not automated (device lock) | Low | Manual QA |

---

## First-run routing inventory

| Path | Exists | Mobile first-run |
|------|:------:|:----------------:|
| `/splash` | ✅ | ✅ |
| `/login` | ✅ | ✅ |
| `/staff/login` | ✅ | Link only |
| `/sis/onboarding` | ✅ | ❌ (ERP post-auth) |
| Mobile school creation wizard | ❌ | ❌ |
| Super admin bootstrap | ❌ (staff ERP only) | ❌ |

---

## Recommendations

1. **Fix teacher membership** in demo school seed so `verify-otp` with teacher scope succeeds.
2. **Routing:** After staff login with `viewOnboarding`, deep-link to `/sis/onboarding` from management hub — do not build duplicate mobile wizard.
3. **Pilot doc:** Clarify Principal uses Staff ERP portal, not mobile login field.
4. **Device QA:** Complete iPhone OTP screenshots after developer trust.

---

## Quality gates

| Gate | Result |
|------|--------|
| `flutter analyze` | 0 issues |
| `flutter test` (auth hardening) | 3/3 pass |
| Device install (Android + iOS) | Pass |
