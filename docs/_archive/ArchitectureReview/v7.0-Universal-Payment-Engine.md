# v7.0 Universal Payment Engine — Architecture Mapping

**Score:** 92/100  
**Status:** Complete (stub gateway)

## Components

| Layer | Path |
|-------|------|
| Migration | `supabase/migrations/20260614600000_universal_payment_engine.sql` |
| Razorpay config | `_shared/payment/razorpay_config.ts` |
| Gateway client | `_shared/payment/razorpay_client.ts` |
| Repository | `_shared/payment/payment_repository.ts` |
| Service | `_shared/payment/payment_service.ts` |
| Handlers/Router | `_shared/payment/payment_handlers.ts`, `payment_router.ts` |
| Parent wiring | `_shared/parent/parent_router.ts` |
| Client flag | `PAYMENT_API_ENABLED` in `repository_config.dart` |

## Lifecycle

1. **Initiate** — idempotency lookup → payment_request upsert → Razorpay order → payment_intent `initiated`
2. **Confirm** — signature verify (live) → `createCollection` when `invoice_id` set → intent `captured`
3. **Webhook** — dedupe by event id → `payment.captured` / `payment.failed`

## Gaps

| ID | Gap | Target |
|----|-----|--------|
| G1 | Refund intent linkage (`refund_id` column unused) | Finance refunds v4B5 extension |
| G2 | Live Razorpay E2E | Staging credentials |

## Tenant Isolation

6 probes on `payment_intents` (174 total isolation probes).
