# Akshara ERP — Red Team Defect Classification (A/B/C/D)

Source of truth: [`docs/RED_TEAM_OPERATIONAL_AUDIT.md`](./RED_TEAM_OPERATIONAL_AUDIT.md)

Audit date: 2026-06-16  
Classification rules:
* A = Confirmed defect
* B = Partial defect
* C = False positive
* D = Already fixed

Notes:
* “Fix status” reflects the code changes already applied in this remediation session (some are still incomplete and untested).
* “Tests covering it” and “Patrol coverage” are reported as *current* coverage; remaining tests/patrol work is listed in the remediation plan.

---

## Top-25 Finding Classifications

| # | Finding | Priority | Classification | Evidence | Impact | Root cause | Files involved | Fix status | Tests covering it | Patrol coverage | 
|---:|---|---|---|---|---|---|---|---|---|---|
| 1 | Student identity silos across all modules | P0 | B | SIS seeds use `SIS-STU-*` (e.g. `MockSisRepository._seedStudents`); teacher marks/attendance use stable mock ids (`MockTeacherRepository._mockStudentsByClass`, `_mockMarks`); parent/student exams were separate fixtures; transport/hostel use SIS ids that must align with teacher/mobile fixtures (`MockTransportRepository`, `MockHostelRepository`). | Teachers/parents/students see mismatched roster/results; allocations & results can’t be correlated. | Mock repositories don’t share a single canonical student identity/relationship model. | `lib/core/repositories/mock/mock_sis_repository.dart`, `lib/core/repositories/mock/mock_teacher_repository.dart`, `lib/core/repositories/mock/mock_student_repository.dart`, `lib/core/repositories/mock/mock_parent_repository.dart`, `lib/core/repositories/mock/mock_transport_repository.dart`, `lib/core/repositories/mock/mock_hostel_repository.dart`, `lib/core/repositories/mock/mock_exam_results_sync_store.dart`, `lib/core/repositories/mock/mock_canonical_student_registry.dart` | **Fix applied on mock identity path:** canonical registry + exam results sync overlays; teacher attendance/marks publish into shared sync store; transport & hostel mocks aligned to canonical primary SIS id (`SIS-STU-10430` / Ravi Kumar). **Remaining:** verify full lifecycle for newly converted admissions enrollments. | Updated impacted widget/provider tests (transport/hostel/SIS repository expectations) | Not yet run |
| 2 | Finance handoff doesn't persist to SIS | P0 | B | Finance completion updates `feeHandoffStatusOverridesProvider` + `financeGeneratedAccountsProvider` only; previous flow did not enqueue SIS conversion. | Admissions→Finance→SIS continuity breaks; fee assignment can appear complete but SIS doesn’t reflect it. | Finance→SIS handoff completion was client-side state only. | `lib/features/finance/integration/finance_admissions_handoff_provider.dart`, `lib/features/finance/finance_workflow_actions.dart`, `lib/features/admissions/fee_handoff/admissions_fee_handoff_provider.dart`, `lib/core/repositories/mock/mock_admissions_sis_bridge.dart`, `lib/features/sis/dashboard/sis_dashboard_screen.dart` | **Fixed (mock):** added `MockAdmissionsSisBridge.completeFinanceHandoff()` to persist fee account link on the handoff and re-queue SIS conversion. | `test/core/repositories/mock_admissions_sis_bridge_test.dart` | Not yet run |
| 3 | API write stubs (18 methods) | P0 | B | API repos explicitly throw on write methods (e.g. HR/transport/hostel/library/inventory/director). Without fallback wiring, API mode breaks writes. | Enabling API mode breaks core operational mutations (HR, transport allocations, hostel admissions, library issue/return, inventory procurements, director compliance/export). | Provider wiring was missing for several modules; hybrid wrappers were incomplete. | `lib/core/repositories/api/hr/api_hr_repository.dart`, `lib/core/repositories/api/transport/api_transport_repository.dart`, `lib/core/repositories/api/hostel/api_hostel_repository.dart`, `lib/core/repositories/api/library/api_library_repository.dart`, `lib/core/repositories/api/inventory/api_inventory_repository.dart`, `lib/core/repositories/api/director/api_director_repository.dart`, `lib/core/repositories/repository_providers.dart`, `lib/core/repositories/api/**/hybrid_*_repository.dart` | **Partial fix:** hybrid write-fallback wrappers added and wired for HR, transport, hostel, library, inventory, director. | Contract/widget tests for impacted modules pass after wiring updates. | Not yet run |
| 4 | RBAC bypass on `/school/*` routes | P0 | B | `canAccessErpRoute()` returns `true` whenever a route is not considered an Admin ERP route (`!isAdminErpRoute(location) return true`). Coverage depends on `RouteNames.adminErpRoutes`. | Staff can reach admin configuration screens without appropriate permissions (security risk). | Route-guard coverage relies on route-prefix registry; missing prefixes allow guard bypass. | `lib/router/route_guards.dart`, `lib/router/route_names.dart` | **Partial fix (mock verified):** expanded `RouteNames.adminErpRoutes` to include evolution/school prefixes (principal command, school completion hub, growth, etc.). | `test/router/route_guards_test.dart` regression cases for evolution routes. | Not yet run |
| 5 | Parent insights/experience unguarded | P0 | C | Parent shell is gated by `UserRole.parent` in `_canAccessRoute()`, and `RolePermissionMatrix` grants the parent ERP role the corresponding permissions (`viewParentExperience`, `viewParentInsights`, `viewParentAcademicSummary`). | No actual unauthorized access observed *given current permission matrix*. | Authorization check is “role-based” rather than “explicit permission-based,” but the role’s permission matrix already allows the view. | `lib/router/app_router.dart`, `lib/core/security/role_permissions.dart`, `lib/router/route_guards.dart` | **No remediation started** (classified as false positive for this session’s authorization outcome). | None needed yet | None |
| 6 | No Vice Principal role | P0 | A | `ErpRole` enum does not contain a vice-principal value; staff role matrix and role-based routing cannot represent VP. | VP cannot log in / cannot be assigned a correct permission subset or delegation workflow. | Role model missing a required delegated principal persona. | `lib/core/security/erp_role.dart`, `lib/core/security/role_permissions.dart`, `lib/features/auth/qa_login_persona.dart`, `lib/router/app_router.dart` | **Fixed:** added `ErpRole.vicePrincipal` + permission matrix and staff demo routing. | `test/core/security/erp_role_test.dart` | Not yet run |
| 7 | Isolated MockSisRepository in academic ops | P0 | D | `academicOperationsRepositoryProvider` now uses `ref.read(sisRepositoryProvider)` instead of `MockSisRepository()` instance. | Promotion/academic operations could diverge from SIS registry; now aligned for mock mode. | Academic operations provider used a separate SIS instance. | `lib/core/repositories/repository_providers.dart` | **Fixed (mock):** academic ops reuse shared SIS repo provider. **No explicit tests yet.** | None added yet | None added yet |
| 8 | No alumni graduation automation | P0 | A | `AlumniRepository` only exposes *read* APIs—no create/sync mutation methods. SIS update status doesn’t trigger alumni onboarding; alumni mock registry is static and not driven by SIS exit events. | SIS exit/alumni status does not populate alumni records automatically. | Missing integration/mutation pipeline between SIS status transition and alumni onboarding. | `lib/core/repositories/interfaces/alumni_repository.dart`, `lib/core/repositories/mock/mock_alumni_repository.dart`, `lib/core/repositories/mock/mock_sis_repository.dart`, `lib/features/alumni/registry/alumni_registry_screen.dart` | **Fixed (mock):** when SIS transitions to `SisStudentStatus.alumni`, the mock now upserts an `AlumniRecord` via `MockAlumniWriteStore`. | `test/core/repositories/mock_alumni_write_store_test.dart` | Not yet run |
| 9 | Teacher marks ≠ student/parent results | P0 | B | Teacher marks were previously stored in teacher-only mocks; student/parent exams were separate fixtures. Sync store + overlays were added but require canonical id alignment and still overlay only results (not all report/card aggregates). | Students/parents can see different marks/results than the teacher entered. | Cross-persona exam/result data silos. | `lib/core/repositories/mock/mock_teacher_repository.dart`, `lib/core/repositories/mock/mock_exam_results_sync_store.dart`, `lib/core/repositories/mock/mock_student_repository.dart`, `lib/core/repositories/mock/mock_parent_repository.dart` | **Partial fix applied:** teacher marks now publish into shared sync store; student/parent exam results overlay from sync store. | `test/core/repositories/mock_exam_results_sync_store_test.dart` | Not yet run |
| 10 | Transport tracking placeholder | P0 | A | Transport GPS tracking screen explicitly shows a map placeholder (“No live maps in MVP — architecture ready for Mapbox/Google Maps”) rather than live integration. | Transport manager cannot operate real-time tracking; parent expectation mismatch. | GPS map integration not implemented; UI uses placeholder architecture. | `lib/features/transport/tracking/transport_tracking_screen.dart`, related transport providers/models | **Fixed:** transport tracking copy updated to telemetry-first messaging (no “Google Maps” live integration promise). | `test/features/transport/transport_screens_test.dart` | Not yet run |
| 11 | Class teacher dashboard → attendance | P1 | B | `class_teacher_dashboard` action routes into teacher attendance (`teacher_navigation.dart`). Screen label implies class command center but navigates to attendance workflow. | Operational friction; teachers may miss class-level tasks. | Action mapping/wiring mismatch. | `lib/router/teacher_navigation.dart`, `lib/features/teacher/dashboard/teacher_dashboard_screen.dart` | **Fixed:** `class_teacher_dashboard` now routes to a dedicated `TeacherClassTeacherDashboardScreen` (class overview + quick actions). | `test/features/teacher/teacher_navigation_pilot_test.dart` | Not yet run |
| 12 | Teacher cannot create homework | P1 | A | Teacher homework UI is titled “Homework Review”; it only supports review of submissions; teacher navigation routes `create_homework` to the review screen and no create mutation is exposed. | Teachers cannot assign homework from mobile persona. | Teacher homework create workflow missing/wired to review-only UI. | `lib/router/teacher_navigation.dart`, `lib/features/teacher/homework/teacher_homework_screen.dart`, `lib/features/teacher/homework/teacher_homework_provider.dart` | **Fixed (mock-first MVP):** `create_homework` now routes to `TeacherHomeworkCreateScreen`, and created assignments appear in homework review. | `test/features/teacher/teacher_navigation_pilot_test.dart` | Not yet run |
| 13 | Messaging read-only (parent + teacher) | P1 | A | Parent/teacher conversation screens render message threads but do not expose compose/input actions. | Communication is broken for daily operations. | Missing “send/compose” wiring in conversation UI. | `lib/features/parent/messages/parent_conversation_screen.dart`, `lib/features/teacher/messages/teacher_conversation_screen.dart` | **Fixed:** reply composer added to conversation screens; send wired via `sendTeacherMessageProvider` (teacher) and `parentRepositoryProvider.sendMessage` (parent). | `test/features/teacher/teacher_module_screens_test.dart`, `test/features/parent/messages/parent_conversation_screen_test.dart` | Not yet run |
| 14 | No parent transport/PTM mobile | P1 | A | Parent mobile routes do not include transport or PTM booking flows; ERP transport/PTM lives in admin ERP modules only. | Parents cannot act on transport/meetings day-to-day. | Missing mobile parity routing/workflow implementation. | `lib/router/app_router.dart`, `lib/router/parent_navigation.dart`, transport/parent_meetings modules | **Fixed:** added `ParentTransportScreen` + `ParentPtmScreen` with routes, dashboard quick actions, and notice/event deep links. | `test/features/parent/parent_navigation_pilot_test.dart` | Not yet run |
| 15 | No student report cards/progress/AI | P1 | A | Student feature tree lacks report card and progress/AI guidance screens; student home is limited to timetable/homework/exams/attendance. | Students cannot access official progress artifacts. | Missing mobile feature surfaces. | `lib/features/student/` | **Fixed:** added `StudentReportCardScreen` + `StudentProgressScreen` wired from dashboard quick actions and router. | `test/features/student/student_navigation_pilot_test.dart` | Not yet run |
| 16 | Principal blocked from transport/hostel | P1 | D | Principal permission matrix now includes `Permission.viewTransport` and `Permission.viewHostel`. | Previously blocked; now should be permitted. | Missing permissions in principal role matrix. | `lib/core/security/role_permissions.dart` | **Fixed:** principal can view transport/hostel (RBAC). | None added yet | None added yet |
| 17 | No HR reports | P1 | A | HR module has HR operations screens but no dedicated reports module; no `hr_reports_*` screens/providers exist. | HR cannot run compliance/performance/leave reports. | Missing HR reports surfaces and/or repository methods. | `lib/features/hr/` | **Fixed:** added `HrReportsScreen` + sub-nav route `RouteNames.hrReports` with mock catalog and honest export preview actions. | Provider/screen compile + HR sub-nav wiring | Not yet run |
| 18 | 28 dead `onPressed: () {}` | P1 | B | Many dashboards/export actions have no-op handlers. | Users click actions and nothing happens. | Placeholder UI handlers. | Multiple `lib/features/**` dashboard screens | **Fixed:** replaced no-op handlers with navigation (`hrReports`), or `showAksharaOperationalPreviewSnackBar` / `showAksharaReportExportPreviewSnackBar` feedback across 28 touchpoints. | Manual compile + existing module screen tests | Not yet run |
| 19 | Fake report exports | P1 | B | Reports show “Export queued” snackbars but no export output pipeline. | Users believe exports were generated when they were not. | Export backend/persistence missing. | `lib/shared/widgets/akshara_analytics_panel.dart`, `lib/features/*/reports/*_reports_screen.dart` | **Fixed:** export snackbars now state “preview only — pipeline not connected”; finance/HR/library/alumni report buttons use honest preview helpers. | Existing finance report widget tests | Not yet run |
| 20 | Copilot stub only | P2 | B | Copilot dock exists; responses are stubbed (`copilot_stub_responses.dart`) and most modules lack rich context scope (label-only). | Copilot doesn’t provide operational decisions. | Stubbed AI provider + incomplete context wiring. | `lib/core/ai/providers/stub_ai_provider.dart`, `lib/features/copilot/**` | **Partial fix:** stub replies now include module-specific operational next steps; still stub-backed (no live LLM). | `test/features/copilot/copilot_capability_filter_test.dart` | Not yet run |
| 21 | `isCopilotTopicEnabled` unwired | P2 | B | Capability registry defines `isCopilotTopicEnabled` but it is never called/enforced in copilot context generation. | Copilot can suggest advice for disabled modules. | Topic capability enforcement missing. | `lib/core/school_config/school_capability_registry.dart`, copilot context providers | **Fixed:** `CopilotCapabilityFilter` enforced in `copilot_provider` send path and quick actions via `schoolCapabilitiesProvider`. | `test/features/copilot/copilot_capability_filter_test.dart` | Not yet run |
| 22 | `/admin` placeholder hub | P2 | B | `/admin` route uses `AdminModulePlaceholderScreen` and stale module info even when module dashboards exist elsewhere. | Users see construction placeholder instead of real hub navigation. | Placeholder screen not replaced with functional hub wiring. | `lib/features/admin/screens/admin_module_placeholder_screen.dart` | **Fixed:** `/admin` now renders `AdminHubScreen` with permission-filtered module cards from `adminNavDestinationsProvider`. | None added yet | Not yet run |
| 23 | `ControlCenterGuard` unused | P2 | B | `ControlCenterGuard` exists in `route_guards.dart` but has no usages. | Defense-in-depth missing; permissions could be incomplete elsewhere. | Guard not wired into navigation. | `lib/router/route_guards.dart` | **Fixed:** all Control Center route builders wrap screens in `ControlCenterGuard`. | Existing route guard tests | Not yet run |
| 24 | `/ai-assistant` unauthenticated | P1 | A | `_isProtectedRoute()` only protects `/parent`, `/teacher`, `/student`, and admin ERP routes; `/ai-assistant` not included. | Unauthenticated access to AI assistant UI. | Route protection missing for AI assistant routes. | `lib/router/app_router.dart`, `lib/router/route_names.dart` | **Fixed:** AI assistant routes are protected and require authentication for access. | `test/router_smoke_test.dart` | Not yet run |
| 25 | Hardcoded demo comparison school IDs | P2 | B | Trust comparison uses demo seed selection (hardcoded demo school ids) rather than tenant config. | Directors see wrong comparison data. | Demo selection seeded; no tenant-driven selection. | `lib/features/**/platform_intelligence_providers.dart` | **Fixed:** `schoolComparisonSelectionProvider` now derives IDs from `controlCenterSchoolsProvider` with tenant school preference. | None added yet | Not yet run |

---

## Backlog Summary (by priority & classification)

* P0 critical backlog includes findings: **#1, #2, #3, #4, #6, #8, #9, #10** (excluding #5 because classified C and #7/#16 because classified D).
* P1 important backlog includes findings: **#11–#15, #17–#19, #24** (excluding #16 because D).
* P2 nice-to-have backlog includes findings: **#20–#23, #25**.

