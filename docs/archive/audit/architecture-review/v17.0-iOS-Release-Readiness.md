# Architecture Review — v17.0 iOS Release Readiness

**Release:** v17.0 (`v17.0-ios-release-readiness`)  
**Date:** June 2026  
**Reviewer:** Agent A / Release Manager  
**Score:** **88 / 100** (pilot-weighted)

---

## Summary

v17.0 completes iOS build infrastructure and documentation. The Flutter iOS target **compiles successfully** for device release. IPA generation remains blocked solely by **missing Apple Developer code signing certificates** on the build Mac — an operational prerequisite, not a code defect.

Android release artifacts are rebuilt at version 17.0.0+170 with staging dart-defines unchanged from v16.6.

---

## iOS build pipeline

```
pubspec.yaml (17.0.0+170)
    ↓
flutter build ipa --release + staging dart-defines
    ↓
ios/Podfile (platform 13.0, post_install targets)
    ↓
ios/Flutter/xcode.env.local (DEVELOPER_DIR)
    ↓
Xcode archive → codesign → .ipa
    ↓
TestFlight (App Store Connect)
```

| Stage | Status | Risk |
|-------|--------|------|
| Pod resolution | ✅ | Low |
| Native assets (`objective_c`) | ✅ with `DEVELOPER_DIR` | Medium if env not sourced |
| Xcode compile | ✅ | Low |
| Code signing | ❌ | **High — blocks IPA** |
| TestFlight upload | ⏳ | Medium — manual steps |

---

## Configuration audit

| Item | Value | Correct? |
|------|-------|----------|
| Bundle ID | `com.akshara.erp.aksharaErp` | ✅ |
| Android applicationId | `com.akshara.erp` | ✅ (separate — intentional) |
| Display name | Akshara ERP | ✅ |
| iOS deployment target | 13.0 | ✅ |
| App icons | Full AppIcon set | ✅ |
| Launch screen | LaunchImage | ✅ |
| Staging API URL | Supabase functions | ✅ |

---

## Hardening findings

### Fixed (v17.0)

- **Admin notification bell** previously showed snackbar placeholder; now navigates to existing `NotificationsScreen` at `RouteNames.parentNotifications`. Reuses mobile notification inbox for ERP shell — acceptable for pilot; dedicated admin notification center deferred.

### Accepted (cosmetic / out of scope)

- Admin profile menu placeholder snackbar
- Student app settings "coming soon" section
- Transport tracking placeholder provider (not in pilot journeys)
- Mock repository placeholder strings (mock-only paths)

### No critical code markers

Grep of `lib/` found no `TODO`, `FIXME`, or user-visible `DEBUG` strings in production paths.

---

## Recommendations

| Priority | Recommendation |
|----------|----------------|
| P0 | Complete Apple Developer signing on build Mac |
| P0 | Run `sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer` |
| P1 | Upload IPA to TestFlight; internal smoke test on iPhone |
| P2 | Register push notification capability when communication push goes live |
| P3 | Migrate `printing` / `flutter_secure_storage` to SPM when plugins support it |

---

## Gate sign-off

| Gate | Pass |
|------|:----:|
| No new ERP modules | ✅ |
| No backend expansion | ✅ |
| `flutter analyze` clean | ✅ |
| `flutter test` green | ✅ |
| Android artifacts | ✅ |
| iOS compile | ✅ |
| IPA artifact | ❌ |
| TestFlight docs | ✅ |

**Verdict:** Ready for **Android pilot**. iOS pilot **hold** until signing + TestFlight upload.
