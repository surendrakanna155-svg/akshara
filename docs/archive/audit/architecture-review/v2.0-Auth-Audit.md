# v2.0 Auth Audit

**Date:** 2026-06-05  
**Scope:** Staff login UX, session persistence, production auth flow  
**Quality gates:** `flutter analyze` 0 issues · `flutter test` 557 passing

## Executive summary

Staff ERP now has a **dedicated production login path** separate from the parent/teacher demo login. Sessions issue JWT-shaped tokens, persist with remember-session support, and route to the admin ERP shell.

**Auth readiness: 78 / 100** (↑ from 62/100 at v1.8)

| Capability | v1.9 | v2.0 |
|------------|------|------|
| Staff production UI | ❌ | ✅ |
| Email login | ❌ | ✅ |
| Remember session | Partial | ✅ |
| Role-aware staff login | Programmatic only | ✅ UI + claims |
| Live auth server | ❌ | ❌ |

## Staff login components

| File | Responsibility |
|------|----------------|
| `staff_login_screen.dart` | Email/mobile, role, remember session |
| `staff_otp_screen.dart` | OTP verify, error/retry, resend |
| `staff_login_provider.dart` | `StaffOtpWorkflow` abstraction, state machine |

## Session integration

- `completeStaffLogin()` on `AuthNotifier` persists tokens + claims
- Staff home route: `RouteNames.admin` (was teacher dashboard)
- `PersistedAuthSession` validates email identifiers for staff role

## v1.7 findings addressed

| Finding | Resolution |
|---------|------------|
| P0-06 No production staff login UI | ✅ Staff portal screens |
| P0-05 JWT mock only | **Partial** — JWT decoder + mock tokens |

## Remaining gaps

| ID | Gap | Priority |
|----|-----|----------|
| AUTH-07 | No live `POST /auth/staff/login` | P0 |
| AUTH-08 | OTP still mock workflow | P0 |
| AUTH-09 | No secure token storage | P1 |
| AUTH-10 | No MFA / SSO | P2 |

## Security readiness score

**78 / 100** (client auth UX + lifecycle foundation)

## Production readiness delta

**74 → 79 (+5)**
