# v11.1–v11.5 — Evolution Foundation Hardening

**Date:** June 2026  
**Program:** Akshara Evolution Phase 7  
**Scope:** Harden v10.5–v11.0 foundations — no new business modules

## Summary

Completed five hardening milestones to make evolution platform features production-ready: full API repository layer, live widget data, setup wizard provisioning, growth ↔ admissions integration, and school OS completion gaps.

## Milestones

### v11.1 — Evolution API Repository

- `ApiEvolutionRepository` + `EvolutionRemoteDataSource` + `EvolutionMapper`
- `HybridEvolutionRepository` wired via `evolutionRepositoryProvider`
- Contract tests: `test/contracts/evolution/evolution_repository_contract_test.dart`
- Integration tests: `test/integration/evolution/evolution_api_integration_test.dart`

### v11.2 — Live Widget Platform

- Backend: `widget_data_service.ts`, `GET /widgets/data`, `POST /widgets/data/refresh`
- 60s per-user cache, permission-aware widget filtering
- Flutter: `WidgetLiveData` model, `widgetLiveDataProvider`, live cards on dynamic dashboard

### v11.3 — School Setup Automation

- `setup_wizard_provision_service.ts` — provisions academic year, classes, sections, fee structures
- Invoked on wizard completion (`complete: true`)
- Warnings: teacher shortages, large class sizes, missing subjects, timetable reminder

### v11.4 — Admissions & Growth Integration

- Migration: `growth_inquiries.lead_id` FK → `admissions_leads`
- `POST /growth/inquiries/:id/convert` — creates admissions lead, links inquiry
- `GET /growth/funnel` — stage, campaign, and source attribution
- Flutter funnel dashboard + convert action on inquiries

### v11.5 — School Operating System Completion

- Teacher: class-scoped insights, lesson history, intervention effectiveness
- Parent: `parent_language_preferences` table + API + UI language selector
- Principal: priority engine score on command center
- Operations: `GET /operations/actions` action center aggregation

## Deployment

1. Apply migrations: `20260623500000_growth_lead_fk.sql`, `20260623600000_parent_language_prefs.sql`
2. Redeploy Supabase Edge API (`./scripts/deploy_staging.sh`)
3. Enable client flag: `EVOLUTION_API_ENABLED=true` with `ENABLE_API_MODE=true`

## Quality Gates

| Gate | Result |
|------|--------|
| `flutter analyze` | 0 issues |
| `flutter test` | 1143 passing (1 skipped) |
