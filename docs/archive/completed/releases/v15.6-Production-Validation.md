# Release v15.6 — Production Validation (Phase 21)

**Tag:** `v15.6-production-validation`  
**Date:** June 2026  
**Scope:** Phase 21 — staging deployment, device validation, timetable/lesson completion, parent analytics, platform hardening

---

## Summary

Phase 21 completes pilot production validation across six milestones: deployment probes, real-device API matrix, timetable editor with room allocation, lesson topic completion, parent activation dashboard, and platform health/webhook hardening. All client quality gates pass; staging Edge redeploy remains blocked on Supabase credentials.

---

## v15.1 — Staging Deployment Completion

- Added `/health/storage` and `/health/providers` Edge routes
- `scripts/phase21_deploy_verify.sh` — migrations + Edge deploy + readiness sweep
- Updated `scripts/pilot_readiness_verify.sh` — storage/provider health as deploy-blocked probes

**Staging result:** 11 probes pass, 11 fail (404 — Edge not redeployed; `deployStatus: blocked`)

---

## v15.2 — Real Device Testing

- `scripts/device_validation_matrix.py` — role-based API smoke (admin, teacher, parent, student)
- Mobile responsiveness tests from v15.0 retained (`mobile_screen_responsiveness_test.dart`)
- Role dashboards verified on staging where deployed (admin/finance/sis/parent/teacher/student: 200)

---

## v15.3 — Timetable Completion

- Backend: `POST /academic/timetables/periods/move` (`moveTimetablePeriod`)
- Flutter: drag-and-drop `TimetableEditorTab` (6th hub tab)
- Room/lab allocation screen + `allocateRooms` repository method
- Conflict detection via existing timetable engine (unchanged, verified in Deno tests)

---

## v15.4 — Lesson & Syllabus Completion

- Lesson log create includes `subjectId` for topic linkage
- Topic completion workflow via popup → `completeTopic(lessonLogId)`
- Teacher/principal progress dashboards wired to existing analytics endpoints

---

## v15.5 — Parent Activation Analytics

- Backend: `GET /school/parent-activation/dashboard` — activation %, adoption %, DAU/MAU
- Flutter: `ParentActivationDashboardScreen` + hub navigation
- Extended `PilotActivationStats` model (adoptionRate, dailyActiveParents, monthlyActiveParents)

---

## v15.6 — Platform Hardening

- `/health/providers` — storage + vault + delivery queue snapshot
- `POST /communications/delivery/webhook` — delivery confirmation webhook
- Vault health probe in readiness script (404 until deploy)

---

## Tests Added

| Test | Path |
|------|------|
| Phase 21 platform (mock) | `test/integration/phase21/phase21_platform_test.dart` |
| Timetable contract (movePeriod) | `test/contracts/timetable/timetable_repository_contract_test.dart` |
| Deno timetable engine | `supabase/functions/_shared/timetable/timetable_engine_test.ts` (6 pass) |

---

## Quality Gates

| Gate | Result |
|------|--------|
| `flutter analyze` | 0 errors (3 info: const/deprecation) |
| `flutter test` | 1204 passed, 1 skipped |
| Deno timetable tests | 6 passed |
| Staging probes | 11/22 green (deploy blocked) |

---

## Blockers

1. **Supabase auth** — `supabase login` or `SUPABASE_ACCESS_TOKEN` required for `phase21_deploy_verify.sh`
2. **v14+ intelligence routes** — 404 on staging until Edge redeploy
3. **Vault route** — `/control-center/vault/health` 404 until deploy
4. **Physical device QA** — Android/iPhone manual pass recommended post-deploy

---

## Readiness Score

**Client/platform code:** 92 / 100  
**Staging deployment:** 50 / 100 (11/22 probes; blocked on credentials)  
**Launch-weighted pilot readiness:** 88 / 100
