# Release v17.2 — iOS Device Install

**Tag:** `v17.2-ios-device-install`  
**Date:** June 2026  
**Version:** 17.2.0+172  
**Scope:** Xcode cache recovery, device install pipeline — **no features or UI changes**

---

## Root cause

| Issue | Cause | Fix |
|-------|-------|-----|
| Build database / output file map errors | Stale Xcode DerivedData + Flutter build cache | `scripts/ios_build_recovery.sh` |
| Native asset failures in Xcode subprocess | `hooks_runner` strips `DEVELOPER_DIR` | Existing `scripts/ios/xcrun` wrapper (v17.1) |
| `flutter run` install failure | Flutter launcher path; app built OK | `devicectl device install app` |
| Launch blocked | iOS security — developer not trusted on device | **Owner: Settings → Trust developer** |

---

## Fixes applied

| File | Change |
|------|--------|
| `scripts/ios_build_recovery.sh` | DerivedData wipe + clean + pod reinstall |
| `scripts/ios_device_install.sh` | Build release + devicectl install |
| `ios/Flutter/xcode.env.local` | Robust PATH to xcrun wrapper |

---

## Device status — Kanna's iPhone

| Step | Status |
|------|--------|
| Xcode build (release) | ✅ 36s, no database errors |
| Code signing | ✅ Team `63YXKNM6UN`, device in profile |
| Install via devicectl | ✅ `com.akshara.erp.aksharaErp` installed |
| Launch | ⏳ **Trust developer on iPhone** (one-time) |

---

## Commands executed

```bash
./scripts/ios_build_recovery.sh
flutter build ios --release + staging dart-defines
xcrun devicectl device install app --device 00008120-001131801A60201E build/ios/iphoneos/Runner.app
```

---

## Quality gates

| Gate | Result |
|------|--------|
| `flutter analyze` | 0 issues |
| Xcode build | ✅ No build database errors |

---

## Remaining blocker

**On iPhone:** Settings → General → VPN & Device Management → Trust **Apple Development: surendra303@gmail.com**

Then tap **Akshara ERP** on home screen.

---

## Docs

- [iOS-Device-Install-Guide.md](../Testing/iOS-Device-Install-Guide.md)
