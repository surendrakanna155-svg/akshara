# v18.6.2 — Final Patrol Suite Recovery

**Date:** June 2026  
**Tag:** `v18.6.2-final-suite-recovery`  
**Version:** `18.6.2+187`

## Summary

Recovered the last failing Patrol suite (Inventory lifecycle) to achieve **17/17 ERP coverage suites green** in targeted verification.

## Root Cause

`workflow: inventory distribution lifecycle` failed because:

1. **Asset Lifecycle** is the last horizontal sub-nav tab — Patrol scroll/tap triggered Flutter viewport `TypeError` on emulator.
2. Direct route navigation exposed a **layout bug**: `ListView` nested inside `AdminContentScaffold`'s scroll `Column` (unbounded height).

This was a **test-navigation and layout constraint** issue, not missing ERP business logic.

## Fix

| Area | Change |
|------|--------|
| QA keys | `QaTestKeys.inventoryLifecycleScreen` on lifecycle screen |
| Patrol helpers | `goToErpRoute` via Riverpod `goRouterProvider`; `navigateErpModuleRoute`; `assertVisibleKey` |
| Lifecycle screen | Replace body `ListView` with shrink-wrapped `Column` |
| Inventory test | Route `/inventory/lifecycle` + key + `Assets tracked` anchor |

## Validation

Targeted re-run (7 previously failed suites):

| Suite | Result |
|-------|--------|
| ERP | 12/12 |
| Finance | 6/6 |
| Inventory | **5/5** |
| SIS | 6/6 |
| HR | 8/8 |
| Transport | 9/9 |
| Screenshot | 10/10 |

Gates: `flutter analyze` 0 issues, `flutter test` 1298 passed.

## Readiness

| Metric | Before | After |
|--------|--------|-------|
| Patrol suites (full mode) | 10/17 | **17/17** |
| Readiness score | 82 | **90** |
