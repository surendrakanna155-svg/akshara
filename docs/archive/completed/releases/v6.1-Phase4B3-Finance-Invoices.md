# v6.1 Phase 4B3 — Finance Invoices

**Status:** Complete  
**Depends on:** Phase 4B2 student accounts + fee assignments  
**Deployment:** Migration `20260612400000_finance_slice3_invoices.sql` + Edge Functions redeploy

---

## Summary

Student fee invoices (`finance_invoices`) auto-generate on fee assignment (N=1 annual). REST APIs expose list/detail/issue/cancel with RBAC and tenant isolation probes. Flutter client extended with `FinanceInvoice` domain model and repository methods.

## Deliverables

| Area | Change |
|------|--------|
| Schema | `finance_invoices` table + RLS + seed fixtures |
| Generation | `createAnnualInvoice` hook in `createAssignmentAndAccount` |
| Routes | `GET /finance/invoices`, `GET /finance/invoices/:id`, `POST .../issue`, `POST .../cancel` |
| Lifecycle | `draft` → `issued` → `partially_paid` / `paid`; `cancelled` void |
| Probes | 6 invoice isolation probes (33 total post-4B3) |
| Client | `FinanceInvoice`, `getInvoices/getInvoice/issueInvoice/cancelInvoice` |
| Tests | Deno repository tests; Flutter contract + integration |

## Feature flags

```
--dart-define=ENABLE_API_MODE=true
--dart-define=FINANCE_API_ENABLED=true
```

## Gate

- `flutter analyze` = 0 issues
- `flutter test` = all passing
- Deno: `finance_invoices_repository_test.ts` pass

## Deferred

- `finance_invoice_lines` multi-term split (4B3b)
- Parent RLS + `viewFinance` seed (4B3d)
- `GET /finance/invoices?studentId=` filter
- UI invoice management screens (repository-only slice)
