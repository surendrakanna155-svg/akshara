# v6.1 Phase 5C.3 — Client Soft FK Alignment

**Status:** Complete  
**Depends on:** Phase 5C.2 (backend soft FK + resolver)  
**Deployment:** Client-only — no backend redeploy required

---

## Summary

Flutter client now dual-writes academic catalog UUIDs alongside existing text labels on Admissions, SIS, and Finance mutation paths. Year-scoped catalog dropdowns prevent cross-year class/section mismatches.

## Deliverables

| Area | Change |
|------|--------|
| Catalog resolver | `academic_catalog_placement.dart` — label → UUID resolution |
| Mutation enrichment | Admissions/SIS/Finance mutation providers resolve IDs before API calls |
| Write DTOs | Dual-write `academic_year_id` / `class_id` / `section_id` (+ camelCase aliases) |
| Read mapper | Finance fee structures map `academicYearId` |
| UI | Year-scoped class/section dropdowns in admissions enrollment wizard |
| Tests | Placement + dual-write contract tests; 1011 Flutter tests passing |

## Feature flags

```
--dart-define=ENABLE_API_MODE=true
--dart-define=ACADEMIC_API_ENABLED=true
--dart-define=ADMISSIONS_API_ENABLED=true
--dart-define=SIS_API_ENABLED=true
--dart-define=FINANCE_API_ENABLED=true
```

## Gate

- `flutter analyze` = 0 issues
- `flutter test` = 1011 passing
