# v6.3 Sprint 5 — Audit Ingestion + Hardening

**Status:** Complete (core pipeline)  
**Deployment:** Migration `20260614500000_audit_ingestion_domain_events.sql` + Edge Functions redeploy

## Summary

`audit_events` persistence, `POST /audit/events/batch` ingestion with client-id idempotency, `domain_events` transactional outbox, mutation audit middleware (admissions createLead wired), 6 tenant probes.

## Flags

`AUDIT_API_ENABLED` (with `ENABLE_API_MODE=true`)
