# v7.1-Pilot Closure Sprint

**Date:** June 2026  
**Status:** Complete — pilot blockers closed

## Summary

Closed the remaining pilot-readiness gaps: operational timetable merged into mobile read APIs, staging seed teacher assignments corrected, probe validation confirmed.

## Changes

| Area | Fix |
|------|-----|
| Timetable reads | `overlayTimetableSnapshotFromSlots()` merges `timetable_slots` into parent/teacher/student snapshot responses with fallback |
| Seed data | `teacher_user_id` corrected to staff user; expanded Mon–Fri 8-A slots |
| Probes | 195 tenant isolation probes — 0 failures |
| Docs | Roadmap + BackendRoadmap updated; v7.2 unblocked |

## Deployment

Apply migrations:
- `20260614820000_pilot_timetable_closure.sql`

Deploy `api` edge function.

## Residual (ops, not code blockers)

- Live FCM / Twilio / Razorpay credentials for production push/SMS/payments
- Payment webhook multi-tenant claim resolution (manual reconciliation in pilot)

## Tests

- `flutter analyze` — 0 issues
- `flutter test` — all passing
- `deno test supabase/functions/_shared/` — all passing
