# v7.1 Pilot Operations Sprint

**Date:** June 2026  
**Scope:** Deep audit (5C.2 → v7.1) + pilot-critical gap closure  
**Status:** Complete (pilot-ready with documented residual blockers)

## Summary

Closed operational gaps for daily school pilot: attendance/timetable persistence, mobile write APIs, Communication Hub inbox wiring, device tokens, broadcast recipient resolution, audit hardening, domain-events worker, CI deploy visibility, and WhatsApp deep-link (Phase 1).

## Backend

| Area | Change |
|------|--------|
| Migration | `20260614800000_pilot_operations.sql` — attendance, timetable, leave, homework, exam marks |
| Pilot module | `supabase/functions/_shared/pilot/*` — teacher/parent/student mobile writes |
| Communication | Mark read, mark-all-read, device token register/unregister, pagination |
| Broadcasts | Recipients from `student_guardians` + `school_memberships` |
| Audit | Finance collections/refunds, SIS student create, pilot mobile writes |
| Event bus | `POST /domain-events/process-pending` minimal worker |
| API router | Fixed nested route bracket bug in `api/index.ts` |

## Flutter

| Area | Change |
|------|--------|
| Notifications | `notifications_provider.dart` wired to `CommunicationRepository` when API enabled |
| Communication repo | Mark read, device token methods |
| WhatsApp | `lib/core/utils/whatsapp_launcher.dart` + teacher compose button |
| Dependency | `url_launcher` |

## CI

- `backend_staging.yml` fails on missing staging secrets (no silent skip)

## Deployment

- Apply migration `20260614800000_pilot_operations.sql`
- Deploy `api` edge function
- Staging secrets required: `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_ID_STAGING`, `JWT_SECRET`

## Residual pilot blockers

- Live FCM / Twilio / Razorpay credentials (stub mode OK for pilot)
- Payment webhook multi-tenant claims (manual reconciliation)
- Parent WhatsApp UI entry (teacher compose only; utility ready)
- Full exam operational module beyond mark update

## Tests

- `flutter analyze` — 0 issues
- `flutter test` — all passing
- `deno test supabase/functions/_shared/` — all passing
