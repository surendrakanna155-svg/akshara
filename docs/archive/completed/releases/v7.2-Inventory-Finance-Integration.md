# v7.2 — Inventory–Finance Integration

**Status:** Complete

## Delivered

- Shared `inventory_vendors` master (live DB, not entity-read only)
- Purchase orders + lines, approve → AP commitment + finance posting
- Goods receipt (GRN) + weighted-average stock valuation
- Domain events: `procurement.approved`, `procurement.received`
- Audit: `procurementFinancePosted`, `vendorCreated`, `goodsReceived`
- API routes under `/inventory/*` (catalog, procurement, stock valuation)
- Tenant probes (+6 → **205 total**)

## Key routes

| Method | Path |
|--------|------|
| GET/POST | `/inventory/vendors/catalog` |
| GET/POST | `/inventory/procurement/orders` |
| POST | `/inventory/procurement/orders/:id/approve` |
| POST | `/inventory/procurement/orders/:id/receive` |
| GET | `/inventory/stock/valuation` |

## Migration

`20260614910000_inventory_finance_vendors.sql`  
`20260614920000_inventory_finance_integration.sql`

## Staging

- Migrations applied to `akshara-staging` (202606149100–149200)
- `goods_receipt_lines` includes denormalized `organization_id` / `school_id` for RLS
- Tenant probes: **205** (parallel execution, concurrency 16)
