# v7.2c — Finance Reconciliation UI

**Status:** Complete

## Scope

Closes remaining inventory–finance client gaps before v7.4 AI Copilot.

## Delivered

| Area | Detail |
|------|--------|
| Reconciliation dashboard | FN-12 tab — KPIs + procurement summary |
| Transaction timeline | Chronological PO / GRN / posting events |
| Goods receipt review | Split list + line detail panel |
| Posting review | AP commitment ↔ finance posting table |
| Vendor history | Vendor picker + PO/AP transaction list |
| Write repository | `InventoryFinanceRepository` — vendor, PO, approve, receive |
| API integration | All v7.2 backend routes + new reconciliation reads |

## Backend reads (new)

| Method | Path |
|--------|------|
| GET | `/finance/inventory-reconciliation/dashboard` |
| GET | `/finance/inventory-reconciliation/timeline` |
| GET | `/finance/inventory-reconciliation/goods-receipts` |
| GET | `/finance/inventory-reconciliation/goods-receipts/:id` |
| GET | `/finance/inventory-reconciliation/postings` |
| GET | `/finance/inventory-reconciliation/vendors/:id/transactions` |
| GET | `/inventory/procurement/orders/:id` |

## Client flag

`--dart-define=INVENTORY_FINANCE_API_ENABLED=true` (with `ENABLE_API_MODE=true`)

## Tests

- Contract: `test/contracts/inventory_finance/`
- Integration: `test/integration/inventory_finance/`
- Deno: reconciliation route + PO detail tests

## Deployment

API function redeploy required (no new migrations).
