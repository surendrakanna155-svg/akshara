# EOS Run Ledger

One line per `/eos` run. Newest at the bottom. The EOS appends here after every
evaluation; reports referenced below live in this same directory.

> ## 🔒 Mandatory pre-pilot sequence — REVISED (owner-set 2026-07-01, **re-ordered 2026-07-03**)
> **2026-07-03 owner re-order:** the single comprehensive Global Red Team must run AFTER
> the Adaptive-AI wave, so the whole product (including Adaptive AI) is audited ONCE — not
> Red-Teamed, then perturbed by a major AI wave, then Red-Teamed again. Run this exact order:
> 1. **Finish all remaining buildable roadmap gaps** (#8 Smart Timetable, #9 Workload, #3 Student Risk)
>    — plus the owner-approved queued waves: Identity/PSID cluster (dedicated wave), Admissions
>    approval maker-checker, PAR-D3 fee certificate ([[akshara-decision-queue]]).
> 2. **Fable UI/UX Audit** (complete it).
> 3. **Implement all approved UI/UX improvements.**
> 4. **Dedicated "Adaptive AI & Product Intelligence" wave** — dynamic dashboards, role-based
>    intelligence, proactive insights, priority cards, workflow suggestions, caching, API
>    optimization. (Do NOT start until steps 1–3 are done. See [[adaptive-ai-product-intelligence-vision]].)
> 5. **ONE comprehensive Global Red Team (/eos) audit** — the COMPLETE product incl. Adaptive AI;
>    audit every module INDEPENDENTLY (adversarial); a SEPARATE gap report per module + a
>    CONSOLIDATED remediation roadmap. Hard gate before any Pilot Simulation.
> 6. **Fix EVERY Red Team finding** → re-verify (EOS gate + full suites).
> 7. **Full Pilot School Simulation.**
> 8. **Production Certification.**
>
> Do NOT start the Global Red Team (step 5) until steps 1–4 are fully complete. The bar it
> applies to every module = the depth of the attendance integrity gates (#1–#8).
> (Superseded order was: gaps → Red Team → remediation → Pilot → Prod. The Fable UI/UX audit,
> UI improvements, and Adaptive-AI wave are now inserted BEFORE the single Red Team.)

*(Roadmap-gap phase started 2026-07-03. Maps done for #3/#8/#9 — all PARTIAL on a real v8.9/v7.5 base; adopted defaults for the flagged design choices, no owner pause.)*

| Date | Scope | Gate verdict | Open P0 / P1 | Report |
|------|-------|--------------|--------------|--------|
| 2026-06-28 | Data Reliability Platform — Phase 0b (integration + live deploy) | **PASS** (clean) | 0 / 0 | [EOS_DATA_RELIABILITY_PLATFORM_PHASE0B_REPORT.md](EOS_DATA_RELIABILITY_PLATFORM_PHASE0B_REPORT.md) |
| 2026-06-28 | QW1 — CI Enforcement Backbone (QA-B-071/072/073, QA-X-035/036/037/038/039) | **CONDITIONAL PASS** | 0 / 4 (device/live CI rows authored + YAML-valid, pending first scheduled run) | [QW1_CI_ENFORCEMENT_CERTIFICATION.md](../../QW1_CI_ENFORCEMENT_CERTIFICATION.md) |
| 2026-06-28 | QW1 — P0 Flutter widget surfaces (QA-F-001/002/005/006/012/014/018) | **PASS** | 0 / 0 (1 P2 tracked: attendance calendar 12px overflow @428px) | (tracker rows; 21/21 widget tests green, analyze clean) |
| 2026-06-28 | QW1 — QA personas + RBAC-isolation journeys + money loop (QA-J-001/019/024/037/047/058/060/061) | **PASS** | 0 / 0 (1 P1 found-and-fixed in-flight: QA-login persona permission resolution) | [QW1_PERSONA_RBAC_MONEY_CERTIFICATION.md](../../QW1_PERSONA_RBAC_MONEY_CERTIFICATION.md) |
| 2026-06-28 | QW1 — Messaging journeys (QA-J-002/013/057) | **PASS** | 0 / 0 (QA-J-013 rich AI/multi-channel screen UI scoped to QW7 QA-C-010..014; send path proven) | (tracker rows; qw1_messaging_e2e_test 2/2 Patrol green) |
| 2026-06-28 | QW1 — Year-end academic-operations commit (QA-J-038/039/059) | **PASS** | 0 / 0 (promotion wizard UI → QW3/QW7; commit logic proven by integration test) | (tracker rows; reshuffle+balance Patrol green + promotion_commit_test green) |
| 2026-06-28 | QW1 — Homework loop + teacher marks (QA-J-008/012/014) | **PASS** | 0 / 0 (marks grid UI → QW3/QW7; write+submit proven by integration test) | (tracker rows; homework Patrol 2/2 + teacher_marks_submit_test green) |
| 2026-06-28 | QW1 — RBAC negatives: Control-Center + ChainScope (QA-J-052/048) | **PASS** | 0 / 0 (chain-org-allowed positive → follow-up needs chain persona) | (tracker rows; qw1_rbac_negatives_e2e_test 3/3 Patrol green) |
| 2026-06-28 | QW1 — Admission approval + Legal gate (QA-J-032/062) | **PASS** | 0 / 0 (approve UI + acceptance UI → QW3/QW7; write/gate logic proven) | (tracker rows; admission_approval_commit_test + legal_gate_redirect_test green) |
| 2026-06-28 | QW1 — Backend RBAC per-route matrix + anti-escalation (QA-B-038/049) | **PASS** | 0 / 0 | (tracker rows; rbac_full_matrix_test.ts 3/3; full backend tree 892/0) |
| 2026-06-28 | QW1 — ChainScope POSITIVE (QA-J-048 chain-org allowed) | **PASS** | 0 / 0 | (QaLoginPersona.chainDirector; qw1_rbac_negatives_e2e_test `qw1-rbac-pos` Patrol green + qa_login_test wiring) |
| 2026-06-28 | QW1 — CLOSEOUT: remaining locally-verifiable P0 rows (QA-J-003/009, QA-F-026/036/038, QA-B-014/066) + tracker-lag reconciliation (QA-F-001/002, QA-X-001/002/006) | **PASS** (locally-verifiable scope) · wave **CONDITIONAL** (5 infra rows tracked) | 0 / 0 locally · 5 P0 infra-blocked (QA-B-051/052/057 live-Postgres RLS · QA-X-010/012 FCM) | [QW1_COMPLETION_CERTIFICATION.md](../../QW1_COMPLETION_CERTIFICATION.md) |
| 2026-06-28 | QW2 — Module Write/Persistence E2E (all 32 rows, 6 batches: staff-functional / teacher / school-admin / principal / platform-director-entitlement / cross-cutting) | **PASS** (locally-verifiable scope) · wave **CONDITIONAL** (4 rows blocked) | 0 / 0 locally · 28 Verified · 4 Open: J-004 (FCM push), J-063 (server-scope/RLS lane), J-005 (PTM feature not shipped), J-010 (student-comms feature not shipped) | [QW2_COMPLETION_CERTIFICATION.md](../../QW2_COMPLETION_CERTIFICATION.md) |
| 2026-06-30 | QW5 — Secondary/Advanced/Verticals Journeys (all 14 P2 rows, 5 batches; backlog cross-check first → 3 owner decisions) | **PASS** (locally-verifiable scope) · wave **CONDITIONAL** (1 Partial + 1 Blocked) | 0 / 0 locally · 12 Verified · 1 Partial (J-055 platform-ops live round-trip — `managePlatformOperations` unseeded server-side, 404 live) · 1 Blocked (J-046 backup→restore — owner-deferred to QW8 QA-R-009) | [QW5_COMPLETION_CERTIFICATION.md](../../QW5_COMPLETION_CERTIFICATION.md) |
| 2026-06-30 | QW8 — Production Readiness & Market Certification (all 12 `QA-R` rows; 6-agent discovery-first → 4 owner decisions; Track A local + Track B live staged) | **PASS** (locally-verifiable scope) · GA gate `QA-R-012` **BLOCKED** (live Track B + Face ID + 7-day cron) | 0 / 0 locally · flutter 3164/0 · deno 47/0 · bash 21/0 · analyze 0 · 126 new tests. 6 Verified-local · 4 Verified-local+staged-live (R-001/002/003/004/006/008/009) · R-011 GA-slice · **R-012 BLOCKED** (Track B live run needs SSH socket + tenant Postgres; staff Face ID O5 parallel blocker; live cron 7-day green) | [QW8_COMPLETION_CERTIFICATION.md](../../QW8_COMPLETION_CERTIFICATION.md) |
| 2026-06-30 | Staff Face ID attendance (O5, parallel GA-blocker; 1-agent discovery → 2 owner decisions: hard-block biometric-only, both in/out) | **PASS** (locally-verifiable scope) · live deploy/on-device = Track B · GA `QA-R-012` still BLOCKED | 0 / 0 locally · flutter 3176/0 (+12) · deno 15/0 (staff 8 + audit-completeness 5 + QA-B-066 2) · analyze 0. Built end-to-end (migration+RBAC seed+ledger+handler+client biometric/controller/UI+native); hard-block enforced in 3 layers. Residual: live migration apply + edge deploy + on-device biometric run | [STAFF_FACE_ID_ATTENDANCE_CERTIFICATION.md](../../STAFF_FACE_ID_ATTENDANCE_CERTIFICATION.md) |
| 2026-07-01 | Phase B (Track B) — B1 Live VPS deployment (first eligible wave; safest-order = B1 first, sole gate = SSH socket [open]) | **PASS** (B1 deploy verified live at HEAD) · GA `QA-R-012` still BLOCKED | 0 P0 / 1 P1 (backup stale → B8/B9). Live evidence: `/health` 200 `version==681d269==HEAD==remote`, `/health/ready` 200 `database:true`, `/health/storage` 200, `/health/providers` 200 (role erp_tenant); **`/health/backup` 503 `backup_stale`** (last dump 108.3h vs 26h). STOP at owner decisions: tenant Postgres (B5), biometric device (B4), backup remediation | [EOS_PHASE_B_B1_LIVE_DEPLOY_REPORT.md](EOS_PHASE_B_B1_LIVE_DEPLOY_REPORT.md) |
| 2026-07-01 | Phase B (Track B) — B9 partial: nightly-backup P1 diagnosed + fixed (owner-approved remediation) | **PASS** (P1 RESOLVED) · GA `QA-R-012` still BLOCKED | Root cause: `akshara-backup.sh:35` bare `"$1"` under `set -u` → no-arg cron aborted pre-`pg_dump` (cron fired nightly Jun27→Jul01, died each time; only `manual` runs succeeded). Fix `6393e81` (`"$1"`→`"${1:-}"`) deployed to VPS + verified: no-arg run SUCCESS, **`/health/backup` 200** ageHours 0. Residual (B8): off-site `rclone` unconfigured (LOCAL-ONLY, breaks 3-2-1); full B8/B9 live cert still gated on tenant Postgres | [EOS_PHASE_B_B1_LIVE_DEPLOY_REPORT.md](EOS_PHASE_B_B1_LIVE_DEPLOY_REPORT.md) |
| 2026-07-01 | Phase B (Track B) — B2 edge live-verify + live `/health/tenant-access` 503 investigation (2 agents: route inventory + 23-probe classification) | **CONDITIONAL** — edge live at HEAD; isolation 503 root-caused to P2 test debt (no breach) · GA `QA-R-012` still BLOCKED | Edge deployed at HEAD (version==681d269; representative routes 401=mounted). `/health/tenant-access` **503**: 23/233 isolation probes fail deterministically → **0 REAL over/under-exposure, all cross-tenant probes PASS**; 19 = stale probe expectations vs certified director-org + parent/student own-child RLS (`20260707000000`/`20260704000000`/`20260703100000`), 4 = unseeded branding/config/subscription fixtures. Highest sev **P2**. Blocks clean B7/B12 (false-red) but adds no P0/P1. **STOP: owner decision** on ratifying/rewriting isolation cert probes + verifying live −2500 payment on invoice `b9000000…01` | [EOS_PHASE_B_TENANT_ACCESS_HEALTH_FINDING.md](EOS_PHASE_B_TENANT_ACCESS_HEALTH_FINDING.md) |
| 2026-07-01 | Phase B (Track B) — B2 CLOSE: 23 stale isolation probes repaired (owner-approved), deployed + live-verified (impl agent + review) | **PASS** — B2 edge live at HEAD + `/health/tenant-access` 200 (233/233) · GA `QA-R-012` still BLOCKED | Commit `20ae776`: 18 over-read probes → "denied OTHERS'" (stronger, `<> own scope`, `===0`); probe-12 → balance-coupling invariant; 4 own-data probes → natural key + 1 branding seed (`20260819000000`, no PK re-key). Deployed to VPS (`docker restart akshara-edge`), `build_info.json`→HEAD; **/health 200 version==20ae776, /health/tenant-access 200 isolation.pass=true 233/233, /health/backup 200**. No RLS/product change. Next executable: B9 (monitoring, needs B1+B2 only); B3/B5-B8/B10/B11 need tenant DB, B4 needs device | [EOS_PHASE_B_TENANT_ACCESS_HEALTH_FINDING.md](EOS_PHASE_B_TENANT_ACCESS_HEALTH_FINDING.md) |
| 2026-07-01 | Phase B (Track B) — B9 Monitoring validation (`QA-R-010`), live (read-only) | **CONDITIONAL PASS** — health/jobs/watchdog verified live; alert-delivery owner-config-blocked · GA `QA-R-012` still BLOCKED | Watchdog cron `/etc/cron.d/akshara-watchdog` active (every 5min); live log green: `api_ready/api_backup/api_storage=OK, disk=29%, cert=82d`, all 5 containers up (backup scheduled-job now OK post-fix). All `/health/*` 200. **Alert DELIVERY not verifiable**: `ALERT_WEBHOOK_URL` + `ALERT_SMS_PHONES` + `ALERT_SMS_API_KEY` all empty on VPS → **owner must configure a webhook/SMS channel** (+ decide on a live test-alert / RECOVERED-path drill). No code change (monitoring already deployed). | (ledger) |
| 2026-07-01 | Phase B (Track B) — B5 Tenant Postgres config + B7 Tenant isolation validation (`QA-R-003/004/008` live-RLS), live | **PASS** — dedicated tenant DB provisioned; enforced 233-probe isolation green via direct `erp_tenant` · GA `QA-R-012` still BLOCKED | Owner-named `akshara_tenant_test` was **not present** on akshara-postgres → **created + cloned** from akshara_db (184 public tables, RLS on, `app_current_user_id`, probe fixtures; 421 benign auth/storage `SET ROLE` errors ignored). NB: owner's cloud `akshara_tenant_test` is a **separate Supabase Cloud project — unused/ignored**; our DB is in the **self-hosted `akshara-postgres` container**. `ERP_TENANT_DATABASE_URL` → `akshara_tenant_test` **for harnesses only** — edge runtime keeps `akshara_db` (`withTenantContext` powers live attendance/exam/promotion RLS). `tenant_isolation_enforced_test.ts` **1 passed / 0 failed** (233 probes, `role=erp_tenant`, `enforced`) via new secret-free `scripts/qa/run_tenant_isolation_enforced.sh` (runs in akshara-edge). | (ledger) |
| 2026-07-01 | Phase B (Track B) — B3 DB migrations verified live at HEAD | **PASS** — schema at HEAD · GA `QA-R-012` still BLOCKED | Ledger at `20260818` (141) + branding seed `20260819` applied directly (idempotent; ledger insert blocked as non-`supabase_admin`, harmless). All effects live: `staff_check_ins`+RLS (20260818), `row_version` on finance_collections (20260817), branding seed present. 184 public tables; schema diff = 0. | (ledger) |
| 2026-07-01 | Phase B (Track B) — B8 Backup & Restore DR drill (`QA-R-009`, closes `QA-J-046`), live | **PASS** — restore→integrity→drop green on self-hosted DB · GA `QA-R-012` still BLOCKED | Ran `akshara-restore-drill.sh`: decrypted+restored the fix-produced backup `akshara_db_20260701T070029Z_monthly.dump.enc` → throwaway `akshara_db_drill` → integrity **184 tables, orgs=4, users=13** → dropped. `ops_restore_drills` = success. Proves the (earlier-fixed) nightly backup is genuinely restorable + runbook reproducible. All self-hosted (akshara-postgres, `supabase_admin`). | (ledger) |
| 2026-07-01 | Phase B (Track B) — isolated 2nd-edge stack (owner-chosen) + B11 pilot full-year FIRST live run | **CONDITIONAL** — isolation proven; B11 harness-seeding gap surfaced (no prod defect) · GA `QA-R-012` still BLOCKED | Owner chose "isolate via 2nd edge": built postgrest-test + gateway-test + edge-test → `akshara_tenant_test` (`docker-compose.tenant-test.yml`, all repointed, roles granted). Test edge healthy (`/health` 200 @HEAD, `ready.database=true`, auth issues OTP) — **prod `akshara_db` never touched** (prod /health 200 throughout). Ran `live_cert_pilot_full_year.py` (now env-driven `API_BASE_URL`+`AKSHARA_DB_NAME`) against it → **11 PASS / 4 FAIL / 8 SKIP** (first-ever live exec). FAILs (05 academic-year=None → 11–13 timetable cascade) + SKIPs (fee/personas) = **harness-completeness gaps**: the sim creates a fresh throwaway school but never seeds academic-year / teacher-student-parent personas / invoice fixture (resolves, doesn't create). All endpoints 200 → **no prod defect**. Test stack torn down (compose+configs+clone DB kept for rebuild). **Decision needed:** how to make B11/B6 fully exercise (self-seed vs pre-seed). | (ledger) |
| 2026-07-01 | Phase B (Track B) — B11 pilot full-year sim FULLY SELF-SEEDED (`QA-R-001`), live-isolated | **PASS** — **24/24 GATE PASS**, stable across 3 fresh runs · GA `QA-R-012` still BLOCKED | Commit `91c86c6`: `seed_tenant()` now creates all realistic data (year + class/section under year + teacher/parent/enrolled-student-with-roll-number via import + teacher→section assignment + 5 `student_entities` snapshot rows + fee-structure→assignment). UNIQUE per-run school id + persona phones (no cross-run accretion); exam created for student's grade; homework delivered by exact name; test edge `AUTH_OTP_DEV_MODE=true` (isolated only). No faked passes / weakened assertions. **Genuine product defect FOUND (P1):** exam mark-provision id = `exam_id\|\|'_'\|\|roll_number` → NULL id → 500 for any student with NULL roll_number (import allows it), breaking open-marks for the whole class — logged [EOS_EXAM_MARKS_NULL_ROLLNUMBER_DEFECT.md](EOS_EXAM_MARKS_NULL_ROLLNUMBER_DEFECT.md), needs a QA-tracker row + fix. Next: B6 (multi-school) + B10 (k6) on the isolated stack. | [EOS_EXAM_MARKS_NULL_ROLLNUMBER_DEFECT.md](EOS_EXAM_MARKS_NULL_ROLLNUMBER_DEFECT.md) |
| 2026-07-01 | Phase B (Track B) — exam-marks P1 defect FIX (owner-approved) deployed to prod | **PASS** — P1 RESOLVED, prod healthy · GA `QA-R-012` still BLOCKED | Commit `bcebbf1`: mark-entry id `exam_id\|\|'_'\|\|student_id::text` (was roll_number). deno check clean (caught+fixed a backtick-in-template slip before it reached exam routes); deployed to both edges + prod `build_info.json`→HEAD. Verified live: open-marks/publish succeed for a **roll-number-less** student; prod `/health` version==bcebbf1, `/health/tenant-access` 200 (233/233, no regression). | [EOS_EXAM_MARKS_NULL_ROLLNUMBER_DEFECT.md](EOS_EXAM_MARKS_NULL_ROLLNUMBER_DEFECT.md) |
| 2026-07-01 | Phase B (Track B) — B6 multi-school concurrent isolation (`QA-R-002/003`), live-isolated | **PASS** — **22/22 GATE PASS**, stable · GA `QA-R-012` still BLOCKED | Commit `b552416`: env-driven harness on the isolated stack; fixed authoring bugs (minted token needs a real active `sessions` row → mint inserts one w/ valid-UUID session_id; `fullName`→`displayName`, `full_name`→`display_name` column, id nested at `data.student.id`). 3 schools concurrently write a marked student; every isolation check passes (row only under own school, scoped-read no bleed, cross-header denied). Minor product note: session_validation 500s on a malformed non-UUID session_id (low-risk, signed-token only). Next: B10 (k6). | (ledger) |
| 2026-07-01 | Completeness Wave 4 — Remove QA test-data pollution from workflow dialogs (Flutter, validated) | **PASS** | 0 P0/P1. Hostel admit/room + transport route + inventory asset-lifecycle dialogs no longer pre-fill fake QA values (`Karthik Sharma`, `SIS-STU-10425`, `Route QA — East Loop`, `asset_1`, …) that a prod user could submit — now empty + hint text. Updated the transport dialog widget test + patrol helper to enter a real value. **flutter analyze clean; flutter test 106/106** (hostel+inventory+transport). First wave validated with the now-available flutter runner (also re-ran B4 cert 15/15). | (ledger) |
| 2026-07-01 | Completeness Wave 3 — Fee-concession maker-checker (FIN-D4, owner-resolved) | **PASS** (locally-verifiable) · migration apply staged · apply-to-fee = flagged follow-up | 0 P0. feeConcession was audit-only (no per-student concession ever persisted; `finance_scholarships`=catalog only). Owner FIN-D4 = two-person maker-checker (SoD already enforced by `decideApproval`). Fix: `finance_fee_concessions` ledger (migration `20260822000000`, maker_id/checker_id, RLS FORCE) + repo + handler persists on decision. **deno 145/145**. Closes the approval domain-effects gap class for ALL reachable types. Residual: reducing live payable needs a resolved student_id + netting rules (client sends a name) — not guessed (financial risk). | [EOS_WAVE3_FEE_CONCESSION_MAKER_CHECKER_REPORT.md](EOS_WAVE3_FEE_CONCESSION_MAKER_CHECKER_REPORT.md) |
| 2026-07-01 | Completeness Wave 2 — Fee-structure approval activation (end-to-end) | **PASS** (locally-verifiable) · migration apply staged | 0 P0. `feeStructure` was a client approval type the server didn't recognise → 422 on submit + never activated. Fix: migration `20260821000000` seeds `approveFeeStructure` (6 roles); added to `F2_APPROVAL_TYPES` + permission map; `setFeeStructureStatus` + handler case (approve→active/reject→inactive). **deno 141/141** (approval+finance, 12 domain tests). Residual: feeConcession owner-gated (FIN-D4); other unrecognised client types to verify. | [EOS_WAVE2_FEE_STRUCTURE_APPROVAL_REPORT.md](EOS_WAVE2_FEE_STRUCTURE_APPROVAL_REPORT.md) |
| 2026-07-01 | Completeness Wave 1 — Approval-decision domain persistence (staffLeave, studentLeave, inventoryPo) | **PASS** (locally-verifiable) · live row-flip staged to Track B | 0 P0 / 0 open P1. Gap: in API mode client adapter effects are skipped (`skipDomainEffects`) but the backend `applyApprovalTypeHandler` only audit-recorded leave/PO decisions → approved leaves stayed `pending`, POs stayed `draft`. Fix: new `flipLeaveDecision` (mobile_leave_requests + snapshot_leave), `rejectPurchaseOrder` + reuse `approvePurchaseOrder`; wired into the handler. **deno 9/9 new + 39/39 suite green**, deno check clean. Residual: feeStructure→W2, feeConcession→owner FIN-D4. | [EOS_WAVE1_APPROVAL_DOMAIN_EFFECTS_REPORT.md](EOS_WAVE1_APPROVAL_DOMAIN_EFFECTS_REPORT.md) |
| 2026-07-01 | Phase B (Track B) — B4 Staff Attendance RE-IMPLEMENTATION (server-authoritative core; owner resolved §7 → CV face match) | **PASS** (locally-verifiable scope) · device/tooling/live-deploy staged · GA `QA-R-012` still BLOCKED | Replaces the superseded O5 device-biometric model per [ATTENDANCE_AUTH_DESIGN_DECISION.md](../../ATTENDANCE_AUTH_DESIGN_DECISION.md) with **GPS geofence → anti-mock → live camera CV face match**. Migration `20260820000000`: drops the biometric row hard-block; adds geofence-config (default 100m) + per-staff face enrollment (embedding, self-RLS) + GA-2 manual-request fallback + `approveStaffAttendance`/`manageSchoolGeofence` RBAC. New `staff_attendance_validation.ts` (Haversine + cosine match 0.82 + anti-mock/accuracy/staleness). 6 routes; JWT-subject actor; 5 new audit specs. **deno 20/20 green** (10 route/RBAC/shape + 10 geo/face unit); NO device-biometric field accepted as proof. Flutter re-implemented to the new chain (capture seams + controller + card), **`dart analyze` clean**; device biometric removed from attendance (login-only). Residual (external-dep): `flutter test` runner absent, concrete geolocator/camera+ML adapters, live apply+deploy+on-device run. | [EOS_PHASE_B_B4_STAFF_ATTENDANCE_REIMPL_REPORT.md](EOS_PHASE_B_B4_STAFF_ATTENDANCE_REIMPL_REPORT.md) |
| 2026-07-01 | Phase B (Track B) — B10 k6 p95 latency (`QA-R-005/006`, `QA-X-025`), live-isolated | **PASS** — all T1a/T1b thresholds green (k6 exit 0) · GA `QA-R-012` still BLOCKED | Commit `084487a` + k6 v0.54 on VPS. Fixed authoring bugs (stale routes `/dashboard`→`/finance/dashboard`, `/exams/`→`/academics/exams/`; `setResponseCallback` 200–499 so http_req_failed counts 5xx only per the probe's documented intent; `setup()` warmup tagged out of thresholds for steady-state). Full 1-min run from a COLD `akshara-edge-test`→`akshara_tenant_test` w/ real fixtures (invoice `b9…01`, exam `exam_math_8a`): **read p95 auth 219ms / dashboard 183ms (<224 T1a), write p95 collect 137ms / publish 148ms (<400 T1b), http_req_failed 0.00% (0/1572)**. Prod untouched. **All runnable Track-B certs now green** — remaining: B4 (attendance re-impl), B12 (7-day cron), B13 (GA gate). | (ledger) |
| 2026-07-01 | Completeness Wave 5 — XCT-1 export foundation: multi-column grid CSV/PDF primitive + wire the last 2 export stubs (admissions AD-09 reports, inventory INV-01 low-stock) | **PASS** | 0 P0 / 0 P1 | `AksharaReportExportService` only had key/value (`Field,Value`) export; added `buildGridReportCsv/Bytes` + `buildGridReportPdf` + `shareGridCsv/shareGridPdf` — the shared multi-column grid (students×subjects / employee×day / transaction-level) shape every report P1 rides on. Wired the two remaining `showAksharaExportQueuedSnackBar` **primary-report** stubs: admissions reports exports the selected tab (funnel/sources/counselors/applications, mirrors on-screen columns), inventory dashboard exports the low-stock/reorder list (RBAC `manageInventory`). Client-only (no migration/backend/external dep). **flutter analyze clean; export-service tests 6/6 (4 new), inventory screens 12/12.** | (ledger) |
| 2026-07-01 | Completeness Wave 6 — XCT-2 scheduled-broadcast foundation (schedule-send write path + scheduled-job runner) | **PASS** (locally-verifiable) · cron trigger + composer schedule-UI deferred | 0 P0 / 0 P1 | `comm_broadcasts.scheduled_at` existed but was never written/read (dead column). Added: `createScheduledBroadcast` (status='scheduled') + `claimDueScheduledBroadcasts` (atomic `FOR UPDATE SKIP LOCKED` claim → no double-send); service `parseScheduledAt` (422 on bad timestamp), `scheduleBroadcastMessage`, `runDueScheduledBroadcasts` (resolves audience at DISPATCH time, fans out via the existing recipient/enqueue/finalize path, audits each). `POST /communications/broadcasts` now schedules when `scheduledAt` present (else unchanged immediate send); new `POST /communications/broadcasts/run-scheduled` (gated `manageCommunications`) is the cron/ops entry point + drains the queue. **No migration** (status CHECK already allows 'scheduled'). **deno check clean; 15/15 comm tests (6 new + 9 route-parity), route-gate contract QA-B-017 green, no regression.** Residual: periodic cron trigger = deployment (like B12); Flutter composer schedule-UI = COM-4 (P2). | (ledger) |
| 2026-07-01 | MODULE Finance — slice 1/3: receipting & counter output (FIN-1, FIN-3, FIN-4, FIN-5, FIN-7) | **PASS** (locally-verifiable) · client-only | 0 P0 / 0 P1 | Owner directive: complete each module end-to-end ([[akshara-execution-model-decision]]). Slice 1 (zero backend/migration): **FIN-3** receipt polish — new `rupeesInWords` Indian amount-in-words helper, ORIGINAL/DUPLICATE stamp, authorised-signatory line, logo placeholder, and **real school name** via `schoolDisplayNameProvider` (also fixed the admin `exportReceiptPdf` hardcoded "Akshara Public School"); **FIN-4** admin duplicate reprint on collection detail (correct student/class/mode from loaded data, DUPLICATE stamp, audited `receiptPdfExported`, RBAC manageFinance); **FIN-5** batch "print today's receipts" (one page/receipt, audited); **FIN-1** daily mode-wise summary CSV; **FIN-7** day-collection transaction grid CSV. **flutter analyze clean; amount-in-words 8/8 new, receipt-PDF service + finance screens (26) green.** Known enhancements (not feature gaps): actual branding logo IMAGE fetch (placeholder box today); full-day server export across pages (FIN-7 exports current view) → folded into the Finance backend slice. Next: slice 2 recovery CRM, slice 3 policy/config. | (ledger) |
| 2026-07-01 | MODULE Finance — slice 2a/3: Fee-Recovery CRM backend (FIN-R1..R6) + defaulters contact-history enrichment | **PASS** (locally-verifiable) · client wiring = slice 2b | 0 P0 / 0 P1 | The defaulters screen had `lastContact:""`/`contactHistory:[]` hardcoded — no recovery follow-up could be recorded. New migration `20260823000000` adds 3 additive tables (RLS FORCE, `erp_tenant` grants): `finance_recovery_contacts` (R4 immutable call/WA/SMS/visit log), `finance_promises_to_pay` (R3 PTP lifecycle), `finance_recovery_targets` (R6). New repository + 8 handlers wired into `finance_router`: log/list contacts, create/list/resolve PTP, recovery dashboard (R1 aggregates + **R5 collector performance** derived from real contacts/PTP/collections), upsert/list targets. Reads gate `viewFinance`, writes `manageFinance` (no new permission). Defaulters handler now populates `lastContact`+`contactHistory` from the log (the client already maps `ContactHistoryEntry`). **deno check clean; finance route-contract 13/13 (8 new recovery routes gate-enforced).** No money-path change. Next: slice 2b client wiring (log-contact + PTP dialogs, recovery dashboard screen, FIN-8 export). | (ledger) |
| 2026-07-01 | MODULE Finance — slice 2b/3: Fee-Recovery CRM Flutter client (FIN-R1..R6 + FIN-8) | **PASS** (locally-verifiable) | 0 P0 / 0 P1 (1 minor design note) | Full client stack to the slice-2a backend: models (`PromiseToPay`, `RecoveryDashboardData`, `CollectorPerformance`, `RecoveryContact` + enums), DTO/codec/mapper, 6 paths + remote datasource, interface/api/hybrid/**mock** parity, providers + 3 mutation notifiers (invalidate defaulters/promises/dashboard on success, RBAC `manageFinance`). Defaulters screen now the recovery hub: Recovery KPI section (PTP pending/overdue/due-today, contacts-this-month, recovered-this-month) + **R5 collector-performance table**, PTP worklist (status chips + kept/broken/cancel), per-row log-contact + promise-to-pay dialogs + contact-history sheet (real `contactHistory`/`lastContact`), **FIN-8** class-wise dues + full-list CSV via `shareGridCsv` (compact menu on mobile). **flutter analyze clean (independently re-run); finance dirs 147/147 (contract+screens+providers), incl. a new defaulters recovery widget test + extended client-alignment/repository-contract.** Fixed a mobile filter-bar overflow. Minor: contact-history sheet reads pre-loaded `record.contactHistory` (refreshes via defaulters invalidation) not the live per-student provider — acceptable. **Finance R1–R6 complete.** Remaining Finance: slice 3 (FIN-6 installments, FIN-D1 day-close, FIN-D2 head allocation, FIN-D3 cancellation register, FIN-D5 late-fee, FIN-9 analytics, FIN-2 ledger); FIN-R7 PDC = P2 backlog. | (ledger) |
| 2026-07-01 | MODULE Finance — slice 3a/3: fee-office policy backend + client data layer (FIN-D1 day-close, FIN-D3 cancellation reason, FIN-D5 late-fee, FIN-2 ledger) | **PASS** (locally-verifiable) · client UI = slice 3b | 0 P0 / 0 P1 | Built by an impl agent (which stalled at the UI step — recovered: fixed the incomplete cancel caller + test). Migrations `20260824/25/26`: cancellation_reason/cancelled_by/cancelled_at on collections; late_fee_amount/accrued_at on invoices; `finance_day_close` table. New repos/handlers: **late-fee** accrue (additive, idempotent, row-locked, min(cap,total×%+flat)) + waive; **day-close** lock (`isDateLocked` guards createCollection AND cancelCollection → `DayLockedError`); **ledger** (student invoices+collections+running balance). Cancellation now REQUIRES a reason (422) + audited + `GET /finance/collections/cancelled` register. Full client data layer (interface/api/hybrid/mock/mapper/dto/paths/datasource/models) + FIN-D3 mandatory-reason cancel dialog wired. Money-path math unchanged (reviewed both accrual + guard). **deno check clean; finance backend 119/0; flutter analyze clean; finance dirs 147/0.** Client UI for accrue/waive, day-close control, ledger export, cancelled register = slice 3b (next). | (ledger) |
| 2026-07-01 | MODULE Finance — slice 3b/3: fee-office policy UI (FIN-D1/D3/D5 + FIN-2 surfacing) | **PASS** | 0 P0 / 0 P1 | New `finance/policy/` provider + actions: read providers (cancelled register, day-close entries, student ledger family) + write notifiers (accrue late fees, waive, close/reopen day) — all `assertManageFinance` + read-invalidation. Collections screen: compact policy overflow menu (Accrue late fees, Cancelled register export) + `_DayCloseControl` card (latest closed date, Close-day/Reopen). Student accounts: "Export statement" → ledger PDF (Date/Ref/Description/Debit/Credit/Balance + totals). Invoices: late-fee indicator + manageFinance "Waive late fee" (mandatory reason). **flutter analyze clean; finance dirs 154/154 (+7 new widget tests).** **Finance FIN-D1/D3/D5 + FIN-2 now complete end-to-end.** Remaining: slice 3c (FIN-6 installments, FIN-D2 head allocation, FIN-9 head-wise analytics — money path); FIN-R7 PDC = P2. | (ledger) |
| 2026-07-01 | MODULE Finance — slice 3c/3: fee-structure depth (FIN-6 installments, FIN-D2 head allocation, FIN-9 head-wise dues) — **owner-approved additive** | **PASS** (money-diff reviewed) | 0 P0 / 0 P1 | **Money-safety verified by diff:** `createCollection`/`cancelCollection` outstanding math is byte-for-byte unchanged — per-head allocation is a DERIVED ledger layered strictly after (two additive calls only). Migration `20260827000000`: `finance_invoice_installments` + `finance_invoice_head_allocations` (RLS, `erp_tenant`). FIN-6: `due_days` setting replaces hardcoded +30d; installment schedule generated at invoice creation (`GET /finance/invoices/{id}/installments`). FIN-D2: head ledger seeded from structure items (tuition-first priority; General fallback = total → always reconciles), allocate-on-collect / reverse-on-cancel, NUMERIC(12,2) same scale (exact). FIN-9: `GET /finance/analytics/head-wise-dues`. Client: due-schedule section (collection detail) + head-wise dues (defaulters). **Reconciliation invariant `SUM(head_paid)==amount_paid` asserted by 3 tests driving the real collect/cancel path.** deno 133/0 (+14); flutter analyze clean; finance dirs 158/0. **🏁 MODULE FINANCE COMPLETE** (FIN-1..9, FIN-R1..R6, FIN-D1..D6; FIN-R7 PDC=P2 backlog). Next module: Attendance (student). | (ledger) |
| 2026-07-01 | MODULE Attendance — integrity hardening (owner completion gates #1-#8) | **PASS** (money/correctness-critical, reviewed) | 0 P0 / 0 P1 | Verified all 8 owner gates against the marking path; #4 (audit) already passed, the rest were gaps — now fixed. Migration `20260829000000`: `period_label` + `submitted_at` on `attendance_sessions`, dedupe, **UNIQUE(org,school,class_label,session_date,period_label)** (#2 duplicate-safe, #3 one session/class/date/period). `upsertAttendanceSession` rewritten: **#1** a `submitted` session is immutable (rejects re-write via SELECT-guard AND `ON CONFLICT … WHERE status<>'submitted'`) → 409; session keyed on class/date/period **not teacher**; **#5** holiday block (`school_calendar_events`) + **#8** archived-year block (`academic_years.status`) via `assertAttendanceDayOpen` → 422; **#6/#7** on submit the marked set must exactly equal the active roster (`sis_student_enrollments.is_current` current-year — withdrawn/transferred excluded, admissions included, promotion moves the row) via `diffRoster` → 422 with missing/extra counts. deno check clean; **6 new roster/error unit tests + pilot 19/19** (probe-count test failure is PRE-EXISTING isolation-probe debt, not attendance). Office reads (ATT-1/2/4/D-alerts) + marking UX (ATT-3 fast-mark, ATT-D3 half-day/auto-excuse) = remaining Attendance slices. | (ledger) |
| 2026-07-01 | MODULE Attendance — office reads slice (ATT-1/2/4 + ATT-D1/D2 alerts) | **PASS** (locally-verifiable) | 0 P0 / 0 P1 | 5 new GET routes (viewSis + school scope): `/attendance/register` (ATT-1 per-student register by class+date), `/attendance/register/monthly` (ATT-2 students×days matrix + %), `/attendance/pending` (ATT-4 classes with no submitted session + teacher/last-saved), `/attendance/alerts/consecutive-absence` (ATT-D1, days=3), `/attendance/alerts/short-attendance` (ATT-D2, <75% over windowDays). New `attendance_office_repository` (tenant-scoped, submitted-only, joins student/roll/teacher names) + handlers + router + 11 route-contract tests. Full Flutter client (models/interface/api/mock/mapper/paths/datasource/providers) + one tabbed **Office Attendance** screen (Register · Monthly · Pending · Alerts) with CSV+PDF export via XCT-1 grid, routed `/management/office-attendance` + reachable (management dashboard "Attendance pending" widget + corrections-screen link). No migration (reuses existing tables). **deno check clean; attendance deno 13/0; flutter analyze clean; office contract+widget 13/0; agent's broad run flutter contracts+management 588/0.** Remaining Attendance: marking UX (ATT-3 fast-mark, ATT-D3 half-day + auto-excuse approved leave). | (ledger) |
| 2026-07-01 | MODULE Attendance — ATT-3 absentees-first fast-mark | **PASS** | 0 P0 / 0 P1 | `fillRemainingAsPresent(ref)` marks only still-UNMARKED students present (preserves explicit absent/late) + a full-width "Fill remaining present" button on the teacher marking screen, so a teacher taps the few absentees then fills the rest in one action (submit gate then opens). flutter analyze clean; teacher attendance 15/0 + 1 new fast-mark widget test. Remaining Attendance: ATT-D3 (half-day mark + auto-excuse approved leave). | (ledger) |
| 2026-07-01 | MODULE Attendance — ATT-D3 half-day mark + auto-excuse approved leave · **🏁 MODULE COMPLETE** | **PASS** (submit-path diff reviewed — guards intact) | 0 P0 / 0 P1 | Migration `20260830000000`: `attendance_records.mark` CHECK += `half_day` (excused already allowed); `mobile_leave_requests` += nullable `from_date`/`to_date` + partial index. Enum `StudentAttendanceMark` += `halfDay`/`excused` (ripple fixed across teacher marking chips, KPI, draft, parent/student/office calendar colors+labels, wire codec `halfDay⇄half_day`). Auto-excuse: `approvedLeaveStudentIdsForToday` + pure `applyApprovedLeaveExcuse` override marks to `excused` for students with an approved leave covering today — applied AFTER `assertAttendanceDayOpen`, BEFORE `diffRoster`, rewriting only marks (studentId set preserved) → **all 8 integrity guards verified intact** (reviewed the diff). Legacy label-only leaves gracefully skip. deno check clean; attendance_integrity 13/0 (7 new); flutter analyze clean; attendance flutter 33/0. **🏁 MODULE ATTENDANCE COMPLETE**: ATT-1..4, ATT-D1/D2/D3 + owner integrity gates #1-#8. Next module: Exams. | (ledger) |
| 2026-07-01 | MODULE Exams — slice A: EXM-D6 absent/leave/debarred status + per-row mark audit + pilot published-immutability fix | **PASS** (correctness-critical, diff reviewed) | 0 P0 / 0 P1 | Owner design (frozen [[exam-result-status-design]]): non-present = **`marks_obtained` NULL** (not 0) with display **AB/ML/DB**. Migration `20260831000000`: `status` col (present/absent/medical_leave/debarred) + drop NOT NULL on marks_obtained (bounds CHECK still holds for NULL). Non-present rows **excluded from totals/averages/percent/rank/pass-fail/grade-distribution** (report-card builder: absent classmate does NOT shift others' ranks, not ranked; proven by tests). Published-immutability preserved. **Per-row mark-update audit** added (`examMarkUpdated`, full before→after of marks+status → supplementary/re-exam-safe). **Integrity hole FOUND+FIXED (red-team depth):** the mobile/pilot `updateExamMark` lacked the `published=false` guard → could overwrite published marks; now guarded (409 EXAM_MARK_PUBLISHED) matching the academics path. deno exam 43/0 (11 new) + audit 15/0 + pilot 67/1 (1 = pre-existing probe-count debt); flutter analyze clean; exam flutter 78/0. Remaining Exams: EXM-1 bulk entry, EXM-2 progress board, EXM-3..7 reports/exports, EXM-D1/D2/D4/D5. | (ledger) |
| 2026-07-01 | MODULE Exams — slice B: EXM-1 bulk marks entry + EXM-2 progress board | **PASS** | 0 P0 / 0 P1 | **EXM-1**: `POST /academics/exams/{examId}/marks/batch` (manageExamMarks) — single + bulk refactored to shared `parseMarkPayload`+`applyMarkUpdate` so guards CAN'T diverge (published fence, RT-08 bounds, AB/ML/DB NULL, row_version, subject-teacher scope, per-row audit); partial success `{updated,failed}` (a bad/published row is reported, never overwrites, never aborts the batch). Grid UI: per-row controllers, **Enter/Tab → next row**, **Save-all** (dirty rows → one batch → "N saved, M failed"). **EXM-2**: `GET /academics/exams/progress` (viewExams OR verifyExamResults) → per marks_entry-phase exam entered/total/pending; new RBAC-gated `ExamMarksProgressScreen`. deno exam 59/0 (route-contract extended +2 routes); flutter analyze clean; exam flutter 77/0 + integration 4/0. Remaining Exams: EXM-3..7 reports/exports, EXM-D1/D2/D4/D5, EXM-6 deadline(rides XCT-2). | (ledger) |
| 2026-07-02 | MODULE Exams — slice C: reports/exports (EXM-3/4/5/7) + EXM-6 deadline field | **PASS** | 0 P0 / 0 P1 | 5 read routes (viewExams): tabulation register (students×subjects term grid), subject toppers + class merit, pass/fail + grade distribution, datesheet. All **exclude non-present (AB/ML/DB, NULL marks)** from totals/avg/percent/rank/pass-fail/distribution (SQL `status='present' AND marks_obtained IS NOT NULL`; Flutter reuses `countsTowardStats`) — proven by exclusion tests on tabulation/merit/toppers/distribution. New `ExamReportsScreen` (4 tabs, CSV+PDF via XCT-1 shareGrid). EXM-6: migration `20260832000000` adds nullable `marks_entry_deadline` + accepted on create/schedule + banner on marks-entry + in progress payload (automated reminder = future XCT-2 reminder-rule engine). pass-mark default 40% (no config setting exists — documented). deno exam 83/0; flutter analyze clean; exam flutter 92/0. Remaining Exams: EXM-D1 batch report-cards, EXM-D2 grace marks, EXM-D4 hall-ticket, EXM-D5 seating. | (ledger) |
| 2026-07-02 | MODULE Exams — slice D: documents + moderation (EXM-D1/D2/D4/D5) · **🏁 MODULE COMPLETE** | **PASS** (grace/marks-adjacent diff reviewed) · agent stalled mid-test, recovered | 0 P0 / 0 P1 | Migration `20260833000000` (exam_mark_adjustments + exam_seating_assignments, RLS+grants). **EXM-D1** batch report-card PDF (one page/card/class-term, published-only; shared `_reportCardPage`). **EXM-D2** grace/moderation — **separate audited `exam_mark_adjustments` record; original mark NEVER overwritten**; effective = clamp(original+Σdelta, 0, max); allowed only phase=processed (`ExamGracePhaseError` → rejected after publish); NOT shown to parents/students (breakdown coordinator-only). **EXM-D4** hall-ticket PDF (standard template). **EXM-D5** seating generator (mixed-class default) + printable chart. Recovery: fixed the one broken test the stalled agent left (`exm_d2_d5_test.dart` — dropped a dead `ExamReportCardStub` bridge, used the real `_cardFromData`). deno exam 118/0; flutter analyze clean; exam flutter 107/0. **🏁 MODULE EXAMS COMPLETE** (EXM-1..7 + EXM-D1/D2/D4/D5/D6; EXM-8 & EXM-D3 = Ph2; EXM-6 reminder rides future XCT-2 rule-engine). Next module: HR. | (ledger) |
| 2026-07-02 | MODULE HR — slice A: payroll/leave integrity guards + HR-8 auto-provisioning | **PASS** (money-critical, payroll guard reviewed) | 0 P0 / 0 P1 | Red-team-depth integrity fixes on the JSONB HR snapshots: **payroll** `applyPayrollRun` rejects re-processing a `processed` run (409 PAYROLL_RUN_ALREADY_PROCESSED, snapshot untouched → no double-pay) + `validatePayrollEntries` enforces net = basic+allowances−deductions (±1 rounding), net≥0, and per-employee uniqueness (422 PAYROLL_ENTRY_INVALID) before disbursing; **leave** `applyLeaveDecision` rejects deciding a non-pending request (409 LEAVE_ALREADY_DECIDED — no re-approve / approved↔rejected flip); audit preserved. **HR-8**: migration `20260834000000` dedupes then adds partial UNIQUE(org,school,user_id); onboarding `commitImportJob` (teacher+staff) now auto-provisions the `employees` projection via `provisionEmployee` (ON CONFLICT DO NOTHING, EMP-<8hex>, never touches employee_code/user_id — canonical users+school_memberships identity preserved; idempotent re-import). Client `api_failure_mapper` now surfaces server 409/422 code+message. **HR integrity 19/0; HR+onboarding 100/0; flutter mapper 7/0; deno check clean.** Remaining HR: HR-1/2/4/5/7 exports + HR-6 muster + attendance-status inference, HR-3 batch leave, HR-D1/D2/D3. | (ledger) |
| 2026-07-02 | MODULE HR — slice B: reporting/exports + muster (HR-1/2/4/5/6/7) | **PASS** | 0 P0 / 0 P1 | 6 read routes (viewHr): HR-1 salary register (+totals), HR-2 payslip data (client per-employee + bundle PDF), HR-6 monthly muster with **attendance-status inference from `staff_check_ins`** (earliest check_in → Present, after cutoff default 09:15 → Late, none on a working day → Absent; holidays via school_calendar_events, % present), HR-4 leave-balance (avail/used/remaining), HR-5 headcount by department (real employees), HR-7 employee directory. All ride XCT-1 (CSV+PDF); reports derive from REAL rows (fresh school = honest zeros, no fabricated numbers). Payroll export stub snackbar replaced. WRITE guards + HR-8 untouched. deno HR 58/0 (route-contract +6, muster/headcount/register unit tests); flutter analyze clean; HR+contracts 560/0. Remaining HR: HR-3 batch leave, HR-D1 doc-expiry, HR-D2 probation, HR-D3 leave-on-behalf. | (ledger) |
| 2026-07-02 | MODULE HR — slice C: batch leave + HR-D defaults · **🏁 MODULE COMPLETE** | **PASS** | 0 P0 / 0 P1 | **HR-3** `POST /hr/leave/batch-decide` (manageHr) loops `applyLeaveDecision` per id → partial `{decided,skipped}` (non-pending skipped, never flipped; per-row audit); multi-select + batch bar UI. **HR-D1** doc-expiry: `expiry_date` on the employee JSONB documents + `GET /hr/documents/expiring?withinDays=30` + days-to-expiry badge (reminder rides future XCT-2). **HR-D2** probation: migration `20260835000000` adds `probation_end_date` to employees; `GET /hr/probation/ending?withinDays=15` + `POST /hr/employees/{id}/probation` (confirm/extend, audited) + countdown UI. **HR-D3** leave-on-behalf: manager creates leave FOR an employee (onBehalf + createdBy), half-day (0.5 days), over-balance → 409 LEAVE_BALANCE_EXCEEDED unless `override` (allowed + separately audited). Payroll guards / HR-8 / slice-B reports untouched. deno HR 73/0; flutter analyze clean; HR+contracts 71/0. **🏁 MODULE HR COMPLETE**: HR-1..8 + HR-D1/D2/D3 + payroll/leave integrity guards. Next module: Homework. | (ledger) |
| 2026-07-02 | MODULE Homework — slice A: security hardening (RLS + cross-class scope) + HWK-1 real due-date | **PASS** (security-critical, RLS reviewed) | 0 P0 / 0 P1 | **Security FOUND+FIXED (red-team depth):** `homework_submissions` had a single FOR-ALL policy with **NO WITH CHECK** → a student could INSERT a row for ANOTHER student. Migration `20260836000000` replaces it: `_school` (FOR ALL, tenant/school USING+WITH CHECK), `_student_own` (own rows only, USING+**WITH CHECK** on student_id=app_current_student_id()), `_parent_read` (SELECT via student_guardians). **Cross-class scope:** `submitHomework` rejects submitting to an assignment not delivered to that student (`student_entities` homework_item probe → 404 HOMEWORK_NOT_DELIVERED); dup submission → 409 (was silent overwrite). **HWK-1**: real `dueDate` (ISO) on the pilot homework payload + Material date picker (replaces free-text dueLabel), `validateDueDate` (422 on bad, past allowed+warn), shared `deriveHomeworkDisplayStatus` overdue logic mirrored server+client (student/parent). deno homework+entity_read 40/0 (pilot 78/1 = pre-existing probe); flutter analyze clean; homework flutter 547/0. Remaining Homework: HWK-2 not-submitted, HWK-3 multi-section, HWK-4 teacher attach, HWK-5 history/export, HWK-6 bulk review, HWK-7 student note/photo, HWK-D1 parent nudge (HWK-8 auto-reminder rides XCT-2). | (ledger) |
| 2026-07-02 | MODULE Homework — slice B: not-submitted/history/bulk-review/multi-section/attachments/nudge (HWK-2/3/4/5/6/7/D1) · **🏁 MODULE COMPLETE** | **PASS** (integrity/RBAC diff reviewed; agent stalled mid-build, recovered) | 0 P0 / 0 P1 | **HWK-2** `GET /teacher/homework/{id}/non-submitters` (delivered roster LEFT JOIN submissions). **HWK-3** multi-section assign — `parseHomeworkClassLabels` de-dupes, ONE assignment delivery + audit **per class** (per-class submission tracking stays clean; single `class_label` back-compat kept). **HWK-4** optional teacher attachment (name/ref on the assignment payload; no bucket yet). **HWK-5** `GET /teacher/homework/history` (own assignments, real submitted/total, due-date range, paginated) + client CSV. **HWK-6** `POST /teacher/homework/bulk-review` partial `{reviewed,skipped}`. **HWK-7** student submit sheet (optional note + attachment) + **late submission on overdue**; parent/student read overlay enriched with the child's real submission + teacher attachment, overdue derived from real dueDate, RLS-scoped to linked child. **HWK-D1** manual teacher no-submit nudge → guardian push via the existing enqueue path, audited. **Slice-A guards preserved** (404 not-delivered, 409 dup, RLS student-own WITH CHECK/parent-read); all new routes gate `manageHomework`, static paths matched before `{homeworkId}`. **No schema change** (notes/attachment_label + unique index pre-existed). Recovered the stalled build by fixing the two widget tests broken by the new UX (2 Submit actions for overdue late-submit; HWK-3 class chip model) — updated to real behavior, not weakened. deno pilot 99/1 (1 = pre-existing probe debt), deno homework/entity_read 52/0, deno check clean; flutter analyze clean; homework+contracts 547/0. **🏁 MODULE HOMEWORK COMPLETE** (HWK-1..7 + HWK-D1; HWK-8 auto-reminder rides XCT-2; HWK-9/10 = Ph2). Next module: Communication. | (ledger) |
| 2026-07-02 | MODULE Communication — BACKEND wave (COM-1/2/3/D1 + P1 security fix); 2 backend agents batched into one commit | **PASS** (security/integrity/money diff reviewed personally) | 0 P0 / 0 P1 | **SECURITY (P1) FOUND+FIXED:** `comm_threads_participant` had a blanket `OR app_current_scope()='school'` and `comm_messages_thread` had NO participation check → any teacher/admin could read+write EVERY parent↔teacher private thread. Migration `20260837000000` rewrites both to participant-only (USING+WITH CHECK); service `sendDirectMessage` now rejects a new thread with no counterparty (422) so participant-only RLS can't orphan a thread. **COM-1** per-broadcast delivery/read report off the authoritative `notification_deliveries` ledger (new nullable `broadcast_id` FK; counts sent/delivered/read/unread + unread names + ack). **COM-3** resend-to-unread (repeatable, audited `broadcast.resent`). **COM-2** class/section audience (`class_parents`/`class_students` resolved from current-year `sis_student_enrollments`→`student_guardians`, parameterized — no injection; `audience_class/section` persisted, resolved at dispatch) + saved segments (`comm_audience_segments`, FORCE RLS, immutable). **COM-D1** acknowledge-required notices (`requires_ack`+`acknowledged_at` on deliveries; `POST /communications/notifications/{id}/acknowledge` recipient-scoped + defense-in-depth; audit `notification.acknowledged` = signed-receipt log; ack counts on report; **fixed a projection gap — `deliveryToNotificationApi` now surfaces requiresAck/acknowledgedAt**). Migrations 20260837/38000000 (additive). All new routes RBAC/scope-gated + inventoried. **deno comm 103/1** — the 1 = PRE-EXISTING probe debt (`communication_probe_validation_test.ts` 233≠stale-220, PROVEN identical on clean HEAD b8c5f8a); deno check clean; validation 14/0; audit-completeness 5/0. COM-5 (template↔composer) + COM-4 client schedule-UI = client wave (in progress). COM-6 = Ph2. | (ledger) |
| 2026-07-02 | MODULE Communication — CLIENT wave (COM-1/2/4/5 + COM-D1 recipient UI) · **🏁 MODULE COMPLETE** | **PASS** (contract-parity + RBAC reviewed) | 0 P0 / 0 P1 (1 P3 note) | Client to the committed backend (c5e00b2): **COM-2** audience picker (6 audiences; class/section revealed for class_*, required-class inline error) + saved-segment apply/save/delete; **COM-4** schedule-send toggle → date/time → `scheduled_at`; **COM-5** use-template prefill + save-as-template + template edit (was create-only); **COM-D1** authoring require-ack switch + recipient Acknowledge affordance (shows only when required-and-unacknowledged; `AppNotification` now carries requiresAck/acknowledgedAt); **COM-1** `BroadcastReportScreen` (counts total/sent/read/unread/acknowledged + unread names) from history; **COM-3** one-tap resend-to-unread; CSV via XCT-1 `shareGridCsv`. Data layer: `BroadcastRequest` + 6 new repo methods with full interface↔api↔hybrid↔mock parity (10→16); DTO sends snake_case (nulls omitted). New mutation notifiers gated `Permission.manageCommunication`. flutter analyze clean; comm-scoped **67/0**. **P3 note:** recipient acknowledge swallows API errors keeping an optimistic "Acknowledged" (server audit is authoritative; low real-world failure since it's the caller's own inbox delivery) — surface-on-failure = future refinement. **🏁 MODULE COMMUNICATION COMPLETE** (COM-1..5 + COM-D1; COM-6 thread-export = Ph2). Next module: Library. | (ledger) |
| 2026-07-02 | MODULE Library — BACKEND wave: P0 integrity guards + LIB-1/2/4/5/D1 | **PASS** (money/integrity diff reviewed personally) | 0 P0 / 0 P1 | **P0 money/integrity FOUND+FIXED (owner-prioritized):** (1) **double-return** raised a duplicate fine + over-counted copies → `assertReturnAllowed` blocks a returned loan before any write; (2) **over-issue at zero stock** + **availableCopies race** (read-modify-write w/o lock) → new `entity_write_store.mutateEntity` (`SELECT…FOR UPDATE` in the tenant txn) makes the decrement/increment atomic with an in-lock last-copy re-check; (3) **ISBN uniqueness per school** on add/update(self-exempt)/import; (4) **duplicate active loan** (owner-required) — a member can't re-issue the same book while an active loan exists (renew/return first). Fine math unchanged; `outstandingFineForMember` excludes waived. **Features:** LIB-2 edit/delete (delete blocked while on loan) + CSV bulk import (per-row SAVEPOINT + `failed[]` + ISBN dedupe); LIB-4 capped renewal; LIB-5 in-app overdue reminder via XCT-2 `sendBroadcastMessage` (external gated); LIB-1 `GET /library/overdue` (accrued fine/row); LIB-D1 configurable guardrails (`snapshot_settings`: maxBooks=2/maxRenewals=2/blockOnFine=₹100) enforced in issue+renew. 9 new routes gated viewLibrary/manageLibrary + inventoried; **no migration** (JSONB store); RLS/audit preserved. deno library **53/0** (27 new), check+lint clean; RBAC/entitlement regression **61/0**. LIB-3 (barcode manual-entry + scanner seam) + client UI = client wave (in progress). LIB-6=Ph2, LIB-7=Future. | (ledger) |
| 2026-07-02 | MODULE Library — CLIENT wave (data-layer checkpoint `613d829` + UI `94c5d97`) · **🏁 MODULE COMPLETE** | **PASS** (contract-parity + guardrail-surfacing reviewed) | 0 P0 / 0 P1 | Client-UI build was interrupted by an agent session-limit mid-wave; recovered by committing the verified data-layer checkpoint (`613d829`: full interface↔api↔hybrid↔mock parity for the 8 new methods + models/requests/providers/mutations/qa-keys; analyze clean, 51/0) then finishing the UI with a fresh agent (`94c5d97`). **LIB-1** `/library/overdue` screen (list + CSV/PDF via XCT-1) + real export replacing the reports stub. **LIB-2** catalog edit/delete (delete surfaces on-active-loan rejection) + paste-CSV import showing `{imported, failed[{row,reason}]}`. **LIB-3** manual barcode/ISBN entry in issue+return (resolve-by-ISBN; camera seam device-gated, no package) + fixed the mislabeled dashboard scan button. **LIB-4** per-row renew (active only). **LIB-5** in-app "Send overdue reminders". **LIB-D1** settings dialog (maxBooks/maxRenewals/blockOnFine) + issue-guardrail rejections surfaced as the real backend `{error.message}` (duplicate-active-loan/cap/threshold/zero-stock). All writes manageLibrary-gated; new route guarded. flutter analyze clean; library+contract+integration+router-smoke **81/0** (12 new widget tests). **🏁 MODULE LIBRARY COMPLETE** (LIB-1..5 + LIB-D1 + 5 P0 integrity fixes; LIB-6=Ph2, LIB-7=Future). Next module: Transport. | (ledger) |
| 2026-07-02 | MODULE Transport — BACKEND wave TRN-1..9 (incl. money-critical TRN-9 + a money bug found in review) | **PASS** (money/integrity diff reviewed personally) | 0 P0 / 0 P1 | **TRN-9 (money-critical, owner-decided):** Transport RAISES a Finance demand ONLY, never collects — proven by a payment-free code-scan test (no `createCollection`/`finance_collections` in transport). Owner decision [[trn9-transport-fee-account-decision]] = reuse the per-year student account: Finance `createAssignmentAndAccount` now GET-OR-CREATEs the account (tuition+transport = many invoices/one account; also fixes the latent "can't assign a 2nd fee structure" limit). Demand idempotent on (sisStudentId,routeId,year,term); student UUID via `resolveStudentId`; audited. **MONEY BUG FOUND+FIXED IN REVIEW:** the reuse path created the transport invoice but did NOT fold its amount into the account's authoritative `outstanding_amount` (which collections decrement + defaulters/dashboard read) → transport dues would be invisible + a later transport payment would corrupt the tuition balance; now the reused account aggregates all structures (50000+12000=62000), test strengthened; invoice/installment/head math byte-for-byte unchanged. **TRN-1** vehicle/driver CRUD (registration-unique, delete-blocked-if-referenced); **TRN-2** ISO doc-expiry dates (insurance/fitness/PUC/permit/road-tax/license, validated, legacy read-through); **TRN-3** route roster by stop; **TRN-4** stop editor (row-locked, contiguous sequence); **TRN-5** bulk allocate (partial {assigned,skipped}); **TRN-7** race-safe capacity guard (row lock → 409 unless allowOverCapacity, override audited); **TRN-8** in-app doc-expiry reminder via XCT-2 rail. All routes gated + inventoried; no migration (JSONB). transport 36/0, finance 133/0, api type-check clean. Client wave in progress. | (ledger) |
| 2026-07-02 | MODULE Transport — CLIENT wave (TRN-1..9 UI) · **🏁 MODULE COMPLETE** | **PASS** (contract-parity + RBAC + payment-free-UI reviewed) | 0 P0 / 0 P1 | Client to the committed backend (fb4a4c5): TRN-1/2 vehicle+driver CRUD dialogs (ISO expiry date pickers; delete surfaces referenced-in-use) + doc-expiry tracker; TRN-4 stop editor; TRN-3/6 real exporters (roster/student/vehicle via XCT-1); TRN-5 bulk allocate by class/section ({assigned,skipped}); TRN-7 capacity-exceeded confirm → allowOverCapacity; TRN-9 raise-demand dialog (FeeStructureCategory.transport, idempotent, **provably NO payment UI** — only labels say "Finance collects"). Data layer: 14 new repo methods, full interface↔api↔hybrid↔mock parity (17→31); 409 CAPACITY_EXCEEDED surfaced as typed ApiFailureException; dates kept ISO-string (no DateTime migration). All new mutations manageTransport-gated. flutter analyze clean; transport features+contracts+integration **69/0** (10 new widget tests); cross-module regressions green (finance 94/0, core/repos 96/0). **🏁 MODULE TRANSPORT COMPLETE** (TRN-1..9; live GPS/parent-map/driver-app = Ph2). Next module: Inventory. | (ledger) |
| 2026-07-02 | MODULE Inventory — BACKEND wave INV-1..7 (stock movements + maker-checker write-offs) | **PASS** (stock/money integrity reviewed personally) | 0 P0 / 0 P1 | Typed-stock now moves both ways. INV-1 issue (posted-immutable, idempotent, FOR-UPDATE lock, reject-below-onhand 422, weighted-avg); INV-2 consumable+reorder CRUD; INV-3 adjust; INV-6 count→variance; INV-4 low-stock (sku+recommendedQty+vendorId → client reuses createPurchaseOrder, no dup PO logic); INV-5 register; INV-7 in-app low-stock alert via XCT-2 rail. **Owner decision = MAKER-CHECKER** for value-reducing movements (damage/wastage write-off + negative variance): recorded pending, no stock move until a DIFFERENT checker approves — SoD in code (StockSelfApproveDenied 409) + DB `CHECK(checker_id<>maker_id)`; low-risk (in/opening/positive) apply immediately. Immutable `stock_movements` ledger (SELECT/INSERT-only), GRN retrofitted to log. **CONCURRENCY FIX (review):** `getPendingAdjustment` now `SELECT…FOR UPDATE` so two checkers can't double-decrement the same write-off. Migration 20260839000000: item_type/reorder_level/CHECK(qty>=0) + **fixes the inventory_stock_valuations RLS hole (adds WITH CHECK)** + all new tables FORCE RLS USING+WITH CHECK. 11 routes gated + inventoried; audit on every write. inventory 39/0, deno check clean, RBAC 14/0. Client wave in progress (pipelined ‖ SIS backend). INV-8=Ph2. | (ledger) |
| 2026-07-02 | MODULE SIS — BACKEND partial: unblocked items SIS-2/3/5 (pipelined ‖ Inventory client) | **PASS** (low-risk; no money path / no new tables) | 0 P0 / 0 P1 | Per the owner "never pause the pipeline for non-blocking decisions" rule: built the 3 SIS items that need NO owner decision — **SIS-3** doc verify (`PATCH …/documents/{docId}/verify` writes the dead verified_by/verified_at/status, manageSis, tenant-scoped 404, + `sisAudit.documentVerified`); **SIS-5** transfer/exit log (`GET /sis/transfers?from&to&status`, transferred/alumni + last enrollment, paginated, viewSis); **SIS-2** primary-guardian name+phone on the directory export (correlated subqueries, guardian-less still list). No migration (columns existed). Routes gated + inventoried. sis 104/1 (1 = shared pre-existing probe-count debt), 48 touched-file tests 0-fail, deno check clean. **DEFERRED to the decision queue ([[akshara-decision-queue]]):** SIS-1 certificates, SIS-D1 TC no-dues, SIS-D2 ID-card, admission-# immutability (C5) — the identity cluster. SIS client (SIS-2/3/5 UI) is the next pipeline stage after Inventory client. | (ledger) |
| 2026-07-02 | MODULE Inventory — CLIENT wave (INV-1..7 UI) · **🏁 MODULE COMPLETE** · recovered from agent session-limit | **PASS** (error-path + RBAC-gating bugs found+fixed in review) | 0 P0 / 0 P1 | Client to backend a90d0f8: issue dialog (422 inline), adjust (out→pending approval), stock-take, **maker-checker "Pending write-offs" approvals screen** (self-approve 409 friendly), consumables+reorder CRUD, raise-PO from copilot low-stock (reuses createPurchaseOrder), real register CSV/PDF export. Full repo parity (10 new methods). **Recovery from stalled agent + 3 review fixes:** (1) REAL BUG — all 6 stock notifiers `return state.value!` after AsyncValue.guard → every failure became a null-check TypeError and friendly 422/409 messages never reached the UI; now rethrow the real error; (2) register export wrapped in AksharaManageAction with a VIEW permission (ManagePermissionGuard only recognizes manage-class → hidden for everyone) → manageInventory per house pattern; (3) maker-checker test now overrides the adjustment MAKER via a mock seam (decide notifier correctly rebinds acting user from auth). analyze clean; inventory 65/0; router smoke+protection 129/0. **🏁 MODULE INVENTORY COMPLETE** (INV-1..7; INV-8=Ph2). | (ledger) |
| 2026-07-02 | MODULE Admissions — BACKEND wave (idempotency money-fix + ADM-3/4/D1/D2/D4 + settings + funnel-vocab) · recovered from agent session-limit | **PASS** (money-fix reviewed personally: 3-layer idempotency) | 0 P0 / 0 P1 | **MONEY FIX (P0-class):** `submitEnrollment` was NOT idempotent — double-click/retry on an approved application created TWO students + TWO admission numbers + TWO fee handoffs (duplicate payables). Now: (1) `SELECT…FOR UPDATE` lock anchor on the application serializes concurrent submits; (2) post-lock re-check returns the existing enrollment; (3) partial unique index `admissions_enrollments_application_uniq` → racing INSERT 23505 mapped to the same idempotent return (never 500); walk-ins (null application_id) stay distinct by design. ADM-3 bulk assign/stage; ADM-4 follow-up complete/reschedule (first writes to the dead status columns); ADM-D1 mark-lost picklist + reports rollup; ADM-D2 dup-phone warn-only check; settings save wired; ADM-D4 offer-letter data endpoint; funnel-vocab fix (conversion advances lead→'joined' once; conversionRate no longer reads 0). Migration 20260840000000 additive; routes gated+inventoried; audit on writes. Recovery: fixed 3 trivial casts in the stalled agent's last test file. admissions 31/0; sis 104/1 (known probe debt); deno check clean. **DEFERRED (queue):** ADM-D3 numbering (PSID cluster), approval maker-checker SoD. Admissions client = next client-chain stage. | (ledger) |
| 2026-07-02 | MODULE Teacher — BACKEND: TCH-9 My Attendance (read-only self-service) + staff_check_ins self-read RLS PRIVACY FIX | **CONDITIONAL PASS** (privacy self-scoping reviewed; live-DB RLS → Track B) | 0 P0 / 1 P1 (live-DB verify of self-read policy, Track B) | `GET /staff-attendance/my-history` returns a staff member's OWN attendance (per-day in/out, working minutes, present/late/absent/holiday, manual-override) + month summary + today/yesterday; reuses HR muster logic (DEFAULT_LATE_AFTER 09:15 / holidays now shared, no re-derivation). **PRIVACY FIX:** staff_check_ins had only a school-scoped read → a naive history route would leak other staff's check-ins. Migration 20260841000000 adds `staff_check_ins_self_read` (FOR SELECT USING user_id=app_current_user_id()); school-read kept for HR/principal; NO UPDATE/DELETE grant (append-only). **Double-enforced self-scope:** handler binds user_id from the verified JWT (subjectOf(claims)) — never a request param — AND the RLS policy pins visibility. Read-only (no mutation/audit). Route inventoried; staff_attendance+hr **111/0**, deno check clean, validation 14/0, audit 5/0. TCH-1..7 = client wave (mostly pure-client); TCH-8=Ph2. Zero owner decisions. | (ledger) |
| 2026-07-02 | MODULE Principal — BACKEND: PRI-1 batch approve/reject | **PASS** (SoD/per-item-authority preserved) | 0 P0 / 0 P1 | `POST /approvals/batch-decide` {ids,decision,comment?}. Extracted a shared `decideOne()` core so single + batch decisions can't diverge (single /approve|/reject now route through it). Per-item authority preserved (each id re-checks its type-specific approve permission → lacking = skipped, NOT blanket 403); SoD self-approve (inventoryPo/examResults verifier) + studentLeave class-teacher scope enforced per id; not-pending → skipped (idempotent, no double-decide); reject requires comment (whole-batch 422). Partial {decided,skipped}; never partial-aborts; exactly one audit row per decided id. Route gated viewManagement (reachability) + inventoried; no migration. approval 54/0 (+14), combined 68/0, deno check+lint clean. PRI-2 (unsubmitted marks, reuse EXM-2)/PRI-3 (daily report PDF)/PRI-5 (stale>48h — already computed, surface it) = client wave; PRI-4 rides XCT-2. | (ledger) |
| 2026-07-02 | MODULE Director — BACKEND: DIR-2 collection report + DIR-D1 audited drill-down (additive to certified B8) + QA-R-008 approvals-audit fix | **PASS** (foreign-school guard + no-cross-org-leak reviewed) | 0 P0 / 0 P1 | Additive to the production-certified B8 portal (no field/route removed). **DIR-2:** DirectorSchoolRow += billedInr/collectedInr/outstandingInr (outstanding=max(0,billed−collected), from amounts getSchoolRows already queried — no re-derivation) + `GET /director/collections` (per-school + org totals). **DIR-D1** (owner default = scoped read-through WITH AUDIT, read-only, NOT impersonation): `GET /director/schools/:id/snapshot` — foreign/unknown school hits the belongs-to-org guard → null → 404 BEFORE any aggregate (no cross-org leak); real hit emits `director.school.drilldown` READ audit. Both viewDirectorPortal+org-scope, inventoried. **Also fixed a cross-cutting regression from the Principal PRI-1 commit** (caught by this wave's full-suite run): `qa_r_008_audit_completeness` failed because the new /approvals/batch-decide made module "approvals" appear unclassified → documented it (approvals audits via the dedicated approval_audit_entries ledger, like communication/attendance). director 32/0, qa_r_008 5/0. **FULL deno suite 1884/5** — all 5 = shared probe-count debt (213/220), zero new regressions across all 5 backend waves. DIR-1/3 = client. Next+last backend: Parent. | (ledger) |
| 2026-07-02 | MODULE SIS — CLIENT wave (SIS-2/3/5 UI) · **🏁 MODULE COMPLETE (buildable scope)** | **PASS** (contract-parity + RBAC; 14 golden fails PROVEN pre-existing) | 0 P0 / 0 P1 | Client to backend 0d2c9a1: SIS-3 doc verify/reject (Pending/Verified/Rejected chip + "by {verifier}", manageSis-gated, optional note); SIS-5 `/sis/transfers` screen (date-range + status filters, paginated, CSV+PDF); SIS-2 registry export → true 7-col grid (+ guardian name/phone). Full repo parity for verifyStudentDocument + listStudentTransfers. flutter analyze clean; SIS features+contracts+integration 97/0; full contracts 569/0; router/protection 129/0. **14 full-suite golden failures (approval/management/finance) PROVEN pre-existing** — artifacts committed a9d7e05 (2026-06-22, before session start a7095bf); those screens untouched this session; transient re-run PNGs never staged. **🏁 SIS COMPLETE (buildable)**: SIS-2/3/5; SIS-1 cert / SIS-D1 TC / SIS-D2 ID-card / admission-# immutability = identity cluster in [[akshara-decision-queue]]. | (ledger) |
| 2026-07-02 | MODULE Parent — BACKEND (the LAST backend): PAR-D1 leave cancel + PAR-3 medical-cert attachment | **PASS** (own-child privacy + immutability reviewed) | 0 P0 / 0 P1 | `POST /parent/leave/:id/cancel` (withdraw a PENDING leave) + `POST /parent/leave/:id/attachment` (medical-cert ref). **Privacy:** shared loadOwnChildLeave resolves the child from the LEAVE ROW (requester_user_id=caller.sub AND student_id=ANY(child_ids)) — never a client id; foreign family/non-own → 404 (id never confirmed, no cross-family leak); double-enforced with mobile_leave_scope RLS. **Immutability:** non-pending → 409, UPDATE WHERE status='pending' closes the school-approval race. Audited. No migration (cancelled + has_attachment cols exist); PAR-3 uses the HWK-7 attachment-ref pattern (real parent-scope storage bucket = residual; admissions bucket is school-scoped). parent-leave 16/0, parent 8/0, pilot 115/1 (probe debt), rbac 7/0, deno check clean. **⇒ ALL 6 MODULE BACKENDS COMPLETE** (SIS/Admissions/Teacher/Principal/Director/Parent). Remaining = the serial CLIENT chain (Admissions→Teacher→Principal→Director→Parent). PAR-D3 certificate = queued. | (ledger) |
| 2026-07-02 | MODULE Admissions — CLIENT wave (ADM-2/3/4/5/D1/D2/D4) · **🏁 MODULE COMPLETE** | **PASS** (RBAC + contract parity) | 0 P0 / 0 P1 | Client to backend bb32ae2: ADM-3 bulk lead actions (wired the inert checkbox column + bulk bar → /leads/bulk, updated/skipped); ADM-4 follow-up sheet (complete/reschedule/call via tel:); ADM-5 real lead-picker (was placeholder); ADM-2 WhatsApp auto-logs a timeline note on send; ADM-D1 mark-lost picklist + lost-reasons card; ADM-D2 dup-phone warn (never blocks); ADM-D4 offer-letter PDF; settings Save wired. Full repo parity for 7 methods; mutation notifiers assertManageAdmissions-gated. flutter analyze clean; admissions features+contracts+integration **113/0** (+30); router green. **🏁 MODULE ADMISSIONS COMPLETE** (idempotency money-fix backend + this client; ADM-D3 numbering + approval-SoD = queue). **7 modules fully complete** (Homework/Communication/Library/Transport/Inventory/SIS/Admissions). | (ledger) |
| 2026-07-02 | MODULE Teacher — CLIENT wave (TCH-1..7 + TCH-9) · **🏁 MODULE COMPLETE** | **PASS** (contract parity; read-only self-attendance) | 0 P0 / 0 P1 | Client to backend ea4dc67: **TCH-9** read-only My Attendance screen (Today/Yesterday, present/late/absent/avg-hours summary, day list, month switcher) → self-scoped /staff-attendance/my-history + repo parity + route + dashboard entry. **TCH-1** today-row deep-link to attendance (class pre-selected); **TCH-2** marks-pending task; **TCH-3/7** XCT-1 export on attendance+timetable; **TCH-4** cover/substitute fields + home CoverAlertCard; **TCH-5** create-homework quick action; **TCH-6** pending-task→filtered homework. Cleanup: replaced all 12 hardcoded unreadNotifications:1 with a real teacherUnreadNotificationsProvider. flutter analyze clean; teacher **127/0**, mobile-integration 15/0, router/protection/persona-nav green. TCH-8=Ph2. **🏁 TEACHER COMPLETE. 8 modules done.** | (ledger) |
| 2026-07-02 | MODULE Principal — CLIENT wave (PRI-1..5) · **🏁 MODULE COMPLETE** · recovered from agent session-limit | **PASS** (batch RBAC + reject-comment reviewed) | 0 P0 / 0 P1 | Client to backend d5f1492: **PRI-1** approval queue → MULTI-select + batch-action bar (approve/reject → /approvals/batch-decide; reject = mandatory-comment; result "N decided, M skipped"); repo parity for batchDecideApprovals. **PRI-5** the already-computed stale>48h count now surfaced on the Approval Center (was inert) + tap-to-filter. **PRI-2** unsubmitted-marks card (marks-entry progress). **PRI-3** daily-report PDF on Operations Hub (new OperationsHubPdfService). **PRI-4** weekly digest card (in-app; cron deferred). Batch gated viewManagement (matches backend reachability; per-item authority server-side → skipped). flutter analyze clean; management+operations+approval **110/0**. Recovered from session-limit (work+tests were complete). **🏁 PRINCIPAL COMPLETE. 9 modules done.** | (ledger) |
| 2026-07-02 | MODULE Director — CLIENT wave (DIR-1/2/3/D1) · **🏁 MODULE COMPLETE** | **PASS** (additive to certified B8; read-only drill-down) | 0 P0 / 0 P1 | Client to backend 812bdf3: **DIR-1** sortable/ranked league table (Rank by fee%/students/health/outstanding/revenue/admissions + rank col; removed dead filter bar); **DIR-2** consolidated collection report (org totals + per-school billed/collected/outstanding) from /director/collections; **DIR-3** DirectorReportExporters CSV+PDF; **DIR-D1** read-only per-school drill-down (/director/schools/:id) + "Read-only · audited" banner (no writes/persona-switch). Repo parity 14→16 (certified 14 untouched); route reuses viewDirectorPortal. flutter analyze clean; director+contracts+rbac+router **178/0**. **🏁 DIRECTOR COMPLETE. 10 modules done.** Only Parent client (last) remains before the 🔒 pre-pilot Red Team sequence. | (ledger) |
| 2026-07-02 | MODULE Parent — CLIENT wave (PAR-1..6 + D1/D2/D4) · **🏁 MODULE COMPLETE — LAST MODULE** | **PASS** (own-child privacy + mock-mapping cleanup reviewed) | 0 P0 / 0 P1 | Client to backend 681f0ca: PAR-1 RSVP accept/decline (optimistic); PAR-6 PTM action-items/follow-ups + next-PTM hero; PAR-2 apply-leave quick action; PAR-3 medical-cert attachment-ref; PAR-D1 withdraw pending leave (409 surfaced); PAR-4 year-statement CSV+PDF; PAR-5 multi-type reminder banners (DETERMINISTIC catalog, no LLM); PAR-D2 family view; PAR-D4 action inbox (fees/PTM first). **INTEGRITY CLEANUP:** removed the mock child↔student mappings (kParentChildToStudentId + 'SIS-STU-RAVI-001') → active child from JWT child_ids; PTM wired to real GET /parent/meetings scoped by studentId==child.id (no cross-child leak). Audited via recordParentAudit. flutter analyze clean; parent+parent_meetings+contracts+router **281/0**. Deferred: PAR-7/8/D5/D6 (Ph2), PAR-D3 (queued). **🏁🏁 ALL 11 MODULES COMPLETE** (Finance/Attendance/Exams/HR + Homework/Communication/Library/Transport/Inventory/SIS/Admissions/Teacher/Principal/Director/Parent). NEXT: buildable roadmap gaps (#8 Smart Timetable, #9 Workload, #3 Student Risk) → then the 🔒 pre-pilot Red Team sequence. | (ledger) |
| 2026-07-03 | ROADMAP GAP #3 — Student Risk: add the fee-defaulter signal | **PASS** (scoped finance read; weights re-verified) | 0 P0 / 0 P1 | The v8.9 risk engine ignored fees despite the roadmap promising "attendance + FEES + academics". Added a fee component reading the authoritative `finance_student_accounts.outstanding_amount` (scoped org+school+current-year, status='open' AND outstanding>0 — the same column collections decrement/defaulters read; READ only, no money mutation), normalized 0-100 saturating at ₹50k, `fee_default` reason (amount+overdue), display-only intervention. Weights rebalanced to sum exactly 1.0 (attendance 0.25 largest, fee 0.12). Wired through predictions_service too. No migration (JSONB inputs). intelligence 51/0 (+5), predictions 4/0, deno check clean. Enhancements (history-trend, persisted intervention loop, cron) = future/out-of-roadmap-intent. **🏁 GAP #3 DONE.** #8 (substitution) building; #9 (workload rollup) queued (shares timetable/ with #8). | (ledger) |
| 2026-07-03 | ROADMAP GAP #8 — Smart Timetable: persisted substitutions + integrity guards (backend) | **PASS** (RLS + published-immutability + clash-recheck reviewed) · recovered from 600s agent stall | 0 P0 / 0 P1 | v7.5 base was done but substitutions were 100% client-side MOCK (never persisted). Built the pragmatic core (constraint-solver deferred). Migration 20260845000000: academic_timetable_substitutions (FORCE RLS school-scope USING+WITH CHECK, UNIQUE(period_id,sub_date), grants) + `reject_published_period_edit()` BEFORE UPDATE/DELETE trigger on academic_timetable_periods (published periods immutable; subs live separately so still allowed) + manageTimetableSubstitution perm. Routes POST/GET(?date)/DELETE /academic/timetables/substitutions (SUBSTITUTE_BUSY 409; on-leave derived SERVER-side from approved HR leave; audited). moveTimetablePeriod/replacePeriods now re-run detectAllConflicts → reject a NEW teacher/room clash (TimetableClash 409) + reject published edits (belt-and-suspenders w/ trigger). Routes gated+inventoried. timetable 36/0, deno check clean. Recovered from a 600s stall (impl complete, only final verify stalled). #8 client (substitution wiring) + #9 backend now pipelined. | (ledger) |
| 2026-07-03 | ROADMAP GAP #9 — Workload: unified per-teacher rollup (backend) | **PASS** (read-only aggregation; school-scoped) | 0 P0 / 0 P1 | Reconciled two disjoint workloads (period-threshold 24 vs subject-threshold 25) into ONE per-teacher rollup + fixed the always-empty sections[] (now from real academic_timetables.section_id). New workload_rollup.ts + `GET /academic/timetables/workload/rollup` (viewAcademicTimetable, ?academicYearId→422); per-teacher {periodCount, sections[], subjectIds[], status over/under/balanced} + school aggregate; honest zeros. Single threshold constant (24; 25 superseded). Legacy /workload untouched (back-compat). No migration. timetable 50/0 (+14), validation 14/0, deno check clean. #9 client (workload dashboard) = next client-chain stage (after #8 client). | (ledger) |
| 2026-07-03 | QUEUED WAVE — Admissions approval maker-checker SoD (owner-approved) | **PASS** (money-path SoD reviewed) · backend-only | 0 P0 / 0 P1 | Closes the self-approve→payable gap: the APPROVER must differ from the SUBMITTER (mirrors FIN-D4, no superAdmin exemption). Migration 20260846000000 adds admissions_applications.submitted_by (maker, from claims.sub) + admissions_approvals.decided_by/decided_at (checker). setApprovalDecision resolves submitted_by and throws SELF_APPROVE_DENIED (409) BEFORE any UPDATE → the application never leaves 'submitted', so no enrollment/fee-handoff fires (tests assert 0 approval-writes/0 status-flips = no payable). Legacy NULL-maker rows unaffected. Client surfaces the 409 via the standard error mapper (no client wave). admissions 36/0 (+5), approval+finance 187/0, deno check clean. **🏁 Admissions maker-checker DONE.** Remaining queued: Identity/PSID cluster (mapping now) + PAR-D3 fee cert (folds into it — cert infra). | (ledger) |
| 2026-07-03 | ROADMAP GAP #8 — Smart Timetable: substitutions client + teacher cover · **🏁 GAP #8 COMPLETE** | **PASS** (contract parity; RBAC) | 0 P0 / 0 P1 | Client to backend 17fef8a: the admin daily-substitutions UI (was 100% MockDailyTimetableStore) now persists via /academic/timetables/substitutions — repo parity (list/create/delete across interface↔api↔hybrid↔mock), screen rewritten (real create/delete, server-sourced on-leave, SUBSTITUTE_BUSY 409 inline), manageAcademicTimetable-gated; a resolved sub now shows on the teacher's day (teacherTodayCoverProvider + banner). flutter analyze clean; timetable 20/20. P2 tracked: parent/student WEEKLY views (separate getTimetable path) not yet fed daily subs. **🏁 ROADMAP GAP #8 COMPLETE** (backend + client). #9 client (workload dashboard) building; #8/#9 close the roadmap-gap phase. | (ledger) |
| 2026-07-03 | IDENTITY CLUSTER slice 1 — admission-number set-once lock (C5) | **PASS** (3-layer lock reviewed) | 0 P0 / 0 P1 (1 known probe debt) | admission_number was freely editable (only a UNIQUE constraint). Locked set-once in 3 layers: repo guard (updateStudent rejects a differing value when one exists → 409 ADMISSION_NUMBER_IMMUTABLE, before the UPDATE; identical=no-op; create untouched); rejected-attempt audit (sisAudit.admissionNumberChangeRejected, category security, in a FRESH txn since the refused update rolls back); migration 20260847000000 reject_admission_number_change() BEFORE UPDATE trigger (defense-in-depth vs direct UPDATE). sis 108/1 (probe debt), deno check clean. Client field read-only = tiny follow-up. **Identity slice 1 DONE.** Next: PSID allocation (building), then SIS-1 certs + PAR-D3 cert; SIS-D2 ID-cards DEFERRED (no photo storage). | (ledger) |
| 2026-07-03 | ROADMAP GAP #9 — Workload dashboard client · **🏁 GAP #9 COMPLETE** · **🏁🏁 ALL ROADMAP GAPS DONE** | **PASS** (contract parity) | 0 P0 / 0 P1 | Client to backend ad6afb1: real per-teacher workload dashboard (summary header + colour-coded over/under/balanced cards with period/section/subject chips + honest empty state + XCT-1 CSV/PDF); fixed subject-assignment raw-UUID→name rendering. Full repo parity for getWorkloadRollup; legacy /workload untouched. flutter analyze clean; timetable+contracts+school-completion 44/0, router 18/0. **🏁 GAP #9 COMPLETE.** **🏁🏁 ALL 3 BUILDABLE ROADMAP GAPS COMPLETE (#3 Student Risk fee, #8 Smart Timetable substitutions, #9 Workload rollup).** Endgame step-1 remaining: identity cluster (PSID allocation building → SIS-1 certs → PAR-D3 cert; SIS-D2 deferred). Then step 2 = Fable UI/UX audit. | (ledger) |
| 2026-07-03 | IDENTITY CLUSTER slice 2 — Public Student ID (PSID) allocation | **PASS** (never-reused invariant + concurrency proven) | 0 P0 / 0 P1 (1 known probe debt) | Frozen PSID track: `<SCHOOL_CODE>-<4-digit seq>`, per-school, NEVER reused; UUID stays only PK; admission_number untouched. Migration 20260848000000: student_profiles.public_student_id + global partial-unique index; school_public_id_counters (FORCE RLS); idempotent backfill (row_number() per school, offset from counter, skips code-less schools); set-once trigger (after backfill). allocatePublicStudentId: `INSERT VALUES(…,2) ON CONFLICT DO UPDATE SET next_seq=next_seq+1 RETURNING (next_seq-1)` → first=0001, subsequent=pre-update value = sequential/no-dup/no-gap; ON CONFLICT DO UPDATE row-lock serializes concurrent same-school allocations (distinct under race). Wired into all 4 profile-creation sites (create/conversion/import/placeholder), set-once. publicStudentId in directory+detail read APIs. sis 120/1 (probe debt), onboarding 60/0, deno check clean; 12 new PSID tests. **Identity slice 2 DONE.** Next: SIS-1 certs + TC engine (building). | (ledger) |
| 2026-07-03 | IDENTITY CLUSTER slice 3 — SIS-1 certificates + SIS-D1 TC engine (backend) | **PASS** (TC no-dues gate + immutable register reviewed) | 0 P0 / 0 P1 (1 known probe debt) | SIS-1: POST /sis/students/{id}/certificates (bonafide/study/conduct) records an immutable issuance + returns client-PDF data; GET register (viewSis). **SIS-D1 TC engine** (one audited txn): no-dues gate sums finance_student_accounts.outstanding_amount (status=open) → 409 DUES_PENDING BEFORE any write (no serial/register/status touched — asserted); status-codec guard rejects already-transferred/graduated (422) before burning a serial; sequential per-school TC serial TC/<CODE>/<YEAR>/<0001> via the PSID ON-CONFLICT counter (concurrency-safe, never-reused); register INSERT; auto status→transferred. Migration 20260849000000: sis_certificate_issues (FORCE RLS, SELECT/INSERT-only=immutable, partial-unique serial) + school_tc_counters. Routes gated manageSis/viewSis + inventoried. sis 138/1 (probe debt), validation 14/0, audit 16/0, deno check clean; 18 new tests. **Identity slice 3 DONE.** Now: PAR-D3 fee/80C cert backend ‖ SIS client wave (PSID display + admission-# read-only + cert UI). SIS-D2 ID-cards DEFERRED (no photo storage). | (ledger) |
| 2026-07-03 | IDENTITY CLUSTER slice 4 — PAR-D3 annual/80C fee-payment certificate (backend) | **PASS** (own-child ×2 + completed-only reviewed) | 0 P0 / 0 P1 (1 known probe debt) | GET /parent/fee-certificate?academicYear= → 80C cert DATA for a parent's OWN child (PDF client-side): schoolName/guardianName/studentName/publicStudentId/admissionNumber/academicYear/totalPaidAmount/payments[]/signatoryTitle. Own-child enforced TWICE (resolveParentStudentId → 403 before DB for a child not in child_ids; finance parent RLS defense-in-depth). Aggregates finance_receipts⋈finance_collections for the Indian FY; ONLY collection_status='completed' counts (cancelled/refunded not 80C-deductible). Signatory from schools.settings->>'feeCertificateSignatoryTitle' default "Principal" (schools.settings JSONB already exists — no migration). Read-only, no audit. Route parent-scoped + inventoried. parent 15/0, pilot fee-cert 7/0, validation 14/0, deno check clean. **Identity slice 4 DONE — ALL IDENTITY BACKENDS COMPLETE** (admission-# lock, PSID, SIS-1 certs+TC, PAR-D3). SIS client wave (PSID/admission-#/cert UI) building; PAR-D3 client (80C PDF) + then SIS-D2 DEFERRED = the tail. | (ledger) |
| 2026-07-03 | IDENTITY CLUSTER — SIS-side client (PSID display + admission-# read-only + SIS-1 cert UI) | **PASS** (contract parity) | 0 P0 / 0 P1 | Client to identity backends: publicStudentId on profile header + registry row + 8th export column; admission-# field read-only when set ("Set once"), unchanged value not resent; SIS-1 cert repo parity + formal certificate PDF service (letterhead/title-per-type/name/PSID/admission#/class/DOB/guardian/TC-serial/signatory) + issue bonafide/study/conduct + transfer-cert actions (DUES_PENDING 409 surfaced verbatim, status→transferred reflected) + register section; manageSis-gated; PDF renderer behind an injectable test seam. flutter analyze clean; SIS features+contracts 124/0 (+31). PAR-D3 80C-cert client (last identity piece) building. | (ledger) |
| 2026-07-03 | IDENTITY CLUSTER — PAR-D3 80C-cert client · **🏁 IDENTITY CLUSTER COMPLETE · 🏁🏁 ENDGAME STEP 1 COMPLETE** | **PASS** | 0 P0 / 0 P1 | PAR-D3 client: getFeeCertificate parity + 80C PDF service (letterhead/certify-clause/payments-table/total-in-words/signatory) + FY-picker action + honest-empty. flutter analyze clean; parent 151/0. **🏁 IDENTITY CLUSTER COMPLETE**: admission-# set-once lock, PSID allocation (never-reused), SIS-1 certs + SIS-D1 TC engine (no-dues gate), PAR-D3 80C cert. SIS-D2 ID-cards DEFERRED (hard blocker: no student-photo storage). **🏁🏁 ENDGAME STEP 1 COMPLETE** = all 3 buildable roadmap gaps (#3/#8/#9) + all approved queued waves (Admissions maker-checker + full identity cluster). NEXT (step 2): Fable UI/UX Audit → step 3 implement improvements → step 4 Adaptive-AI wave → step 5 ONE Global Red Team → 6 fix → 7 Pilot → 8 Prod Cert. | (ledger) |
| 2026-07-03 | Fable Final Independent Audit — verification gate over deliverables (docs/audits/00–10 + roadmap) | **PASS** — audit internally consistent, evidence re-verified verbatim, complete (accessibility depth disclosed→folded into roadmap W6); *project-level gate remains BLOCKED* on open P0s (unproven tenant isolation, hardcoded DB credential DB-1, DR untested, money-dup risk) | Audit P0s: QA-2 (RLS isolation never run), DB-1 (git DB password), OPS-1/2 (DR), REL-1/ENG-1 (idempotency/money) | docs/engineering/eos/EOS_FABLE_FINAL_AUDIT_VERIFICATION_REPORT.md |
| 2026-07-03 | Fable audit — LIVE VPS read-only verification (owner opened SSH socket) | Resolved repo-Unknowns: DB-2 satisfied (edge=erp_tenant NOBYPASSRLS), DB-1 live-rotated (P0→P2), ENTITLEMENT_ENFORCEMENT=true, AI live via OpenRouter. **Corrected a false-positive:** automated encrypted backups + monthly restore-drill DO run/pass (LV-2 refuted); 10 tenants = demo/pilot, no real data. Real residual: no off-site + no WAL/PITR (P1) + alert-delivery unwired (P2) | Confirmed: LV-1 no-WAL, LV-3 no-offsite (P1); refuted: LV-2; DR re-rated 4.5→6.0 | docs/audits/11_LIVE_VPS_VERIFICATION_ADDENDUM.md |
| 2026-07-03 | Fable audit — LIVE cross-tenant RLS isolation probe (owner-approved, rolled-back, as erp_tenant) | **PASS** — read 7/7 + write 2/2: cross-tenant, cross-school (same-org), and parent-scope isolation all enforced for the real NOBYPASSRLS app role against real data; cross-org INSERT rejected by WITH CHECK; cross-school UPDATE=0. **Closes QA-2 (audit #1 P0).** Nothing mutated (SAVEPOINT+ROLLBACK). | QA-2 CLOSED; DB score 8.0→8.5; QA score 5.0→5.5; still wire 233-probe suite into CI | docs/audits/11_LIVE_VPS_VERIFICATION_ADDENDUM.md §3b |
| 2026-07-03 | Fable audit — consolidation into Master Execution Roadmap (owner-directed) | **Consolidated** all 11 audit reports + live findings → one authoritative plan (Phases 0–7) with full finding-traceability ledger; merged duplicates; excluded audit-fixed items; FINAL_QA_ROADMAP + FABLE_FINAL_ROADMAP pointed at it | n/a (planning artifact) | docs/audits/MASTER_EXECUTION_ROADMAP.md + docs/audits/AUDIT_FINDINGS_LEDGER.md |
| 2026-07-04 | **P0 · W1 — Documentation Truth** (DOCS scope: DOC-1/2/4/5) | **PASS** (docs reconciled to HEAD; no claim exceeds evidence; working tree coherent) | 0 P0 / 0 P1 | First implementation-phase wave (planning frozen 2026-07-04). **DOC-1:** rewrote `docs/ProjectStatus.md` from stale June-2026/HEAD-`42b7018`/"modules not started" → HEAD `68f15cb` reality (47 built modules, engineering local-complete, GA gated on P0→P8) with an explicit evidence-grade framing. **DOC-4:** added a governing **evidence-grade** section (LIVE/LOCAL-LOGIC/CONTRACT/RENDER-MOCK/STAGED) + per-wave grade-of-record to the *frozen* `FINAL_QA_MASTER_TRACKER.md` (no row rewritten) and explicitly re-scoped the over-claims — "universal idempotency" (~4% real, REL-1→P1-CODE-1), "row_version universal" (1/4, inert on finance until P0-CODE-1), "237 Verified"/"PRODUCTION CERTIFIED" (local-evidence/historical, not live GA). **DOC-5:** `TD-P0-01` RLS debt marked substantially CLOSED (live-verified `erp_tenant` NOBYPASSRLS + FORCE RLS + 7/7 read + 2/2 write; residual → P0-TEST-2/P0-INFRA-6); `AuditArchitecture.md` retention/partitioning/hash-chain marked target-not-built (→P1-CODE-3); backup runbooks consolidated to one canonical (`BACKUP_RESTORE_RUNBOOK.md`), the two `Operations/*` runbooks → redirect stubs (removed the stale Supabase-PITR/RPO-15min claims). **DOC-2:** committed the ~600-file doc cleanup (moves-to-archive, content preserved) + version-controlled the governance layer (Constitution/EOS skill/CLAUDE/PROJECT_INDEX) via `.gitignore` `!.claude/skills/` `!.claude/commands/`; excluded transient junk (golden-failure diffs, `flutter_*.log`) by gitignore. **Regression:** `flutter analyze` **0**; docs-only — zero `lib/**`/`supabase/**` changes. | commit `c2b8e27` |
| 2026-07-04 | **P0-SEC-1** (SEC scope: SEC-1 release-default-dev, SEC-2 debug-signing) | **PASS** (fail-closed; no automatic-failure conditions) | 0 P0 / 0 P1 | First code leg of P0·W2. **SEC-1:** `Environment.fromDartDefine()` now routes through a pure, testable `guardForRelease(env, isRelease, rawAppEnv)` — in `kReleaseMode` the env MUST be `production` with `ENABLE_API_MODE=true` (else `StateError`, app refuses to start/auth), and demo/mock + QA-persona login are force-off regardless of defines (also closes the SEC-9 mis-built-release→MockAuthRepository fallback). **SEC-2:** `android/app/build.gradle.kts` release `signingConfig=null` when no `key.properties` (never the debug key) + `gradle.taskGraph.whenReady` hard-fails any release assemble/bundle/package without a real keystore; debug builds unaffected. **Regression:** `flutter analyze` 0; `environment_test` 11/11 (5 new fail-closed tests), `production_environment_test` green. | commit `c80f18c` |
| 2026-07-04 | **P0-SEC-2** (SEC scope: SEC-3 PII at rest) | **PASS** (PII encrypted; legacy scrubbed; 0 new test failures) | 0 P0 / 0 P1 (1 pre-existing UX-7 tracked) | Moved the persisted auth session (phone/name/child/claims) from plaintext `SharedPreferences` to the encrypted `SecureStorageBackend` (mirrors `TokenStorage`) under `auth_session_secure_v1`; non-PII UI prefs (demo/staff role, remember) stay in prefs (need sync reads, no PII). One-time migration copies a legacy plaintext session into secure storage and **scrubs** the plaintext copy; `clear()` scrubs too; `writeSelectedChild` now preserves claims. `authSessionStorageProvider` wired to `secureStorageBackendProvider`+prefs. **Regression:** `flutter analyze` 0; **full suite 3563 pass, 0 new failures** — the 2 `TeacherDashboard` 360×640 overflow tests fail on clean HEAD too (pre-existing layout defect, logged **UX-7 → P2-UX**). New SEC-3 tests: no-plaintext-PII + legacy-migrate+scrub. | commit `619338b` |
| 2026-07-04 | **P0-CODE-1** (RELIABILITY scope: ENG-1 money lost-update) | **PASS** (money lost-update prevented; prevents a duplicate/erroneous financial reversal, no automatic-failure) | 0 P0 / 0 P1 | Made the inert `finance_collections.row_version` optimistic lock EFFECTIVE. `cancelCollection` takes optional `expectedVersion`: early compare (before any balance reversal, mirrors exam-marks guard) + atomic `AND ($6::int IS NULL OR row_version=$6)` UPDATE predicate → 0 rows ⇒ `CollectionConflictError` (enclosing `withTenantContext` txn rolls back the reversal). Handler maps → **409 CONFLICT** carrying the current row incl. `rowVersion` (`collectionRowToApi`) so the reliability client resolves+retries; omitting `expectedVersion` is back-compat. No migration (column present since 20260817000000). createCollection stays INSERT+idempotency-key+FOR UPDATE (not a lost-update path). **Regression:** deno finance **136/0** (+3 ENG-1 tests: stale→conflict+no-money-reversed, matching-version-OK, back-compat); `deno check` clean; api entrypoint typechecks; backend-only (flutter analyze unaffected, 0). | commit `6408d90` |
| 2026-07-04 | **P0-SEC-3** (SEC scope: SEC-9 mock in release, SEC-10 QA/demo prod-guard) | **PASS** (mock/QA auth fail-closed + tree-shaken from release; no automatic-failure) | 0 P0 / 0 P1 | **SEC-9:** `authRepositoryProvider` throws in `kReleaseMode` instead of returning `MockAuthRepository`; the `return const MockAuthRepository()` is behind the const branch → dead-code-eliminated + tree-shaken from release (mock referenced only here). **SEC-10:** `/qa-login` GoRoute wrapped in `if (!kReleaseMode)` collection-if → `QaLoginScreen` (+ hardcoded personas/phones) dead-code-eliminated + tree-shaken from release (screen referenced only at the route). Demo-auth prod-guard via SEC-1. Behaviourally inert in debug/test. **Regression:** `flutter analyze` 0; full suite **3563 pass, 0 new failures** (2 pre-existing UX-7); router/auth/security 157/0. Note: binary-exclusion is via Dart const dead-code elimination (standard mechanism; no release-binary scan run in this env — gradle SEC-2 guard blocks keyless release builds). | commit `63358bc` |
| 2026-07-04 | **P0-INFRA-4/5/6** (OPS+SEC+MIGRATION: LV-10, DB-1/OPS-6, DB-2) | **PASS** (credential removed from git; deploy-time role gate added; no automatic-failure) | 0 P0 / 0 P1 (2 live legs ⏳ deferred) | **INFRA-4 (LV-10):** `$1` bug already `${1:-}` in-repo (line 35); all 5 backup scripts `bash -n` clean; live warning was a stale deployed script → redeploy ⏳. **INFRA-5 (DB-1/OPS-6):** removed the `akshara_erp_tenant_staging_v1` literal from `20260610100000`; erp_tenant password now read from the `erp.tenant_password` GUC (deploy secret) via dynamic SQL + dev-only non-secret fallback; live role unchanged (IF NOT EXISTS no-op). **No credential literal remains in code.** **INFRA-6 (DB-2):** `probeTenantConnection` returns role+bypassRls; pure `assertEdgeTenantRole()` rejects non-`erp_tenant`/bypass-RLS roles; `/health/tenant-access` → 503 on wrong role + `production_launch_verify.sh` asserts it ⇒ deploy fails if the role is wrong. **Regression:** `tenant_db_test` +4 assertion tests; health/tenant 14/0; api entrypoint typechecks; verify-script `bash -n` clean. Live redeploy (INFRA-4) + live assertion run (INFRA-6) deferred to the live lane. | commit `bacc56a` |
| 2026-07-04 | **P0-CODE-2** (FEATURE: ENG-3/MOD-4 backend-less surfaces) | **PASS** (no mock surface reachable in a live build; no automatic-failure) | 0 P0 / 0 P1 | Owner decision: **hide all 8**. New `isBackendLessSurfaceHidden(ref, location)` maps the 8 surfaces' route prefixes → their `*ApiEnabledProvider`; true only when `enableApiMode` is on and the flag is off. Wired into `ErpRouteGuard` (deep-links → AccessDeniedScreen; covers surfaces 1-4 that had zero gating + `/control-center/white-label`) and the management/SIS/control-center sub-navs (ConsumerWidget, drops dead tabs). No-op in local/mock builds. **Regression:** `surface_backend_gate_test` 4/4 (local visible; live+flag-off hidden ×10 routes; live+flag-on visible; backed routes never hidden); `flutter analyze` 0; full suite **3567 pass, 0 new failures** (2 pre-existing UX-7). | commit `3cbf45c` |
| 2026-07-04 | **P1-CODE-3** (SECURITY+ARCH: ENG-4/5/7/8/9/10 + DB-6 "Backend hardening") | **PASS** (info-leak reduced, DoS-guarded, auth-choke verified; no automatic-failure — no security breach / no broken auth / no data loss) | 0 P0 / 0 P1 (5 PRE-EXISTING tenant-isolation probe-count drifts, unrelated → tracked `ISO-COUNT`) | Backend security/arch hardening across 4 commits. **ENG-7 (SEC-6):** the central `handleRequest` choke returned raw `error.message` in SERVER_ERROR (every unhandled throw) + CONFIG_ERROR — leaking DB/driver text, stack detail, env-var names. Both now return a GENERIC client message; real detail logged server-side only (correlation-id traced). Handler-level `.message` sites are all typed DOMAIN errors (safe) or internal-health-gated. **ENG-8 (SEC-11):** 6 unbounded client bulk-write arrays (exam marks, HR/approval batch-decide, transport assign, admissions leads, pilot grade) now capped at `MAX_BULK_ITEMS=500`, rejected 422 BEFORE any per-row DB work (audit already capped 100). **ENG-10:** 18 `VALIDATION_ERROR`/`INVALID_BODY` outliers 400→422 (house convention). **ENG-4+ENG-5:** consolidated forced-auth lint — 12 representative routes across all module groups + catch-all, unauth ⇒ 401/404 (13/13); the single-gate invariant now continuously enforced. **ENG-9:** taxonomy already clean (canonical generics + UPPER_SNAKE domain codes; dynamic forwarders use controlled codes); new lint walks the tree asserting every literal code is stable UPPER_SNAKE_CASE + generics in use — no risky mass-rename. **DB-6:** non-destructive audit-retention seam (`auditRetentionDays` cfg default 730, pure `auditRetentionCutoff`, read-only `countAuditEventsBeyondRetention`); destructive purge/partition-drop deferred to live lane (append-only immutability). **Regression:** `deno check` clean; full `deno test` **2021 passed, 0 NEW failures**; **+new tests** (eng8 3, eng4/5 13, eng9 2, db6 3, error-path updated); `flutter analyze` 0 (client untouched). **Pre-existing (NOT this wave):** 5 "tenant isolation probe count" tests (communication/payment/pilot/sis) fail — the probe registry has 233 probes vs stale 220/213 assertions; **confirmed to fail at pre-wave commit `3957fab` with this wave stashed** → tracked `ISO-COUNT` (reconcile the counts, verify the +13 probes). | commits `370028c` `56e4942` `3957fab` `b4bee40` |
| 2026-07-04 | **P1-CODE-2** (RELIABILITY: REL-6..9 "Reliability polish") | **PASS** (fixes a latent dropped-write DATA-LOSS path; no automatic-failure) | 0 P0 / 0 P1 (2 pre-existing UX-7 tracked → P2-UX) | **REL-6 crash-safe dequeue:** `pendingOperations()` returned only `pending`, but the drain marks an op `inFlight` before sending — a crash/kill (or any non-network throw) mid-send stranded it `inFlight` forever ⇒ the queued write was **silently dropped** on next boot. New `reclaimInFlightOperations()` (atomic sqflite txn rewriting BOTH the status column AND the embedded envelope JSON; in-memory too) flips orphans `inFlight`→`pending`; `flush()` reclaims first (idempotency-key ⇒ safe re-send; `_draining` single-flight ⇒ only orphans, never a live claim). **REL-7 read-cache TTL:** `OfflineReadCacheInterceptor.cacheTtl` (default 24h) — a body older than the TTL is a cache miss (error surfaces) + dropped from the LRU; no stale-past-TTL serve. **REL-8 store-fallback telemetry:** the encrypted-DB→in-memory fallback was silent; `openReliabilityStore()`→`ReliabilityStoreOpenResult(degraded)`→`reliabilityStoreDegradedProvider`→`SyncSummary.durabilityDegraded`→Sync banner "storage unavailable — work not being saved". **REL-9 ordering+reachability:** the drain now holds every later same-`entityRef` op once an earlier one fails to confirm (newer edit never overtakes older; different entities unaffected); added `ConnectivityService.isReachable()` — a real DNS-lookup probe (not just the OS flag) that gates the drain (skips sends behind a captive portal, fails open). **Regression:** `flutter analyze` 0; full suite **3599 pass, 0 new failures** (only the 2 known UX-7); **+15 tests**. Backend untouched ⇒ no deno leg. | commit `c0f450f` |
| 2026-07-04 | **P1-CODE-1** (RELIABILITY: REL-1..5 "Reliability finish"; first P1 code wave) | **PASS** (data-loss surface reduced; lost-update guard strengthened; no automatic-failure — no data loss / no duplicate financial txn / no broken sync) | 0 P0 / 0 P1 (2 pre-existing UX-7 tracked → P2-UX) | Closes the reliability-finish wave across 3 commits. **REL-1/REL-4** (`5908509`): universal `Idempotency-Key` Dio interceptor on all mutating verbs (was ~4%) + boot/app-resume `syncEngine.flush()`. **REL-2** (`66f9f35`): marks "Save all" (`bulkUpdateMarks`) routed through `ReliableWriter`. **REL-3/REL-5** (`afd1106`, this run): `DraftAutosaveMixin` wired into the marks grid `_MarksEntryBodyState` (keystroke autosave + resume banner + background flush + discard-on-Save-all; `didUpdateWidget` tightened so a restored draft / unsaved typing is never clobbered) and the fee `showRecordCollectionDialog` (extracted `_RecordCollectionForm` ConsumerStatefulWidget; **money-safe** — drafted amount asked-not-auto-filled). **REL-5**: `ExamMarkRecord.rowVersion` (mapper parses `rowVersion`/`row_version`) → `UpdateExamMarkRequest.expectedVersion` → sent on the FIRST per-cell mark save so the backend lost-update guard (`updateExamMark`→`ExamMarkConflictError`) engages before any conflict/retry (was set only on retry via `sync_engine._withServerVersion`). Fees stay INSERT+idempotency-key (no base row to overwrite) — documented at the call site. Backend untouched (server already serialized `rowVersion` + honoured `expectedVersion`). **Regression:** `flutter analyze` 0; full suite **3584 pass, 0 new failures** (only the 2 known UX-7 `TeacherDashboard` 360×640 overflow, pre-existing → P2-UX); **+11 new tests** (rel5_first_write_version 7, rel3_marks_draft_autosave 2, rel3_fee_collection_draft 2). Backend-touch none ⇒ no deno leg. | commit `afd1106` |
| 2026-07-04 | **P1-CODE-5** (FEATURE: MOD-2/MOD-3 "HR payroll engine") | **PASS** (money-safe engine; no automatic-failure — no duplicate financial txn / no data loss / no broken auth) | 0 P0 / 0 P1 (2 pre-existing UX-7 tracked → P2-UX) | HR payroll engine end-to-end across 2 commits. **MOD-2 backend** (`56939bb`): pure engine in `hr_write_handlers.ts` — `parseSalaryStructure` (422 `SALARY_STRUCTURE_INVALID`: components finite+non-negative, basic>0, net never negative), `upsertSalaryStructure` (keyed by employeeId in `snapshot_payroll.structures`), `generatePayrollRun` (draft run + per-employee lines, netPay=basic+allow−deduct SERVER-side, reuses the processor's `validatePayrollEntries`; 409 `PAYROLL_RUN_ALREADY_PROCESSED` on regen-of-processed; 422 `PAYROLL_NO_STRUCTURES` on a fresh school; idempotent draft regen; ENG-8 cap on the employeeIds filter); routes POST `/hr/payroll/structures` + `/hr/payroll/run/generate`; audits `hr.salary_structure.upserted`/`hr.payroll_run.generated`. **MOD-3 backend**: `handleCreateEmployee` → 409 `EMPLOYEE_CODE_TAKEN` via case-insensitive `employeeCodeExists` (DB unique index = live/schema lane). **Client** (`770ed00`): manageHr-gated `_PayrollManageActions` (Salary structure / Generate run / Process payroll) on the payroll screen INCLUDING the empty state (fresh school can bootstrap); dialogs + 2 mutation notifiers + full API repo lane + hybrid fallback + mock engine parity; run id derived `pay_run_<period-slug>` so regenerate hits the idempotent draft path; **MOD-3 client**: leave dialog's hardcoded `employeeId:'HR-EMP-102'` + free-text name replaced with a real employee picker (department auto-syncs; id always matches selection). **Un-hidden**: payroll is a first-class HR tab behind the `module.hr_payroll` 402 entitlement gate (`api/app.ts:105`) + viewHr/manageHr RBAC (route-contract test). **Regression:** deno HR **85/0** + `deno check` clean; `flutter analyze` **0**; full suite **3605 pass, 0 NEW failures** (only the 2 known UX-7); **+17 tests** (11 backend engine + 6 client incl. the fresh-school structure→generate→process flow against the real mock store). | commits `56939bb` `770ed00` |
| 2026-07-04 | **P1-PROD-0** (FOUNDATION: XCT-1 export-pipeline · XCT-2 reminder rail · XCT-3 date pickers) | **PASS** (foundation for C-waves; no automatic-failure — no data loss / security / money-dup / auth-break / tenant-isolation regression) | 0 P0 / 0 P1 (2 pre-existing UX-7 → P2-UX; 1 pre-existing ISO-COUNT carried) | The 3 cross-cutting foundations that unblock P1-PROD-1..21 + P3 W1.4. **XCT-1 (verify+close-gaps, NOT rebuild):** CSV export already fully centralized on `AksharaReportExportService` (~15 modules ride it — confirmed ≥3). The only real duplication was 3 PDF services hand-rolling `pw.TableHelper.fromTextArray` styling; new shared primitive `buildGridTable({headers,rows,rightAlignFrom})` (single border/header/cell-style source; `buildGridReportPdf` now delegates to it) + routed `management_dashboard_pdf_service` (2 tables), `operations_hub_pdf_service` (4), `finance_audit_register_service` (register table) onto it. **XCT-2 (reminder/scheduling rail):** the scheduled-broadcast substrate (persisted `scheduled_at` → `FOR UPDATE SKIP LOCKED` claim → fan-out → pending in-app delivery) existed but its fire-through was UNPROVEN + no module-facing API. New `_shared/reminders/reminders_service.ts`: `scheduleReminder` (authoring seam, validates/normalizes remindAt, delegates) + `runDueReminders = runDueScheduledBroadcasts` (literally ONE runner — no fork). Fires in-app into `notification_deliveries` (client inbox source); external push/SMS/WhatsApp stays owner-gated by design. **XCT-3 (date pickers):** new shared read-only `AksharaDateField` → `showDatePicker`, writes ISO `yyyy-MM-dd` (call sites unchanged, malformed input now impossible); applied to the named offenders — HR leave create+on-behalf from/to (×4) + probation-end (×1) + intelligence meeting-date. **Regression:** `flutter analyze` 0; full flutter suite **3611 pass / 0 NEW fails** (2 known UX-7); deno touched (communication+reminders) **108 pass / 1 known ISO-COUNT** (untouched, pre-existing); **+11 new tests** (XCT-1 3, XCT-2 5 deno, XCT-3 3) + 2 HR flow tests updated to drive the picker. commit `83bc267`. | docs/engineering/eos/EOS_P1_PROD_0_XCT_FOUNDATIONS_REPORT.md |
| 2026-07-04 | **P1-PROD-1** (FEATURE Finance: C1 fee-recovery CRM, FIN-R1..R5) | **PASS** (additive — never touches the money path; no automatic-failure — no duplicate financial txn / data loss / auth break) | 0 P0 / 0 P1 (2 pre-existing UX-7 → P2-UX) | Discovery-first: FIN-R1 dashboard, FIN-R3 promise-to-pay, FIN-R5 collector metrics (real backend aggregate over contacts⋈promises⋈completed-collections) were ALREADY built — verified, not rebuilt. Closed the real gaps. **FIN-R2 (telecaller call queue — was entirely missing):** server-authoritative "who to call next" riding the SAME open/overdue accounts the defaulters list uses (no duplicate source), enriched with last-contact / nearest-pending-promise / broken-promise signals; PURE ranking `callQueuePriority`/`callQueueReason` (broken promise=0 → due promise=1 → never-contacted=2 → stale=3 → recent=4; FUTURE promise de-prioritised=5) + `handleFinanceCallQueue` sort; new `GET /finance/recovery/call-queue` (viewFinance, route-contract asserts holder/403). Client: `CallQueueEntry` model + full repo lane (interface/api/remote/hybrid/mock) + DTO/mapper + `financeCallQueue*` providers + `_CallQueueSection`/`_CallQueueTile` on the defaulters screen, REUSING the existing log-contact/PTP dialogs (no dup dialogs); logging a contact/promise re-ranks the queue live (invalidation wired). **FIN-R4 (contact-history sheet read stale embedded data):** `showContactHistorySheet` now reads the live `financeStudentContactsFutureProvider` (fallback to embedded while loading) → a contact logged this session shows immediately. Recovery CRM stays additive (never touches collection/invoice money path); reads gate viewFinance, writes manageFinance; RLS scopes every recovery table to tenant+school. **Regression:** `flutter analyze` 0; full flutter suite **3613 pass / 0 NEW fails** (2 known UX-7); deno finance **143/0** (incl. new FIN-R2 7 unit tests — first recovery-repo coverage — + call-queue route-contract); `deno check` api entrypoint clean; **+9 new tests** (backend 7, client 2 incl. the pick→log→re-rank round-trip). commit `c1b9feb`. | docs/engineering/eos/EOS_P1_PROD_1_C1_FINANCE_RECOVERY_CRM_REPORT.md |
| 2026-07-04 | **P1-PROD-2** (FEATURE Finance: C2 Counter/Statements/Reports, FIN-1/2/6/7/8) | **PASS** (aging is informational — no money movement; no automatic-failure) | 0 P0 / 0 P1 (2 pre-existing UX-7 → P2-UX) | Discovery-first: FIN-1 (daily collection summary), FIN-2 (student ledger, running balance), FIN-7 (transaction-level day report), FIN-8 (class-wise dues) ALL already export real transactions through the XCT-1 shared pipeline — verified, not rebuilt. One real gap = **FIN-6 (installment due dates drive aging — the C2 completion criterion).** The term schedule existed but was informational; aging keyed off the single `finance_invoices.due_date` + a hardcoded `+30` survived in the draft→issued transition. Fixed: `issueInvoice` now honours `payments.due_days` + generates the schedule (removes the last hardcoded +30); new shared SQL helper `finance_aging.overdueDaysSql` computes days-overdue from the earliest installment term due date `COALESCE(MIN(installment.due_date), invoice.due_date)`, applied to EVERY finance aging read so they can't diverge — defaulters, recovery call queue, finance-intelligence defaulter predictions, and (to keep its documented parity) intelligence/student_risk. **Behaviour-preserving by default:** the default one-term schedule's due date == invoice.due_date + legacy/un-scheduled invoices fall back via COALESCE → single-term invoices age exactly as before; only genuine multi-term schedules change (correctly age from term 1). Money-affecting late-fee accrual (`due_date + grace_days`) deliberately UNTOUCHED. **Regression:** backend-only (no `lib/**` → flutter unaffected, last full run 3613 pass/2 known UX-7); `flutter analyze` 0; deno finance **147/0** (+4 FIN-6 tests), intelligence **51/0**; api entrypoint typecheck clean. Evidence-grade: SQL query-shape + pure-logic (no live DB in this lane; live numeric multi-term re-verify rides the deferred live lane — bounded by the default-preserving COALESCE). **C3 defers** (needs Phase B B4 / GA-1 live). Next = C4 (Exams). commit `fa30e00`. | docs/engineering/eos/EOS_P1_PROD_2_C2_FINANCE_REPORTS_REPORT.md |
| 2026-07-04 | **P1-PROD-3** (FEATURE Exams: C4 Fast Marks & Tabulation, EXM-1/2/3) — VERIFICATION | **PASS — covered by existing implementation + tests (no gap, no code change)** | 0 P0 / 0 P1 | Discovery-first found C4 already built end-to-end + tested on both client and backend, with the frozen AB/ML/DB exclusion enforced. **EXM-1:** bulk batch save (`POST .../marks/batch`) + client `_saveAll` + keyboard fast-nav (Enter/Tab) + digits-only + 0..maxMarks validation + AB/ML/DB status + REL-3 draft autosave. **EXM-2:** `loadTabulationRegister` / `ExamReportsBuilder.tabulation` — totals/%/rank/grade, present-only exclusion (`status='present' && marks!=null`), standard rank, non-present kept as AB/ML/DB `rank:null` excluded from total/avg/rank/dist. **EXM-3:** `exam_reports_screen` tabulation register CSV/PDF via `AksharaReportExportService.shareGridCsv/shareGridPdf` + report cards + hall tickets + seating. Per EOS rule #4 (don't re-audit/rebuild covered work) + "don't invent features," NO speculative spreadsheet-grid rebuild was made. **Evidence:** deno exam-administration **118/0**; flutter exam client (features/academics/exam_admin + core/exams + contracts) **109/0**; no files changed (analyze/full-suite unaffected). Noted for P2-UX (not a C4 gap): EXM-1 entry is a vertical ListView, not a spreadsheet grid — the "fast marks entry" criterion is met (bulk+keyboard+validation); a grid affordance is UX polish. **C3 remains deferred** (GA-1 live). Next = C5 (Academic Registers & Certificates). docs(eos)-only close. | docs/engineering/eos/EOS_P1_PROD_3_C4_EXAMS_VERIFICATION_REPORT.md |
| 2026-07-04 | **P1-PROD-4** (FEATURE Academics/SIS: C5 Registers & Certificates, ATT-1/ATT-2/SIS-1) | **PASS — covered by existing implementation + tests; one test-coverage gap closed** | 0 P0 / 0 P1 (2 UX-7 + 1 ISO-COUNT known, carried) | Discovery-first found all three already built with real backend + client + shared export. **ATT-1** office attendance register (class+date filter, present/absent/late/excused, CSV+PDF via `AksharaReportExportService`; real `getAttendanceRegister`, `GET /attendance/register`, viewSis). **ATT-2** monthly students×days register (`_MonthlyMatrix`, P/A/L/E cells + % column, CSV+PDF; real `getMonthlyRegister` off submitted `attendance_records`). **SIS-1** Bonafide/Study/Conduct certs (identity cluster): `POST /sis/students/{id}/certificates` immutable register (SELECT+INSERT only) + audit + client `SisCertificatePdfService` print-ready PDFs. Per EOS rule #4 + "don't invent features," NO rebuild. **One genuine gap closed:** ATT-2 had no cell-level render test → added `office_attendance_screen_test.dart` "ATT-2 monthly matrix" (DataTable renders student row + per-day code cells + % + export affordances). Test-only (no lib/**). **Evidence:** flutter attendance+sis **73/0** (incl. new test); deno attendance+sis **151/0 + 1 known ISO-COUNT** (`sis_probe_validation_test`, untouched, pre-existing); analyze 0; full suite unaffected (3613→3614). **Noted (not a gap):** ATT-1 filters are class+date only (department/status = P2-UX, not a completion gap); staff muster HR-6 is separate. **Deferred:** C6 (HWK-1 owner-gated schema change `due_label`→`due_date DATE`); C3 (GA-1 live). Next = C7 (HR Payroll & Salary Registers). commit `1fc6104`. | docs/engineering/eos/EOS_P1_PROD_4_C5_REGISTERS_CERTIFICATES_REPORT.md |
| 2026-07-04 | **P1-PROD-5** (FEATURE HR: C7 Payroll & Salary Registers, HR-1/HR-2) | **PASS** (read-only rendering — no new money math; no automatic-failure) | 0 P0 / 0 P1 (2 pre-existing UX-7 → P2-UX) | Discovery-first: the P1-CODE-5 payroll engine + **HR-1 salary register export** (rows + column TOTAL, CSV+PDF via `AksharaReportExportService`) + **HR-2 all-employees payslip batch** (`sharePayslipsBundlePdf`) already exist — the roadmap "stub snackbar" is stale (`hr_payroll_screen.dart:180` already replaced it). Verified, not rebuilt. **One real gap closed — HR-2 individual per-employee payslip PDF** (item = "per-employee PDF + all-for-run"; only the batch existed): new `AksharaReportExportService.buildPayslipPdf`/`sharePayslipPdf` (per-document earnings+deductions+net, mirrors `buildReceiptPdf`, decoupled primitive params); `HrReportExporters.sharePayslipPdf` maps `HrPayslip` onto it; "Individual payslips" export-bar action opens a per-employee picker sheet (`_IndividualPayslipSheet`) reusing the loaded `hrPayslipsProvider` bundle. Client-only, read-only (figures from the server engine — no new money math). **Regression:** `flutter analyze` 0; full suite **3616 pass / 0 NEW fails** (2 known UX-7); HR+reports+contracts **103/0**; no `supabase/**` changes (no deno leg). **+2 tests** (individual-payslip widget flow + `buildPayslipPdf` non-empty PDF). Next = C8 (Transport). commit `524105e`. | docs/engineering/eos/EOS_P1_PROD_5_C7_HR_PAYROLL_REGISTERS_REPORT.md |
| 2026-07-04 | **P1-PROD-6** (FEATURE Transport: C8 Fleet, Roster & Fee, TRN-1/2/3/4/9) — VERIFICATION | **PASS — covered by existing impl + tests; money boundary verified intact & test-enforced (no safe gap to close)** | 0 P0 / 0 P1 | Discovery-first found all five items built with real RLS-scoped audited persistence (generic `transport_entities` JSONB store). **TRN-1** vehicle/driver CRUD (dup-reg/licence 409 + in-use delete guards); **TRN-2** doc-expiry (strict ISO `isStrictIsoDate`, `showDatePicker` not free-text, expiring-soon scanner); **TRN-3** stop roster (`buildRoster`) export via `AksharaReportExportService`; **TRN-4** stop editor (row-locked add/update/remove/reorder + auto-resequence + permutation-validate); **TRN-9** fee→Finance demand (`handleRaiseTransportDemand`→`assignFeeStructure`). **MONEY BOUNDARY CONFIRMED (Part 7B no-dup-financial-txn):** zero payment/collection code in Transport — enforced by passing test `transport_write_handlers_test.ts:540` ("transport contains ZERO payment/collection code"); `createAssignmentAndAccount` get-or-creates the per-year account (test `finance_assignments_repository_test.ts:261`); double-invoice blocked by Finance `finance_fee_assignments` per-(student,structure,year) uniqueness. Per EOS rule #4 + "no duplicate payment logic," NO rebuild. **One candidate gap NOT hot-patched (money-adjacent, unsafe to drive-by-fix):** TRN-9 demand dedupe `(sisStudentId,routeId,academicYear,term)` is application-level (no DB UNIQUE) → race-prone, BUT money-contained downstream; a naive catch-DuplicateAssignmentError is INCORRECT (dedupe is route-scoped, assignment uniqueness is structure-scoped → a different-route demand would be wrongly swallowed) → tracked `TRN9-DEDUPE` (design + live-lane partial-unique-index hardening). **Evidence:** deno transport **36/0** (incl. money-boundary test), finance-assignments **12/0**, flutter transport **42/0**; no files changed (analyze/full-suite unaffected). Next = C9 (Inventory/Library/Communication). docs(eos)-only close. | docs/engineering/eos/EOS_P1_PROD_6_C8_TRANSPORT_VERIFICATION_REPORT.md |
| 2026-07-04 | **P1-PROD-7** (FEATURE Operations: C9 Inventory·Library·Communication, INV-1/2·LIB-1/2·COM-1/2) | **PASS** (governance intact; no automatic-failure) | 0 P0 / 0 P1 | Discovery-first: all six already built (2-day-old inventory memory was STALE — INV-1/3/6 stock ledger fully implemented). **INV-1** stock issue (`issueStock` FOR UPDATE lock + decrement; negative-block `InsufficientStockError`→422 + migration `20260839000000` DB `CHECK(quantity_on_hand>=0)`; immutable `stock_movements` ledger SELECT/INSERT-only qty_before→after; **maker-checker** `stock_adjustments CHECK(checker<>maker)`, adjust_out→pending, self-approve 409); **INV-2** consumable+reorder CRUD (`upsertStockItem`/`listLowStock`); **LIB-1** overdue export (shared service); **LIB-2** catalog edit/delete + CSV import; **COM-1** `getBroadcastDeliveryReport`+CSV; **COM-2** `comm_audience_segments`+`resolveBroadcastRecipients`. Governance boundary VERIFIED intact + test-green (`inventory_stock_repository_test.ts`). Per EOS rule #4 no rebuild. **One gap closed — LIB-2 bulk-import handler had no unit test:** extracted pure `planImportRow(raw,id,seenIsbns)` (behavior-preserving — handler still owns SAVEPOINT/INSERT; dedupe decided before savepoint, dropping a wasted savepoint on dedupe-rejected rows; same `{imported,failed}`) + 4 tests (valid/non-object/missing-field/dup-ISBN). Backend-only. **Regression:** deno inventory+library **76/0** (+4), comm report+audience **27/0**; api entrypoint typecheck clean; no `lib/**` (flutter unaffected, 3616/2 known UX-7). ISO-COUNT unchanged (untouched). Next = C10 (Principal Approval Center). commit `2ecee77`. | docs/engineering/eos/EOS_P1_PROD_7_C9_INVENTORY_LIBRARY_COMM_REPORT.md |
| 2026-07-04 | **P1-PROD-8** (FEATURE Principal/Approvals: C10 Approval Center batch, PRI-1) | **PASS** (closes a permission-escalation exposure; no automatic-failure) | 0 P0 / 0 P1 | Discovery-first: PRI-1 batch approve/reject already built end-to-end (multi-select UI `approval_batch_action_bar`/`approval_queue_table` + `POST /approvals/batch-decide`→`handleBatchDecideApprovals` one-tx/de-dupe/`decideOne`-per-item/partial-success/MAX_BULK cap; reuses single `decideApproval` audit-each+idempotent; client repo/api/mock/provider wired; comprehensive `approval_batch_decide_test.ts`). Per EOS rule #4 no rebuild. **Real money-SoD gap CLOSED:** `decideApproval` enforced the self-approve (requester≠approver) guard ONLY for `inventoryPo` — despite comments claiming maker≠checker was guaranteed. A requester holding the approve permission could approve their OWN `feeConcession` (money waiver, FIN-D4 two-person maker-checker), `refund` (money out), or `feeStructure` — single AND batch. Fix: `SELF_APPROVE_DENIED_TYPES={inventoryPo,feeConcession,refund,feeStructure}`, approve-time guard denies self-approval (`ApprovalSelfApproveDeniedError`) on both paths; **reject-by-same-person stays allowed**. Aligns code with documented intent + [[fin-d4-fee-concession-decision]]. Verified non-breaking (studentLeave self-actor still ok; domain-effect tests use `applyApprovalTypeHandler` not `decideApproval`). **+4 tests** (3 SoD single incl. reject-allowed, 1 batch fee-concession self-approve skipped). Backend-only. **Regression:** deno approval **59/0** (+4), finance fee-concession **2/0**; api typecheck clean; no `lib/**` (flutter unaffected). Tracked: `PRI1-CLIENT-TEST` (client batch UI widget test), mock self-approve parity (pre-existing). Next = C11 (Admissions). commit `7c9294b`. | docs/engineering/eos/EOS_P1_PROD_8_C10_PRINCIPAL_APPROVAL_BATCH_REPORT.md |
| 2026-07-04 | **P1-PROD-9** (FEATURE Admissions: C11 Front-office productivity, ADM-1..5) | **PASS** (client-only; no automatic-failure) | 0 P0 / 0 P1 (2 known UX-7) | Discovery-first: **ADM-2** auto-log WhatsApp/call to lead timeline (`admissions_lead_activities` + `handleAddLeadNote` + client `autoLogWhatsAppNote`), **ADM-3** bulk lead actions (`handleBulkLeadActions` MAX_BULK + per-lead audit; client multi-select bar), **ADM-4** follow-ups-due inline (complete/reschedule handlers + client sheet), **ADM-5** New-Application real lead picker (no placeholder junk) — ALL already built + tested. Admission approval is maker-checker SoD-gated (`setApprovalDecision` self-approve→409, migration `20260846000000`), consistent with C10. Per EOS rule #4 no rebuild. **One gap closed — ADM-1 PDF export:** reports screen exported CSV only (shared `shareGridPdf` existed but unused, unlike every other module). Extracted `_gridForTab(data,tab)` so CSV+PDF share identical columns; added PDF export via `AksharaReportExportService.shareGridPdf`; filter bar now offers both (compact keyed icon buttons — a transient two-labelled-button layout overflowed the router-smoke viewport, caught in-wave + fixed before commit). Client-only. **Regression:** `flutter analyze` 0; full suite **3618 pass / 0 NEW fails** (2 known UX-7); admissions **110/0** (+2 export tests); no `supabase/**` (no deno leg). Tracked: ADM-2 client auto-log test (minor). Next = C12 (Finance productivity). commit `10f8461`. | docs/engineering/eos/EOS_P1_PROD_9_C11_ADMISSIONS_PRODUCTIVITY_REPORT.md |
| 2026-07-06 | **P1-PROD-10** (FEATURE Finance: C12 productivity & receipting, FIN-3/4/5/9 + FIN-R6/R7) | **PASS** (no automatic-failure; money-safety intact) | 0 P0 / 0 P1 (2 known UX-7) | Discovery-first: **FIN-4** (client reprint stamps `DUPLICATE`+audits `reprintReceipt`, manageFinance-gated), **FIN-5** (`buildBatchReceiptsPdf` one-per-page + provider + UI), **FIN-9** (aging via shared `overdueDaysSql` + defaulters/reports/intelligence on real `finance_collections`/`finance_invoices`) verified already built — per EOS rule #4 no rebuild. **FIN-3 gap closed:** three copies of INR formatting used Western 3-digit grouping, contradicting the (correct) Indian amount-in-words on the same receipt; added shared `indianDigitGroups` (Lakh/Crore) in `amount_in_words.dart`, wired the receipt `_formatInr` to it (+5 tests). **FIN-R7 built (money-safe):** the `finance_offline_payments` ledger is tracking-only — neither create nor reconcile posts to `finance_collections`, nothing sums it into dues (grep-verified: referenced solely by its own repo/handlers). Added `pdc` method + `instrument_date`/`bank_name` + terminal `bounced` status with bounce audit trail; migration `20260850000000`, `bounceOfflinePayment` (only pending→bounced; reconciled cannot bounce; bounced cannot reconcile — 409s), `handleBounceOfflinePayment`, `POST /finance/payments/offline/{id}/bounce`, `offlinePaymentBounced` audit; full client stack incl. Bounced tab + mark-bounced dialog. A bounce reverses NO money (Finance stays the sole payment engine; any refund is a separate manual action). +6 backend tests + route-contract row. **FIN-R6:** owner resolved **FIN-D6** (2026-07-06: principal sets, collectors see own) → enriched the recovery dashboard `collectorPerformance` with `target`+`attainmentPct` by joining the existing FIN-R5 `collectorPerformanceForMonth` with `listRecoveryTargets` (read-only, no new money math); principal-gated (`manageFinance`) set-target action → `setCollectionTarget` (POST /finance/recovery/targets, backend audits `finance.recovery.target_set`) + notifier + repo×3 + mock attainment; +2 contract tests. **Regression:** `flutter analyze` 0; full flutter suite no NEW fails (2 known UX-7 Teacher-dashboard, unrelated); deno finance+audit **172/0**; `deno check` green. Tracked: none new. Next = C13 (Academic-work productivity, Exams half EXM-4/5/6/7; Homework half defers with C6). commit `2bd7ecd`. | docs/engineering/eos/EOS_P1_PROD_10_C12_FINANCE_PRODUCTIVITY_REPORT.md |
| 2026-07-06 | **P1-PROD-11** (FEATURE Exams: C13 Academic-work productivity, Exams half EXM-4/5/6/7) | **PASS** (no automatic-failure; exam-result governance intact) | 0 P0 / 0 P1 (2 known UX-7) | Discovery-first: **EXM-4** (per-subject topper `loadExamToppers` + class merit `loadMeritList` reusing the tabulation register; present-only), **EXM-5** (pass/fail + grade distribution `loadExamDistribution`, present-only, excluded counted separately), **EXM-7** (datesheet `loadDatesheet`) — all verified built end-to-end incl. client CSV+PDF via `akshara_report_export_service`. Per EOS rule #4 no rebuild. (Noted, NOT a gap: EXM-5 pass-mark hardcoded 40% `DEFAULT_PASS_MARK_PERCENT` — the report is complete; a *configurable* per-exam pass mark is a future config item, structurally ready.) **EXM-6 gap closed — the FIRST caller of the XCT-2 reminder rail:** the deadline field + a per-exam pending query existed but zero `scheduleReminder` callers wired it. Added `listOverdueMarksEntry` (marks_entry phase, `marks_entry_deadline < now`, HAVING pending>0) + `handleRemindPendingMarks` (manageExams) — checks pending AT trigger so it NEVER fires a false reminder, then schedules ONE in-app `all_teachers` reminder through `scheduleReminder` (the shared rail; recipients resolved at fire time by the scheduled-broadcast runner `POST /communications/broadcasts/run-scheduled`; in-app inbox only, external channels owner-gated). Pure `buildMarksReminder` digest (subject·class·pending·due) — unit-tested. Audited `exam.marks_reminder.sent` keyed on the reminder id. Route `POST /academics/exams/marks/remind` (before the generic `{examId}` matcher) + route-contract RBAC (403 w/o manageExams, 503 with). Client: marks-progress board surfaces the deadline + an "Overdue" badge (`MarksEntryProgress.isOverdue`), a manageExams-gated "Remind teachers" app-bar action → count snackbar; repo interface/api/remote/mock + notifier method. **No new scheduler/channel** (rides the one rail); **no marks/results mutated** (governance untouched). **Homework half HWK-3..8 deferred with C6** (HWK-1 owner migration). **Regression:** `flutter analyze` 0; full suite no NEW fails (2 known UX-7 Teacher-dashboard, unrelated); deno exam-administration **124/0**, reminders+audit **24/0**; `deno check` green. **+12 tests** (EXM-6 digest builder ×4, route-contract ×2, `isOverdue` model ×4, progress-screen widget ×2). Tracked: none new (EXM-5 configurable pass-mark = future config, not a wave gap). Next = C14 (Teacher & Attendance productivity, TCH-1..4/ATT-3/4). commit `0a8c2a3`. | docs/engineering/eos/EOS_P1_PROD_11_C13_EXAMS_PRODUCTIVITY_REPORT.md |
