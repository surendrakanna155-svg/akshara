# Akshara ERP — Implementation Progress (Permanent Execution Journal)

**Status:** 🟢 Permanent journal · **Started:** 2026-07-03 · **HEAD at start:** `68f15cb`
**Governs:** execution of [`../roadmap/FINAL_EXECUTION_MASTER_ROADMAP.md`](../roadmap/FINAL_EXECUTION_MASTER_ROADMAP.md) per [`../roadmap/AUTONOMOUS_EXECUTION_PLAN.md`](../roadmap/AUTONOMOUS_EXECUTION_PLAN.md).
**Rule:** append **one row per completed task/wave**, immediately after its EOS PASS + commit. Never rewrite history; only append.

> This is the single, permanent record of what was actually built. It is the ground truth for the
> roadmap's progress dashboard — the two must always agree (Autonomous Plan §9).

---

## 1. Journal schema (every completed task records)

| Field | Meaning |
|---|---|
| **Date** | ISO date of completion |
| **Commit** | short SHA (the commit made *after* EOS PASS) |
| **Phase** | P0…P8 |
| **Task ID** | roadmap task (e.g. `P0-CODE-1`) |
| **Files changed** | key paths / count |
| **EOS result** | PASS / CONDITIONAL PASS (P1s tracked) — never BLOCKED (BLOCKED never commits) |
| **Evidence** | test/gate/artifact proving the outcome (path or count) |
| **Audit finding** | the finding ID(s) closed (from `AUDIT_FINDINGS_LEDGER.md`) |
| **Roadmap item** | same as Task ID; links back to the roadmap row |

---

## 2. Progress summary

| Phase | Tasks total | ✅ Complete | 🔵 In progress | ⚪ Pending | EOS-gated |
|---|---:|---:|---:|---:|---|
| **Planning** | — | ✅ FROZEN 2026-07-04 | — | — | audit-verified |
| P0 — Truth/Docs/Live-Verify | 19 | 14 (DOC-1/2/3/4/5, SEC-1/2/3, INFRA-2/4/5/6, CODE-1/2) | 0 | 5 (⏳ live-lane: INFRA-1/3, TEST-1/2/3) | per task |
| P1 — Backend & Code Fixes | 13 (+22 PROD waves incl. P1-PROD-22 staff-attendance GA track) | 24 (CODE-1/2/3/5, PROD-0, C1–C21 = PROD-1..19) + P1-CI-0 seam ✅ | 0 | 11 (CODE-4/6/7/8, SEC-1, TEST-1/2, INFRA-1 · PROD-22 GA track · C3 ⏳ GA-1-live · C6 👤 HWK-1) | per wave |
| P2 — UI/UX | 5 | 3 (P2-UX-1 ✅ · P2-UX-2 ✅ · P2-UX-3 ✅) | 0 | 2 (next: P2-UX-4) | per wave |
| P3 — Adaptive AI (W1.1–1.5 · W2.0–2.9) | 2 (15 sub-waves) | 0 | 0 | all | per sub-wave |
| P4 — Red Team Prep | 2 | 0 | 0 | 2 | verdict |
| P5 — Red Team Fixes | 1 (+findings) | 0 | 0 | 1 | per fix |
| P6 — Pilot Simulation | 1 | 0 | 0 | 1 | QA-R-001/002 |
| P7 — Production Cert | 1 | 0 | 0 | 1 | QA-R-012 |
| P8 — GA Readiness | 5 | 0 | 0 | 5 | RELEASE |

**Overall:** 🔵 **EXECUTING.** Planning frozen 2026-07-04; **Wave 1 (P0 · W1 — Documentation Truth) ✅ COMPLETE
(commit `c2b8e27`, EOS DOCS PASS).** The **P1 non-gated lane is now COMPLETE** (P0 code/security ✅ 14/19 + P1-CODE-1/2/3/5 ✅ + P1-PROD-0 ✅ + Phase-C C0–C21 ✅ + P1-CI-0 ✅). The next autonomous wave is defined in
[`../roadmap/NEXT_ACTIVE_WAVE.md`](../roadmap/NEXT_ACTIVE_WAVE.md) (now **P2-UX-1 — "feel & trust" pack**, a phase boundary; remaining P1 = owner-gated P1-CODE-4/6/7/8 + P1-PROD-22, plus deferred C3/C6 and the live-lane tail). Each wave:
implement → validate → `/eos` PASS → commit → append a journal row here.

---

## 3. Pre-execution baseline (verified during the Fable audit — NOT re-work; recorded so it isn't repeated)

> These were verified during the audit (see `docs/audits/`, `AUDIT_FINDINGS_LEDGER.md §A`). They are **not**
> roadmap tasks and must **not** be restarted. Listed here as the execution baseline.

| Date | Item | Result | Source |
|---|---|---|---|
| 2026-07-03 | `flutter analyze` | 0 issues | audit (live run) |
| 2026-07-03 | Cross-tenant RLS isolation (read+write, cross-tenant/cross-school/parent) | PASS (verified) | `docs/audits/11 §3b` (QA-2/LV-11) |
| 2026-07-03 | Edge connects as `erp_tenant` (NOBYPASSRLS) | Confirmed | `docs/audits/11 §2` (DB-2) |
| 2026-07-03 | Entitlement enforcement ON | Confirmed | `docs/audits/11 §2` (ENG-2/OPS-5) |
| 2026-07-03 | Automated encrypted backups + monthly restore drill | Running + passing | `docs/audits/11 §3` (LV-2/LV-8) |
| 2026-07-03 | Watchdog monitoring | Running | `docs/audits/11 §3` (LV-9) |
| 2026-07-03 | AI live via OpenRouter (key present) | Confirmed | `docs/audits/11 §2` (AI-4 part) |
| 2026-07-03 | Live tenant DB password | Rotated (≠ git default) | `docs/audits/11 §2` (DB-1 live) |

*(These do not require re-verification to start Phase 0. Where a task exists to make them permanent/regression-guarded — e.g. P0-TEST-2 isolation-in-CI, P0-INFRA-6 role assertion — it is tracked in the roadmap.)*

---

## 4. Execution log (append one row per completed task — newest at bottom)

> **Wave 0 = Planning (frozen 2026-07-04).** Recorded below as history; it is *not* an implementation wave.
> **Implementation history begins at Wave 1** (P0 · W1 — Documentation Truth, per `NEXT_ACTIVE_WAVE.md`).

| Date | Commit | Phase | Task ID | Files changed | EOS result | Evidence | Audit finding | Roadmap item |
|---|---|---|---|---|---|---|---|---|
| 2026-07-03 | (uncommitted) | Planning | P0-DOC-3 | docs/roadmap/*, docs/audits/*ROADMAP*, FINAL_QA_ROADMAP banner | n/a (planning) | ONE roadmap + ledger + pointers | DOC-3 | P0-DOC-3 |
| 2026-07-04 | (uncommitted) | Planning | FREEZE | docs/roadmap/* (finalized), docs/design/adaptive-ai/ folded into P3, NEXT_ACTIVE_WAVE + FINALIZATION report | n/a (planning) | ROADMAP_FINALIZATION_REPORT.md | — | planning freeze |
| 2026-07-04 | `c2b8e27` | P0 | **P0-DOC-1/2/4/5 (W1 — Documentation Truth)** | `docs/ProjectStatus.md` (rewrite), `docs/FINAL_QA_MASTER_TRACKER.md` (evidence-grade framing + over-claim re-scope), `docs/TechnicalDebt/TD-P0-01-RLS-Enforcement.md` (closed-with-residual), `docs/AuditArchitecture.md` (target-not-built banner), `docs/Operations/{Backup,Restore}-Runbook.md` (→ redirect stubs), `docs/README.md`, `.gitignore` (govern `.claude/skills|commands`; ignore golden-failure diffs + `flutter_*.log`), + ~600-file cleanup/governance tree committed · 766 files | **PASS** (EOS DOCS) | `flutter analyze` 0; docs-only (0 `lib/**`/`supabase/**`); tracker frozen (0 rows rewritten); cleanup = moves-to-archive (content preserved) | DOC-1, DOC-2, DOC-4/QA-1, DB-9/DOC-5, DB-6/DOC-6, DOC-7 | P0-DOC-1/2/4/5 |

| 2026-07-04 | `c80f18c` | P0 | **P0-SEC-1** (W2 — Safety Fixes) | `lib/core/config/environment.dart` (fail-closed `guardForRelease`), `android/app/build.gradle.kts` (no debug-signing + task-graph guard), `test/core/config/environment_test.dart` (+5) | **PASS** (EOS SEC) | `flutter analyze` 0; env tests 11/11 | SEC-1, SEC-2 | P0-SEC-1 |

| 2026-07-04 | `619338b` | P0 | **P0-SEC-2** (W2) | `lib/features/auth/auth_session_storage.dart` (secure backend + legacy migrate/scrub), `lib/features/auth/auth_provider.dart` (provider wiring), +4 test files | **PASS** (EOS SEC) | `flutter analyze` 0; full suite 3563 pass, 0 new failures (2 pre-existing UX-7) | SEC-3 | P0-SEC-2 |

| 2026-07-04 | `6408d90` | P0 | **P0-CODE-1** (W2) | `supabase/functions/_shared/finance/finance_collections_repository.ts` (row_version guard + CollectionConflictError), `finance_collections_handlers.ts` (409 + expectedVersion parse), `finance_mapper.ts` (collectionRowToApi), +4 test files (row_version literals + 3 ENG-1 tests) | **PASS** (EOS RELIABILITY) | deno finance 136/0; deno check clean; api typechecks | ENG-1 | P0-CODE-1 |

| 2026-07-04 | `63358bc` | P0 | **P0-SEC-3** (W2) | `lib/router/app_router.dart` (QA route behind `kReleaseMode`), `lib/core/auth/auth_repository_providers.dart` (mock fail-closed) | **PASS** (EOS SEC) | `flutter analyze` 0; full suite 3563 pass, 0 new failures; router/auth/security 157/0 | SEC-9, SEC-10 | P0-SEC-3 |

| 2026-07-04 | `bacc56a` | P0 | **P0-INFRA-4/5/6** (W2) | `supabase/functions/_shared/tenant_db.ts` (+`assertEdgeTenantRole`, bypassRls), `tenant_handlers.ts` (health 503 on wrong role), `tenant_db_test.ts` (+4), `supabase/migrations/20260610100000_*.sql` (credential→GUC), `scripts/production_launch_verify.sh` (role assertion) | **PASS** (EOS OPS+SEC) | tenant_db 7/0; health 14/0; api typechecks; bash -n clean | LV-10, DB-1/OPS-6, DB-2 | P0-INFRA-4/5/6 |

| 2026-07-04 | `3cbf45c` | P0 | **P0-CODE-2** (W2) | `lib/router/surface_backend_gate.dart` (new gate), `lib/router/route_guards.dart` (ErpRouteGuard wiring), management/sis/control-center sub-navs (ConsumerWidget + filter), `test/router/surface_backend_gate_test.dart` | **PASS** (EOS FEATURE) | gate 4/4; analyze 0; full suite 3567 pass, 0 new failures | ENG-3/MOD-4 | P0-CODE-2 |

| 2026-07-04 | `5908509` | P1 | **P1-CODE-1 · REL-1/REL-4** (Reliability finish, part 1) | Idempotency-Key Dio interceptor (all mutating verbs) + boot/app-resume `syncEngine.flush()` outbox flush | **PASS** (EOS RELIABILITY, wave-close `afd1106`) | universal idempotency (was ~4%); no dup on retry | REL-1, REL-4 | P1-CODE-1 |

| 2026-07-04 | `66f9f35` | P1 | **P1-CODE-1 · REL-2** (part 2) | marks "Save all" (`bulkUpdateMarks`) routed through `ReliableWriter` (was raw `_dio.post`) | **PASS** (EOS RELIABILITY, wave-close `afd1106`) | grid Save-all queues idempotent/resumable | REL-2 | P1-CODE-1 |

| 2026-07-04 | `afd1106` | P1 | **P1-CODE-1 · REL-3/REL-5** (part 3, wave-close) | `lib/features/academics/exam_admin/exam_marks_entry_screen.dart` (DraftAutosaveMixin on `_MarksEntryBodyState` + didUpdateWidget tighten), `lib/features/finance/finance_workflow_actions.dart` (extract `_RecordCollectionForm` + money-safe resume), exam store/requests/mapper/datasource/provider (`rowVersion`→`expectedVersion`), `lib/core/testing/qa_test_keys.dart`, +3 test files | **PASS** (EOS RELIABILITY) | analyze 0; full suite **3584 pass, 0 new failures** (2 known UX-7); +11 tests; backend untouched | REL-3, REL-5 | P1-CODE-1 |

| 2026-07-04 | `c0f450f` | P1 | **P1-CODE-2** (Reliability polish, REL-6..9) | `lib/core/reliability/store/{reliability_store,in_memory_reliability_store,sqflite_reliability_store,reliability_store_opener}.dart` (reclaim + open result), `sync/sync_engine.dart` (reclaim + ordering + reachability gate), `connectivity/*` (isReachable), `sync_center/{sync_summary,sync_center_controller,sync_banner}.dart` + `reliability_providers.dart` + `main.dart` (degraded telemetry), `lib/core/network/interceptors/offline_read_cache_interceptor.dart` (TTL), +3 test files + fakes | **PASS** (EOS RELIABILITY) | analyze 0; full suite **3599 pass, 0 new failures** (2 known UX-7); +15 tests; backend untouched | REL-6, REL-7, REL-8, REL-9 | P1-CODE-2 |

| 2026-07-04 | `370028c` | P1 | **P1-CODE-3 · ENG-7/ENG-8** (Backend hardening, part 1) | `api/app.ts` (central generic SERVER/CONFIG error), `_shared/http.ts` (`MAX_BULK_ITEMS`), exam/hr/approval/admissions/pilot/transport handlers (bulk caps), `api/qw4_error_paths_test.ts` + `api/eng8_bulk_cap_test.ts` | **PASS** (EOS SECURITY) | no raw internal error to client; 6 bulk arrays 422-before-DB; api 14/0; exam+hr+approval 132/0 | ENG-7(=SEC-6), ENG-8(=SEC-11) | P1-CODE-3 |

| 2026-07-04 | `56e4942` | P1 | **P1-CODE-3 · ENG-10** (part 2) | attendance/promotion/parent-experience/memories/school-config/inventory-intelligence/subscription-admin handlers (18 val 400→422), memories/school-config/inventory route-contract tests | **PASS** (EOS ARCH) | validation failures uniformly 422; affected module tests 116/0 | ENG-10 | P1-CODE-3 |

| 2026-07-04 | `3957fab` | P1 | **P1-CODE-3 · ENG-4/ENG-5** (part 3) | `api/eng4_5_forced_auth_test.ts` (forced-auth choke + route-registry lint) | **PASS** (EOS SECURITY) | unauth→401 across all module groups; 13/13; invariant continuously enforced | ENG-4, ENG-5 | P1-CODE-3 |

| 2026-07-04 | `b4bee40` | P1 | **P1-CODE-3 · ENG-9/DB-6** (part 4, wave-close) | `_shared/config.ts` (`auditRetentionDays`), `_shared/audit/audit_repository.ts` (retention seam), `api/eng9_error_code_taxonomy_test.ts`, `_shared/audit/db6_retention_seam_test.ts`, 7 mock-config builders | **PASS** (EOS ARCH) | error-code taxonomy lint 2/2; audit-retention seam 3/3; **full deno 2021 pass, 0 new failures** (5 pre-existing probe-count drifts → ISO-COUNT); analyze 0 | ENG-9, DB-6 | P1-CODE-3 |

| 2026-07-04 | `56939bb` | P1 | **P1-CODE-5 · MOD-2/MOD-3 (backend)** (HR payroll engine, part 1) | `supabase/functions/_shared/hr/hr_write_handlers.ts` (`parseSalaryStructure`/`upsertSalaryStructure`/`generatePayrollRun` pure transforms + `handleUpsertSalaryStructure`/`handleGeneratePayrollRun`; `employeeCodeExists` → 409 `EMPLOYEE_CODE_TAKEN` on create), `hr_router.ts` (POST `/hr/payroll/structures` + `/hr/payroll/run/generate`), `mod2_3_payroll_engine_test.ts` (11), `qw4_hr_route_contract_test.ts` (manageHr gate on both new writes) | **PASS** (EOS FEATURE, wave-close `770ed00`) | deno HR 85/0; deno check clean; netPay = basic+allow−deduct server-side; 422 `PAYROLL_NO_STRUCTURES`/`SALARY_STRUCTURE_INVALID`; 409 regen-processed; idempotent draft regen | MOD-2, MOD-3 | P1-CODE-5 |

| 2026-07-04 | `770ed00` | P1 | **P1-CODE-5 · MOD-2/MOD-3 (client, wave-close)** | `lib/features/hr/payroll/hr_payroll_screen.dart` (`_PayrollManageActions` structure/generate/process bar + empty-state bootstrap), `hr_workflow_actions.dart` (salary-structure + generate-run dialogs; leave dialog → real employee picker, hardcoded `HR-EMP-102` removed), `hr_mutations_provider.dart` (+2 notifiers), `hr_requests.dart`/`hr_models.dart` (+`HrSalaryStructure`), API repo lane (paths/DTOs/datasource/mapper), hybrid fallback, mock engine parity (+ write store), `qa_test_keys.dart`, `test/features/hr/mod2_3_payroll_engine_client_widget_test.dart` (+6) | **PASS** (EOS FEATURE) | analyze 0; full suite **3605 pass, 0 new failures** (2 known UX-7); fresh-school structure→generate→process flow test green; payroll un-hidden behind `module.hr_payroll` 402 (`app.ts:105`) | MOD-2, MOD-3 | P1-CODE-5 |

| 2026-07-04 | `83bc267` | P1 | **P1-PROD-0 · XCT foundations (C0)** — export pipeline · reminder rail · date pickers | **XCT-1:** `lib/core/reports/akshara_report_export_service.dart` (new shared `buildGridTable` primitive; `buildGridReportPdf` delegates), `management/reports/management_dashboard_pdf_service.dart` (2 tables), `operations/operations_hub_pdf_service.dart` (4), `core/reports/finance_audit_register_service.dart` (register table), `test/core/reports/xct1_shared_grid_table_test.dart` (+3). **XCT-2:** `supabase/functions/_shared/reminders/reminders_service.ts` (`scheduleReminder` + `runDueReminders = runDueScheduledBroadcasts`), `reminders_service_test.ts` (+5). **XCT-3:** `lib/shared/forms/akshara_date_field.dart` (new `AksharaDateField`), `hr/hr_workflow_actions.dart` (leave create+on-behalf from/to ×4 + probation), `intelligence/teacher_effectiveness/teacher_effectiveness_screen.dart` (meeting-date), `test/shared/forms/akshara_date_field_test.dart` (+3), 2 HR flow tests updated to drive the picker | **PASS** (EOS FOUNDATION) | analyze 0; full suite **3611 pass, 0 new failures** (2 known UX-7); deno touched (communication+reminders) **108 pass / 1 known ISO-COUNT** (untouched); ≥3 exports on ONE pipeline ✓, reminder fires end-to-end (test) ✓, date pickers on named offenders ✓ | XCT-1, XCT-2, XCT-3 | P1-PROD-0 |

| 2026-07-04 | `c1b9feb` | P1 | **P1-PROD-1 · C1 — Finance fee-recovery CRM (FIN-R1..R5)** | Discovery-first (FIN-R1/R3/R5 verified existing, not rebuilt). **FIN-R2 call queue (new):** `finance_recovery_repository.ts` (`listCallQueue`), `finance_recovery_handlers.ts` (pure `callQueuePriority`/`callQueueReason` + `handleFinanceCallQueue`), `finance_router.ts` (GET `/finance/recovery/call-queue`), `qw4_finance_route_contract_test.ts` (+1 route), `finance_recovery_test.ts` (+7); client `finance_models.dart` (`CallQueueEntry`), interface/api/remote/hybrid/mock repo lane, `finance_recovery_dto.dart`+mapper, `finance_recovery_provider.dart` (call-queue providers + invalidation), `finance_defaulters_screen.dart` (`_CallQueueSection`/`_CallQueueTile`), `qa_test_keys.dart`, `test/features/finance/fin_r2_call_queue_widget_test.dart` (+2). **FIN-R4 fix:** history sheet → live `financeStudentContactsFutureProvider`. | **PASS** (EOS FEATURE) | analyze 0; full suite **3613 pass, 0 new fails** (2 known UX-7); deno finance **143/0**; api typecheck clean; round-trip (queue→log→re-rank) test green; recovery CRM additive (no money-path touch) | FIN-R1, FIN-R2, FIN-R3, FIN-R4, FIN-R5 | P1-PROD-1 |

| 2026-07-04 | `fa30e00` | P1 | **P1-PROD-2 · C2 — Finance Counter, Statements & Reports (FIN-1/2/6/7/8)** | Discovery-first (FIN-1/2/7/8 verified existing — real exports on the XCT-1 shared pipeline, not rebuilt). **FIN-6 (real gap):** new `finance_aging.ts` (`overdueDaysSql` — days-overdue from earliest installment term due date, COALESCE→invoice.due_date), applied in `finance_defaulters_handlers.ts`, `finance_recovery_repository.ts` (listCallQueue), `finance_intelligence_service.ts`, `intelligence/student_risk_repository.ts`; `finance_invoices_repository.ts` issueInvoice drops hardcoded `+30` → `due_days` setting + generates schedule; `finance_aging_test.ts` (+4). | **PASS** (EOS FEATURE) | backend-only (no lib/** → flutter unaffected, 3613 pass/2 known UX-7); analyze 0; deno finance **147/0**, intelligence **51/0**; api typecheck clean; behaviour-preserving for default single-term config; aging informational (no money movement) | FIN-1, FIN-2, FIN-6, FIN-7, FIN-8 | P1-PROD-2 |

| 2026-07-04 | (no code) | P1 | **P1-PROD-3 · C4 — Exams Fast Marks & Tabulation (EXM-1/2/3) — VERIFICATION** | Discovery-first found C4 already built end-to-end + tested (fast bulk marks save + keyboard nav + validation + AB/ML/DB status; tabulation totals/%/rank/grade with present-only exclusion; tabulation register CSV/PDF via the shared service). Per EOS rule #4 + "don't invent features," NO rebuild made. | **PASS** (EOS FEATURE — covered by existing impl + tests) | deno exam-administration **118/0**; flutter exam client **109/0**; no files changed. Noted for P2-UX: EXM-1 grid affordance (not a C4 gap). | EXM-1, EXM-2, EXM-3 | P1-PROD-3 |

| 2026-07-04 | `1fc6104` | P1 | **P1-PROD-4 · C5 — Academic Registers & Certificates (ATT-1/ATT-2/SIS-1)** | Discovery-first: all three already built (ATT-1 office register + ATT-2 monthly students×days grid, both real SQL + shared CSV/PDF export; SIS-1 Bonafide/Study/Conduct immutable-register + print-ready PDFs from the identity cluster). Per EOS rule #4 no rebuild. One gap closed: **ATT-2 cell-level render test** (`office_attendance_screen_test.dart` — monthly matrix DataTable + code cells + % + export). Test-only. | **PASS** (EOS FEATURE — covered by existing + test gap closed) | flutter attendance+sis **73/0**; deno **151/0 + 1 known ISO-COUNT**; analyze 0; suite unaffected. C6 defers (HWK-1 owner schema change); C3 defers (GA-1 live). | ATT-1, ATT-2, SIS-1 | P1-PROD-4 |

| 2026-07-04 | `524105e` | P1 | **P1-PROD-5 · C7 — HR Payroll & Salary Registers (HR-1/HR-2)** | Discovery-first: HR-1 salary register export (rows + TOTAL, CSV+PDF) + HR-2 all-employees payslip batch already built (roadmap "stub snackbar" stale). Closed the one gap — **HR-2 individual per-employee payslip PDF**: `AksharaReportExportService.buildPayslipPdf`/`sharePayslipPdf` (per-document, decoupled), `HrReportExporters.sharePayslipPdf`, "Individual payslips" picker sheet (`_IndividualPayslipSheet`) reusing `hrPayslipsProvider`; `qa_test_keys.dart`. Client-only, read-only rendering. | **PASS** (EOS FEATURE) | analyze 0; full suite **3616 pass, 0 new fails** (2 known UX-7); HR+reports+contracts **103/0**; no backend changes | HR-1, HR-2 | P1-PROD-5 |

| 2026-07-04 | (no code) | P1 | **P1-PROD-6 · C8 — Transport Fleet, Roster & Fee (TRN-1/2/3/4/9) — VERIFICATION** | Discovery-first: all five items already built (fleet CRUD w/ guards, ISO doc-expiry + date picker, stop roster export via shared service, stop editor w/ reorder, TRN-9 fee→Finance demand). **Money boundary CONFIRMED intact + test-enforced** (zero payment code in Transport; get-or-create per-year account; double-invoice blocked by Finance assignment uniqueness). Per EOS rule #4 no rebuild. One candidate gap (TRN-9 app-level dedupe race) NOT hot-patched — money-contained + unsafe to drive-by-fix (route-vs-structure semantics) → tracked `TRN9-DEDUPE`. | **PASS** (EOS FEATURE — covered by existing + tests; money boundary verified) | deno transport **36/0** (incl. money-boundary test), finance-assignments **12/0**, flutter transport **42/0**; no files changed | TRN-1, TRN-2, TRN-3, TRN-4, TRN-9 | P1-PROD-6 |

| 2026-07-04 | `2ecee77` | P1 | **P1-PROD-7 · C9 — Inventory, Library & Communication (INV-1/2·LIB-1/2·COM-1/2)** | Discovery-first: all six already built (inventory memory was stale — INV-1/3/6 stock ledger fully done; governance intact: FOR UPDATE lock, negative-block 422+DB CHECK, immutable stock_movements ledger, maker-checker SoD 409). Per EOS rule #4 no rebuild. **One gap closed — LIB-2 bulk-import test:** extracted pure `planImportRow` (behavior-preserving) in `library_write_handlers.ts` + 4 unit tests (dedupe/partial-success). | **PASS** (EOS FEATURE) | deno inventory+library **76/0** (+4), comm report+audience **27/0**; api typecheck clean; backend-only (flutter unaffected) | INV-1, INV-2, LIB-1, LIB-2, COM-1, COM-2 | P1-PROD-7 |

| 2026-07-04 | `7c9294b` | P1 | **P1-PROD-8 · C10 — Principal Approval Center batch (PRI-1)** | Discovery-first: PRI-1 batch approve/reject already built (multi-select UI + `POST /approvals/batch-decide` + audit-each + partial-success + client wiring + backend tests). Per EOS rule #4 no rebuild. **Closed a money-SoD gap:** `decideApproval` self-approve guard was `inventoryPo`-only → extended to `{inventoryPo, feeConcession, refund, feeStructure}` (`SELF_APPROVE_DENIED_TYPES`) so a requester can't approve their OWN money waiver on single OR batch (FIN-D4); reject-by-same-person still allowed. `approval_separation_of_duties_test.ts` (+3), `approval_batch_decide_test.ts` (+1). | **PASS** (EOS FEATURE — closes a permission-escalation exposure) | deno approval **59/0** (+4), finance fee-concession **2/0**; api typecheck clean; backend-only (flutter unaffected) | PRI-1 | P1-PROD-8 |

| 2026-07-04 | `10f8461` | P1 | **P1-PROD-9 · C11 — Admissions / Front-office productivity (ADM-1..5)** | Discovery-first: ADM-2 (auto-log WhatsApp/call to timeline), ADM-3 (bulk lead actions), ADM-4 (follow-ups-due inline), ADM-5 (New-App real lead picker) all built + tested; admission approval SoD-gated (self-approve→409). Per EOS rule #4 no rebuild. **Closed ADM-1 PDF export gap:** `admissions_reports_screen.dart` extracted `_gridForTab`, added `shareGridPdf` path (CSV+PDF, compact icon buttons — fixed a filter-bar overflow in-wave), `qa_test_keys.dart` (+2 keys); `adm1_reports_export_widget_test.dart` (+2), phase3 test updated. | **PASS** (EOS FEATURE) | analyze 0; full suite **3618 pass, 0 new fails** (2 known UX-7); admissions **110/0**; client-only (no backend) | ADM-1, ADM-2, ADM-3, ADM-4, ADM-5 | P1-PROD-9 |
| 2026-07-06 | `2bd7ecd` | P1 | **P1-PROD-10 · C12 — Finance productivity & receipting (FIN-3/4/5/9, FIN-R6/R7)** | Discovery-first: **FIN-4** (duplicate reprint stamps `DUPLICATE`+audits, RBAC), **FIN-5** (batch receipt PDF), **FIN-9** (aging/defaulters/reports/intelligence on real data) verified already built. Per EOS rule #4 no rebuild. **Closed FIN-3:** shared `indianDigitGroups` (Indian lakh/crore) on the receipt PDF `_formatInr`, consistent with the existing Indian amount-in-words (was Western 3-digit). **Built FIN-R7** cheque/DD/PDC + bounce on the `finance_offline_payments` **tracking ledger** — added `pdc` method, `instrument_date`/`bank_name`, terminal `bounced` status + bounce audit; **money-safe** (that ledger never posts to `finance_collections`; bounce reverses no money; test-enforced); migration `20260850…` + repo + `handleBounceOfflinePayment` + route + `offlinePaymentBounced` audit + full client (enum/codec/model/mapper/request/repo×3/notifier/api-path/screen: PDC option, instrument fields, Bounced tab, mark-bounced). **FIN-R6:** owner resolved **FIN-D6** (principal sets, collectors see own) → enriched recovery dashboard `collectorPerformance` with `target`+`attainmentPct` (reuses FIN-R5 `collectorPerformanceForMonth` + `listRecoveryTargets`); principal-gated set-target action + notifier + repo×3. Money-safety tripwire intact (Finance = sole engine; targets/reprints/analytics read-only). | **PASS** (EOS FEATURE) | analyze 0; full suite no-new-fail (2 known UX-7); deno finance+audit **172/0**; deno check green; **+13 tests** (FIN-3 ×5, FIN-R7 backend ×6, FIN-R7+FIN-R6 contract ×2) | FIN-3, FIN-4, FIN-5, FIN-9, FIN-R6, FIN-R7 | P1-PROD-10 |
| 2026-07-06 | `0a8c2a3` | P1 | **P1-PROD-11 · C13 (Exams half) — Academic-work productivity (EXM-4/5/6/7)** | Discovery-first: **EXM-4** (merit list + subject topper), **EXM-5** (pass/fail + grade-distribution report), **EXM-7** (datesheet PDF) all verified built end-to-end (backend + client + CSV/PDF exports). Per EOS rule #4 no rebuild. **Closed EXM-6** — the FIRST caller of the shared **XCT-2** reminder rail: `listOverdueMarksEntry` (marks_entry phase, deadline passed, marks still pending) + `handleRemindPendingMarks` (manageExams) checks pending AT trigger (no false reminder) then schedules ONE in-app `all_teachers` reminder via `scheduleReminder` (fires through the scheduled-broadcast runner into teacher inboxes); pure `buildMarksReminder` digest; audited `exam.marks_reminder.sent`; `POST /academics/exams/marks/remind` + route-contract. Client: marks-progress board shows deadline + "Overdue" badge, manageExams-gated "Remind teachers" action → count snackbar; `MarksEntryProgress.isOverdue` + repo/notifier/api-path. **Homework half HWK-3..8 deferred with C6.** Exam-result governance untouched (no marks/results mutated). | **PASS** (EOS FEATURE) | analyze 0; full suite no-new-fail (2 known UX-7); deno exam-administration **124/0**, reminders+audit **24/0**; deno check green; **+12 tests** (EXM-6 builder ×4, route-contract ×2, model isOverdue ×4, screen ×2) | EXM-4, EXM-5, EXM-6, EXM-7 | P1-PROD-11 |
| 2026-07-06 | `9e5602a` | P1 | **P1-PROD-12 · C14 — Teacher & Attendance productivity (TCH-1/2/3/4, ATT-3/4)** | Discovery-first: **TCH-4** (weekly timetable `TeacherTimetableScreen` + server-backed cover/substitution alert `CoverAlertCard`/`teacherTodayCoverProvider`), **ATT-3** (absentees-only "fill remaining present" fast-mark `fillRemainingAsPresent`), **ATT-4** (office not-yet-marked monitor `/attendance/pending` + `_PendingTab`) all verified built. Per EOS rule #4 no rebuild. Closed 3 gaps: **TCH-1** — the today-schedule-row tap landed on the weekly timetable; re-pointed via a new `schedule_attendance_<classLabel>` action → `teacherAttendance?class=<label>` (attendance screen already supported the preselect), leaving the pending-banner `mark_attendance_` path untouched. **TCH-2** — teacher-home "Marks to enter" task now reflects overdue: reuses EXM-6 `MarksEntryProgress.isOverdue` → urgent (error) tone + "Marks overdue" label (`PendingTask.overdue`). **TCH-3** — my-class marks-summary export (Class/Subject/Entered/Total/Pending/Status incl. Overdue) on the shared XCT-1 grid pipeline (`TeacherReportExporters.shareMarksSummaryCsv/Pdf`), wired as an export action on the teacher exams screen. Attendance integrity untouched (nav-only, no register mutation); no new scheduler. Client-only (0 `supabase/**`). | **PASS** (EOS FEATURE) | analyze 0; full suite no-new-fail (2 known UX-7); teacher-dashboard golden unchanged; **+4 tests** (TCH-1 nav, TCH-2 overdue tone, TCH-3 rows-unit + export-button) | TCH-1, TCH-2, TCH-3, TCH-4, ATT-3, ATT-4 | P1-PROD-12 |
| 2026-07-06 | `4b65ce1` | P1 | **P1-PROD-13 · C15 — HR & SIS productivity (HR-3/4/7, SIS-2/5)** | Parallel HR∥SIS discovery + parallel build under **disjoint file ownership** (HR-3 SoD by main loop; SIS-2/5 by a delegated implementation agent). Discovery-first: **HR-3** batch leave approve/reject (`/hr/leave/batch-decide` + multi-select UI + partial success), **HR-4** leave-balance report/export, **HR-7** employee directory export, **SIS-2** richer registry export (incl. guardian name/phone), **SIS-5** transfer/exit log — all verified built. Per EOS rule #4 no rebuild. Closed 3 gaps: **HR-3 leave SoD** (owner 2026-07-06 chose deny-self-approval) — `applyLeaveDecision`/`applyBatchLeaveDecision` now enforce approver ≠ `createdBy` (the acting filer), single raises 403 `LEAVE_SELF_APPROVE_DENIED`, batch skips it (partial success); mirrors the C10 money maker-checker; legacy/empty-actor rows unaffected. **SIS-2** dedicated class-list export (roster) + parent contact-sheet export on the XCT-1 grid (new `SisReportExporters` methods + registry buttons + QA keys). **SIS-5** exit reason on the transfer/exit log — read-only correlated sub-select to the latest `sis_certificate_issues` transfer-certificate `reason` (org/school-scoped, null when no TC), threaded through mapper/model/exporter (+Reason column) + transfers screen. Reviewed the delegated diff: join verified vs the real certificates schema; transfers test strengthened (8-col + populated-reason), not weakened. No self-approval; exports read-only; SIS-5 join read-only + scoped. | **PASS** (EOS FEATURE — closes a leave SoD control gap) | analyze 0; full suite no-new-fail (2 known UX-7); HR deno **90/0**; SIS deno **139/0** (+1 pre-existing ISO-COUNT, confirmed unrelated); deno check green; **+~11 tests** (HR SoD single ×4 + batch ×1; SIS class-list/contact-sheet rows+render, transfers reason ×2, backend join) | HR-3, HR-4, HR-7, SIS-2, SIS-5 | P1-PROD-13 |
| 2026-07-06 | `5eba37d` | P1 | **P1-PROD-14 · C16 — Transport & Inventory productivity (TRN-5/6/7/8, INV-3/4/5/6/7)** | Parallel Transport∥Inventory discovery + parallel build under **disjoint file ownership** (TRN-8 by a transport agent; INV-7/INV-5 by an inventory agent; EXM-6 one-liner by the main loop). Discovery-first: **TRN-5** bulk student→route allocation (`handleBulkAllocateTransport`: roster/id targets, single-assign payload shape, capacity guard under route lock, bulk audit), **TRN-6** roster/student/vehicle exports on XCT-1, **TRN-7** capacity 409 + audited override + occupancy card, **INV-3** governed manual adjust (adjust_out = maker-checker), **INV-4** low-stock + raise-PO reusing `createPurchaseOrder` (inventoryPo approval), **INV-6** stock-count sessions (negative variance via the governed path) — all verified built; per EOS rule #4 no rebuild. Closed 3 gaps: **TRN-8** onto the XCT-2 rail (`runDocumentExpiryReminder` + pure `collectDocumentExpiries`, EXM-6 pattern, ONE `all_staff` digest, reminderId in audit) **fixing a latent runtime defect** — old `audience:"staff"` is not a `comm_broadcasts` CHECK token, so the insert failed whenever ≥1 expiry existed (empty path masked it); regression-guard test pins the audience to the CHECK set. **INV-7** low-stock alert re-targeted to a new `storekeepers` audience on XCT-2 — migration `20260851` widens the audience CHECK (**restoring `all_staff`**, accidentally dropped by `20260838`) + seeds the never-seeded storekeeper role/grants; resolver falls back to all-staff when no storekeeper exists; `scheduleLowStockReminderIfNeeded` dedupes pending reminders + runs in a SAVEPOINT so alerting can never fail a posted issue. **INV-5** GRN register endpoint (`GET /inventory/procurement/grns`, viewInventory) + client list/provider + CSV/PDF on XCT-1. **Bonus:** EXM-6 reminderId reads `scheduled.broadcastId` (was undefined `.id`); ISO-COUNT partial repair (communication tripwire 220→233). **Tripwire:** `inventory_stock_repository.ts` untouched (12/12 governance green); bespoke stock maker-checker kept (SoD fully enforced; no risky refactor onto the approval center); Transport demand-only (payment-free test green; bulk does NOT auto-raise demands); ONE reminder rail. | **PASS** (EOS FEATURE — also fixes a latent TRN-8 defect + audience-CHECK regression) | analyze 0; full suite **3643 pass / 2 known UX-7** (no new fails); deno transport **40/0** · inventory_finance **29/0** · communication **104/0** · exam_administration **124/0** · reminders **5/0**; deno check green; **+17 tests** | TRN-5, TRN-6, TRN-7, TRN-8, INV-3, INV-4, INV-5, INV-6, INV-7 | P1-PROD-14 |
| 2026-07-07 | `d5edab9c` | P1 | **P1-PROD-15 · C17 — Library & Communication productivity (LIB-3/4/5, COM-3/4/5)** | Discovery-first (parallel read-only map): **LIB-3** keyed/keyboard-wedge ISBN quick issue+return (real counter pattern; camera = documented staged seam, not a gap), **LIB-4** loan renewal/re-issue (`handleRenewLoan` + `assertRenewAllowed`), **COM-3** resend-to-unread (`resendBroadcastToUnread`), **COM-4** schedule-send (`scheduled_at` already active: `scheduleBroadcastMessage` + `runDueScheduledBroadcasts` + client UI; only the periodic cron trigger remains = live-lane/deploy, owner-deferred), **COM-5** save-as-template (message → `notification_templates`; audience reuse = the separate built `comm_audience_segments`, not a gap) — all verified built; per EOS rule #4 no rebuild. **One gap closed — LIB-5 was off the XCT-2 rail:** re-pointed `handleSendOverdueReminders` from an IMMEDIATE `sendBroadcastMessage(audience:"parents")` (blasted ALL parents; misleading "You have N" copy) to `scheduleReminder` via a testable core `runOverdueReminder` (mirrors TRN-8) — ONE `all_parents` digest on the shared rail, remindAt=now, corrected copy, audit carries reminderId; response adds `reminderId` (recipientCount preserved). Library issue/return integrity untouched; tenant-scoped; ONE reminder rail. Backend-only. | **PASS** (EOS FEATURE — re-points LIB-5 onto the ONE reminder rail) | analyze 0; flutter suite unchanged (0 Dart touched; 3643 / 2 known UX-7); deno library **59/0** (+2 LIB-5 rail tests), reminders **5/0**, communication **104/0**; deno check green | LIB-3, LIB-4, LIB-5, COM-3, COM-4, COM-5 | P1-PROD-15 |
| 2026-07-07 | `c2306daa` | P1 | **P1-PROD-16 · C18 — Leadership productivity (PRI-2/3, DIR-1/2)** | Discovery-first: all four already built + wired (pre-wave commits `b1dd65c4` principal + `1e7e7186` director, 2026-07-03). **PRI-2** unsubmitted-marks exception card (`ApprovalUnsubmittedMarksCard` on the Principal Approval Center; reuses the EXM-2 `examMarksEntryProgressProvider`; RBAC-gated), **PRI-3** daily school report (`OperationsHubPdfService.buildDailyReportPdf` — ops-hub snapshot → PDF on XCT-1; `assertViewOperationsHub`), **DIR-1** cross-school league table (pure `rankDirectorSchools` client sort), **DIR-2** consolidated collection report (per-school fee%/outstanding + org totals; XCT-1 exporters) — per EOS rule #4 no rebuild. DIR-1/2 already tested; **closed the two verified test gaps**: PRI-2 card (×3: render-with-pending · hidden-all-complete · hidden-without-RBAC) + PRI-3 export (×2: PDF bytes for a viewer · denied without `viewOperationsHub`). Leadership surfaces read-only (approval maker-checker untouched); no migration; test-only (0 `lib/`). | **PASS** (EOS FEATURE — verify-mostly) | analyze 0; 2 files **7/7**; test-only → suite otherwise unchanged (3643 / 2 known UX-7); **+5 tests** | PRI-2, PRI-3, DIR-1, DIR-2 | P1-PROD-16 |
| 2026-07-07 | (no code) | P1 | **P1-PROD-17 · C19 — Parent self-service (PAR-1..5) — VERIFICATION** | Discovery-first: all five already built end-to-end. **PAR-1** PTM RSVP (`handleMeetingRsvp`, own-child; accept/decline UI + optimistic status), **PAR-2** Apply-Leave quick action (`apply_leave` → `parent_navigation.dart:43` → `ParentLeaveScreen`; nav verified, not a dead tap), **PAR-3** medical-cert attach on a pending own-child leave (`handleParentLeaveAttachment`, own-child+pending guards, audited), **PAR-4** payment-history export (read-only XCT-1 CSV/PDF, own-child receipts, zero writes), **PAR-5** proactive reminder banners (`ParentReminderBanners` from real module data). Per EOS rule #4 no rebuild. Parent RLS strong + multi-layer (FORCE-RLS `student_guardians`, JWT `child_ids` guard `resolveChildId` 403, `loadOwnChildLeave` foreign→NOT_FOUND) — no cross-family leakage. Two documented residuals tracked (NOT gaps): **PAR3-UPLOAD** (reference-only attach; real file bytes deferred per HWK-7; storage service exists via admissions — surfaced to owner batch) + PAR-5 client-derived-vs-rail (by design). | **PASS** (EOS FEATURE — verified; covered by existing + tests) | deno parent+pilot **31/0**; flutter parent widget/contract **21/0**; no files changed | PAR-1, PAR-2, PAR-3, PAR-4, PAR-5 | P1-PROD-17 |
| 2026-07-07 | `8cd87436` | P1 | **P1-PROD-18 · C20 — Teacher & Leadership polish (TCH-5/6/7, PRI-4/5, DIR-3)** | Discovery-first: all six already built. TCH-5 create-homework quick action (→ existing `TeacherHomeworkCreateScreen`; **real `due_date` contract already live → NOT blocked by HWK-1; roadmap note stale**), TCH-6 pending-task deep-link (`hw_review`→`?filter=pending`), TCH-7 timetable export (XCT-1), DIR-3 school-comparison CSV/PDF (co-built with DIR-1/2), PRI-4 weekly digest card (`PrincipalDigestCard`), PRI-5 stale-approval banner (`ApprovalStaleBanner` surfaces the inert >48h count + stale-filter deep-link) — per EOS rule #4 no rebuild. Closed the two verified test gaps: PRI-4 card (×3) + PRI-5 banner (×3). PRI-5 read/filter-only → maker-checker SoD intact. Two tracked residuals (owner decisions): PRI-4/5 scheduled-send (new XCT-2 caller) + DIR-3 native `.xlsx`. Test-only (0 `lib/`). | **PASS** (EOS FEATURE — verify-mostly) | analyze 0; 2 files **6/6**; test-only → suite otherwise unchanged (3643 / 2 known UX-7); **+6 tests** | TCH-5, TCH-6, TCH-7, PRI-4, PRI-5, DIR-3 | P1-PROD-18 |
| 2026-07-07 | `799713f1` | P1 | **P1-PROD-19 · C21 — Records & Parent polish (SIS-3/4, HR-5, PAR-6) — LAST Phase-C wave** | Discovery-first: SIS-3 (audited non-destructive doc verify), HR-5 (headcount-by-dept + XCT-1), PAR-6 (next-PTM hero + action-items, own-child RLS) all verified built + tested — per EOS rule #4 no rebuild. **Built SIS-4 (the one real feature gap): clerk family/sibling view** — `GET /sis/students/:id/siblings` (viewSis, org+school scoped, 404 out-of-scope) → `listStudentSiblings` (EXISTS shared-active-guardian join, self-excluded); **cross-school isolation STRUCTURAL** (both join sides + outer row pinned to org+school; dedicated test green); full client stack + profile "Siblings" section + QA keys + RBAC-inventory row. Read-only (no mutation/migration/audit/new-PII); clerk scoping = org+school (not `child_ids`). **Phase-C (C0–C21) COMPLETE** (minus deferred C3/C6). | **PASS** (EOS FEATURE — builds SIS-4) | analyze 0; SIS flutter **126/126** (+5 SIS-4); deno SIS **150/1** (only the tracked pre-existing ISO-COUNT; probe registry untouched); deno check green; **+13 tests** | SIS-3, SIS-4, HR-5, PAR-6 | P1-PROD-19 |
| 2026-07-07 | `ba47f065` | P1 | **P1-CI-0 · Curriculum-engine seam (CI-0a golden-pin · CI-0b edu_exam_paper_links · CI-0c dormant E1a) — first CI code wave** | Golden-first: **CI-0a** pinned `education_blueprint_solver.ts` BYTE-STABLE (golden test + `.snap`: 5 template-absent totals 7/20/33/50/100 + explicit-mix + solveBlueprint match/fallback/dedup/gap; **solver UNTOUCHED**, existing tests green → future edits must update the golden). **CI-0b** `edu_exam_paper_links` migration `20260852000000` (v3.0 §5.2: TEXT exam↔UUID paper, unique org/school/exam/set, certified RLS) + `linkExamToPaper`/`getExamPaperLink` repo (upsert) + 5 tests — additive seam, NOT wired to publish (zero behaviour change). **CI-0c** dormant E1a seed migration `20260853000000` (response spine + trust columns [existing→'trusted'] + exposure; migrations only, zero UI). Additive-only; certified invariants I1–I8 intact. | **PASS** (EOS FEATURE — additive seam, zero behaviour change) | analyze 0 (0 `lib/`); education deno **47/0** (+8: golden 3 + link 5); deno check green; golden asserts vs committed snap | CI-0a, CI-0b, CI-0c | P1-CI-0 |
| 2026-07-07 | `aeb79938` | P2 | **P2-UX-1 (slice 1 — trust core) · feel & trust pack** | Opens the P2 UI/UX phase. **AksharaSuccessView** (shared success ceremony extracted from the parent-payment view; re-wired pixel-preserving; fires success haptic), **AksharaHaptics** (tick/success/warning vocabulary), **A6** killed the fabricated KPI-card sparkline (`AksharaExecutiveKpiCard` shows real-series-or-nothing; removed `decorativeSparkline()` + `management_kpi_row` caller) → 6 baselines regenerated deliberately (approval_center + intelligence_dashboard). Client-only, no migration. Tracked continuation (next slice): skeleton sweep · pull-to-refresh · freshness chip · tabular numerals · draft chips · copy dictionary · A9 verify. | **PASS** (EOS UX) | analyze 0; +4 widget tests; golden suite green; payment+management+intelligence green; 0 `supabase/` | P2-UX-1 (slice 1) | P2-UX-1 |
| 2026-07-08 | `e25d1282`·`f829c473`·`9424b722`·`f1144250`·`b316e849` | P2 | **P2-UX-1 (slice 2 — feel & trust pack) · COMPLETE** | Five staged sub-commits close the pack. **2a `AksharaSkeleton`** (shimmer primitives + list/dashboard presets, test-stable) + an optional `skeleton` slot on `AksharaLoadingState`/`ErpAsyncBody`/`MobileAsyncBody` (loading TYPE preserved → all `find.byType(AksharaLoadingState)` tests stay green) wired to the 3 mobile dashboards; **2e** extended it to all 8 ERP module dashboards (11 dashboards on skeletons; zero golden churn — goldens render data). **2b A2 `AksharaFreshnessChip`** (reads the LIGHTWEIGHT `ConnectivityService` — not the sync engine — so it never leaks a reachability timer; "Live"/"Offline · saved data") on finance-collections + office-attendance; **A3 `TextStyle.tabularFigures`** on every KPI value (test-font renders identically → 0 golden churn, real Roboto gets tabular on-device). **2c A4 pull-to-refresh** on the 3 mobile dashboards; **`AksharaDraftChip`** (surfaces REL-3 autosave) wired to the parent apply-leave form + **`AksharaUnsavedChangesScope`** (PopScope guard for non-autosave forms). **2d copy dictionary** `AksharaCopy` + **permission-as-its-own-state** (`AksharaErrorState.fromFailure`: forbidden→"You don't have access"/no-retry, unauthorized→"Session expired", network→"You're offline"); **A9** empty-state coverage verified (346 files on the kit) + closed one bare-`Text` residual. **Residual (tracked, not a gap):** the raw `AsyncValue.when()` bulk migration (142 call sites) is per-screen work deferred to P2-UX-2's grid pass — the capability + all wrapper-using screens are done. | **PASS** (EOS UX) | analyze 0 (lib+test); full suite **3686 pass / 2 fail (2 known UX-7 only, 0 new)**; +23 widget/theme tests; 4 qa_x_027 goldens regenerated deliberately (A2 chip); 0 `supabase/` | P2-UX-1 (slice 2) | P2-UX-1 |

| 2026-07-08 | `88d76df1`·`723927b7`·`fb781751`·`fec526e5`·`ce45cf87`·`b34f2116`·`87841013`·`43c8f8df` | P2 | **P2-UX-2 — "the five daily tasks" (B2) · COMPLETE** | **Quick-win sub-waves (prior):** haptic vocabulary on attendance/marks/fee (`88d76df1`) · A5 exam top-level nav + A8 keyboard-type sweep (`723927b7`) · C2 `AksharaTableDensity` compact mode + SIS clerk toggle (`fb781751`) · fee-counter success ceremony (`fec526e5`). **Heavy builds (this run):** **§2.4 fee counter** (`ce45cf87`) — killed hardcoded `5000` (real dues prefill + re-seed on invoice change + dues-breakdown line + late fee; QR Pay real outstanding), "Collect fee" origination from the PSID/name/class accounts search (`manageFinance`-gated; filter matches class), new shared **`AksharaQueuedView`** amber queued-receipt (warning haptic) on offline `pendingSync`. **§2.3 Approvals Inbox** (`b34f2116`) — **server-owned `sodBlocked` flag** (`isSelfApproveDeniedType` single source + additive on list/pending/get; `decideApproval` 403 unchanged) → badge + Approve-disable + swipe-approve-block reading the flag only (no client SoD), on-card facts, type-grouped/critical-sorted queue (`approvalCenterQueueProvider`), swipe-to-decide. **§2.1 attendance exception grid** (`87841013`) — `AttendanceExceptionGrid` (tap-cycle P→A→L, long-press full picker) + single sticky present/absent/late summary bar replacing KPI cards; same marking workflow/gate/draft/RBAC. **§2.2/2.5 shared marks grid** (`43c8f8df`) — new `lib/shared/marks_grid/` kit (`MarksEntryField`·`MarksCellSaveDot`·`MarksColumnStats`) rendered by BOTH chains; teacher grid fully upgraded (focus chaining + per-cell dot + Save-all reusing its existing save path + column stats + locked number-pad); certified admin chain adopts the shared footer additively (AB/ML/DB · REL-5 · grace · save-all · REL-3 draft untouched). Files: `lib/features/finance/**`, `lib/features/management/approval/**`, `supabase/functions/_shared/approval/**`, `lib/features/teacher/attendance/**`, `lib/features/teacher/exams/**`, `lib/features/academics/exam_admin/**`, `lib/shared/{marks_grid,widgets/akshara_queued_view}.dart`, `lib/core/testing/qa_test_keys.dart` + tests. | **PASS** (EOS UX) | `flutter analyze` 0; full suite **3711 pass / 1 skip / 2 fail (2 known UX-7 only, 0 new)**; deno approval **62/0** (+3) + `deno check` green; **+~40 tests**; 3 approval goldens regenerated deliberately (no other golden churn); certified exam-marks + money paths intact; SoD server-owned + strengthened. Report `docs/engineering/eos/EOS_P2-UX-2_FEATURE_REPORT.md`. | B2 (five daily tasks); §1/§5 Product Excellence Master Plan | P2-UX-2 |

| 2026-07-08 | `64613e07` | P2 | **P2-UX-3 — Tier-3 design-system enforcement · COMPLETE** | Pure-additive enforcement (tests + goldens only, **0 `lib/` changes**): the M15 tokens + `AksharaAccessibility` contrast helper + 4 persona shells already existed — nothing enforced them. **DS lint guard** `test/theme/ds_enforcement_test.dart` (no analyzer plugin in-repo → the test IS the lint: scans `lib/features/**`, blocks new violations via a committed ratchet baseline — hex `Color(0x` locked 0 · `Colors.<swatch>` ≤60 · `TextStyle(` ≤159; white/black/transparent allowed). **Contrast checker gate** `test/theme/contrast_checker_test.dart` (runs `criticalSchemePairs` on light+dark — all clear WCAG AA + calibration check). **Persona-shell goldens** `test/golden/persona_shell_golden_test.dart` (admin/teacher/parent/student nav chrome at 390×844 via ShellRoute+role-auth; QA-login off → deterministic; 4 goldens). CI-wiring leg (contrast-in-CI) owner/CI-gated (deferred). | **PASS** (EOS UX) | `flutter analyze` 0; theme + golden suites **142 pass** (+22 new gates: 3 DS + 15 contrast + 4 persona goldens); no production code → rest of suite unaffected. | UX-4 / Tier-3 DS enforcement | P2-UX-3 |

| 2026-07-08 | `d412b78b` | P2 | **P2-UX-4 — Accessibility pass · COMPLETE (closes UX-7)** | **lib (5):** `theme/accessibility.dart` (+`clampDenseGridTextScale` [the ONE permitted dense-grid clamp, cap 1.3×] +`onColorFor` max-contrast glyph +`maxDenseGridTextScale`), `shared/widgets/akshara_navigation.dart` (`AksharaAppBarIconButton` now announces an accessible name `semanticLabel ?? tooltip` — was an unlabeled icon-only button), `shared/widgets/akshara_quick_action_card.dart` (**UX-7**: narrow <162px two-column cells get a taller aspect so 2-line labels don't overflow at 360×640; golden viewports ≥390px → zero churn), `features/teacher/attendance/widgets/attendance_exception_grid.dart` (dense-grid 1.3× clamp + **badge glyph colour-by-contrast** fixing a dark-mode white-on-bright-tone ≈1.7:1 WCAG failure), `shared/marks_grid/marks_grid.dart` (dense-cell 1.3× clamp). **tests (+21):** `test/theme/rendered_contrast_audit_test.dart` (rendered contrast beyond the token-pair gate — 7 KpiAccent tones × badge/tile pairs + alpha-blended freshness chip, light+dark), `test/theme/tap_target_lint_test.dart` (theme 48dp lint + `androidTapTargetGuideline`/`labeledTapTargetGuideline`), `test/a11y/semantics_daily_flows_test.dart` (screen-reader labels on the daily-flow surfaces), `test/a11y/dense_grid_text_scale_test.dart` (clamp + no-overflow at 2.0× + `onColorFor`). Presentation-only; 0 `supabase/`. | **PASS** (EOS UX; no behaviour/governance/money/RBAC/nav/isolation regression) | analyze **0**; full suite **3756 pass / 1 skip / 0 fail** (**2 known UX-7 RESOLVED**); goldens **70/70 unchanged**; +21 a11y tests. Tracked (not a blocker): dark `tertiary` tone chip contrast ≈2.5:1 → P2-UX-5 dark-theme contrast pass. | EOS a11y gap / C-ISS-13 | P2-UX-4 |

| 2026-07-08 | `dd471f2e` | P2 | **P2-UX-5 — Dark-theme user toggle · COMPLETE (🎉 Phase P2 EXIT)** | Discovery-first: the toggle is **already built + wired** (`lib/theme/theme_mode_storage.dart` + `theme_mode_provider.dart`, `features/settings/appearance_settings_screen.dart` Light/Dark/System, `app/app.dart` `MaterialApp.themeMode`, routed + `test/theme/theme_mode_preference_test.dart`; default Light) — per EOS rule #4 **no rebuild**. Closed the one carried gap (dark `tertiary` contrast): `lib/theme/color_tokens.dart` (`tertiaryContainer` dark `#134E4A`→`#0F3D38`: **2.53:1→3.21:1**), `test/theme/rendered_contrast_audit_test.dart` (pin raised to the uniform 3.0 floor — all tones clear WCAG AA both schemes), 4 dark parent-dashboard goldens re-baselined (the parent reminder banner fills with `tertiaryContainer`). | **PASS** (EOS UX; token/presentation-only, no behaviour/governance regression) | analyze **0**; theme 82/82; goldens 70/70; full suite **3756 pass / 1 skip / 0 fail**. | UX-6 | P2-UX-5 |

| 2026-07-08 | `31d22f96` | CI | **CI-C1 — Blueprint templates + template-aware solver (keystone Curriculum engine wave)** | `supabase/migrations/20260854000000_edu_blueprint_templates.sql` (v3.0 §9.1 — **CREATE-only, dormant**, FORCE RLS + school-scope + platform-read + narrow `erp_tenant` GRANT no-DELETE; **zero data seed**), `supabase/functions/_shared/education/education_blueprint_solver.ts` (**+473/−0 additive**: `solveBlueprint` early-returns via new pure `solveTemplateBlueprint` ONLY when `spec.template` set — else the certified legacy path, unchanged; template path honours sections/slot-groups · internal-choice pools · chapter-weightage + cognitive/difficulty quotas as HARD constraints · full-bank no-100-cap; unfillable⇒honest gaps I3/I4; bank-first deterministic, zero runtime AI), `education_blueprint_template_test.ts` (+16: generic board/exam-agnostic fixture + 2 new goldens in a **separate** snap + sections/quotas/choice-pool/weightage/HOTS/R-U-cap/no-cap/determinism/dedup/governance/seam). **Local-storage lock honored:** real CBSE blueprints = LOCAL reference only; **no prod DB seed / VPS import**. | **PASS** (EOS FEATURE — additive seam; certified golden byte-unchanged; invariants I1–I8 intact) | **verified independently:** certified golden byte-stable (no `--update`); education deno **63/0** (47 baseline + 16 new); `deno check` clean; migration dormant (no seed/DROP/DELETE); changed set = 4 in-ownership files only. | CI-C1 | CI-C1 |

| 2026-07-08 | `fab2807f` | CI | **CI-C3 — Multi-set papers + PDF v2 + JSON v2 export** | `education_paper_export.ts` (NEW — `buildPaperDocumentV2`: deterministic multi-set A/B/C, master=identity + seeded within-section Fisher-Yates + MCQ option-scramble, per-set realigned key, **key separation**; pure seeded PRNG, zero runtime AI; AI-1 approved-only gate via `paperV2InputFromStored`), `education_handlers.ts` (additive opt-in branch — v1 byte-identical unless `?format=v2`/`?sets`), `lib/core/reports/akshara_report_export_service.dart` (**rides XCT-1** — `buildQuestionPaperV2Pdf` via shared `buildGridTable`; branded sections/instructions + key on own page; new decoupled view-models — no existing caller changed), `lib/features/education/education_pdf_service.dart` (`printQuestionPaperV2` opt-in delegator; v1 untouched), +2 test files (deno +12 incl. JSON v2 golden; flutter +4 PDF v2). **Local-storage lock honored:** local fixtures only, no prod seed/VPS. | **PASS** (EOS FEATURE — additive/dormant; v1 byte-identical; certified goldens byte-unchanged; no XCT-1 caller regressed; I1–I8 intact) | **verified independently:** education deno **75/0** (63+12), deno check clean, flutter analyze 0, reports+education flutter tests green; existing solver+template `.snap` byte-stable. | CI-C3 | CI-C3 |

| 2026-07-08 | `2379a529` | CI | **CI-C7 — Exam Profile Engine (profiles · weighting · compatibility · gating)** | `supabase/migrations/20260855000000_edu_exam_profiles.sql` (dormant, CREATE-only, no seed — profile scope + `selection_weighting` + time/reasoning/diagram + `question_family_distribution` [A1-11] + `foundation`/`allow_scope_extension`/`additional_chapters` [A1-3] + optional `entitlement_slug` [B7]; certified RLS shape mirrors CI-C1), `education_exam_profile.ts` (NEW pure/deterministic zero-AI: `orderBankForProfile` weighting [empty profile = certified `sortBank` order], `validateProfileCompatibility` "never silently downgrade" [explicit `INSUFFICIENT_*`/`SCOPE_WIDENING_FORBIDDEN`], **A1-3 depth-not-scope**, `resolveExamProfileGating` **B7 default-allow**), `education_question_paper_service.ts` (optional 4th param `profile?` + guarded seam — no profile ⇒ `solveBank === bank`, byte-identical; handler unchanged), +19 tests. **Local-storage lock honored:** no prod seed/VPS. | **PASS** (EOS FEATURE — additive/dormant; production byte-identical; certified goldens byte-stable; I1–I8 intact) | **verified independently:** education deno **94/0** (75+19), deno check clean; solver+template+export `.snap` byte-stable; handler + `lib/` untouched. | CI-C7 | CI-C7 |

| 2026-07-08 | `e62c367c` | CI | **CI-C8 — Item rotation-cooldown policy + selection-reason explainability** | (worktree-isolated agent → integrated + re-verified in-tree.) `supabase/migrations/20260856000000_edu_item_rotation_policy.sql` (dormant `edu_item_rotation_policies` — cooldown/max-uses/deprioritize + scope; CREATE-only, no seed; certified RLS shape). **No duplication** of E1a's `edu_item_exposures`/`times_used`/`last_used_at` (already dormant-seeded). `education_item_rotation.ts` (pure `orderBankForRotation` — filtered/reordered COPY the certified solver walks; **byte-identical when absent** — `rotationCompare`→`canonicalCompare`; explicit `referenceAt`, no `Date.now`), `education_selection_reason.ts` (pure read-only `explainSelection` — per-item reason + per-gap cause; legacy + template paths; zero AI/DB-write), `education_types.ts` (+9 optional `times_used?`/`last_used_at?`), +19 tests. **Local-storage lock honored:** no prod seed/VPS. | **PASS** (EOS FEATURE — additive/dormant; certified goldens byte-stable; I1–I8 intact) | **verified independently in-tree:** education deno **-A 113/0** (94+19), deno check clean; solver/template/export `.snap` byte-stable; cherry-picked cleanly (parent = branch tip), 0 out-of-scope leak. | CI-C8 | CI-C8 |

*(Wave-2 onward: the executing session appends one row per task as each passes EOS and commits.)*

---

## 5. How to use this journal (executor)

1. Complete a wave through the Autonomous-Plan loop (implement → validate → regression → docs → `/eos`).
2. **Only on EOS PASS**, commit.
3. **Immediately** append one row here with the real commit SHA + evidence + finding + roadmap id.
4. Flip the wave's Status in the roadmap and its finding in the ledger.
5. Add the EOS verdict to `docs/engineering/eos/EOS_RUN_LEDGER.md`.
6. Never edit past rows; the journal is append-only and permanent.

*Progress dashboard (roadmap §0) + this journal must always agree. If they diverge, execution has drifted — halt and reconcile (Autonomous Plan §9).*
