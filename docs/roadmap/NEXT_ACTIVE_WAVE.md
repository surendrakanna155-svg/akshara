# Akshara ERP — NEXT ACTIVE WAVE

**This is the ONLY file Opus 4.8 reads before each autonomous wave** — together with its state companion [`../execution/EXECUTION_DASHBOARD.md`](../execution/EXECUTION_DASHBOARD.md). Keep it small — current work only.
**Updated by:** the executor at each wave boundary (on EOS PASS + commit → advance to the next wave; refresh the dashboard at the same moment).
**Authority:** [`FINAL_EXECUTION_MASTER_ROADMAP.md`](FINAL_EXECUTION_MASTER_ROADMAP.md) · run per [`AUTONOMOUS_EXECUTION_PLAN.md`](AUTONOMOUS_EXECUTION_PLAN.md) · journal to [`../execution/IMPLEMENTATION_PROGRESS.md`](../execution/IMPLEMENTATION_PROGRESS.md).

> **Previous waves:** **P1-CODE-1 (REL-1..5) ✅** · **P1-CODE-2 (REL-6..9) ✅** · **P1-CODE-3 (backend hardening) ✅** · **P1-CODE-5 — HR payroll engine ✅** · **P1-PROD-0 — XCT foundations ✅ COMPLETE (2026-07-04, `83bc267`)** — EOS FOUNDATION PASS. **XCT-1** shared PDF-table primitive `buildGridTable` (3 bespoke tabular-PDF builders consolidated onto the ONE pipeline that ~15 modules already ride). **XCT-2** reminder rail `_shared/reminders/reminders_service.ts` (`scheduleReminder` + `runDueReminders = runDueScheduledBroadcasts` — one runner; a due reminder fires end-to-end into a pending in-app delivery, proven by test). **XCT-3** shared read-only `AksharaDateField` → `showDatePicker` (HR leave create+on-behalf ×4 + probation + intelligence meeting-date). · **P1-PROD-1 — C1 Finance Fee-Recovery CRM ✅ COMPLETE (2026-07-04, `c1b9feb`)** — EOS FEATURE PASS. Discovery-first: FIN-R1/R3/R5 verified existing (not rebuilt). Built **FIN-R2 telecaller call queue** (server-ranked "who to call next" riding the same open/overdue accounts the defaulters list uses — no duplicate source; broken→due→never-contacted→stale ranking; `GET /finance/recovery/call-queue`; client `_CallQueueSection` reusing the existing dialogs; log/PTP re-ranks live) + fixed **FIN-R4** (history sheet → live provider). · **P1-PROD-2 — C2 Finance Counter/Statements/Reports ✅ COMPLETE (2026-07-04, `fa30e00`)** — EOS FEATURE PASS. Discovery-first: FIN-1/2/7/8 verified existing (real exports on the XCT-1 shared pipeline). Closed **FIN-6**: `issueInvoice` drops the hardcoded +30 (uses `due_days` + generates the schedule); new shared `finance_aging.overdueDaysSql` makes **installment due dates drive aging** across defaulters / recovery call-queue / finance-intelligence / student-risk (behaviour-preserving for the default single-term config). Backend-only · analyze 0 · deno finance 147/0 · intelligence 51/0. · **P1-PROD-3 — C4 Exams Fast Marks & Tabulation ✅ VERIFIED COMPLETE (2026-07-04, no code)** — discovery-first found EXM-1/2/3 already built + tested (bulk marks save + keyboard nav + validation + AB/ML/DB; tabulation totals/%/rank/grade with present-only exclusion; register CSV/PDF via shared service). Per EOS rule #4 no rebuild. deno exam 118/0 · flutter exam client 109/0. · **P1-PROD-4 — C5 Academic Registers & Certificates ✅ COMPLETE (2026-07-04, `1fc6104`)** — discovery-first found ATT-1 (office register) / ATT-2 (monthly students×days grid) / SIS-1 (certs) all built (real SQL + shared export + immutable cert register). Closed one gap: ATT-2 cell-level render test. attendance+sis 73/0. · **P1-PROD-5 — C7 HR Payroll & Salary Registers ✅ COMPLETE (2026-07-04, `524105e`)** — discovery-first found HR-1 register + HR-2 batch payslips already built; closed HR-2's individual per-employee payslip PDF (`buildPayslipPdf` + picker sheet). Client-only, read-only. analyze 0 · suite 3616 pass (0 new). · **P1-PROD-6 — C8 Transport Fleet, Roster & Fee ✅ VERIFIED COMPLETE (2026-07-04, no code)** — discovery-first found TRN-1/2/3/4/9 all built; **money boundary CONFIRMED intact + test-enforced** (zero payment code in Transport; get-or-create per-year account; Finance = sole payment engine). One candidate gap (TRN-9 app-level dedupe race) money-contained + unsafe to hot-patch → tracked `TRN9-DEDUPE`. deno transport 36/0 · finance-assignments 12/0 · flutter transport 42/0. · **P1-PROD-7 — C9 Inventory·Library·Communication ✅ COMPLETE (2026-07-04, `2ecee77`)** — discovery-first found all six (INV-1/2·LIB-1/2·COM-1/2) built (inventory governance intact: FOR UPDATE lock, negative-block 422+DB CHECK, immutable ledger, maker-checker SoD 409; memory was stale). Closed one gap: LIB-2 bulk-import test via a behavior-preserving `planImportRow` extraction. deno inv+lib 76/0 · comm 27/0. · **P1-PROD-8 — C10 Principal Approval Center batch ✅ COMPLETE (2026-07-04, `7c9294b`)** — PRI-1 batch approve/reject already built; **closed a money-SoD gap**: `decideApproval` self-approve guard was inventoryPo-only → extended to `{inventoryPo, feeConcession, refund, feeStructure}` so a requester can't approve their OWN money waiver (single+batch; FIN-D4). deno approval 59/0. · **P1-PROD-9 — C11 Admissions productivity ✅ COMPLETE (2026-07-04, `10f8461`)** — ADM-2/3/4/5 verified built + tested (auto-log, bulk actions, follow-ups inline, real lead picker; admission-approval SoD-gated). Closed ADM-1 PDF export gap (`shareGridPdf` wired; CSV+PDF). analyze 0 · suite 3618 pass (0 new) · admissions 110/0. · **P1-PROD-13 — C15 HR & SIS productivity ✅ COMPLETE (2026-07-06, `4b65ce1`)** — parallel HR∥SIS discovery + parallel build under disjoint ownership (HR-3 SoD by main, SIS-2/5 by a delegated agent). HR-3 batch leave / HR-4 leave-balance report / HR-7 directory export / SIS-2 richer registry / SIS-5 transfer-exit log verified built. Closed: **HR-3 leave SoD** (owner 2026-07-06: deny self-approval → approver ≠ `createdBy` filer, single+batch, 403 `LEAVE_SELF_APPROVE_DENIED`, mirrors C10); **SIS-2** dedicated class-list + parent contact-sheet exports (XCT-1); **SIS-5** exit reason on the transfer/exit log (read-only correlated sub-select to the latest transfer-certificate reason, org/school-scoped). analyze 0 · HR deno 90/0 · SIS deno 139/0 (+1 pre-existing ISO-COUNT) · +~11 tests · suite no-new-fail (2 known UX-7). · **P1-PROD-12 — C14 Teacher & Attendance productivity ✅ COMPLETE (2026-07-06, `9e5602a`)** — discovery-first: TCH-4 (weekly timetable + server-backed cover/substitution alert), ATT-3 (absentees-only fast-mark), ATT-4 (office not-yet-marked monitor) verified built. Closed **TCH-1** (today-schedule-row tap → mark attendance for that class via `schedule_attendance_<label>` → `teacherAttendance?class=`, was going to the weekly timetable), **TCH-2** (teacher-home marks task now reflects overdue — reuses EXM-6 `MarksEntryProgress.isOverdue`, urgent tone + "Marks overdue"), **TCH-3** (my-class marks-summary export on the XCT-1 grid pipeline + export action on the exams screen). Attendance integrity untouched (nav-only); client-only. analyze 0 · +4 tests · golden unchanged · suite no-new-fail (2 known UX-7). · **P1-PROD-11 — C13 Academic-work productivity, EXAMS HALF ✅ COMPLETE (2026-07-06, `0a8c2a3`)** — discovery-first: EXM-4 (merit + subject topper), EXM-5 (pass/fail + grade-distribution), EXM-7 (datesheet PDF) verified already built (backend + client + CSV/PDF). Closed **EXM-6** — the **first caller of the XCT-2 reminder rail**: `handleRemindPendingMarks` (manageExams) finds exams past `marks_entry_deadline` with marks still pending (checked at trigger → no false reminder), schedules ONE in-app `all_teachers` reminder via `scheduleReminder` (fires through the scheduled-broadcast runner); audited `exam.marks_reminder.sent`; route `POST /academics/exams/marks/remind`. Client: deadline + Overdue badge on the marks-progress board + manageExams-gated "Remind teachers" action. Exam-result governance untouched. analyze 0 · deno exam 124/0 + reminders/audit 24/0 · +12 tests · suite no-new-fail (2 known UX-7). **Homework half HWK-3..8 DEFERS with C6.** · **P1-PROD-10 — C12 Finance productivity & receipting ✅ COMPLETE (2026-07-06, `2bd7ecd`)** — discovery-first: FIN-4 (duplicate reprint DUPLICATE+audit) · FIN-5 (batch print) · FIN-9 (outstanding/aging analytics) verified already built. Closed **FIN-3** (Indian lakh/crore digit grouping on the receipt PDF via shared `indianDigitGroups`, consistent with the existing Indian amount-in-words). Built **FIN-R7** cheque/DD/PDC + bounce tracking on the `finance_offline_payments` **tracking ledger** (adds `pdc` method, `instrument_date`/`bank_name`, terminal `bounced` status + audit) — **money-safe**: that ledger never posts to `finance_collections`, so a bounce reverses NO money (test-enforced); full stack migration→repo→handler→route→audit→client. **FIN-R6**: **FIN-D6 RESOLVED** (owner 2026-07-06: *principal sets, collectors see own*) → wired collection targets + attainment onto the recovery dashboard (reuses FIN-R5 `collectorPerformance` + `listRecoveryTargets`), principal-gated set-target. Money-safety tripwire intact (Finance = sole engine; targets/reprints/analytics read-only). analyze 0 · deno finance+audit 172/0 · +13 tests · suite no-new-fail (2 known UX-7). · **P1-PROD-14 — C16 Transport & Inventory productivity ✅ COMPLETE (2026-07-06, `5eba37d`)** — parallel Transport∥Inventory discovery + build under disjoint ownership (TRN-8 by a transport agent, INV-7/INV-5 by an inventory agent, EXM-6 one-liner by main). TRN-5 bulk allocation / TRN-6 exports / TRN-7 capacity guard / INV-3 governed adjust / INV-4 low-stock+raise-PO / INV-6 stock-take verified built. Closed: **TRN-8** onto the XCT-2 rail (`runDocumentExpiryReminder`, ONE `all_staff` digest, EXM-6 pattern) **fixing a latent defect** — old `audience:"staff"` violated the `comm_broadcasts` CHECK whenever ≥1 expiry existed (regression-guard test pins the token set); **INV-7** low-stock alert re-targeted to a new `storekeepers` audience on XCT-2 (migration `20260851` widens the CHECK — RESTORING `all_staff`, accidentally dropped by `20260838` — and seeds the never-seeded storekeeper role/grants; all-staff fallback; SAVEPOINT-isolated + pending-dedupe so alerting can never fail a posted issue); **INV-5** GRN register endpoint + XCT-1 export. Bonus: EXM-6 reminderId reads `broadcastId`; ISO-COUNT comm tripwire 220→233. Stock governance untouched (12/12); Transport demand-only (payment-free test green). analyze 0 · deno transport 40/0 · inv_finance 29/0 · comm 104/0 · exams 124/0 · +17 tests · suite 3643 pass, no-new-fail (2 known UX-7). · **P1-PROD-15 — C17 Library & Communication ✅ COMPLETE (2026-07-07)** — EOS FEATURE PASS. Discovery-first verified **LIB-3** (keyed/keyboard-wedge ISBN quick issue+return; camera = staged seam), **LIB-4** (renewal `handleRenewLoan`), **COM-3** (resend-to-unread `resendBroadcastToUnread`), **COM-4** (schedule-send — `scheduled_at` already active; only the cron trigger to POST `/communications/broadcasts/run-scheduled` remains = live-lane/deploy), **COM-5** (save-as-template; audience reuse = the separate built `comm_audience_segments`) all built — per EOS rule #4 no rebuild. Closed **LIB-5** — the ONE genuine gap: re-pointed `handleSendOverdueReminders` off the IMMEDIATE blast-all-parents `sendBroadcastMessage` onto the XCT-2 rail (`scheduleReminder`) via a testable core `runOverdueReminder` (TRN-8 pattern) — ONE `all_parents` digest, corrected copy, reminderId in audit + response. Backend-only. analyze 0 · deno library 59/0 (+2 LIB-5 rail tests) · reminders 5/0 · comm 104/0. · **P1-PROD-16 — C18 Leadership productivity ✅ COMPLETE (2026-07-07, `c2306daa`)** — EOS FEATURE PASS. Discovery-first: **PRI-2** (unsubmitted-marks exception card), **PRI-3** (daily school report → PDF on XCT-1), **DIR-1** (cross-school league table `rankDirectorSchools`), **DIR-2** (consolidated collection report + XCT-1 exporters) all built + wired (pre-wave commits `b1dd65c4`/`1e7e7186`); DIR-1/2 already tested. Closed the two verified test-coverage gaps: PRI-2 card (×3) + PRI-3 export (×2). All read-only (approval maker-checker untouched); no migration; test-only. analyze 0 · +5 tests. · **P1-PROD-17 — C19 Parent self-service ✅ VERIFIED COMPLETE (2026-07-07, no code)** — EOS FEATURE PASS. Discovery-first found **all five PAR items built + tested + wired**: PAR-1 PTM RSVP (`handleMeetingRsvp`, own-child), PAR-2 Apply-Leave quick action (`apply_leave`→`parent_navigation.dart:43`), PAR-3 medical-cert attach (own-child + pending guards, audited), PAR-4 payment-history export (read-only XCT-1), PAR-5 proactive reminder banners (from real module data). Per EOS rule #4 no rebuild. Parent RLS strong + multi-layer (no cross-family leakage). Two documented residuals tracked (not gaps): PAR3-UPLOAD (reference-only attach → owner batch) + PAR-5 client-derived (by design). deno parent+pilot 31/0 · flutter parent 21/0. · **P1-PROD-18 — C20 Teacher & Leadership polish ✅ COMPLETE (2026-07-07, `8cd87436`)** — EOS FEATURE PASS. Discovery-first: TCH-5 create-homework quick action (**real `due_date` contract already live → NOT blocked by HWK-1; roadmap note stale**), TCH-6 pending-task deep-link, TCH-7 timetable export (XCT-1), DIR-3 school-comparison CSV/PDF, PRI-4 weekly digest card, PRI-5 stale-approval banner — all built. Closed the two verified test gaps: PRI-4 card (×3) + PRI-5 banner (×3). PRI-5 read/filter-only → SoD intact. Tracked residuals: PRI-4/5 scheduled-send + DIR-3 native .xlsx (owner decisions). Test-only. analyze 0 · +6 tests. · **P1-PROD-19 — C21 Records & Parent polish ✅ COMPLETE (2026-07-07, `799713f1`) — LAST Phase-C wave** — EOS FEATURE PASS. Discovery-first: SIS-3 (audited doc verify), HR-5 (headcount-by-dept + XCT-1), PAR-6 (next-PTM hero + action-items, own-child RLS) all verified built. **Built SIS-4 — the one real feature gap: clerk family/sibling view** (`GET /sis/students/:id/siblings`, viewSis, org+school scoped; shared-active-guardian join, self-excluded; **cross-school isolation structural + tested**; full client stack + profile section). Read-only, no migration. SIS flutter 126/126 (+5) · deno SIS 150/1 (only the tracked pre-existing ISO-COUNT) · +13 tests. **🎉 Phase-C module program (C0–C21) COMPLETE.** · **P1-CI-0 — Curriculum-engine seam ✅ COMPLETE (2026-07-07, `ba47f065`)** — EOS FEATURE PASS, additive-only, ZERO behaviour change, certified invariants I1–I8 intact. **CI-0a** golden-pinned `education_blueprint_solver.ts` byte-stable (new golden test + `.snap`: 5 template-absent totals + explicit-mix + solveBlueprint match/fallback/dedup/gap; **solver untouched**). **CI-0b** `edu_exam_paper_links` (v3.0 §5.2) migration + `linkExamToPaper`/`getExamPaperLink` repo (additive seam, NOT wired to publish) + 5 tests. **CI-0c** dormant E1a seed migration (response spine + trust columns [existing→'trusted'] + exposure; zero UI). analyze 0 · education deno 47/0 (+8) · deno check green. **▶ P1 non-gated lane COMPLETE.**

---

## ▶ CURRENT

- **Phase:** **🎉 P2 — UI/UX COMPLETE (2026-07-08)** — all 5 sub-waves done (**P2-UX-1..5 ✅**). **Two lanes now run in parallel** (owner policy 2026-07-08):
  - **ERP lane:** next phase = **P3 — Adaptive AI** (P3-AI-1 cost/safety foundation). ⏸ **owner-timing-gated** ([[adaptive-ai-product-intelligence-vision]] "DO NOT build yet"; the endgame may route roadmap-gaps → Fable UI/UX audit → UI improvements → Adaptive AI). Remaining P1 (P1-CODE-4/6/7/8) owner-gated. **Note:** P3-AI-1 also unblocks the Curriculum AI waves (CI-C5/C6/C10).
  - **Curriculum lane (ACTIVE):** Option A superseded → engine layer running pre-pilot. **✅ CBSE Class-X SQP acquisition · ✅ CI-C1 (`31d22f96`) · ✅ CI-C3 (`fab2807f`) · ✅ CI-C7 Exam Profile Engine (`2379a529` — profiles/weighting/compatibility/gating, additive/dormant, production byte-identical, deno 94/0).** **Next CI code = CI-C8** (paper↔exam exposure/rotation + selection-reason, zero-dep) **then CI-C4-schema** (single agent in `education/**`). 🟡 A2 pending ratification. AI waves (C5/C6/C10/C11) gated on P3-AI-1 + concepts; full-board acquisition ⏳ owner/network-gated. **🔒 curriculum data local-only; VPS = app-server only; no prod seed.**
  - **🔒 Infra decision (locked 2026-07-08):** curriculum data stays **local + in Git**; **VPS = app-server only**; **no production DB import / VPS storage dependency for curriculum yet** ([[curriculum-local-storage-decision]]). CI-C1 builds schema+engine+**local fixtures**, does NOT seed the production DB.
- **⚙ EXECUTION POLICY (owner, 2026-07-08):** run **both lanes (ERP P2→P8 ∥ Curriculum CI-A*→CI-C*) in parallel**, never blocking one for the other. **Maximize parallel implementation agents** — 1 primary ERP agent + 1 primary Curriculum agent + extra agents per **disjoint** submodule; unlimited read-only discovery. **At every new wave open with a short execution plan** (parallel work packages · assigned agents · shared-file owner · merge point) *before* building. Strict file-ownership: never two implementation agents on the same file/logical module; shared docs (roadmap/dashboard/journal/ledger/indexes) have exactly ONE owner agent per wave; shared-file tasks run sequentially.
- **✅ P2-UX-4 — Accessibility pass COMPLETE (2026-07-08, `d412b78b`).** EOS UX PASS (inline; ledger). Verification+lint layer over already-built a11y primitives (Polish §7 / C-ISS-13) + the genuine gaps it surfaced. **Semantics:** `AksharaAppBarIconButton` announces `semanticLabel ?? tooltip` (was unlabeled icon-only). **Tap-targets:** theme-48dp lint + `androidTapTargetGuideline`/`labeledTapTargetGuideline`. **Dynamic type:** shared `clampDenseGridTextScale` (cap 1.3× — the ONLY permitted clamp) on the attendance exception grid + marks cell. **Contrast:** rendered audit beyond the token-pair gate (7 KpiAccent tones × badge/tile + alpha-blended freshness chip, light+dark) — **found + FIXED a dark-mode badge white-on-bright-tone ≈1.7:1 failure** via `onColorFor`. **Closes UX-7** (TeacherDashboard 360×640 overflow — width-gated quick-action aspect, golden-safe). analyze 0; full suite **3756 pass / 1 skip / 0 fail** (**2 known UX-7 RESOLVED**); goldens **70/70 unchanged**; +21 a11y tests. **⚠ Tracked → P2-UX-5:** dark `tertiary` tone chip contrast ≈2.5:1 (dark-theme contrast pass).
- **✅ P2-UX-3 — Tier-3 design-system enforcement COMPLETE (2026-07-08, `64613e07`).** EOS UX PASS (inline). Pure-additive (tests + goldens only, **0 `lib/` changes**): the M15 tokens + `AksharaAccessibility` contrast helper + persona shells already existed — added the three missing GATES. **DS lint guard** (`ds_enforcement_test.dart` — the test IS the lint: scans `lib/features/**`, blocks new violations via a ratchet baseline: hex `Color(0x` locked 0 · `Colors.<swatch>` ≤60 · `TextStyle(` ≤159) · **contrast checker gate** (`contrast_checker_test.dart` — `criticalSchemePairs` clear WCAG AA in light+dark) · **persona-shell goldens** (admin/teacher/parent/student chrome at 390×844). analyze 0; theme+golden 142 pass (+22 gates). **⚠ CI-wiring leg (contrast-checker-in-CI) is owner/CI-gated — deferred** (consistent with the standing live-lane deferral).
- **✅ P2-UX-2 — "the five daily tasks" (B2) COMPLETE (2026-07-08).** EOS UX PASS (`docs/engineering/eos/EOS_P2-UX-2_FEATURE_REPORT.md`). Quick-win sub-waves (`88d76df1` haptics · `723927b7` A5 nav + A8 keyboard · `fb781751` C2 density + SIS toggle · `fec526e5` fee ceremony) + four heavy builds: **§2.4 fee counter** (`ce45cf87` — real dues prefill killing the hardcoded `5000` + dues-breakdown line + PSID/name/class "Collect fee" origination + shared **`AksharaQueuedView`** amber offline receipt) · **§2.3 Approvals Inbox** (`b34f2116` — **server-owned `sodBlocked`** maker-checker flag [no client SoD] + badge + Approve-disable + swipe + type-grouping/critical-sort + on-card facts) · **§2.1 attendance exception grid** (`87841013` — compact tap-cycle grid + single sticky present/absent/late bar; same marking workflow) · **§2.2/2.5 shared marks grid** (`43c8f8df` — ONE `lib/shared/marks_grid/` kit both chains render; teacher grid upgraded, certified admin path untouched). analyze 0 · full suite **3711 pass / 2 known UX-7 (0 new)** · deno approval 62/0 · +~40 tests · 3 approval goldens deliberate. **Residual (tracked): raw `.when()` bulk migration carried forward** (per-screen; not a P2-UX-2 exit blocker).
- **🎉 Phase-C module program (C0–C21) ✅ COMPLETE (2026-07-07)** — all 22 C-waves done (minus deferred **C3** GA-1-live, **C6 + C13-Homework-half** HWK-1-schema *(⚠ basis may be stale — see below)*). **C1/C2/C4/C5/C7/C8/C9/C10/C11/C12/C13-Exams/C14/C15/C16/C17/C18/C19/C20/C21** all landed.
- **✅ P1-CI-0 — Curriculum-engine seam wave COMPLETE (2026-07-07, `ba47f065`)** — golden-pinned `education_blueprint_solver.ts` byte-stable (solver untouched) + `edu_exam_paper_links` (v3.0 §5.2, additive seam) + dormant E1a seed migrations. **▶ P1 NON-GATED LANE COMPLETE.**
- **✅ P2-UX-1 — "feel & trust" pack COMPLETE (2026-07-08).** Slice 1 (`aeb79938`) `AksharaSuccessView` + `AksharaHaptics` + **A6** kill fake sparklines. Slice 2 (5 sub-commits `e25d1282`·`f829c473`·`9424b722`·`f1144250`·`b316e849`): **`AksharaSkeleton`** + skeleton slot on `AksharaLoadingState`/`ErpAsyncBody`/`MobileAsyncBody` wired to **11 dashboards** (3 mobile + 8 module; loading TYPE preserved → tests green, goldens render data → 0 churn) · **A2** `AksharaFreshnessChip` (lightweight connectivity, timer-safe) on money/attendance · **A3** `TextStyle.tabularFigures` on all KPI values (0 golden churn) · **A4** pull-to-refresh on the mobile dashboards · **`AksharaDraftChip`** (parent leave) + **`AksharaUnsavedChangesScope`** PopScope · **copy dictionary** `AksharaCopy` + **permission-as-its-own-state** · **A9** empty-state verified. **Residual (tracked, not a gap):** the raw `AsyncValue.when()` bulk migration (142 sites) is per-screen work deferred to **P2-UX-2's grid pass** — the skeleton capability + all wrapper-using screens are done. EOS UX PASS · analyze 0 · suite 3686/2-known-UX-7. *(Remaining P1 owner-gated: **P1-CODE-4** Identity + **P1-CODE-6/7/8** module scope.)*
- **App code lane — ✅ P2-UX-2 + P2-UX-3 COMPLETE (2026-07-08).** P2-UX-3 added the design-system enforcement gates (DS lint guard · contrast gate · persona-shell goldens; pure-additive). **Next executable app-lane wave = P2-UX-4 (accessibility pass) — a NEW sub-wave; ⏸ awaiting owner approval to open** (see Next + Exit criteria below). ⚠ P2-UX-3's contrast-checker-in-CI leg stays owner/CI-gated.
- **CI-DATA lane (∥, `curriculum/` tree ONLY):** **CI-A0 remainder ✅ COMPLETE** (2026-07-07) — full MCIP Part-04 dir standard (11 top-level dirs + subdirs), all 8 Part-02 PM files, 11 index JSONs + 14 reports + 7 logs seeded, `scripts/maintenance/{validate_structure,dry_run}.py`; **dry-run cycle proven** (one synthetic PDF through discover→download→verify→organize→metadata→index→audit→certify in an isolated temp workspace); **D-5 gate correctly NOT-CERTIFIED** on the empty repo; tests **22/22** (13 existing + 9 new). **Next CI-DATA wave = CI-A1 CBSE acquisition — ⏳ OWNER/NETWORK-GATED:** needs a networked environment + a legal/licence review (Risk R1) to resolve per-cell `source_url`s and fetch real content (`downloader.py` https path is guarded behind `--allow-network`); catalogues emit `PENDING_DISCOVERY` until then. **P1-CI-0 (the network-free CI code seam) is now ✅ landed** (`ba47f065`) — golden-pin + `edu_exam_paper_links` + dormant E1a; it precedes P4-RT-0 as required. The next CI code wave (CI-C1 templates + solver upgrade) depends on CI-B3 (a hand-transcribed CBSE SQP template) — data-lane/owner-gated. *(AIMS spec-check: no mechanical change to CI-A0/A1/P1-CI-0; deltas land in CI-B4 + CI-C10/C11.)* **🟡 Planning proposal A2** (deterministic per-student practice/DPP engine; owner-approved direction + D-7 + I9, ratification pending) is tracked in [`../curriculum-intelligence/proposals/README.md`](../curriculum-intelligence/proposals/README.md) — it **extends** CI-C10/C1/C3/C8 + v3.0 §13 on ratification.
> **⚑ CI-DATA vs CI-CODE split (owner directive 2026-07-08 — [`../curriculum-intelligence/planning/CONTENT_DEPENDENCY_MAP.md`](../curriculum-intelligence/planning/CONTENT_DEPENDENCY_MAP.md)):** the Curriculum lane is **NOT** fully blocked. Only **content-acquisition** (CI-A1..A6 + the CI-B1/B2/B4 datasets) is network/licence-gated. The **deterministic engine + schema + APIs + tests + docs are content-independent and RUNNABLE NOW** on fixtures: **CI-C8** (zero-dep additive), **CI-C1** (template-aware solver, keystone), **CI-C3** (multi-set/export), **CI-C7** (profile engine), **B12 family schema**, **CI-E1b dormant seed**. AI-using waves (CI-C5/C6/C10-engine/C11) stay gated on **P3-AI-1** (governed runtime); C10/C11 also need concepts (CI-B4=content). **⚠ Running the engine layer pre-pilot reverses frozen Option A — owner confirm (see the map §4).** Curriculum code lane owns `supabase/functions/_shared/education/**` + `supabase/migrations/**` + `lib/features/education/**` (disjoint from ERP `lib/theme/**` → true parallel).
- **Sequencing:** P1 non-gated lane COMPLETE; **P2-UX-1 ✅ · P2-UX-2 ✅ · P2-UX-3 ✅**. The next executable app-lane wave is **P2-UX-4** (accessibility pass; depends on P2-UX-3) — **a new sub-wave, ⏸ awaiting owner approval to open**. Remaining P1 (P1-CODE-4/6/7/8) stays owner-gated; live lane owner-deferred.

### P1-PROD tail — all remaining P1 waves are OWNER-GATED (surface in the batch below)
- **P1-CODE-4 (Identity)** — gated on the identity cluster (see the owner-decisions section).
- **P1-CODE-6/7/8 (module scope)** — Finance-posting (MOD-1) / Hostel / Alumni scope decisions.

### 🎉 Phase P2 — UI/UX COMPLETE (2026-07-08) — all 5 sub-waves
> **P2-UX-5 — ✅ ALL EXIT CRITERIA MET (2026-07-08, `dd471f2e`):** dark-theme user toggle — discovery-first found it **already built + wired** (theme_mode storage/provider · appearance-settings Light/Dark/System · `MaterialApp.themeMode` · routed · tested; default Light) → no rebuild; closed the carried dark `tertiary` contrast gap (`tertiaryContainer` #134E4A→#0F3D38, 2.53→**3.21:1**), audit pin raised to uniform 3.0 (all tones clear WCAG AA both schemes), 4 dark parent-dashboard goldens re-baselined. analyze 0; theme 82/82; goldens 70/70; suite 3756/0-fail. EOS UX PASS. **Phase P2 EXIT.**
> **P2-UX-4 — ✅ ALL EXIT CRITERIA MET (2026-07-08, `d412b78b`):** WCAG contrast audit **beyond the token-pair gate** (rendered KpiAccent tones + alpha-blended freshness chip, light+dark — found+fixed a dark badge failure) · screen-reader semantics on the daily-flow surfaces (unlabeled app-bar button fixed) · dynamic-type 1.3× dense-grid clamp · tap-target 48dp lint + guideline · **UX-7 CLOSED** (2 known failures resolved). No behaviour/governance regression; goldens 70/70 unchanged; analyze 0; +21 tests. EOS UX PASS; ledger + journal + this file + dashboard refreshed. **1 item carried → P2-UX-5** (dark `tertiary` chip contrast).
> **P2-UX-3 — ✅ ALL EXIT CRITERIA MET (2026-07-08):** DS lint guard **blocks new violations** (the Done-when) — hex `Color(0x` locked 0 · swatch ≤60 · `TextStyle(` ≤159 ratchets; contrast checker gate (all M15 pairs clear WCAG AA light+dark); 4 persona-shell goldens. Pure-additive (0 `lib/`); analyze 0; +22 gates. EOS UX PASS; ledger + journal + this file + dashboard refreshed. **CI-wiring leg (contrast-in-CI) owner/CI-gated — deferred.**
> **P2-UX-2 — ✅ ALL EXIT CRITERIA MET (2026-07-08):** five daily tasks (attendance exception grid · shared marks grid both chains · fee-counter §2.4 · Approvals-Inbox §2.3 server-provided SoD · A5/A8/C2); no behaviour/governance regression; EOS UX PASS.

### EOS gate
- Scope: **UX (P2-UX-5)** when opened. PASS required (EOS UX per wave). **Tripwire:** no behaviour/governance regression; `flutter analyze` 0 · `flutter test` no NEW failures · dark goldens re-baselined deliberately · `deno test`+`deno check` green for any touched `supabase/functions/**`. **CI-DATA lane** (CI-A1) stays owner/network-gated.
- **P2-UX-4 ✅ EOS UX PASS (2026-07-08, `d412b78b`)** — inline verdict (a11y verification wave; ledger `EOS_RUN_LEDGER.md`); 0 P0 / 0 P1; **2 known UX-7 RESOLVED**; 1 tracked → P2-UX-5 (dark `tertiary` chip contrast).
- **P2-UX-3 ✅ EOS UX PASS (2026-07-08)** — inline verdict (enforcement wave; ledger `EOS_RUN_LEDGER.md`); 0 P0 / 0 P1. CI-wiring leg owner/CI-gated.
- **P2-UX-2 ✅ EOS UX PASS (2026-07-08)** — `docs/engineering/eos/EOS_P2-UX-2_FEATURE_REPORT.md`; 0 P0 / 0 P1 (2 known UX-7).
- **Carried residuals (into a later P2-UX slice):** raw `AsyncValue.when()`→wrapper bulk migration (~142 sites); the DS ratchet baselines (swatch 60 / TextStyle 159) can be lowered as feature code migrates onto tokens — neither is a wave blocker.

### 👤 SURFACED OWNER DECISIONS — identity cluster (gates P1-CODE-4, resolve in a batch)
See [[akshara-decision-queue]] + [[student-identity-architecture-decision]]. These do **not** pause the pipeline; they gate **P1-CODE-4 (Identity finish)** only:
- **PLAT-0** — build Public Student ID (PSID) now vs defer post-pilot.
- **C5 / ADM-D3** — admission-number immutability (set-once-locked vs editable).
- **IC-1…IC-6** — esp. `users.phone` NOT NULL UNIQUE = de-facto identity; is a **change-phone flow (PLAT-4)** in scope for P1-CODE-4, or deferred?
- **SIS-D1** — TC / no-dues gate on identity transitions.
- **Admissions approval SoD** — maker-checker on admission approval.
- **MOD-1 (👤)** — Finance-posting scope also gates the unified recovery-CRM scope (C1 did not reach that edge — call queue rode the existing defaulter source); surface if a later finance wave needs it.
- **HWK-1 / C6 (👤 — ⚠ BASIS MAY BE STALE, flagged C20 2026-07-07)** — the deferral said homework used a free-text `due_label` needing an owner migration to real `due_date DATE`. **C20 discovery found the `due_date DATE` contract is ALREADY LIVE end-to-end** (`edu_homework_assignments.due_date` column exists; `handleTeacherHomeworkCreate` requires+validates `due_date`; client date-picker → DTO `due_date`). **Owner re-check needed:** is C6 (+ HWK reminders/overdue/sorting) actually still blocked, or can it now schedule? The migration keystone appears already in place.
- **PAR3-UPLOAD (👤 NEW, C19)** — parent medical-certificate attach currently persists only a filename/label **reference**, not the file bytes (documented HWK-7 residual). A real storage bucket already exists (admissions uses `storage_service.ts` + presigned URLs). Decision: **wire real parent-leave file upload before pilot, or accept reference-only?** (For medical-cert data this is a real-school consideration — bucket + storage RLS [own-child only] + size/type validation.) Does not block C19; tracked.
- **PRI-4/PRI-5 scheduled-send (👤 NEW, C20)** — the weekly principal digest (PRI-4) + stale-approval escalation (PRI-5) ship as **in-app read surfaces** (card + banner); the code notes "XCT-2/deployment owns the scheduled send." Decision: **do you want a genuine SCHEDULED weekly digest / escalation push** (a new `scheduleReminder` caller, management audience, SoD-preserving), or is the in-app surface sufficient? Does not block C20; tracked.

### Next (two lanes in parallel — owner policy 2026-07-08)
**🎉 Phase P2 (UI/UX) COMPLETE — P2-UX-1..5 ✅.**
- **ERP lane:** next phase = **P3 — Adaptive AI** (P3-AI-1 cost/safety foundation). ⏸ **owner-timing-gated** ([[adaptive-ai-product-intelligence-vision]] — "DO NOT build yet"; endgame may route roadmap-gaps → Fable UI/UX audit → UI improvements → Adaptive AI). Building P3-AI-1 also unblocks the Curriculum AI waves. Remaining P1 owner-gated (**P1-CODE-4** + **P1-CODE-6/7/8**).
- **Curriculum lane (ACTIVE, running now):** CBSE Class-X 2025-26 SQP acquisition → CI-B3 blueprint model → **CI-C1** (keystone solver, schema+engine+**local fixtures only** — no prod seed). AI waves (C5/C6/C10/C11) gated on P3-AI-1 + concepts.
- **Owner-decision batch (open):** **A2 final ratification** · identity cluster (P1-CODE-4) · PAR3-UPLOAD · PRI-4/5 scheduled-send · HWK-1-basis re-check · module-scope (P1-CODE-6/7/8) · **P3 (Adaptive AI) timing** · P2-UX-3 contrast-checker-in-CI wiring. **Resolved 2026-07-08:** Option A superseded (engine pre-pilot) · curriculum local-storage lock ([[curriculum-local-storage-decision]]) · CBSE network acquisition authorized.

> **Deferred live-lane tail (run in the dedicated live phase):** `P0-INFRA-1` off-site backup · `P0-INFRA-3` alert delivery · `P0-TEST-1/2/3` CI + isolation-in-CI + live-regression cron.
> **Tracked pre-existing defect (NOT a wave regression):** `ISO-COUNT` — "tenant isolation probe count" tests assert stale totals vs the registry's actual 233 probes; fails at pre-wave commits too. **C16 repaired the communication one (220→233)**; the remaining stale counts (payment/pilot/sis) still need reconciling + the +13 probes verified. Small, isolated.

### Regression required (per wave)
- `deno test` + `deno check` green for touched `supabase/functions/**` · `flutter analyze` 0 · `flutter test` no NEW failures.

### Exit criteria (NEXT wave, when opened — ⏸ owner-timing-gated)
> **ERP P3-AI-1** (or the endgame's roadmap-gaps / Fable UI/UX audit, per owner) · **Curriculum CI-C1** (keystone solver): each opens with an execution plan (packages/agents/owner/merge) and closes on its own EOS gate (analyze 0 · tests no-new-fail · deno green for touched supabase · goldens deliberate).

> **P2-UX-5 exit criteria — ✅ ALL MET (2026-07-08, `dd471f2e`):** toggle ships (already built; default Light, persisted, routed) · dark contrast pass green (carried dark `tertiary` 2.53→3.21:1) · 4 dark goldens deliberate · analyze 0 · suite 3756/0-fail · EOS UX PASS. **🎉 Phase P2 COMPLETE.**
> **P2-UX-4 exit criteria — ✅ ALL MET (2026-07-08, `d412b78b`):** WCAG contrast audit beyond the token-pair gate · screen-reader semantics · dynamic-type 1.3× dense-grid clamp · tap-target 48dp lint · **UX-7 CLOSED**; goldens 70/70 unchanged; analyze 0; suite 3756/0-fail; +21 tests; EOS UX PASS. 1 item carried → P2-UX-5 (dark `tertiary` chip).
> **P2-UX-3 exit criteria — ✅ ALL MET (2026-07-08):** DS lint guard **blocks new violations** (Done-when); contrast checker gate green (light+dark WCAG AA); 4 persona-shell goldens; pure-additive (0 `lib/`); analyze 0; +22 gates; EOS UX PASS; ledger + journal + this file + dashboard refreshed. CI-wiring leg owner/CI-gated (deferred).
> **P2-UX-2 exit criteria — ✅ ALL MET (2026-07-08).**
> **P2-UX-1 exit criteria — ✅ ALL MET (2026-07-08).**

> **Rule:** never begin the next wave with an open P0 or an EOS BLOCKED. Owner-decision (👤) tasks surface in a batch and do not pause the pipeline; live-lane (⏳) tasks defer until provisioned; non-blocked tasks proceed.
