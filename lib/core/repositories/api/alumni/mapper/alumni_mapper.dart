import '../dto/alumni_dashboard_dto.dart';

/// Maps Alumni API DTOs to domain models (scaffolding).
class AlumniMapper {
  const AlumniMapper();

  dynamic toDashboard(AlumniDashboardDto dto) {
    throw UnimplementedError('AlumniMapper.toDashboard not implemented');
  }
}
