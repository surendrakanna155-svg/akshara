import '../dto/control_center_dashboard_dto.dart';

/// Maps Control Center API DTOs to domain models (scaffolding).
class ControlCenterMapper {
  const ControlCenterMapper();

  dynamic toDashboard(ControlCenterDashboardDto dto) {
    throw UnimplementedError(
      'ControlCenterMapper.toDashboard not implemented',
    );
  }
}
