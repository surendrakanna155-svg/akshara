import '../dto/library_dashboard_dto.dart';

/// Maps Library API DTOs to domain models (scaffolding).
class LibraryMapper {
  const LibraryMapper();

  dynamic toDashboard(LibraryDashboardDto dto) {
    throw UnimplementedError('LibraryMapper.toDashboard not implemented');
  }
}
