import 'package:flutter/material.dart';

/// DS V2 persona accents — a subtle hue over the shared M15 surfaces
/// (DESIGN_SYSTEM_V2.md §3). Mid-tone so contrast holds in Light AND Dark.
abstract final class AksharaPersonaAccent {
  static const Color parent = Color(0xFF2170E4);
  static const Color student = Color(0xFF10B981);
  static const Color teacher = Color(0xFF6366F1);
  static const Color admin = Color(0xFF6366F1);
}
