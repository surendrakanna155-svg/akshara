# Akshara ERP — Maestro workspace configuration
# Usage: maestro test --config qa/maestro/config.yaml qa/journeys/<flow>.yaml

appId: com.akshara.erp

# QA automation build (ENABLE_QA_LOGIN=true or QA_AUTOMATION=true)
env:
  QA_PERSONA: "Super Admin"
  ACCOUNT_CHIP: Parent
