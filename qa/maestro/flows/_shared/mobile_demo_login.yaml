appId: com.akshara.erp
---
# Instant QA login — maps ACCOUNT_CHIP to QA persona button label.
# Requires env: ACCOUNT_CHIP (Parent|Teacher|Student|Principal)
- runFlow:
    file: qa_persona_login.yaml
    env:
      QA_PERSONA: ${ACCOUNT_CHIP}
