appId: com.akshara.erp
---
# Instant QA staff login — defaults to Super Admin; override QA_PERSONA for module personas.
- runFlow:
    file: qa_persona_login.yaml
    env:
      QA_PERSONA: ${QA_PERSONA}
