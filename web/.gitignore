node_modules
dist
# separate demo artifact (npm run build:demo -- --outDir dist-demo)
dist-demo
dist-ssr
coverage
*.tsbuildinfo
*.local
.env
.env.*
!.env.example
.DS_Store
*.log
.vite

# font subset tooling
.venv-fonts

# UI/visual-regression certification artifacts (generated, ~150MB)
visual-baselines
scripts/live_validate_results.json
